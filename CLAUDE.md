# 김하현수학연구소 — 작업 노트

마지막 갱신: **2026-09-08 낮** — 인증 끝(순위까지) · 학생 달력 새로 · 시험 일정 셋 고침 ·
**배포본을 눈으로 다 봤다** · 🔴 코드 숨기기는 «고민 중».
· ✅ 인증 구멍을 찾았다 → 테스트 데이터 419건을 걷어냈다 → **다시 세웠고 배포본에서 확인했다.**
· 🔵 09-08 낮에 노트의 틀린 줄 하나를 잡았다 — 「실패가 그대로 하루치를 깎는다」는 **이제 아니다.**

### 📖 이 노트를 읽는 법

| 어디 | 무엇 |
|---|---|
| **여기 (`CLAUDE.md`)** | **지금 할 일 · 지금 지켜야 할 규칙 · 다시 밟으면 안 되는 함정 · 결정** |
| [`docs/기록-2026-09.md`](docs/기록-2026-09.md) | 9월에 «무엇을 왜 했나»의 기록 |
| [`docs/기록-2026-08.md`](docs/기록-2026-08.md) | 8월 기록 |
| [`docs/기록-2026-08-이식.md`](docs/기록-2026-08-이식.md) | 리디자인 이식 기록 (08-06~08-09 · **끝났고 구버전은 한 줄도 안 남았다**) |

🔴 **기록을 여기로 도로 들이지 말 것.** 2026-09-07에 노트가 **37만 자**까지 자라
  «한 번에 안 읽히는» 상태가 됐다. 그래서 갈랐다. 새로 적을 것이 **끝난 일**이면 `docs/` 로 간다.
  여기 남길 것은 셋뿐이다 — **아직 할 일 · 지켜야 할 규칙 · 밟으면 안 되는 함정.**
⚠ 한 줄도 안 버렸다. 옮기고 나서 «옛 노트의 모든 줄이 어딘가에 그대로 있는지» 견줬다.

### 지금 서 있는 자리 (2026-09-08 낮)

· ✅ **인증 구멍이 닫혔다** — 익명에게 20개 컬렉션이 전부 막혔고, 마지막 자리였던 순위도
  `ranks/{주}__{uid}` 로 갈랐다. 사이트를 열기만 한 사람은 이제 아무것도 못 읽는다.
· ✅ **로그인 읽기 334 → 144건** — 변경 이력을 화면 열 때만 읽는다. `tools/read-cost.mjs` 로 잰다.
· ✅ **SCENE 3(실전) 검토 AI 58제 84.5%** — 표본을 늘려도 안 흔들렸다. 이제 믿어도 되는 값이다.
· ✅ **09-08 낮에 배포본을 눈으로 다 봤다** — 아래 「▶ 배포본에서 눈으로 본 것」. 남은 확인 없음.
· 🔴 **아직 안 정한 것 둘** — ① 씨앗 테스트 계정(`seedTestAccountIfEmpty`)을 뺄지
  ② 코드를 남의 눈에 안 보이게 심을지([`docs/코드-숨기기.md`](docs/코드-숨기기.md) · 사용자가 고민 중).
· ✅ **주기나 심기 «전»에 해야 할 것은 끝냈다** — 창고는 이제 원본만 세운다(아래 「▶ 다음 차례」).
· 🔴 **적어 둔 것도 다시 재 볼 것** — 09-07에 둘이 틀렸고(Cerebras 402 · `max_tokens` 4000 은 잘림),
  09-08에 또 하나가 틀렸다(「실패가 그대로 하루치를 깎는다」는 이제 아니다 — 아래 워커 상자).

### 🔴 다시 밟지 말 것 — 실제로 밟았던 자리만

⚠ **이 목록은 날짜로 밀려나지 않는다.** 여태 머리말이 이 일을 겸했는데, 갱신할 때마다
  아래로 흘러 내려가다 2026-09-07에 한 줄을 잃을 뻔했다. 함정은 «어제 일»이 아니라 «내일도 밟을 자리»다.

| 무엇 | 어떻게 물리나 | 자세히 |
|---|---|---|
| **셸을 지나면 백슬래시가 깎인다** | 패치·검사 자료는 **파일로 짓는다**(heredoc 금지). `String.fromCharCode(92)` 로 짓거나 Write 로 적는다. `replace` 는 **함수꼴**로 | 09-07에 또 밟았다 · [기록-09](docs/기록-2026-09.md) |
| **node 정규식에 백슬래시가 빠져 있었다** | `source-code.mjs` 의 `[s()·,]` 는 공백이 아니었다 — 02·03·04 는 돌리면 멈췄을 것 | [기록-09](docs/기록-2026-09.md) |
| **«한 칸 밀림»이 모양만 바꿔 세 번** | 코드 줄 · 덩이≠미주 · 거르기 차례. **세는 자리를 두 벌로 두지 말 것** | [기록-09](docs/기록-2026-09.md) |
| **담았는데 목록에 안 뜬다** | `storeItems()` 가 장부만 보고 있었다. **코드가 사는 곳은 둘**(장부·창고) | 「▶ 다음 차례」 |
| **씨앗 심기가 기록을 덮었다** | `record:test` 를 덮고 있었다. 씨앗은 «비어 있을 때만» 심는다 | `tools/seed-guard-test.mjs` |
| **검사 통과를 «봤다»로 읽었다** | 하루에 세 번 같은 자리에서 넘어졌다. **배포본에서 눈으로 볼 것** | 「▶ 다음 차례」 |
| **자른 값에서 무엇을 찾았다** | 429 말을 90자로 자른 뒤 거기서 `per day` 를 찾았다 — 영영 못 문다. **판정은 «온 말»에서** | 09-07 |
| **«0건»과 «아직 못 셌다»를 뭉갰다** | 화면이 재 보지 않고 하는 말은 언젠가 거짓말이 된다 | `auditlog-lazy-test.mjs` |
| **숨긴 것이 복사되면 조용히 따라간다** | 개체 설명문에 코드를 숨기려다 사용자가 찾았다 — 틀을 복사하면 코드가 중복된다. **보이는 것이 오히려 스스로 고쳐진다** | 09-08 |
| **없는 CSS 토큰·클래스를 썼다** | `--warn`·`--info`·`.sap-sec` 은 없다. 없는 토큰은 조용히 «색 없음»이 되어 화면은 도는데 안 보인다 | 09-08 |
| **판단하는 자리를 옮기고 «지우는 자리»를 안 옮겼다** | 로그아웃이 localStorage 만 지우고 토큰은 살려 뒀다 — 새로고침하면 도로 로그인됐다. **옮길 때는 «세우는 곳»과 «지우는 곳»을 같이 볼 것** | 09-08 |
| **주석을 정규식으로 걷었다** | 문자열 안의 `/*` 를 주석 시작으로 읽어 **코드를 29% 삼켰다.** 검사가 «없어서»가 아니라 «먹혀서» 통과했다 → `tools/strip-comments.mjs` | 09-08 |
| **규칙 파일에 한글 이름을 썼다** | Firestore 규칙 언어는 한글 식별자를 못 받는다. 이 프로젝트는 뭐든 한글로 짓기에 **또 저지르기 쉽다** — 검사가 붙든다 | 09-08 |
| **컬렉션만 지우고 옛 블롭을 뒀다** | `migrateBlobToCollection` 이 `kv/<이름>` 에 옛 배열을 남긴다. **컬렉션과 kv 를 같이 볼 것** | 09-07 |
| **«상태만 흉내 낸다»가 진짜로 돌았다** | `render()` 가 `state.autoFill.on` 을 보고 고리를 깨운다(`index.html:10295`). **깃발을 세우는 것 = 일을 시키는 것**이라 AI 를 실제로 불렀다 | 09-08 · 「▶ 다음 차례」 |
| **«되긴 되는데 틀린 값»** | 반 일정이 요일 셋을 다 잡으면서 시간은 맨 뒤 하나만 남겼다 — 등록은 된 것처럼 보여 **화면을 봐도 틀린 줄 모른다.** 안 되는 것보다 찾기 어렵다 | 09-08 · `schedule-test.mjs` |
| **사람에게 «형식»을 지키게 했다** | 「`/` 로 나눕니다」를 화면에 적어 뒀는데도 사용자는 **쉼표로 적었다.** 그게 자연스러운 글자다 — **글자가 아니라 «꼴»로 읽어 낼 것** | 09-08 · 「▶ 다음 차례」 |
| **곁말을 `label` 안에 넣었다** | `.cls-form` 은 한 줄 flex 라 **한 칸만 키가 커지면 줄이 통째로 어긋난다.** 설명은 `st-note` 로 | 09-08 |
| **부르는 자리를 셀 때 화면 하나를 빠뜨렸다** | 「오늘의 수업」이 요일을 안 넘겨, 파서를 고쳐도 **그 화면만 여전히 틀렸다.** 날짜를 아는 화면은 반드시 요일을 넘길 것 | 09-08 |
| **`getAIQuotaUsed()` 가 실패를 0으로 삼킨다** | `catch` 에서 0을 돌려준다. 「0이니 안 깎였다」는 거짓 결론이 여기서 나온다 — **워커 `/quota` 에 직접 물을 것**(POST + Bearer) | 09-08 |
| **백업이 «일부만» 받고 있었다** | kv 를 정해진 열쇠 6개만 받아 31건이 빠졌다. 받았다고 믿고 지울 뻔했다 | 09-07 |
| **적어 둔 값도 낡는다** | 노트가 `index.html` 을 **4,875줄**이라 적고 있었다(실제 22,825줄 · 4.7배). Cerebras·max 4000 도 그랬다 | 09-07 |

---

## 0. 지금 상태 — 여기부터 읽으면 된다

**2026-08-06에 시작한 이식이 2026-08-08에 끝났다.**
`index.html`에 **구버전 스타일이 한 줄도 남아 있지 않다** — `.legacy` 스타일 블록(476줄)도,
`body.legacy`도, `lg-` 클래스도 전부 사라졌다. `design-v1/`·`landing-v2/`는 «따라 그릴 기준»으로 남는다.
이식의 진행 기록은 [`docs/기록-2026-08-이식.md`](docs/기록-2026-08-이식.md) 로 옮겼다(옛 0-B).

> 그래서 `tools/check-css-collisions.js`도 지웠다 — 두 CSS가 공존하는 동안만 필요한 도구였다.

| | 상태 |
|---|---|
| 디자인 시스템 | **확정** — 루트의 `ds.css` + `ds.js`. 로고 기준 색까지 정리 완료 |
| 관리자 화면 | **16개 완성 · 실서비스 이식 16/16 완료** — 렌더링 검증 전부 0건. 설정에 갈래가 둘 늘었다 (시안 밖) — 「신호 규칙」 · 「시험 범위」 |
| 학생 마이페이지 | **11개 완성 · 실서비스 이식 11/11 완료** (4탭) — 검증 전부 0건. 학습 탭에 「자료」가 늘었다 (2026-08-09). **데일리퀴즈는 오답 간격 반복으로 다시 만들었다** (2026-08-09~10). **태블릿 대응 완료** — 폭 구간 셋 (2026-08-12) |
| 시험 일정 | **2026-08-13 신설** — 노션에서 손으로 정리하던 표를 「설정 > 시험 일정」으로 옮겼다. 기존 «시험 범위»에 **날짜와 교재**를 더한 것이고 표를 새로 만들지 않았다. **D-day가 학생 홈과 명단에 뜬다** |
| 오답 파이프라인 | 시험지 등록(문항마다 **단원**) → 오답 등록 → **쌍둥이 일괄 생성** → 3단계 검토(+**그림 필요**) → 오답숙제 → **데일리퀴즈 간격 반복**. 2026-08-10에 한 바퀴를 다시 세웠고, 2026-08-11에 그림 문항의 갈래를 갈랐고, **2026-08-12에 (B)의 그림을 기계가 그리게 됐다** |
| 기록 저장 | ⚠ **저장하는 쪽에 문이 둘 있다** (2026-08-12) — 못 읽은 문서에는 안 쓰고(`dbReadFailed`), 저장되는 기록은 «읽어 온 그 객체» 하나뿐이다(`recordLoaded`). **학생 화면은 그리기만 해도 저장한다** |
| 파일 업로드 | **정상** — 드라이브(워커 `/upload`). 2026-08-09에 토큰 만료로 죽었다가 복구했다 (9절 2-B) |
| 워커 | **최신** — 2026-08-11에 네 번 배포해 갈래를 셋(A/B/C)으로 맞췄고, **2026-08-12에 `/figure`(그림 장면)를 열어 배포했다.** 배포본에서 전부 확인 완료. **AI 한도의 진실은 워커 하나뿐이다** |
| 랜딩페이지 | **2026-08-13에 전면 재구성** — 컨셉 「하루」 → **「생각의 흐름」**. 주인공이 시스템이 아니라 **수학강사 김하현**이다. 원칙 셋이 뼈대, 시스템은 70% 지점. 여섯 폭 실측 0건. ⚠ **교재는 표지 시안(목업)이라 실물 사진 대기 중** · `landing-v2/`는 이제 기준이 아니다 |
| 로그인 · 조교 | **시안 2개 완성 · 이식 완료** (2026-08-08). **조교는 여러 반을 맡는다** — 설정에서 담당 반을 바꿀 수 있다 (2026-08-11) |
| 출석 도장 | **2026-08-21 신설 · 배포 완료** — 학생 홈의 「똥피하기」. **하루 한 판**이고 끝나면 도장이 찍힌다. ⚠ **강사 출결과는 완전히 별개다**(`rec.checkin` ↔ `rec.attendance`) — 출석률에 한 숫자도 안 섞인다. 순위는 **학원 전체 · 주마다 새로 · 상위 5명 · 실명**. **강사는 「소통 > 출석 도장」에서 전원을 본다**(2026-08-24 · S-F · 읽기 전용 · 주 넘기기 됨). ⚠ **캔버스는 `#app` 밖**(`#gamehost`)에 있어야 한다 |
| 실제 구현 | **구버전 화면도 스타일도 하나도 없다.** 「legacy」라는 낱말이 파일에 0회 |

### 이어서 시작하는 절차
```bash
cd "C:\Users\hahyun\Desktop\claude work"
git log --oneline -8              # 어디까지 왔는지
git status -sb                    # main == origin/main 이어야 정상
node tools/serve.js               # 127.0.0.1:8777
```
`file://`로 열면 iframe 썸네일이 막힌다. Windows Store 파이썬 스텁은 동작하지 않으므로 **node**를 쓴다.

그 다음 이 노트의 **「▶ 다음 차례」**를 읽고 이어가면 된다.

**`tools/`에 있는 것 넷** — 전부 node로 곧장 돌린다.
| | |
|---|---|
| `serve.js` | 로컬 서버 (127.0.0.1:8777) |
| `test-json-repair.mjs` | 워커의 JSON 복구를 8건으로 시험한다. **배포 없이 돌아간다** |
| `probe-worker.mjs` | 배포된 워커를 화면 없이 부른다. 쌍둥이 «갈래»와 figureSpec만 볼 때. ⚠ 한 번에 하루 한도 하나를 쓴다 |
| `figure-preview.mjs` | 장면(scene)을 검산해 SVG로 그리고 대조표를 낸다. **배포도 한도도 안 쓴다** → 6절 「그림을 코드로 그린다」 |
| `probe-figure.mjs` | 배포된 워커의 `/figure`를 눌러 받은 장면을 그 자리에서 검산·렌더한다. **클라이언트를 배포하기 전에 모델 품질부터 볼 수 있다.** ⚠ 한 건에 한도 하나 |
| `measure-landing.html` | 공개 홈을 진짜 뷰포트 iframe에 띄워 **잘림·침범·넘침·척추 정렬**을 잰다. **브라우저 확장 없이 헤드리스 크롬으로 돈다** → 6절 「확장 없이 재는 길」 |
| `render-pdf-cover.html` | 인쇄용 표지 PDF를 캔버스로 그린다 (poppler 대신 pdf.js). ⚠ 본체는 `.gitignore` — 파일 머리말의 curl 두 줄로 받는다 |
| `make-book-mockup.html` | 투명 목업 PNG(`tools/mock-*.png`)를 `--paper` 위에 앉힌다 → `book-set.jpg`. **책이 늘면 여기만 고쳐 다시 굽는다** |
| `screen-shot.html` | **로그인한 화면**(관리자·학생)을 진짜 뷰포트로 찍는다. 확장 없이 헤드리스로 돈다. ⚠ `?pre=`에 저장 함수를 넣지 말 것 — 실 DB에 쓴다 |
| `cut-sprite.html` | 검은 배경 위 그림에서 **인물만 따낸다** (`?src=../그림.png`). 가장자리에서 흘려보내고, 자르고, 줄이고, «색이 있는 픽셀의 무게중심»으로 기준점을 낸다. `game-char.webp`를 이걸로 만들었다 → 「▶ 다음 차례」 S-D-1. ⚠ **이 컴퓨터에 ImageMagick·Pillow가 없다** — 이미지 가공은 브라우저 캔버스가 유일한 길이다 |
| `hwpx-node.mjs` | hwpx 를 node 에서 읽는 **다리**. zip 풀기 + DOMParser 자리를 메우는 작은 XML 셈. 🔴 **가르는 규칙은 여기 없다** — 루트 `hwpx.js` 를 글자로 읽어 부른다. ⚠ 흉내 내는 DOM 은 여섯뿐(childNodes·nodeType·localName·textContent·getAttribute·getElementsByTagNameNS) — 일곱 번째를 쓰면 여기도 고쳐야 한다 |
| `item-bodies.mjs` | 코드가 붙은 문항의 **본문만** 낸다(`code`·`content`). 정답·해설은 안 담는다. `--push` 면 Firestore `items` 로 바로 — 웹 `dbSetDoc` 과 **같은 모양**(문서 하나에 `value` 필드 하나, 그 안에 JSON)으로 쓴다. ⚠ 낸 파일에는 본문이 그대로 들어 있다 — `.gitignore` 의 `*bodies*.json` |

### 바로 열어볼 곳
```
http://127.0.0.1:8777/index.html                     실서비스 (공개 홈이 첫 화면)
http://127.0.0.1:8777/design-v1/preview.html         시안 전체 (관리자 16 + 아래 넷 링크)
http://127.0.0.1:8777/design-v1/00-system.html       디자인 시스템 문서
http://127.0.0.1:8777/design-v1/20-student-app.html  학생앱 11개
http://127.0.0.1:8777/design-v1/17-login.html        로그인 5상태
http://127.0.0.1:8777/design-v1/18-assistant.html    조교 2화면
http://127.0.0.1:8777/landing-v2/preview.html        랜딩 (데스크톱+모바일 동시)
```
관리자·학생·조교 화면은 로그인이 필요하다. 브라우저 콘솔에서 곧장 들어갈 수 있다.
```js
// 관리자
state.currentUser={type:'teacher'}; state.view='teacher'; state.teacherTab='roster'; render();
// 학생 (기록이 있어야 화면이 찬다)
state.currentUser={type:'student',studentId:'test',name:'테스트학생'};
state.view='student'; loadRecord('test').then(render);
// 조교 (계정 없이도 화면은 볼 수 있다)
state.currentUser={type:'assistant',name:'김하현',classId:'c_test',taId:'t',id:'a'};
state.view='assistant'; loadAllRecordsIfNeeded();
```
> ⚠ **`ALLOW_ORIGIN` 때문에 로컬에서는 워커를 못 부른다.** AI 생성·이미지 업로드는
> 배포된 사이트에서만 확인된다 (9절 1번).

---

> 📁 **기록 — 리디자인 이식 (2026-08-06 ~ 08-09 · 0-B)** — [`docs/기록-2026-08-이식.md`](docs/기록-2026-08-이식.md) 로 옮겼다 (1988줄).
### ▶ 다음 차례

> ### 🔵 **휴강·보강, 그리고 반별 공지** (2026-09-08 · 사용자 요청)
> 「추석이나 설날 등에 휴강이나 수업 날짜 변경등 할 수 있으면 좋겠어. 그게 학생 캘린더에
> 반영되고! 또 공지사항도 반별로 공지할 수 있는 기능도 있으면 좋겠어.」
>
> **① 휴강 · 보강** — 설정 › **반** 화면의 둘째 구역(`teacherOffDaysHTML`).
> 🔵 **「휴강」과 「날짜 변경」은 같은 것이다** — 날짜 변경 = 그 날은 쉬고 **다른 날에 한다**.
>   그래서 `moveTo` 한 칸이면 된다. ⚠ 둘을 따로 만들면 **「옮겼는데 원래 날에도 수업이 남는」**
>   짝 안 맞는 상태가 생긴다.
> 🔵 **반을 안 고르면 «학원 전체»다** — 추석은 모든 반이 쉬므로, 반이 일곱이면 일곱 번
>   넣게 하면 아무도 안 쓴다. 특정 반만 쉬는 날은 그 반을 골라 둔다.
> 🔴 **쉬는 날을 그냥 «빼지» 않는다 — 쉰다고 적는다.** 아무 말 없이 비면 학생은
>   「내 수업이 왜 없지」가 된다. 옮긴 날이면 **어디로 갔는지까지** 말해 준다.
> 🔴 **강사 수업 화면에서는 «숨기지» 않는다** — 「휴강」 딱지를 달고 그대로 보인다.
>   이미 출결을 찍어 둔 날을 나중에 휴강으로 넣으면, 숨기는 순간 **그 기록에 닿을 길이 사라진다.**
>   🔵 보강 온 날은 `sessionsOn` 이 **요일이 안 맞아도 세운다** — 그 날 출결을 찍어야 하니까.
> ⚠ 저장은 `kv/class-offdays` 하나 — **컬렉션도 규칙도 안 늘렸다.** 읽기 한 건이 는다.
>
> **② 반별 공지** — 공지에 `classIds` 가 생겼다. 🔴 **빈 배열은 «전체»다** —
>   그래서 **필드가 없는 옛 공지도 자연히 전체**가 되어 옮길 것이 없다.
> 🔴 **세는 자리 셋이 다 같은 함수를 지나야 한다** — 학생 공지 목록 · 서브내비 숫자 · 홈의 벨.
>   한 곳이라도 «전체 개수»로 세면 **남의 반 공지로 벨이 울리고, 들어가면 새 것이 없다.**
>   검사가 「안 거른 `DATA.notices.length` 로 세는 자리가 0곳」을 붙든다.
> ⚠ 강사 화면에도 «누구에게 갔는지»를 적었다(목록의 날짜 옆 · 본문의 딱지) —
>   안 적으면 반별 공지를 낸 뒤 「이거 누구한테 갔더라」를 알 길이 없다.
>
> ✅ **잰 것** — 새 검사 `tools/offday-notice-test.mjs` **29벌**(망가뜨려 무는 것 둘 포함) ·
>   달력 검사에 ⑨절(휴강·보강) **11벌** · 학생 읽기 검사 갱신 · **검사 42벌 전부 통과.**
> 🔴 **화면으로 굴려 봤다**(실 DB 0줄) — 추석(9/24)을 9/26으로 옮기고 9/17은 0.5B만 휴강.
>   학생 달력에서 9/24 「휴강 — 날짜가 바뀌었습니다 · 9월 26일(토)로」,
>   9/26 에 **원래 토요일 수업(14:00~16:00)과 옮겨 온 수업(19:30~22:00)이 나란히** 섰다.
>   공지는 0.5B 학생이 둘(전체+제 반), GA1 학생이 둘(전체+제 반), 반 없는 학생이 하나를 본다.

> ### 🔴 **이미 만든 반을 고칠 문이 «아예 없었다»** (2026-09-08 · 사용자 요청)
> 「올려놓은 반 시간 수정이 안되네. 이미 올려둔 반에 대해서 수정가능하게 했으면 좋겠어」.
> 재 보니 **만들기·종료·삭제만 있었다.** 오타 하나를 고치려면 반을 지우고 다시 만들어야 했는데,
> 그러면 **그 반에 딸린 출결·성적·과제·진도가 통째로 날아간다.**
>
> ▶ **설정 › 반 목록의 줄에서 편다** — 「고치기」를 누르면 그 줄 아래로 폼이 펴진다.
>   🔵 다른 화면으로 보내지 않는다. 어느 반을 고치는지 눈에서 안 놓친다.
>   ⚠ 고치는 값은 만들 때 받는 **«같은 넷»**이다(이름·시간·기간·갈래). 두 벌로 두지 않는다.
>
> 🔴 **`Object.assign(c, 후)` 으로 «덮어쓴다» — 통째로 갈아 끼우지 않는다.**
>   반 문서에는 `progress`·`books`·`status` 같은 **딸린 것**이 함께 산다.
>   새 객체를 만들어 쓰면 그것들이 조용히 날아간다. 검사 ⑤절이 그것을 붙든다
>   (덮어쓰기를 «갈아 끼우기»로 바꾸면 진도가 사라지는 것을 실제로 보여 준다).
> 🔴 **변경 이력에 남긴다** — 시간을 고치면 **학생 달력이 통째로 달라진다.** 누가 언제
>   무엇을 무엇으로 바꿨는지 남을 값이 있다. ⚠ 학생 한 명의 일이 아니라 학생 자리는 비운다.
> ⚠ **바뀐 값이 없으면 안 쓴다** — 안 그러면 변경 이력이 «아무 일도 없었던 줄»로 지저분해진다.
> ⚠ 이름은 비울 수 없다(까닭을 말하고 되돌려 보낸다).
>
> 🔵 **덤으로, 원문을 그대로 뿌리던 자리 둘을 마저 고쳤다** — 반 관리 머리말과 명단 부제.
>   이제 세 화면이 다 `classScheduleLabel()` 로 **같은 꼴**이라 같은 반으로 읽힌다.
>
> ⚠ **검사에서 밟은 것 하나** — `lift()` 가 `'function 이름('` 을 찾다 보니 **`async` 를 떼어**
>   왔고, 몸통의 `await` 가 문법 흠이 됐다. 다른 검사들은 동기 함수만 떠 와서 이 자리를 안 밟았다.
>   `tools/class-edit-test.mjs` 의 `lift` 는 앞의 `async ` 를 같이 가져온다.
>
> ✅ **잰 것** — 새 검사 `tools/class-edit-test.mjs` **28벌**(망가뜨려 무는 것 포함) ·
>   검사 **41벌 전부 통과** · 로컬에서 실제로 고쳐 봤다(실 DB 0줄) —
>   시간을 고치니 목록이 곧바로 「화·목 18:00~21:00 · 토 10:00~13:00」이 됐고
>   **진도·교재가 그대로 살아 있었다.**

> ### 🔴 **반 일정이 «요일마다 다른 시간»을 못 담고 있었다 — 조용히 거짓말했다** (2026-09-08)
> 사용자가 실제 반을 등록하다 물었다 — 「한 반이 화목은 5시-7시반이고 토요일은 오전10-오후2시야
> 이건 뭐라고 등록해야해?」. **답이 「지금은 못 담는다」였다.**
>
> **재 보고 알았다** — 「화,목 17:00~19:30 / 토 10:00~14:00」을 넣으면
> · 요일은 **화·목·토 셋 다 제대로** 잡힌다 (출결·달력에 수업이 정상적으로 선다)
> · 🔴 그런데 시간은 **맨 뒤 하나만** 남는다 — `sessionTimeText` 가 «마지막 요일 글자 뒤»를
>   잘라 썼기 때문이다. 그래서 **화·목 수업도 학생 달력에 「10:00~14:00」이라고 적혔다.**
> · 🔴 정렬(`sessionStartHour`)도 10시가 되어 **저녁 반이 오전 반처럼 앞에** 섰다.
>
> 🔵 **이것이 가장 나쁜 종류의 흠이다 — «되긴 되는데 틀린 값»**이다. 등록은 된 것처럼 보이고
>   요일도 맞으니, 화면을 봐도 틀린 줄 모른다. 안 되는 것보다 찾기 어렵다.
>
> ▶ **고친 것 — 덩이로 나눈다.** `parseSchedule()` 이 `/`(또는 줄바꿈)로 끊고
>   덩이마다 «요일들 + 그 뒤 시간»을 읽는다. `sessionTimeText(schedule, dow)` 가 그 요일의 시간을 준다.
>   ⚠ **쉼표로는 못 끊는다** — 옛 형식이 「월,수,금」처럼 요일 사이에 쉼표를 쓴다.
> 🔵 **옛 형식이 한 글자도 안 바뀌고 그대로 돈다** — 덩이가 하나면 그 시간이 모든 요일의 시간이다.
>   이미 등록된 반을 손댈 필요가 없다(검사 ②절이 그것을 붙든다).
> 🔵 **요일을 «안» 주면 통째로 말한다** — 「화·목 17:00~19:30 · 토 10:00~14:00」.
>   반 목록·반 상세가 이것을 쓴다. 🔴 예전처럼 마지막 것 하나만 주면 그게 곧 거짓말이다.
>   ⚠ 시간이 하나뿐이면 예전처럼 시간만 준다 — 목록이 시끄러워지지 않는다.
>
> **고친 자리 넷** — 학생 달력(요일별) · 학생 홈 「다음 수업」(그 날짜의 요일) ·
>   `sessionsOn` 정렬(그 요일) · 반 만들기 폼의 안내(형식을 화면이 가르친다).
> 🔴 **형식은 화면이 가르쳐야 한다** — 「/ 로 나눈다」를 아무 데도 안 적으면 아무도 안 쓴다.
>   폼 아래에 사용자의 그 반을 예로 적어 두었다.
>
> ### 🔴 **그 첫 판이 배포본에서 바로 틀렸다 — 셋을 다시 고쳤다** (2026-09-08 저녁)
> 사용자가 실제로 반을 등록해 보고 짚었다 — 「수업탭에서 오늘은 화요일인데 시간이
> 토요일시간으로 뜨고, 두번째 반추가하는 페이지에서도 줄도 안맞고 이상해졌어」.
>
> **① 🔴 구분자를 `/` 로 강요한 것이 틀렸다.** 사용자는 **쉼표**로 적었다 —
>   「화,목 19시반~22시**,** 토 14시~16시」. 그게 사람이 쓰는 자연스러운 글자다.
>   🔵 **그런데 쉼표로 끊을 수도 없다** — 옛 형식이 「월,수,금」처럼 **요일 사이에** 쉼표를 쓴다.
>   ▶ 그래서 **«글자»가 아니라 «꼴»로 끊는다** — 요일이 나오고, 시간이 나오고,
>     **거기서 다시 요일이 나오면 새 덩이**다. 구분자는 쉼표든 `/` 든 **없어도** 된다.
>   🔵 **일반화 — 사람에게 «형식»을 지키게 하지 말고, 사람이 쓰는 글자를 읽어 낼 것.**
>     안내를 화면에 적어 뒀는데도 사용자는 쉼표로 적었다. 그것이 정답이다.
>
> **② 🔴 「오늘의 수업」이 요일을 안 넘기고 있었다** (`teacherSessionHTML`).
>   ①을 고쳐도 이 자리는 여전히 틀렸을 것이다 — **두 흠이 겹쳐 있었다.**
>   ⚠ 부르는 자리를 셀 때 이 화면을 빠뜨렸다. **날짜를 아는 화면은 반드시 요일을 넘길 것.**
>
> **③ ⚠ 폼 줄이 어긋났다 — 곁말을 `label` 안에 넣어서다.**
>   `.cls-form` 은 칸들을 한 줄에 세우는 flex 라, **한 칸만 키가 커지면 그 줄이 통째로 어긋난다.**
>   설명은 아래 `st-note` 로 옮겼다 — 원래 그러라고 있는 자리다.
>
> ### 🔵 **적는 것은 자유롭게, 보이는 것은 한 꼴로** (2026-09-08 · 사용자 요청)
> 「등록할땐 19시반 이렇게 자유롭게 등록하더라도 뜨는건 동일하게 뜨도록 정리해줘」.
> `normTimeText()` 가 화면에 그릴 때만 다듬는다 — **적어 둔 글자는 안 고친다.**
> 「19시반~22시」·「오전10시~오후2시」·「14시-16시」가 다 「19:30~22:00」 꼴로 보인다.
> 🔴 **앞자리 0을 안 붙인다** — 「5시」를 `05:00` 으로 찍으면 **새벽 5시라고 단정하는 셈**이다.
>   「오전·오후」를 적어 준 것만 24시로 옮긴다(그건 사용자가 «말해 준» 것이다).
>   **꼴만 고르게 하고 없는 뜻은 만들지 않는다.**
> 🔴 **못 알아보면 원문 그대로 둔다** — 반쯤 알아듣고 고치면 「7시~」 같은 반 토막이 남는다.
> 🔵 반 목록도 원문 대신 `classScheduleLabel()` 로 한 꼴로 — 「화·목 19:30~22:00 · 토 14:00~16:00」.
>
> ✅ **잰 것** — `tools/schedule-test.mjs` **51벌**(⑤구분자·⑥시간꼴·⑦망가뜨리기 포함) ·
>   검사 **40벌 전부 통과** · 로컬 화면에서 확인 —
>   수업 탭 화요일 `19:30~22:00` · 토요일 `14:00~16:00` · 수요일 `7:00~9:00`,
>   폼 네 칸이 `top=52~55` 로 한 줄, 목록 두 줄이 다 한 꼴.

> ### 🔵 **직보 일정표를 웹 안에서 짓는다 — 찍으면 학생 달력에 바로 뜬다** (2026-09-08 · 사용자 요청)
> 「시험기간마다 학생들 시험일정 모아서 직보일정표를 만들어 애들에게 주는데, 이걸 웹 안에서
> 만들고 만들고 나면 어제 만든 학생 달력에도 한 명 한 명 바로 뜨게」.
>
> 🔵 **옮긴 값은 «나눠 주는 일이 없어진다»는 것이다.** 엑셀은 만든 뒤 따로 돌려야 하고,
>   한 칸 고치면 또 돌려야 했다. 이제 고치는 즉시 그 학생 달력이 바뀐다.
>
> | 어디 | 무엇 |
> |---|---|
> | 강사 | **설정 › 직보 일정표** (시험 일정 바로 아래 — 사용자가 골랐다) |
> | 학생 | 일정 탭 달력의 **일곱째 갈래**(`plan`). 색은 ds.css 의 `gone` |
> | 저장 | 🔴 **`kv/exam-plan` 하나** — 등급컷과 같은 결(시즌 한 판이 열쇠) |
>
> 🔴 **컬렉션을 안 늘렸으므로 Firestore 규칙을 한 줄도 안 건드렸다.** kv 는 「읽기는 진짜 계정,
>   쓰기는 강사」라 «학생이 읽고 강사가 쓰는» 이 표에 그대로 맞는다. 읽기는 로그인당 **한 건** 는다.
>
> 🔴 **읽을 때는 시즌을 안 가린다.** 「등원시작」은 대개 **시즌을 넘긴 뒤에 오는 날**이라,
>   시즌으로 걸러 읽으면 넘기는 순간 학생 달력에서 그 날이 통째로 사라진다. 쓸 때만 지금 시즌에 쓴다.
>
> **정한 것 셋 (사용자가 골랐다):**
> · **밑그림을 깔아 준다** — 🔴 다만 **«확실한 것»만**이다: 수학 시험일 + 그 전날 직보2시.
>   나머지(등원X·보충·가능하면·상담진행·등원시작)는 **학생마다 갈린다** — 사용자의 표를 보면
>   같은 반 같은 날에 한 학생은 「등원X」, 다른 학생은 「가능하면 5시」다(제 시험이 가까운가 먼가).
>   **짐작으로 찍으면 조용히 틀린다.** 손으로 고친 칸은 다시 깔아도 안 건드린다.
> · **칸은 «종류 + 시간»** — 자유 글자가 아니다. 종류가 정해져야 색이 저절로 맞고, 오타가
>   학생에게 그대로 가지 않는다. 「직보」+「2시」→「직보2시」, 「가능하면」+「5시」→「가능하면 5시」
>   (⚠ 이 하나만 띄어 쓴다 — 사용자의 엑셀이 그렇다).
> · **자리는 설정 › 시험 일정 아래** — 「학교 시험 날짜를 적는다 → 표를 짓는다」가 한 줄로 이어진다.
>
> 🔵 **붓으로 칠한다** — 칸마다 팝업을 띄우면 40명 × 16일 = 640칸을 못 채운다.
>   위에서 종류를 한 번 고르고 칸을 누르면 찍힌다. 같은 것을 다시 누르면 지워진다.
> 🔴 **⇊ 가 이 표에서 손을 가장 많이 던다** — 이름 옆의 그것 하나가 그 줄을
>   **«같은 반 · 같은 학교»** 학생 전원에게 옮긴다.
>   ⚠ 처음엔 「이름을 누르면 그 줄이 한 값으로 통째로 칠해진다」로 만들었는데, **사용자의 실제
>     표를 보니 그런 줄이 하나도 없었다** — 줄은 서너 칸만 차 있다. 쓸모없는 것을 만들 뻔했다.
>   🔵 표에서 진짜 되풀이되는 결은 **«같은 반이면서 같은 학교면 줄이 똑같다»**였다.
>     0.5B 의 광남고 열 명이 글자까지 같고, **같은 광남고라도 GA1 은 상담·등원시작 날이 다르다.**
>     🔴 그러니 «학교만» 보고 옮기면 안 된다 — 반과 학교를 함께 봐야 한다.
> ⚠ **저장은 모아서 한 번**(0.8초). 붓질마다 쓰면 쓰기 한도를 태우고 붓이 밀린다.
>   창을 닫을 때 남은 것을 밀어 넣는다(`beforeunload`).
>
> 🔴 **만들면서 화면에서 흠 하나를 잡았다 — 같은 말을 두 번 했다.**
>   6/25 에 시험 일정의 「수학 시험」과 표의 「수학시험」이 나란히 떴다.
>   ▶ 표에는 그 칸이 있어야 한다(강사가 표를 읽는 기준점이다). **달력에서만 뺀다.**
>   🔴 **갈래로 뭉뚱그려 빼면 안 된다** — 시험 일정을 «안 적고» 표에만 찍은 학생은
>     달력에서 그 날이 통째로 사라진다. **«시험 일정이 그 날을 수학 시험일이라 말할 때»만** 뺀다.
>
> ✅ **잰 것** — 새 검사 `tools/exam-plan-test.mjs` **46벌**(망가뜨려 무는 것 포함) ·
>   달력 검사에 6벌, 학생 읽기 검사에 1벌을 더했다 · **검사 39벌 전부 통과.**
> 🔴 **화면으로도 봤다 — 로컬로 띄워 «로그인도 DB도 없이»**(`dbSet` 을 가로채 실 DB 에 0줄).
>   사용자 엑셀과 색까지 같다(등원X·가능하면=파랑 · 직보·등원시작=빨강 · 나머지=회색) ·
>   가로로 끝까지 밀어도 반·이름·학교가 붙어 있다 · ⇊ 로 옮긴 줄이 글자까지 같다 ·
>   학생 달력에서 **제 것만** 오고 남의 것은 안 온다.
> ⚠ **아직 실제 학생·실제 시험 일정으로는 안 돌려 봤다** — 지금 학생이 둘뿐이고 시험 일정도
>   건대부고 하나뿐이라, **표가 표다워지는 것은 시험기간에 학생이 다 찼을 때**다.
>
> ▶ **아직 안 한 것 (필요하면 말해 주세요)** — 학생에게 «표 전체»를 보여 주는 화면은 안 만들었다.
>   지금은 제 달력에서 제 것만 본다. 엑셀은 통째로 나눠 주던 것이라 **다르게 굴러간다.**
>
> ### ✅ **곧이어 둘을 더 고쳤다 — 사용자가 그 자리에서 짚었다** (2026-09-08)
>
> **① 「시험일정 수집한거 토대로 시험기간과 수학시험일은 미리 표시가 되어있으면 좋겠어」**
> 표의 칸에 **바탕으로** 깔았다 — 시험기간은 옅게, 수학 시험일은 밑줄 + 「수학시험」을 옅은 글자로.
> 🔴 **그러면서 밑그림에서 「수학시험」 찍기를 뺐다.** 이것이 이 고침의 요지다 —
>   그 날은 «시험 일정에서 나오는 값»이지 강사가 정하는 값이 아니다. 찍어서 담으면
>   **같은 사실이 두 군데 살고, 시험 날짜를 고쳤을 때 표가 옛말을 한다.**
>   🔵 덤으로 학생 달력의 «같은 말 두 번» 문제도 뿌리에서 사라졌다(표에 그 칸이 아예 안 생긴다).
>   ⚠ 종류 자체는 남겨 뒀다 — 시험 일정에 없는 시험을 손으로 적을 길은 있어야 한다.
>   ⚠ CSS 차례가 중요하다 — `.pl-c.sp`(시험기간 바탕)를 `k-*`(찍은 색) **앞에** 둬야
>     찍은 칸이 제 색을 지킨다. 같은 굵기라 뒤에 오는 것이 이긴다.
>
> **② 「시험기간 전날부터 시험보는 끝날까지는 그 안에 수업 일정이 안떴으면 좋겠어」**
> `stuClassOffSpan()` 을 새로 뒀다 — `stuExamSpan` 위에 세워서 **시험기간의 처음·끝을
> 두 벌로 셈하지 않는다.** 구간은 «시험기간 전날 ~ 끝날».
> 🔴 **가린 것은 말해 준다** — 달력 밑 붉은 띠에 「6월 23일(화)부터 정규 수업은 안 뜹니다 —
>   직보·보충은 그대로 보입니다」를 덧붙였다. 아무 말이 없으면 학생은 「내 수업이 왜 없지」가 된다.
> 🔴 **가리는 것은 «정규 수업»뿐이다** — 직보·보충·상담은 그대로 뜬다. 그것이 이 기간에
>   «진짜로 오는» 날이고, 직보 일정표가 바로 그 말을 하려고 있는 것이다.
> ⚠ **시험 날짜가 없으면 아무것도 안 가린다** — 멀쩡한 학생의 수업을 지우면 안 된다.
>
> ✅ 실측 — 0.5B(화요일 반) 광남고 학생의 6월 화요일 넷 중 **6/23 하나만 빠졌다**(2·9·16·30 은 그대로).
>   6/23 에는 「직보 등원X」만 남고 수업 점이 사라진다.
> ⚠ 검사가 둘 다 물었다(정확히 내가 건드린 자리다) — 고쳐서 **exam-plan 46벌 · 달력 45벌 ·
>   검사 39벌 전부 통과.** 달력 검사에 ⑧절(시험기간 휴강) 7벌을 새로 더했다.

> ### ✅ **▶ 배포본에서 눈으로 본 것 — 남은 확인이 0이 됐다** (2026-09-08 낮)
> 「사용자가 저녁에 볼 것」으로 며칠 밀려 있던 것들이다. **다시 보지 말 것.**
>
> | 무엇 | 본 것 |
> |---|---|
> | 설정 › 변경 이력 — 열면 뜨나 | ✅ 「최근 60일 · 20건」. **「불러오는 중」이 스칠 틈도 없었다** |
> | — 맨 위가 오늘 것인가 | ✅ `26-09-08 02:11` |
> | — 「전체에서 찾기」 | ✅ 누르니 **「최근 60일」 딱지가 사라지고** 전 구간에서 다시 셌다. 단추도 같이 사라진다 |
> | 시험 일정 — 표가 뜨나 | ✅ 두 줄(건대부고·광남고). **날짜가 없는 줄도 선다** |
> | — 앞 시즌으로 되돌아가기 | ✅ 「← 여름방학」. 변경 이력에 실제로 되돌린 기록도 남아 있다 |
> | — 시즌 시작일 고치기 | ✅ `2026-08-03` 을 고칠 수 있다 |
> | 자동채우기 단추가 풀리나 | ✅ **멈추는 길 일곱이 전부 `autoFillStop` 한 곳을 지난다.** 한도 소진 경로에서 단추가 풀리고 까닭이 띠로 떴다(`localStorage` `'0'` · 타이머 `null` 까지 봤다) |
> | 학생 달력 — 뜨나 | ✅ 「일정」 탭. 21개 일정 · 오늘 강조 · 범례 여섯 |
> | — 날짜를 누르면 | ✅ 아래에 그 날 상세. 「내 일정 · 안T · 19:30」 + 「이 날에 내 일정 추가」 |
> | — 시험기간이 «면»인가 | ✅ 9/30 에서 시작해 **10월로 넘어가서도 1~7일이 이어진다.** 수학 시험일(10/1)만 진하다 |
> | — 색으로만 말하지 않나 | ✅ 달력 밑에 글로도 「시험기간 9월 30일(수) ~ 10월 7일(수) · 수학 10월 1일(목)」 |
> | — 내 일정은 «매주»가 기본인가 | ✅ 「매주」가 눌린 채로 시작. 「언제까지(선택)」로 자른다. 「선생님께는 보이지 않습니다」도 적혀 있다 |
> | 빨간 띠 · 콘솔 | ✅ 강사·학생 양쪽 다 인증 관련 0건 |
>
> ⚠ **학생 로그인은 사용자가 직접 했다** — 비밀번호는 대신 넣지 않는다.
> ⚠ 콘솔의 503 넷은 **같은 순간(12:30:35) 한 건**이 여러 번 보고된 것으로 본다.
>   겹침 방지는 코드에 있다(`autoFillKick` 이 `st.busy`·`autoFillTimer` 를 보고 빠져나오고,
>   틱도 `state.aiProcessing` 이면 비켜선다). **재현하려면 한도를 또 태워야 해서 단정하지 않는다.**

> ### 🔴 **`state.autoFill.on` 을 손으로 켜면 «진짜 AI 호출»이 나간다** (2026-09-08 · 내가 밟았다)
> 「단추가 풀리는지」를 한도를 안 쓰고 보려고 **상태만 흉내 내려** 했다. 그런데 `render()` 가
> 맨 끝에서 `if(state.autoFill && state.autoFill.on) autoFillKick();` 을 한다(`index.html:10295`).
> **깃발을 세우는 것이 곧 고리를 깨우는 것**이라, 1.5초 뒤 틱이 돌아 Gemini 를 실제로 불렀다.
>
> 🔵 **덕분에 노트의 틀린 줄 하나를 잡았다** — 그 실패가 하루치를 «안» 깎았다(위 워커 상자).
> 🔴 **그래도 다음엔 이렇게 하지 말 것.** 화면만 보고 싶으면 **`autoFillStop(...)` 쪽만** 부르면 된다
>   (그건 끄는 일이라 아무것도 안 부른다). 켜는 쪽은 **언제나 진짜로 돈다.**
> 🔵 **일반화 — 이 앱에서 «상태를 흉내 낸다»는 말은 위험하다.** `render()` 가 상태를 보고
>   일을 시작하는 자리가 있으므로, **깃발을 세우는 것과 일을 시키는 것이 같은 일**이 된다.

> ### ✅ **SCENE 3(실전) 검토 AI — 58제에서 84.5% 로 확정** (2026-09-08)
> 44제일 때 84.1% 였고 58제에서 84.5% — **표본을 늘려도 안 흔들렸다.** 이제 믿어도 되는 값이다.
> · 섞인 60제는 96.7% 다. **어려운 것에서 갈리는 15%가 이 기능이 실제로 하는 일**이다.
> · ⚠ 2제는 `--max 7000` 으로도 «답을 적기 전에 잘렸다». 추론이 유난히 긴 문항이라 못 잰 채로 둔다.
> · 🔵 갈린 9제는 대개 «문항이 틀린 것»이 아니라 «AI가 못 푼 것»이다 —
>   그래서 판정을 suspect(문항 의심)와 unsure(검토 못 함)로 갈라 둔 것이 맞았다.
> ⚠ 이틀에 걸쳐 쟀다. `review-bench.mjs --resume` 이 그걸 가능하게 했다(하루 20만 토큰이 천장).

> ### 🔴 **코드를 «남의 눈에 안 보이게» 심기 — 사용자가 고민 중** (2026-09-08)
> 「교재를 공유하는 사람들이 있는데, 코드는 내 개인의 것이라 심어두되 안 보이게 하고 싶어.」
>
> ▶ **잰 값과 미결이 전부 [`docs/코드-숨기기.md`](docs/코드-숨기기.md) 에 있다. 거기부터 볼 것.**
>   내일 다시 재지 말 것 — 한글의 숨김 수단·개체 설명문·문항마다 걸 자리까지 다 재 뒀다.
>
> 🔴 **사용자가 이 방법을 무너뜨리는 것을 찾았다** — 선생님들이 «문제 틀을 복사»하면
>   딸린 그림도 복사되어 **코드가 조용히 중복된다.**
>   🔵 **숨긴 것은 복사될 때 조용히 따라간다. 보이는 것이 오히려 스스로 고쳐진다.**
>     어느 자리에 숨기든 똑같다 — 그러니 «어디에 숨길까»가 아니라 «복사본을 어떻게 알아볼까»다.
> ▶ 낸 안: 코드 옆에 **본문 지문(해시)**을 같이 심는다. 지문이 안 맞으면 «복사본»으로 잡는다.
>
> 🔴 **아직 안 잰 것 하나 — 한글이 개체 설명문을 «저장 뒤에도» 지키는가.**
>   이게 안 되면 위 이야기 전체가 무의미하다. **여기부터 재고 시작할 것.**
>
> ⚠ 곁가지로 찾은 것 — **개체 설명문에 사람 이름(「장은영」)이 5곳 있다.** 안 보이는 자리라
>   사용자도 몰랐고, 지금 나눠 주는 교재에 그대로 딸려 간다. **확인이 필요하다.**

> ### 🔵 **학생 달력을 만들었다 — 「일정」이 다섯째 탭** (2026-09-08 · 사용자 요청)
> 수업·시험·과제·영상 마감·클리닉·**내 일정**을 한 곳에 모은다.
> 학생이 가장 자주 묻는 것이 「오늘 뭐 있지」인데, 여태 화면 다섯을 돌아야 했다.
> · 🔵 **컬렉션도 규칙도 안 늘렸다** — 「내 일정」은 제 기록(`records/{uid}`) 안에 산다
> · 🔵 **시험기간은 «면»이다** — 점 셋만 찍으면 «그 사이가 시험기간»이 안 보인다.
>   ⚠ 색으로만 말하지 않는다 — 밑에 글로도 적는다(색을 못 가리는 사람 · 달을 넘는 기간)
> · 🔵 **내 일정은 «매주»가 기본** — 학원은 되풀이한다. 날마다 적게 하면 아무도 안 쓴다.
>   🔴 되풀이는 «너무 많이 뜨는» 쪽으로 틀리기 쉽다(그만둔 학원이 영영 남는다) — from/to 로 자른다
> · ⚠ 칸에는 «점»만 담는다 — 390px 에서 글자를 우겨넣으면 둘 다 못 읽는다
> ⚠ 검사 `tools/student-calendar-test.mjs` 32벌.

> ### ✅ **시험 일정에서 셋을 고쳤다** (2026-09-08 · 사용자가 짚었다)
> | 무엇 | 까닭 |
> |---|---|
> | 표가 아예 안 떴다 | 줄을 세울 때 «과목 없는 학생»을 통째로 뺐다. 그런데 **날짜·교재는 학교·학년 단위**라 과목과 무관하다 — 이제 과목이 없어도 줄이 선다(범위 칸만 빈다) |
> | 시즌을 넘기면 못 돌아왔다 | 넘기는 문만 있고 **되돌아가는 문이 없었다.** `← 앞 시즌` 을 냈다. 🔴 `saveSeason` 을 쓰면 안 된다 — 그건 범위·날짜를 «비운다» |
> | 시작일이 «넘긴 날»로 박혔다 | 이제 고치거나 비울 수 있다. 🔴 이 값은 **등급컷 열쇠의 한 조각**이라 바꿀 때 **등급컷도 같이 옮긴다** |
> 🔵 앞의 둘은 **한 가지였다** — 표가 안 떠서 아무것도 못 적었고, 적어 둔 것이 없으니 보관도 안 됐다.

> ### ✅ **인증 옮기기가 끝났다 — 사이트를 열어도 아무것도 안 내려간다** (2026-09-08)
> 자세한 것은 [`docs/인증-옮기기.md`](docs/인증-옮기기.md). 지금 상태는 `tools/rules-check.mjs` 로 잰다.
> ✅ 실측 — **익명에게 20개 컬렉션이 전부 막혔다.**
>
> 🔴 **게시한 «뒤»에 흠 둘이 나왔다 — 둘 다 «옮기다 뒤처진» 자리였다:**
> · 로그인 «전»에 DB 를 읽어 그 실패가 빨간 띠로 남았다 → 이제 «들어온 뒤에» 읽는다
> · **로그아웃이 흉내만 냈다**(사용자가 찾았다) — localStorage 만 지우고 토큰은 살려 뒀다
>   🔵 **판단하는 자리를 옮기면, 지우는 자리도 같이 옮겨야 한다.**
>
> ✅ **마지막 자리였던 순위도 닫혔다** (2026-09-08 01:16 · 커밋 `4b3d4f8`).
>   `kv/rank:<월요일>` 이 «공용 배열 한 문서»라 **한 학생이 남의 점수를 통째로 지울 수 있었다.**
>   `ranks/{주}__{uid}` 로 갈랐다 — 이제 «내 uid 가 박힌 문서»만 쓴다.
>   이름이 보이는 것은 **의도다**(2026-09-08에 사용자가 확인했다). 닫은 것은
>   «보는 것»이 아니라 «남의 것을 쓰는 것»이다. `kv` 의 `rank:` 예외도 같이 걷어냈다.

> ### 🔵 **인증 옮기기 ①~⑥ 이 배포본에서 돈다** (2026-09-07 저녁 · 기록)
> 차례와 남은 일은 [`docs/인증-옮기기.md`](docs/인증-옮기기.md) 에 표로 있다. **거기부터 볼 것.**
>
> ✅ 사용자가 배포본에서 직접 확인했다 — 강사 로그인 · 학생 계정 만들기 · 학생 로그인 ·
>   🔵 **강사가 학생 비밀번호를 바꾸고 그 비밀번호로 학생이 로그인했다**(워커 관리자가 진짜로 돈다).
> ✅ 평문 비밀번호가 코드에서 사라졌다 — 주석을 걷어낸 코드에 `.pw =` 가 **0개**다.
>
> ▶ **다음 일 = 6-c 「학생이 제 것만 읽게 가르기」.** 이걸 안 하면 ⑦(규칙 게시)에서
>   학생 화면이 막힌다 — 앱이 아직 명단 전체를 읽으려 들기 때문이다.
> 🔴 **`assistants` 가 `firestore.rules` 에 빠져 있다** — 그대로 게시하면 조교가 아무것도 못 본다.
>
> 🔵 **새 도구 `tools/rules-check.mjs`** — 「빨간 딱지가 떴다」 할 때 **어느 컬렉션이 막혔는지 잰다.**
>   여태는 짐작으로 찾았다. 오늘 실제로 값을 했다(새 컬렉션 넷이 막혀 있었다).
> 🔵 **새 도구 `tools/teacher-doc.mjs`** — 「누가 강사인가」를 적고/센다.
>
> ### 🔴🔴 **왜 이 일을 하는가 — 사이트를 열기만 하면 DB 가 통째로 내려갔다** (2026-09-07)
>
> **셋이 맞물려 있다. 하나만 고쳐서는 안 닫힌다.**
> | | 무엇 |
> |---|---|
> | ① | `index.html` 맨 끝에서 **모든 방문자**가 `fbAuth.signInAnonymously()` 로 인증된다 |
> | ② | 규칙이 `allow read, write: if request.auth != null` — **①이 이걸 그냥 통과한다** |
> | ③ | 로그인이 브라우저 안에서 견준다(`s.pw===pwInput`) — 그래서 **비밀번호가 미리 내려가 있어야 한다** |
>
> 🔴 그래서 주석의 「완전 공개 접근 차단용 최소 보안 장치」가 **아무것도 막지 못한다.**
>   문을 잠그고 열쇠를 문에 붙여 둔 꼴이다. 주소만 알면 **로그인 전에** 학생 이름·학년·학교·
>   본인/학부모 전화번호·비밀번호와 **강사 비밀번호**가 브라우저에 들어와 있고, `allow write` 라
>   **창고·시험지를 지울 수도** 있다.
>
> 🔴 **규칙만 고쳐서는 못 막는다** — 익명 인증은 «우리 앱»과 «남»을 구분하지 못한다.
>   남을 막는 규칙은 앱도 똑같이 막는다(학생이 퀴즈 기록·오류 신고를 쓴다).
>   **구분이 가능해져야** 고칠 수 있다.
>
> ▶ **할 일 — 학생·강사를 Firebase Auth 진짜 계정으로 세운다**
>   · 비밀번호가 **Firestore 를 떠난다**(Auth 가 해시로 보관) — 내려받을 것이 없어진다
>   · 로그인 견주기가 **서버에서** 된다
>   · 규칙을 `request.auth.uid` 로 쓴다 — 학생은 제 기록만, `students` 전체는 강사만
>   🔵 **테스트 계정을 다 지워 둬서 «옮기기»가 없다** — 처음부터 Auth 로 만들면 된다. 반나절~하루.
>   ⚠ 먼저 **콘솔에서 지금 규칙을 눈으로 볼 것.** 위 규칙 원문은 노트에 적힌 09-04 값이다.
>
> ### ✅ **테스트 데이터 419건을 걷어냈다 — 지금 새는 것은 0** (2026-09-07 · 사용자가 정했다)
> 「학생 계정은 전부 테스트용이니 지우고 진행하자. 문항 데이터만 살아 있으면 된다」.
>
> **남은 것 — `items` 564 · `variants` 29 · `kv/itemsVer` 하나.** 그 밖은 전부 지웠다.
> · 학생 55 · 개인기록 55 · 변경이력 190 · 클리닉시간 35 · 과제 12 · 시험지 11 · 반 7 …
> · 설정값(등급컷·학교교재·시험범위·신호규칙·시즌)과 `teacher-pw` 도 **사용자가 지우기로 했다** —
>   비밀번호는 이미 새어 나간 것으로 치고 다시 정한다. **처음 로그인할 때 새로 정하게 된다.**
> · ⚠ 구글 드라이브의 그림은 **안 건드렸다**(사용자가 그대로 두기로 했다).
>
> 🔴 **여기서 하마터면 학생 명단을 남길 뻔했다 — `kv` 의 «옛 블롭»** (이 상자의 값은 이것이다)
>   `migrateBlobToCollection` 이 「예전 블롭 문서는 안전하게 그대로 남겨둔다」고 해서,
>   `kv/students`·`kv/classes` … **아홉 개가 컬렉션과 따로 살아 있었다.**
>   `students` 컬렉션 55건만 지웠으면 **명단이 통째로 그대로 남았다.**
>   → **컬렉션을 지울 때는 `kv` 의 같은 이름을 같이 볼 것.**
>
> 🔴 **그리고 백업이 «일부만» 받고 있었다** — `backup.mjs` 가 kv 를 정해진 열쇠 **6개**만 받아서
>   수업기록·상담일지·문제은행그림·옛블롭 **31건이 빠져 있었다.** kv 를 **통째로** 훑게 고쳤다(93건).
>   ⚠ **「받는다」는 도구가 일부만 받으면 안 받느니만 못하다** — 받았다고 믿고 지우게 된다.
>
> ⚠ 도구 **`tools/reset-test-data.mjs`** (기본은 세기만 · `--push` 라야 지운다 ·
>   **오늘 백업이 없으면 거부한다**). 컬렉션 목록은 `backup.mjs` 것을 읽어 쓴다(두 벌로 두지 않는다).
>   🔴 `listCollectionIds` 로 «실제로 있는 것»을 받으려 했으나 **403(관리자 전용)** 이다.
>
> ▶ **다음에 사이트를 열면 씨앗이 다시 심긴다** — `seedTestAccountIfEmpty()` 가
>   `c_test`(테스트반)와 `test`/`1234` 학생을 만든다. 가짜라 새는 것은 없지만,
>   **본격 시작 전에 이 씨앗을 뺄지 정할 것.**

> ### ✅ **#2 끝 — 로그인 읽기가 334 → 144건이 됐다** (2026-09-07)
> 사용자가 「필요하다면 바로 진행해줘」라고 해서, **고치기 전에 먼저 쟀다.** 그게 값을 했다.
>
> 🔵 **새 도구 `tools/read-cost.mjs` — 로그인 한 번이 몇 건을 읽는지 센다.**
>   🔴 **세는 것 자체가 한도를 쓰면 안 된다** — 그래서 문서를 «받지» 않고 `runAggregationQuery`
>     로 COUNT 만 묻는다(1000건까지 읽기 1건). 컬렉션 열둘을 다 세도 열댓 건이면 끝난다.
>
> **잰 값이 짐작을 뒤집었다 —**
> | | 잰 것 |
> |---|---|
> | 로그인 한 번 | **334건** |
> | 그중 **변경 이력 하나** | **190건 (57%)** |
> | 나머지 컬렉션 «전부» | 합쳐서 137건 (학생 55 · 클리닉시간 35 · 과제 12 · 시험지 11 …) |
>
> 🔴 **그래서 「나머지 컬렉션에 캐시를 붙인다」는 원래 계획은 값이 없다.** 다 합쳐야 137건이고,
>   클리닉·질문은 재우면 **낡은 화면**을 보게 된다. 얻는 것보다 잃는 것이 크다.
>
> ✅ **대신 변경 이력을 «로그인 때»가 아니라 «그 화면을 열 때» 받게 했다** —
>   `DATA.auditLog` 를 쓰는 곳은 둘뿐이다: 새 줄을 앞에 꽂는 자리(빈 배열이어도 된다)와
>   「설정 › 변경 이력」 화면. **그 화면을 안 열면 한 건도 안 쓴다.**
>   · `auditLogEnsureRecent()` — 화면이 그릴 때 부른다(기다리지 않는다. 받아 오면 다시 그린다)
>   · 🔴 **받은 것으로 그냥 덮지 않는다** — 받는 사이에 찍힌 줄이 사라진다. 코드로 겹치는 것만
>     걸러 «합친다». 기록이 사라지는 것은 이 화면에서 가장 나쁜 흠이다
>   · 🔴 **«아직 못 받았다»를 «없다»라고 하지 않는다** — 받는 동안은 「불러오는 중」이라 적고,
>     건수도 안 적는다. 그 숫자는 이 세션에서 새로 찍힌 줄 수일 뿐이다
>   · ⚠ 깃발이 **둘**이다(`auditLogRecent` · `auditLogAll`). 합치면 화면이
>     「최근 60일 안에서만 찾았다」는 사실을 말할 수 없게 된다
>
> **334 → 144건.** 하루 5만 건이면 새로고침 **149번 → 347번**이다.
> ⚠ 검사 **`tools/auditlog-lazy-test.mjs`** 17벌. 망가뜨려 무는지 봤다(덮게 바꾸니 물었다).
> ⚠ **브라우저로는 아직 안 봤다** — 볼 곳은 설정 › 변경 이력. 묻는 것 셋:
>   열면 목록이 뜨나 · 맨 위가 오늘 것인가 · 「전체에서 찾기」가 그대로 도나.
> 🔵 **다음에 읽기를 더 줄여야 하면 «학생 55건»이 그다음**이다. 다만 명단은 늘 쓰는 것이라
>   캐시가 답인지는 또 재 봐야 한다 — **여기서도 짐작으로 고르지 말 것.**

> ### ✅ **사용자가 남은 일 목록을 훑고 여섯을 닫았다** (2026-09-07)
> 「남은 것이 또 뭐가 있나」를 물어 목록을 함께 보고, 그 자리에서 정한 것이다.
> **다시 꺼내지 말 것** — 다시 물으면 사용자가 같은 말을 두 번 하게 된다.
>
> | 무엇 | 사용자가 정한 것 |
> |---|---|
> | **변형 코드(`-N01`)로 창고 검색** | 🔴 **안 만든다.** 「원본 위주로 깔끔하게 보관 창고를 구성하는 게 낫다」 |
> | **폰 출석 도장**(M-2) | ✅ 「문제없는 것 같다」 — 닫는다 |
> | **여러 명 리포트 PDF**(R-3) | ✅ 「문제없는 것 같다」 — 닫는다. 흐린 것을 알고 고르신 그대로 간다 |
> | **「특강만」이라는 이름** | ✅ **그냥 「특강만」으로 둔다.** `SPECIAL_ONLY_LABEL` 은 그대로 |
> | **주기나 02~05 심기**(F) | ▶ 「교재에 오류가 조금 있어 수정할 게 있다」 — **조금 더 뒤에.** 재촉하지 말 것 |
> | **검토 AI 유료 전환** | ▶ 「써 보고 부족하면 결정할 일」 — 지금 견적을 다시 내밀지 말 것 |
> | **리포트를 종이로 뽑아 보기**(R-2b) | ✅ **닫는다.** 「당장은 출력해서가 아니라 **문자로 이미지 파일로 전송**할 거라 필요하지 않다」 |
> | **`.mono` 안의 한글** | ✅ **고칠 것 없다.** 내가 「24곳」이라 했는데 **틀렸다** — 대부분 주석과 숫자였다. 살아 있는 자리는 `'시간 미정'` 하나, 나머지(`\| 칸 \|`·`(가)`)는 **문법을 보여 주는 자리라 일부러 그렇게 둔 것**이다. 규칙만 남긴다 |
> | **새벽 커밋 8개 확인** | ✅ AI 검토 · 변형 드로어 · 창 폭은 **사용자가 눌러 봤다.** ▶ **자동채우기 단추가 꺼지는 것만 저녁에** |
>
> 🔵 **`.mono` 규칙은 왜 있나** (사용자가 물어서 적어 둔다) — 고정폭 글꼴이라 **코드·번호가
>   세로로 줄이 맞는다.** 한글은 그 글꼴에 글자가 없어 다른 글꼴로 대신 그려지므로,
>   ① 한글만 모양이 튀고 ② 줄 맞춤이 깨져 **`.mono` 를 쓴 까닭이 사라진다.**
>   ⚠ 「두부 상자(□□)」는 그게 심해진 경우다. **문법을 보여 주는 자리는 예외** — 거기는 맞다.
>
> ### 🔵 **창고는 «원본만» 세운다 — 사용자가 정한 결이다** (2026-09-07)
> 위의 「변형 코드로 검색」을 접은 까닭이 그것이다. **검색으로 변형을 찾아가는 대신,
> 창고에 애초에 변형 카드가 서지 않게 한다.**
>
> ✅ **지금은 맞다 — 실측 564제 전부 원본, 변형 꼴 0건**(창고·장부 양쪽).
>   AI 가 만든 변형은 `variants` 에 따로 살고 **원본 카드의 드로어 안에서만** 보인다.
>
> ### ✅ **셋 다 끝냈다 — 주기나를 심어도 변형이 원본 옆에 안 선다** (2026-09-08 · 사용자 요청)
> 「깨질 자리는 F(주기나)다」로 적어 두고 **심을 때 같이 하기로** 했던 것인데, **심기 전에** 해 뒀다.
> 까닭은 `ITEM_CODE_RE` 가 `-N01` 꼬리를 **허용**하기 때문이다(`(?:-([NUD])(\d{2}))?`) —
> 안 막으면 주기나를 심는 순간 `1050919-N01` 이 원본 옆에 제 카드로 선다.
>
> | | 한 것 |
> |---|---|
> | ① 거르기 | `storeItems()` 를 **`storeSplitVariants()`** 로 갈랐다 — `{items, hidden}` 을 돌려준다 |
> | ② 드로어로 | 이미 되고 있었다(`variantsOfCodeAll` 이 둘을 합쳐 본다). 손댈 것 없었다 |
> | ③ 딱지 | 창고 드로어엔 **이미 있었다**(`v.fromBook`). 🔴 **정작 «가져오는» 시험지 띠에 없어서** 거기에 넣었다 |
>
> 🔴 **원본이 창고에 없는 변형은 «안» 거른다** — 드로어는 원본 카드로만 열리므로,
>   거르면 그 문항을 **어디서도 못 보게 된다.** 대신 카드에 「변형 · 원본 없음」 딱지를 단다.
>   🔵 **조용히 사라지는 것이 목록이 조금 지저분한 것보다 나쁘다.**
> 🔴 **접은 수를 화면이 말한다** — 「변형 N개는 목록에 따로 안 세웁니다 — 원본 카드의 「코드 화면」 안에」.
>   안 적으면 09-06의 「담았는데 목록에 안 뜬다」가 **모양만 바꿔 다시 난다.**
> ⚠ **가르는 자리는 한 곳뿐이다** — 「원본만 세운다」와 「몇 개 접었나」가 같은 셈에서 나온다
>   (이 노트가 세 번 물린 「세는 자리를 두 벌로 두지 말 것」).
> ⚠ **`itemOfCode` 는 장부에 있으면 장부 객체를 그대로 돌려준다** — 그래서 항목에 표시를 달면
>   `state.itemByCode` 가 더럽혀진다. «고아인가»는 **카드가 코드만 보고 스스로 안다.**
>
> ✅ **잰 것** — `tools/src-web-test.mjs` 에 ④절 9벌을 더했다(25 통과 · 0 실패).
>   **망가뜨려 무는지 봤다** — 거르는 줄을 빼고 같은 것을 재면 변형이 도로 선다.
>   검사 38벌 전부 통과. `index.html` 스크립트 블록도 컴파일해 봤다(문법 흠 0).
> 🔴 **화면으로도 봤다 — 로컬로 띄워 «로그인도 DB도 없이»** `teacherItemStoreHTML()` 만 불렀다.
>   원본 둘이 서고 변형 둘이 접히고 고아 하나에 딱지가 붙었다.
>   🔵 그리고 **원본 카드 밑에 「변형 1 · 숫자변형 1」이 그대로 세어졌다** — 접은 것이
>     사라진 게 아니라 원본 밑으로 들어갔다는 증거다. 이 한 줄이 이 일의 요지다.
> ⚠ **지금 창고(엔딩크레딧 564제)는 전부 원본이라 배포본 화면에서는 아무 변화가 없다** —
>   달라지는 것은 주기나를 심는 날이다. 그래서 **눈이 아니라 검사가 유일한 확인 방법**이다.

> ### 🔵 **SCENE 3(실전)을 44제까지 쟀다 — 84.1%** (2026-09-07)
> 어제 8제에서 「약 75%」라 적어 둔 자리다. 표본을 44제로 늘리니 **37/44 · 84.1%** 였다.
> 섞인 60제의 96.7% 보다는 낮지만, **8제짜리 75% 보다는 높다** — 8제는 점수가 아니었다.
> ⚠ **60제 중 16제는 아직 «안 물었다»** — 하루 토큰이 바닥났다. 한도가 돌아오면 아래 명령 그대로:
> ```
> node tools/review-bench.mjs groq:openai/gpt-oss-120b --scene 3 --n 60 --resume
> ```
> 🔵 갈린 7제는 «문항이 틀린 것»이 아니라 대개 «AI가 못 푼 것»이다(그래서 판정을 suspect/unsure 로 가른다).
>   다만 눈으로 볼 값은 있다 — **K2-03-E-0312**(정답 -2 · AI 는 √2-1) · **K2-02-E-0182**(정답 ③ · AI 는 73).
>
> ### 🔴 **어제 노트에 적어 둔 것 둘이 틀렸다 — 재 보니 그랬다** (2026-09-07)
> | 어제 적은 것 | 오늘 잰 것 |
> |---|---|
> | 「한도가 모자라면 **Cerebras** 로 옮기면 된다(하루 100만 토큰)」 | 🔴 **`402 payment_required`. 무료로 안 준다.** 재 보지 않고 적은 짐작이었다 |
> | 「`max_tokens` 는 실제 필요치(**4000**쯤)만 준다」 | 🔴 **답이 잘린다.** 새로 물은 3제 중 2제가 「답을 적기 전에 잘림」 |
> 🔵 `max_tokens` 는 **두 벽 사이**다 — 위로는 분당 한도(8000)를 예약으로 다 먹으면 안 되고,
>   아래로는 추론이 잘리면 안 된다. **6000** 으로 잡았다(문항 글 600을 얹어도 8000 밑).
> 🔵 Groq TPD 는 **하루 지나면 실제로 돌아온다** — 09-07 13시에 200 OK 였다.
>
> ### 🔵 **시험지를 «이틀에 나눠» 끝낼 수 있게 했다 — `review-bench.mjs --resume`** (2026-09-07)
> SCENE 3 은 한 건이 3700토큰이라 하루 20만으로 **60제를 한 번에 못 잰다.** 그래서 이어받는다.
> · `.bench/*.jsonl` 에 남겨 둔 답을 다시 쓴다 — **묻지 않고 다시 «센다»**(채점을 고쳐도 다시 셀 수 있다)
> · 🔴 **같은 모델이라도 «다른 곳»의 답은 안 섞는다** — groq 와 cerebras 를 합치면 어느 곳의 점수도 아니다
> · ⚠ 흠(429·잘림)은 안 이어받는다 — 그건 「답」이 아니라 「못 잰 것」이다
> · 바닥이 나면 **묻기만 그만두고 걷기는 끝까지 한다** · 몇 제를 안 물었는지 끝 줄에 적는다
> ⚠ 검사 **`tools/bench-resume-test.mjs`** 13벌. 망가뜨려 무는지 봤다.
>
> 🔴 **오늘 같은 자리에서 세 번 넘어졌다 — 셋 다 «두 값을 하나로 본» 탓이다:**
> | 무엇 | 무엇을 뭉쳤나 |
> |---|---|
> | max 4000 이 답을 잘랐다 | 「너무 크면 429」만 보고 **「너무 작으면 잘림」을 안 봤다** |
> | 바닥 감지가 한 번도 안 물었다 | 판정을 **90자로 «자른» 흠 글자**에서 했다. `per day` 는 그 뒤에 나온다 |
> | 8제짜리 점수가 나왔다 | 바닥에서 `break` 했다 — **「더 안 묻는다」와 「더 안 센다」는 다른 말**인데 |
> 🔵 **자른 값에서 무엇을 찾지 말 것.** 판정은 «온 말»에서 하고, 자른 것은 보여 주기용이다.

> ### 🔴 **검토 대기에서 «안 보이는 문항»이 승인될 수 있었다** (2026-09-06 · 사용자가 짚었다)
> 「내용없음 애들은 키보드로 편한데 **검토대기로 넘어가면 그게 없어져버려**」.
>
> 까닭 — 「검토 대기」에는 두 갈래가 **한 줄로 섞여** 그려진다:
> ① AI 가 만든 창고 변형(`pendingVariants`) — 목록 위 · ② 시험지 문항(`reviewQueueList`) — 아래
> 그런데 키보드는 ②만 봤다. AI 변형을 고르면 `reviewSelectedId` 가 null 이 되고
> `reviewSelected(list)` 가 **`list[0]` 을 돌려준다**. 그래서
> · J/K 는 ②의 첫 줄로 튀고 · 🔴 **E(승인)가 화면에 안 보이는 문항을 승인한다.**
>
> 🔵 고침 — **화면에 그리는 차례 그대로 한 줄로 모아**(`reviewWalkList`) 키보드에 준다.
>   손대는 것은 언제나 `reviewWalkCur()` 하나다 — **그리는 곳과 다니는 곳이 같은 목록을 봐야 한다.**
>   AI 변형에서는 E=통과 · X=버리기. 단추에도 키를 적었다(안 적혀 있으면 아무도 안 쓴다).
> ⚠ 검사(`tools/review-key-test.mjs` 12벌)가 「AI 변형을 골랐을 때 시험지 문항이 «아닌가»」를 붙든다.

> ### 🔴 **AI 실패율이 높던 까닭 — 우리 흠이 아니라 «위쪽»이었다** (2026-09-06)
> 사용자가 「남는한도 돌리는데 실패율이 너무높아」라며 화면 말을 그대로 보내 주었다.
> 🔵 **그 말이 값진 것은 09-06 오전에 `detail` 을 살려 둔 덕이다** — 그 전이었으면
>   「gemini error」 다섯 글자뿐이라 여기까지 못 왔다.
>
> **실측 — Gemini 가 두 가지로 엎어진다(둘 다 워커가 502 로 뭉쳐 보낸다):**
> | | 무엇 | 뜻 |
> |---|---|---|
> | 429 | `You exceeded your current quota` | Gemini 쪽 **한도**. 한참 쉬어야 한다 |
> | 503 | `This model is currently experiencing high demand` | **붐빔**. 곧 풀린다 |
> ⚠ 우리 워커 한도는 그때 **10/20** 이었다 — 우리 한도가 아니라 **Gemini 한도**다.
> ⚠ 모델은 `gemini-3.6-flash`.
>
> 🔴 **가장 나쁜 것 — 실패해도 우리 하루치가 깎인다.** 워커가 Gemini 를 부르기 «전»에
>   센다(`bumpQuota` 가 라우팅 문턱에 있다). 그래서 **위쪽이 붐비는 날은 20건을 통째로 버린다.**
> 🔴 **그리고 내가 09-06 오전에 넣은 «한 번 다시 해 본다»가 이 경우엔 해로웠다** —
>   붐비는 데 13초 뒤 또 들이받으니 **한 건이 두 건이 된다.** 고친 것이 다른 자리를 상하게 한 꼴이다.
>
> **고친 것 —**
> · 앱: `aiUpstreamKind()` 로 429·503 을 알아본다. 위쪽이면 **끄지도, 곧바로 다시 하지도 않고
>   쉰다**(한도 30분 · 붐빔 5분). ⚠ 쉬었다 오는 것도 한 건을 쓰므로 **다섯 번에서 멈춘다** —
>   그래야 스무 건이 아니라 다섯 건만 버린다. 되면 셈을 푼다.
> · 워커: `refundQuota()` — **위쪽이 거절(429·5xx)하면 한 건을 돌려준다.**
>   ⚠ 「모델이 이상한 답을 냈다」는 안 돌려준다 — 그건 Gemini 를 진짜로 쓴 것이다.
> ✅ **되돌려주기가 배포본에서 실제로 돈다 — 2026-09-08 낮에 실측했다.**
>   여기 「워커를 올려야 산다 · 그때까지는 실패가 그대로 하루치를 깎는다」고 적혀 있던 자리다.
>   **이제 아니다.** 12:30:35에 Gemini 503(붐빔)으로 한 건이 엎어졌는데, 1분 뒤 워커의 `/quota` 가
>   `{"used":0,"limit":20,"remaining":20}` 을 냈다. **날이 바뀔 틈이 없는 간격이다.**
>   🔵 **재는 법 — `/quota` 는 공짜다**(세지 않고 보기만 한다). 다만 **`POST` 에
>     `Authorization: Bearer <getAuthToken()>`** 이라야 한다. GET 은 405 다.
>   🔴 **앱의 `getAIQuotaUsed()` 를 콘솔에서 그냥 부르면 안 된다** — 실패를 `catch` 해서
>     **0으로 삼킨다.** 「0이니 안 깎였다」는 거짓 결론이 여기서 나온다. 워커에 직접 물을 것.
>
> ### ▶ **워커 올리는 법** (2026-09-06 · 사용자가 밖에 있어 적어 둔다)
> 이 저장소에는 `wrangler.toml` 이 없다 — 여태 **Cloudflare 대시보드에 붙여넣어** 올렸다.
> 1. Cloudflare 대시보드 → **Workers & Pages** → **`hahyunmath-gemini-proxy`**
> 2. **Edit code**(빠른 편집) → 편집기 안을 전부 지우고 → 아래 글을 통째로 붙여넣기
>    `https://raw.githubusercontent.com/khhyun827-lang/hahyunmath/main/worker/gemini-proxy.js`
> 3. **Deploy**
>
> ✅ **설정은 손댈 것이 없다** — KV `QUOTA` · `GEMINI_API_KEY` · `ALLOW_ORIGIN` · 드라이브 OAuth 는
>   이미 붙어 있다. **코드만 갈아 끼우면 된다.**
> 🔵 **이번 변경은 «더하기»뿐이다 — 32줄 추가, 지운 줄 0.** 잃을 것이 없다.
>
> **올린 뒤 확인 —** `node tools/worker-check.mjs`
> · 인자 없이 = **한도만 본다(한 건도 안 쓴다)**
> · `--try` = 한 건 불러서 «되돌려주는지» 실제로 잰다. 위쪽이 거절했는데 한도가 **안 깎이면 최신**이다.
>   ⚠ Gemini 가 제대로 답하면 그건 진짜로 한 건 쓴 것이다(그게 정상이다).
>
> ⚠ 사용자가 본 화면에 `1080913OR` 이 떴다 — **옛 코드**다. 창고에서 지운 뒤라 브라우저가
>   재워 둔 것을 보고 있었다. 새로고침하면 사라진다.

> ### 🔴 **주기나를 통째로 지우고 다시 시작한다 — 코드를 ①꼴로 통일했다** (2026-09-06)
> 사용자가 화면에서 짚었다 — 「1050919OR 이 원본이면 1050919NC01 은 변형인데 **이게 둘 다
> 각각을 원본처럼 여기는 것 같아**」. 맞는 말이었다. 그리고 값이 컸다:
> · 창고가 「변형 없음」이라 말했다 — 교재에 둘이나 있는데
> · 🔴 **AI 가 그걸 못 보고 이미 있는 변형을 또 만들었다** — 실측 3건(하루 20건 한도를 태운다)
> · `storeSubjectCode`·`itemDocKey`·`srcSiblings` 세 자리가 ②꼴 때문에 «조용히» 갈렸다
>
> **바꾼 꼴 — 정보는 하나도 안 잃는다**(NC→N · UP→U · DW→D):
> ```
> 1050919OR    → 1050919        (원본)          2160321BOR   → 2160321B
> 1050919NC01  → 1050919-N01    (숫자변형)      2160321BNC01 → 2160321B-N01
> 1050919UP01  → 1050919-U01    (상향)
> ```
> 🔵 **`splitItemCode` 가 ①꼴과 «같은 규칙»으로 읽는다** — 옮겨 적을 것이 없어졌다.
>
> 🔴 **변형이 사는 곳도 둘이 됐다** — `variants`(우리·AI 가 만든 것) + `items`(교재가 준 것).
>   `variantsOfCodeAll` 이 둘을 합쳐 본다. 그러지 않으면:
>   · `nextVariantCode` 가 **N01 을 다시 내준다** — 이미 인쇄된 코드가 다른 문제를 가리킨다
>   · AI 가 또 중복을 만든다 ← 09-06에 실제로 겪은 그것
>   ⚠ `autoFillCandidates`·`autoFillWhyEmpty` 가 `state.variants` 를 **직접** 보고 있었다 —
>     둘 다 `variantsOfCodeAll` 로 옮겼다. **세는 자리를 두 벌로 두지 말 것.**
>   ✅ 실측으로 확인: N01·N02 가 있으면 다음은 **N03** · AI 후보에서 **빠진다**.
>
> ✅ **지운 것** — 창고 71제 + AI 가 헛만든 변형 3개 + 장부 `codes/K2-J.json`.
>   **시험지·문제은행이 쓰던 것은 하나도 없었다**(도구가 그것부터 확인하고 지운다).
>   백업: `백업/20260906-주기나지우기전`. 드라이브 그림은 **안 지웠다** — 다시 올리면 새로 붙는다.
> ⚠ 도구: **`tools/jugina-wipe.mjs`** (기본은 세기만 · `--push` 라야 지운다).
>
> ### ▶ **사용자가 교재를 고치는 중 — 받으면 다섯을 한 번에 심는다**
> | 자리 (교재 번호) | 어떻게 |
> |---|---|
> | 02단원 **138·139·140번** | 출처에 **「나형」** — `2016년 3월 21번 나형` |
> | 03단원 **243·244·245번** | 출처에 **「가형」** |
> | 01단원 **1·2번** | 「2023년 9월 28번」 → **「2013년 9월 28번」** |
> | 03단원 **262·263번** | 「2023년 9월 28번」 → **「2023년 9월 29번」** |
> | 04단원 **288·289·290번** | 셋 다 **「2016년 3월 28번 나형」** |
> | 05단원 **471번** | 출처 「2024년 10월 30번」 · 딱지를 **OR → NC** |
> | 04단원 **336·337번** | 원본 문항을 넣기(규칙은 그대로 둔다) |
> 🔵 교재 번호는 **한 권으로 이어 매긴다** — 01:1~ · 02:**72**~ · 03:**153**~ · 04:**288**~ · 05:**385**~.

> ### 🔵 **남은 것은 이 다섯뿐이다** (2026-09-06 밤에 «적힌 것과 실제»를 맞대 보고 추렸다)
> ⚠ 아래 상자들은 대부분 **끝난 일의 기록**이다. 열어서 하나씩 읽지 말고 **이 목록만 볼 것.**
>
> | | 무엇 | 왜 안 끝났나 |
> |---|---|---|
> | ~~**A**~~ | ✅ **브라우저에서 실제로 파싱해 봤다** (2026-09-06 · 사용자가 크롬을 열어 주었다) | **끝났다.** 아래 상자를 볼 것 |
> | ~~**B**~~ | ✅ **사용자가 원본을 고쳐 주어 71제가 다 담겼다** (2026-09-06) | **끝났다.** 아래 상자를 볼 것 |
> | ~~**B-2**~~ | ✅ **그림 44개까지 담았다** (2026-09-06) — 다만 **터졌다가 고쳤다.** 아래 상자 |
> | ~~**C**~~ | ✅ **폰 폭(390px)으로 봤다 — 레이아웃은 멀쩡, 대신 «검은 그림»을 찾았다** (2026-09-06) | 아래 상자 |
> | ~~**D**~~ | ✅ **재 봤다 — 정말 0건. 다만 «걷어낸 것은 도구가 아니라 거짓말»이다** (2026-09-06) | 아래 상자 |
> | **E** | (보류) **새로 담기는 문항만 닮음 대조하는 화면** | 사용자와 «그게 낫다»고 합의했고 **교재가 더 쌓인 뒤로** 미뤄 뒀다. `tools/similar-items.mjs --show` 가 이미 있다 |
> | **F** | ▶ **주기나 02~05 — 사용자가 교재를 고치는 중.** 받는 대로 넷을 한 번에 심는다 | 답은 다 나왔다(아래 상자). 코드 쪽 준비(가형/나형)는 **끝났다** |

> ### 🔴 **주기나 02~05 — 사용자가 교재를 고치는 중. 받으면 넷을 한 번에 심는다** (2026-09-06)
> **사용자가 준 답 (이대로 고쳐 주기로 했다):**
> | | 자리 (교재 번호) | 어떻게 |
> |---|---|---|
> | ㄱ | 02단원 **138·139·140번** / 03단원 **243·244·245번** | **둘 다 맞다** — 02 는 **나형**, 03 은 **가형**. 출처에 적어 주기로 |
> | ㄴ | 01단원 **1·2번** | 「2023년 9월 28번」 → **「2013년 9월 28번」** |
> | ㄴ | 03단원 **262·263번** | 「2023년 9월 28번」 → **「2023년 9월 29번」** |
> | ㄷ | 04단원 **288·289·290번** | 셋 다 **「고2 2016년 3월 28번 나형」** 이 진짜 출처 |
> | ㄹ | 05단원 **470·471번** | **「2024년 10월 30번」**이 맞고, 471번은 **OR 이 아니라 NC** |
> | 별건 | 04단원 **336·337번** | 원본이 **빠진 것** — 교재를 고치기로. 규칙은 그대로 둔다 |
>
> 🔴 **ㄴ의 01단원은 이미 창고에 담겨 있다** — 코드가 `1230928*` 에서 `1130928*` 로 바뀐다.
>   담을 때 **옛 코드를 지우고 새로 넣어야 한다**(안 그러면 옛 것이 유령으로 남는다).
> ⚠ **아직 아무것도 안 심었다** — 장부도 안 고쳤고 새 hwpx 도 안 냈다.
> 사용자가 02·03·04·05 파일을 주며 「이것도 코드 붙여줘」 했다. **02·03 은 도구를 고치자 통과했고,
> 04·05 는 멈춘다. 그리고 02·03 사이에 코드가 겹친다.** 겹치는 것들의 **본문이 전부 다르다.**
>
> **먼저 고친 도구 흠 둘 —**
> 🔴 **①「멈춘다」를 말하기 «전»에 터졌다.** `hwpxMakeSourceCodes` 는 못 냈을 때 `셈` 을 안 주는데
>   그것을 먼저 찍어 `TypeError` 로 죽었다 — **까닭을 말하는 코드가 까닭 때문에 죽었다.**
>   그래서 03·04·05 가 왜 막히는지 한동안 안 보였다. `뽑은것.셈` 을 쓰게 고쳤다.
> 🔴 **② 1990년대 기출을 못 읽었다.** `20[0-9]{2}` 만 받아서 `1994년 11월 11번` 세 문항의 출처를
>   놓쳤고, 미주 135 대 출처 132 로 어긋나 **135제가 통째로 막혔다.** 한 해 표기 때문이다.
>   🔵 코드 쪽은 이미 19xx 를 알고 있었다(`년<=30 ? 20xx : 19xx`) — 막고 있던 것은 그 한 줄뿐이었다.
>   실측: `1994년 11월 11번` → `1941111OR`, 화면에도 「1994년 11월 11번」으로 뜬다.
> 🔵 **③ 이제 도구가 «어디»인지 짚는다** — 멈출 때 어긋난 덩이를 **앞뒤와 함께** 보여 준다.
>   셈만 말하면 「132 대 135」뿐이라 사람이 고칠 수가 없다.
>
> **남은 넷 — 전부 «출처 표기»가 어긋난 것으로 보인다(기계가 못 정한다).**
> 🔵 **번호는 «미주 차례»다** — 교재의 문항 번호는 한글 자동 번호(`1)` `2)` …)라 글에 안 들어
>   있다. 미주는 문항마다 하나씩이므로 **n번째 미주 = 교재 n번**이다(정답으로 맞대 보면 확실하다).
>
> | | 자리 | 무슨 문제인가 |
> |---|---|---|
> | **ㄱ** | **02단원 67·68·69번** 「2016년 3월 21번」 (정답 ⑤·②·②) — 정사각형 종이접기<br>**03단원 91·92·93번** 「2016년 3월 21번」 (정답 ④·①·①) — 반지름 1인 원의 중심 | **전혀 다른 문제인데 출처가 같다.** 한쪽이 틀렸다 |
> | **ㄴ** | **01단원 1·2번** 「2023년 9월 28번」 (정답 $116$·④) — x축 위 네 점 정사각형<br>**03단원 110·111번** 「2023년 9월 28번」 (정답 $17$·$17$) — 삼각형 무게중심 | 마찬가지. ⚠ **01 은 이미 창고에 담겨 있다** |
> | **ㄷ** | **04단원 1번** [OR] 「2016년 **3월 28번**」 정답 $26$ — 삼각형 $OAB$ $A(4,0)$ $B(0,3)$, $A'(9,2)$<br>**04단원 2번** [NC] 「2016년 **10월 26번**」 정답 ③ — 같은 문제, 숫자만 $A(8,0)$ $B(0,6)$ $A'(18,4)$<br>**04단원 3번** [UP] 「2016년 **10월 26번**」 정답 $244$ | **셋이 한 묶음인데 1번만 출처가 다르다** |
> | **ㄹ** | **05단원 86번** [OR] 「2024년 10월 30번」 정답 $36$ — $\vert f\vert=1$<br>**05단원 87번** [OR] 「2022년 **3월 30번**」 정답 $50$ — $\vert f\vert=**3**$ | **87번은 86번의 숫자변형처럼 보인다** — 딱지가 OR 이 아니라 NC, 출처는 「2024년 10월 30번」이어야 할 듯 |
> ⚠ **(별건) 04단원 49·50번** 「2020년 9월 13번」 [NC][UP] — 본문이 서로 달라 **원본이 교재에 안 실린 것**으로 보인다.
>   이것이 정상이라면 「원본이 묶음마다 하나」 규칙을 «하나 이하»로 풀면 된다.
>
> 🔴 **혼자 정하지 않았다.** 코드는 곧 출처라, 잘못 정하면 **서로 다른 문제가 같은 코드**를 받는다 —
>   「이미 인쇄해 나간 시험지의 코드가 다른 문제를 가리킨다」와 같은 종류의 위험이다.
>   ⚠ ㄴ은 **이미 창고에 담긴 01 단원**에도 걸린다(`1230928OR` 이 이미 있다).
> ⚠ **아무것도 안 심었다** — 장부도 안 고쳤고 새 hwpx 도 안 냈다. 사용자 답을 듣고 한 번에 할 것.
>
> 🔵 **오늘(09-06) 끝난 것** — ①웹 ②꼴 올리기 · ②AI 채우기(12건 만들고 내용까지 검산) ·
>   ③수식 빨간 것 0개 · ④교재 재업로드 없이 창고 수식 고치기. 아래 상자에 자세히 적었다.
> ⚠ **`project2/`(1.5GB)는 사용자가 「지울거야」라고 했다** — 미완성 별건 작업이라 **내가 안 지웠다.**

> ### 🔴 **B-2 — 웹으로 올렸더니 원본 24개가 덮였다. 화면은 「71개 담았습니다」라고 했다** (2026-09-06)
> **이 세션에서 가장 위험했던 자리다.** 내가 직접 올려 보다 냈고, 창고를 직접 읽어서야 알았다.
>
> 🔴 **까닭** — `icFillBodies` 가 문서 열쇠를 `splitItemCode(code).origin` 으로 «접고» 있었다.
>   ①꼴에서는 옳다(교재에 원본만 실리고 변형은 `variants` 컬렉션에 산다).
>   **②꼴에서는 재앙이다** — 기출 교재에는 원본과 변형이 **나란히** 실려 있고 둘 다 `items` 의
>   제 문서를 가지는데 `splitItemCode('1230928NC01').origin` 은 `1230928OR` 이다.
>   → 한 묶음의 변형들이 원본 문서를 **차례로 덮어썼다.** 실측: `1230928OR` 에 `1230928UP02` 의 글.
>   71개를 올렸는데 **문서는 24개만 바뀌었다**(= 출처 묶음 수).
>
> 🔴 **화면이 거짓말을 한다 — 눈으로는 못 잡는다.** 「71개 문항의 본문을 담았습니다.
>   그림 44개도 함께 담았습니다」라고 또렷이 말했다. `saved` 는 «쓰기가 성공한 횟수»라
>   같은 문서에 71번 써도 71이다. **창고를 읽어 `updatedAt` 을 세어 보고서야** 알았다.
>   → **웹으로 담은 뒤에는 화면 말을 믿지 말고 창고를 세어 볼 것.**
>
> 🔵 **고침** — `itemDocKey(code)` 를 세우고 세 자리가 다 그것을 부른다(규칙이 셋이면 또 갈린다).
>   ②꼴은 코드를 그대로 쓴다 · ①꼴은 예전대로 접는다. 검사가 «한 기출의 형제 넷이 서로 다른
>   문서로 가는가»를 붙든다(`tools/exam-type-test.mjs` 50벌).
> ⚠ **node 쪽(`hwpx-push.mjs`)은 처음부터 코드를 그대로 써서 안 났다** — 같은 일을 두 길로
>   하면 한쪽만 틀리는 일이 생긴다. 그래서 검사는 «화면 함수»를 떠서 봐야 한다.
>
> ✅ **되돌리고 다시 올렸다** — 원본 24개의 본문·정답을 파일에서 도로 넣고, **엉뚱하게 붙은
>   그림 17개를 뗐다**(그건 형제의 그림이라, 안 떼면 「있던 것이 이긴다」 규칙에 걸려 남는다).
>   그다음 고친 코드로 다시 올려 **71/71 본문 일치 · 그림 44 · 정답 58 · 형제 넷이 서로 다른 글**.

> ### 🔵 **2026-09-05 — 모의고사 기출 교재(주기나)를 통째로 들였다. 배포·확인 끝**
> 커밋 `d2fcbfb` → `252f7f3` → `ca7ff2b`. 검사 **18벌 전부 통과**.
> 창고: **items 633 · 장부 635제**(엔딩크레딧 564 + 모의고사 기출 71).
>
> **무엇을 만들었나** — 「2017년 09월 21번」이라는 출처와 문항 오른쪽 위의 **그림 딱지**를
> 읽어 `1170921OR` · `1170921DW01` 꼴 코드를 만든다. 사용자가 정한 세 가지:
> ① 변형은 **언제나 두 자리**(`NC01`) ② **3월 시행이면 2학년**, 그 밖은 1학년
> ③ 단원은 **파일 이름에서**(`[1.평면좌표]` → 01), 단 우리 단원표와 **대조해서 안 맞으면 멈춘다**
>
> 🔴 **딱지는 «글»이 아니라 «그림»이다.** 글자로는 못 읽는다 — **sha256 으로만** 알아본다.
>   ⚠ 참조 이름(`image8`)에 기대면 안 된다 — 파일마다 다르게 붙는다
>   ⚠ `shapeComment` 도 못 쓴다 — 넷 다 「번호.png」로 같다(실측)
>   🔵 **열어 보기 «전»에 검산할 길이 있었다** — 원본(OR)은 출처 묶음마다 정확히 하나여야 한다.
>     서로 다른 출처 24개에 `image8` 이 24개, 묶음마다 하나씩. 그래서 OR 을 먼저 알아냈고, 열어 보니 맞았다.
>
> 🔴 **딱지는 그 문항의 «뒤»에 적힌다.** 화면에서는 우측 상단이지만 떠 있는 개체라 미주보다 나중이다:
> ```
> ▣미주(116) 딱지[OR] 출처(23-09-28) ▣미주(④) 딱지[NC] 출처(23-09-28) …
> ```
>   「앞에서 마지막으로 본 출처」를 쓰면 **첫 문항이 통째로 빠지고 마지막 딱지가 주인 없이 남는다.**
>   🔴 **내가 실제로 그렇게 냈고, 「UP 이 XML 엔 14개인데 13개」라고 적어 두고 넘어갔다.**
>     사용자가 「미주가 71개인데 70개로 인식된거야?」라고 물어서야 잡혔다 —
>     **세어 보고 안 맞으면 절대 넘어가지 말 것.** 그 1개가 첫 문항이었다.
>   고침: 미주·출처·딱지를 각각 모아 **지퍼처럼** 맞춘다(셋 다 71). 수가 다르면 멈춘다.
>
> 🔵 **「변형끼리 엮어 줄까」의 답이 여기서 나왔다** — **닮음을 잴 필요가 없다.**
>   앞 7자리가 같으면 한 기출에서 나온 형제다(`srcSiblings`). 교재가 이미 적어 둔 관계다.
>   ```
>   1230928OR · 1230928NC01 · 1230928UP01 · 1230928UP02
>   ```
>
> 🔵 **거르개를 새로 안 만들었다** (사용자 요청 — 「같이담긴하지만 모의고사 기출을 필터링할수 있으면」).
>   창고는 `source.book` 으로 거르는 줄을 이미 그리고 있어서, 장부에 「모의고사 기출」로 적으니
>   **거르개가 저절로 생겼다.** 화면 코드는 한 줄도 안 늘었다.
>
> 🔴 **코드 «꼴»이 이제 둘이다 — 손대는 자리를 다 알아야 한다.**
>   ① `K2-01-E-0013` 줄표 있음 · ② `1230928UP01` 줄표 없음. 생김새로 절대 안 겹친다.
>   ②에는 **과목·단원 자리가 없다**(기출 번호에 그런 자리가 없다) → 장부가 들고 있어야 한다.
>   그래서 `storeSubjectCode` 를 «코드»가 아니라 **«항목»을 받게** 바꿨다. 코드에서 짐작하면
>   기출 71제가 통째로 «과목 없음»이 되어 창고에서 사라진다.
>   🔴 **`HWP_CODE_RE` 도 ①꼴만 알고 있었다** → 본문이 «0건» 나왔다. ②를 안 떼면 코드가
>     정답 글자에 통째로 섞인다 — ①에서 이미 겪은 그 흠이다.
>
> 🔴 **족보닷컴 워터마크가 «수식 안»에 숨어 있었다** — 2문항(`1230928NC01` `1140927UP01`).
>   거른 것은 규칙대로지만, 앞 판은 그것을 「심기가 빗나갔다」고 말해 **엉뚱한 데를 뒤지게 했다**
>   (내가 실제로 그랬다). 이제 걸린 문항 «이름»을 대고, 까닭 없는 것만 경고한다.
>   ⚠ **그래서 창고에 담긴 것은 71 중 69다.** 코드는 71개 다 있으니 필요하면 손으로 넣으면 된다.
>
> **새 파일이 오면 두 줄이다:**
> ```
> node tools/source-code.mjs "<파일.hwpx>" --out codes/K2-J-02.json
> node tools/item-code-stamp.mjs "<파일.hwpx>" codes/K2-J-02.json --chapter 02 --out "<코드.hwpx>" --push
> ```
> 첫 줄은 **원본을 한 글자도 안 고친다** — 코드를 눈으로 본 뒤에 둘째 줄을 돌린다.
> 심고 나면 **심은 파일을 다시 읽어** 제자리인지 확인할 것(71/71 확인했다).
>
> 새 도구·검사: `tools/source-code.mjs` · `tools/source-code-test.mjs`(22) · `tools/source-store-test.mjs`(17).
> ⚠ `source-store-test` 는 화면 함수를 **index.html 에서 글로 떠서** 돌린다 — 옮겨 적지 말 것.

> ### ▶ **2026-09-06에 이어서 할 것** (위에서부터)
> 2. ✅ **09-06에 재 봤다 — 까닭을 받아 왔다. 그런데 답이 뜻밖이었다.**
>    · **후보는 563개 잡힌다** (화면 함수를 그대로 떠서 실측). 고리가 돌 거리는 있다.
>      ⚠ 정답이 없는 24개는 **모의고사 기출의 원본(OR)** 이다 — 기출 OR 에는 미주 정답이 없다.
>    · 🔴 **워커가 «가끔» `502 gemini error` 를 낸다.** 두 번 불러 한 번 엎어졌고,
>      **똑같은 입력이 다음 번에는 그대로 됐다** — 요청이 잘못된 것이 아니라 위쪽(Gemini)이
>      가끔 엎어지는 것이다. 엎어져도 **한도는 한 건 나간다**(실측: 실패 뒤 quotaUsed 가 늘었다).
>    · 🔴 **진짜 까닭은 «브라우저가 먼저 포기하는 것»이었다** (사용자가 「두번연속 안되는데」
>      라고 해서 다시 팠다. 내 첫 짐작 「가끔 엎어진다」는 **표본 둘로 낸 성급한 답**이었다).
>      성공한 호출이 **31.9초** 걸리는데 브라우저 시간제한이 **45초**였다 — 여유가 13초뿐이다.
>      node 도구에는 시간제한이 없어서 **도구로는 되고 화면에서만 안 되는** 모습이 나왔다.
>      🔴 **먼저 포기해도 한도는 이미 나갔다** — 워커는 받자마자 세고 나서 일한다. 실측:
>        내가 2건 쓴 뒤 사용자가 두 번 눌렀더니 `/quota` 가 4가 됐다(요청은 다 갔다는 뜻이다).
>      → **180초로 늘렸다.** 기다리는 것은 공짜고, 그만두면 한 건을 버리고 아무것도 못 얻는다.
>    · 🔴 **화면이 까닭을 삼키고 있었다** — 워커는 `detail` 에 Gemini 말을 담아 보내는데
>      `generateTwinViaAI` 가 `error` 만 집어서 언제나 「gemini error」 다섯 글자만 떴다.
>      시간 초과 때는 그냥 `timeout` 이었다. 09-04에 「왜 안 됐는지를 버리지 않는다」고 적어
>      놓고 **한 칸 덜 간 자리**다. `detail` 을 붙이게 고쳤다(`tools/probe-worker.mjs` 도 같이).
>    · 🔵 **첫 실패에서 멈추던 것을 «한 번은 다시 해 본다»로 바꿨다.** 버리는 것은 최대 두 건이라
>      09-04에 겪은 스무 건과 다르다. `tools/autofill-test.mjs` 가 그 «두 건»을 붙든다(45벌).
>    · 🔵 **한도는 «uid 별»이 아니라 통째로 하나였다** (전에 두 번 잘못 적었다). 실측 —
>      익명 로그인을 새로 해도 `/quota` 가 같은 수를 돌려준다(갓 만든 uid 가 `used:2` 를 본다).
>      ⚠ 그러니 **도구로 재는 것도 사용자의 하루치를 쓴다.** 09-06에 내가 2건을 썼다.
>    · `/quota` 는 **세지 않고 보기만 한다** — 워커가 살아 있는지는 그것으로 공짜로 잰다.
> 3. **워터마크 2문항** — 창고 「고치기」로 손수 넣을지는 **사용자가 정할 일**이다. 먼저 묻지 말 것.
> 4. (보류) **새로 담기는 문항만 기존 것과 닮음 대조하는 화면** — 사용자와 «그게 낫다»고 합의했고,
>    교재가 더 쌓인 뒤로 미뤄 뒀다. `tools/similar-items.mjs --show` 가 이미 있다.


> ### 🔵 **2026-09-04 저녁까지 한 것 — 사용자가 화면에서 짚은 아홉을 다 고쳤다**
> 배포·확인까지 끝났다. 다시 안 해도 된다. **다음에 이어서 할 것은 이 상자 아래를 볼 것.**
>
> | | 무엇 | 실측 |
> |---|---|---|
> | 1 | 🔴 **변경 이력이 안 읽히던 것** — 인덱스를 요구하는 질의였다(한도 아님) | 175건 뜬다 |
> | 2 | **관리자 화면을 접었다** — 평소 한 줄, 이상하면 저절로 펴진다 | |
> | 3 | 문항 창고 **과목 › 단원 층**이 보이게 · **한 줄에 두 문항** | |
> | 4 | **(가)(나) 빈칸**이 사라지던 것 — 그건 «사각형 도형»이었다 | 40문항 |
> | 4 | 문제 뒤에 딸려 오던 **유형 제목 · SCENE 딱지** | 122→10 · 10→0 |
> | 5 | **수식이 안 그려지던 것** — 뿌리는 «붙어 버린 낱말» | 짝 안 맞음 69→0 |
> | 6 | **선지 격자** — 간격이 일정하게 | 365문항 |
> | 7·8 | 검토 큐 둘을 없앴다(공개됨 · AI가 만든 변형→검토 대기로) | |
> | 9 | 「다 찼습니다」가 거짓말이던 것 — 까닭을 갈라 말한다 | |
>
> 🔴 **오늘 세 번 같은 자리에서 넘어졌다 — 배포본에서 안 보고 «검사 통과»로 넘긴 것이다.**
>   ① `orderBy __name__ DESC` 가 인덱스를 요구 ② `referenceValue` 에 호스트가 붙음
>   ③ 그 둘을 잡겠다고 만든 probe 가 **질의를 스스로 새로 만들어** 보내서 앱과 다른 것을 검사했다
>   → **`tools/firestore-probe.mjs` 는 이제 index.html 의 함수를 글자로 떼어 와 그대로 부른다.**
>     질의 «모양»을 바꾸면 배포 전에 이것부터 돌릴 것. 스텁은 인덱스도 규칙도 모른다.
>
> 🔴 **`hwpx.js` 를 고치면 `?v=` 도 같이 올릴 것.** index.html 과 따로 받아지는 파일이라
>   그대로 두면 브라우저가 **캐시의 옛 파서**를 쓴다 — 교재를 다시 올려도 옛 글이 담긴다.

> ### 🔴 **기록이 지워지던 자리를 막았다 — 그리고 백업이 생겼다** (09-04)
> 사용자가 「출석도장 … 일부 없어진것 같아」라고 물어서 찾았다. **진짜로 지워져 있었다.**
> `dbGetCollection` 은 읽기가 엎어져도 빈 배열을 주는데, `seedTestAccountIfEmpty` 가 그것을
> «학생이 없다»로 읽고 씨앗을 심으면서 **`record:test` 를 `emptyRecord()` 로 덮었다.**
> 증거 — `rank:2026-08-17`·`rank:2026-08-24` 에는 도장이 남아 있는데 기록에는 없다.
>
> 🔴 **막이를 «쓰는 문»에 세웠다** — 같은 자리가 셋이었다(씨앗 · 학생 추가 · 명단 가져오기).
>   자리마다 막으면 네 번째가 생긴다. `saveRecord` 에서 «빈 것을 쓰려는데 이 열쇠를 읽어 본 적이
>   없으면» 한 번 읽어 보고, 저장된 것이 비어 있지 않으면 막는다.
> 🔵 **`tools/backup.mjs` · `tools/backup-diff.mjs`** — 이번에 답을 못 한 까닭이 «견줄 것이
>   없어서»였다. 큰 작업 전에 한 번 받아 둘 것. 백업/ 는 .gitignore.
>   ⚠ diff 가 「통째로 없어졌다」고 크게 울면 **폴더 이름부터 확인할 것** — 한 번은 셸 변수가
>     비어서 빈 폴더와 견준 것이었다(오경보). 도구가 시끄러운 것은 의도한 대로다.

> ### 🔵 **창고를 node 에서 바로 채운다 — `tools/items-push.mjs`** (09-04)
> 사용자가 밖에 있어 hwpx 가 없다고 해서 만들었다. 브라우저도 교사 로그인도 필요 없다.
> 🔴 **기존 `item-bodies.mjs --push` 를 그대로 쓰면 안 된다** — `{code, content, updatedAt}` 만
>   써서 **그림 54개와 정답 564개를 통째로 날린다.** 그래서 «먼저 읽고 → 합치고 → 쓴다».
> ⚠ 그림은 여기서 못 만든다(드라이브 업로드는 브라우저의 일). 있던 것을 그대로 옮겨 싣는다.
> 지금 창고: **items 564 · 본문 564 · 정답 564 · 그림 54.**

> ### ▶ **다음에 할 것**
> · ✅ **AI 자동 채우기가 실제로 돌았다 — 그리고 내용을 읽어 봤다** (2026-09-06 01:23~01:25).
>   시간제한을 180초로 늘린 뒤 사용자가 켜서 **12건**이 만들어졌다
>   (`K2-01-E-0001~0009`·`0011~0013` 의 N01). 그다음은 하루치를 다 써서 멈춘 것이고 흠이 아니다.
>   🔴 **수를 세는 것과 내용을 보는 것은 다른 일이라, 다섯을 손으로 풀어 봤다 — 다 맞다.**
>     · 0001 `(2a+1)²+9=58` → `a=3` ✓  · 0002 두 근 9·1 의 합 `10` ✓  · 0003 `P(-6,0)·Q(0,8)` → `10` ✓
>     · 0005 외심 `(2,2)`·`r²=10` → `10π`(②) ✓  · 0011 두 점 `(-2,-1)`·`(4,7)` 사이 거리 `10` ✓
>   🔵 **꼴이 바뀌는 것은 «흠이 아니라 설계»다** — 객관식 원본이 주관식 변형이 된다
>     (데일리퀴즈는 학생이 답을 «치는» 화면이라 `dqAnswerable` 을 통과해야 나간다).
>   ⚠ **이것들은 검토를 «안 거치고» 창고로 갔다** — 09-04에 적어 둔 그 위험이 실제로 실행됐다.
>     지금까지는 좋았다. 그래도 **쌓일 때마다 몇 개는 손으로 풀어 볼 것.**
>   ⚠ 다음 바퀴는 `K2-01-E-0014` 부터다(`0010` 은 09-03에 N01~N06 이 이미 있다).
> · ✅ **수식에 남은 넷 — 09-06에 파서에서 고쳤다.** 「고치기」로 손댈 일이 아니었다.
>   564제를 다시 훑어 보니 **넷이 아니라 셋**이었다 — `abx` 는 흠이 아니라 진짜 변수 곱(a·b·x)이다.
>   ① `2rmbar{AC}` — `\brm` 이 «숫자에 붙은 rm» 을 놓쳤다(K2-02-E-0171 · 덤으로 `4rm km` 도)
>   ② `alphabeta+gammadelta` — 붙여 쓴 그리스 문자(K2-03-E-0278)
>   🔴 **셋 다 «같은 문항 안에서 띄어 쓴 쪽은 멀쩡»했다.** 그래서 눈으로는 원인이 안 보인다.
>   🔴 고치다가 한 번 틀렸다 — 뒤따르는 옛 고리가 방금 만든 `\alpha` 를 또 바꿔 `\alpha` 가 됐다.
>     이 파일이 곳곳에서 경고하는 그 함정에 그대로 걸렸다. 검사가 이제 그것을 붙든다.
>   ✅ **564제를 통째로 다시 뽑아 견줬다 — 바뀐 것이 딱 3문항, 셋 다 의도한 것.**
>   🔴 **③ 대문자 `RM` 이 통째로 남아 있었다** — 창고를 읽어 보다 눈에 걸렸다(찾으러 간 것이 아니다).
>     `$RMO \left( 0,~0 \right)$` — 화면에 「RMO」로 뜬다. **660건 중 36건.**
>     한글 수식 명령은 대소문자를 안 가리는데 이 규칙만 소문자였다(DEG·TIMES·LEFT 는 이미 안 가린다).
>     고친 뒤 564제를 다시 뽑아 견줬다 — **바뀐 것 13문항, 전부 `RMA`→`A` 꼴.** 남은 것 0.
>   ✅ **그리고 「다시 올리기」 자체가 필요 없어졌다** — 사용자가 물어서 길을 냈다(아래 상자).

> ### 🔴 **2026-09-06 — 푸시가 밀려 있다. 4시 이후에 «한 번에» 할 것**
> 커밋 여섯이 쌓였는데 **아직 안 올렸다.** 09-06 낮에 Firestore 읽기 한도가 소진된 상태였고,
> 배포해도 **확인이 안 되는** 상태라 사용자와 「태평양 자정(한국 오후 4시경) 뒤에 하자」로 정했다.
> 푸시 뒤에 배포본에서 볼 것 (순서대로):
> 1. **설정 › 변경 이력** — 맨 위가 «오늘 것»인가. ← 이게 `runQuery` 가 도는지 보는 유일한 자리다.
>    ⚠ 안 되면 `auditlog` 가 「못 읽음」으로 찍히고 **그 컬렉션 쓰기가 막혀 이력이 안 쌓인다.**
> 2. **문항 코드 화면을 두 번 열고** 설정 › 관리자 › **읽기 아끼기** → 「재워 둔 것을 썼습니다 (읽기 1건)」
> 3. **문항 코드 › 정답 채우기** 로 `answer-key.json` 올리기 → 564개
> 4. 그다음 **「남는 한도로 채우기」 켜기**
>
> ### 🔴 **곁가지로 흠 하나를 찾아 같이 고쳤다 — 「최근 300건」이 실은 «가장 오래된» 300건이었다**
> Firestore 목록은 **문서 이름 오름차순**으로 온다. 그걸 그대로 `slice(0,300)` 하고 있었으니
> 변경 이력 화면은 **맨 처음 300건**을 「최근 300건」이라 적어 놓고 보여 주고 있었다.
> (새로 찍히는 것은 `unshift` 라 맨 위에 붙어서, **오늘 것은 맨 위에 있고 그 아래는 옛날 것**인
> 이상한 줄이었다 — 그래서 여태 안 드러난 것 같다.)
> 🔵 **정렬은 `__name__` 으로 한다** — 문서 안이 `value` 글자 하나뿐이라 `at` 으로는 못 세운다.
>   로그 id 가 `log` + `Date.now()` 라 **글자 순서가 곧 시간 순서**다(13자리 고정).
>   🔴 **id 모양을 바꾸면 이 정렬이 조용히 깨진다.** 검사표에 그 가정을 못 박아 뒀다.
>
> ✅ `tools/auditlog-recent-test.mjs` — **14 통과 0 실패**. DB 를 한 건도 안 읽는다.
> ⚠ **브라우저로는 아직 안 봤다** — 볼 곳은 설정 › 변경 이력. 묻는 것 셋:
>   맨 위가 «오늘 것»인가 · 「최근 300건」이라 적히나 · 「전체에서 찾기」가 도나.
>
> ### 🔵 **#2 에 남은 것 — 나머지 컬렉션은 «캐시»가 답인지 아직 모른다**
> `problembank` 는 자라지만 «문항이 늘 때»만 바뀌므로 #1 과 같은 «버전 한 건» 수법이 맞는다.
> ⚠ **클리닉·질문은 캐시하면 안 된다** — 낡은 화면을 보게 된다. 골라야 한다.
> ⚠ 그리고 `migrateBlobToCollection` 은 **컬렉션이 비면 옛 kv 블롭까지 한 번 더 읽는다** —
>   이제 안 쓰는 옛 열쇠라면 그 한 건도 매번 새는 것이다. 언젠가 걷어낼 것.

> ### 🔵 **2026-09-05 아침에 여기서 시작했다 — 읽기량 줄이기 (#1 ✅ · #2 남음 · 까닭은 여기 적혀 있다)**
> 09-04 밤에 **Firestore 무료 읽기 한도(하루 5만 건)를 다 써서 429 가 났다.** 앱은 모든
> 컬렉션을 빈 배열로 읽었고 화면은 「학생 1명 · 기록 0건」으로 **멀쩡하게** 그려졌다 —
> 사용자가 「그동안의 기록들도 거의다 사라졌어」라고 물어볼 만큼 진짜 삭제처럼 보였다.
> **데이터는 하나도 안 없어졌다.** 태평양 자정(한국 오후 4시경)에 저절로 풀린다.
>
> 그날 밤 **#3(경고 띠)·#4(컬렉션 쓰기 차단)는 만들어서 푸시했다**(`d574b01`).
> 남은 것이 읽기량 자체를 줄이는 둘이다:
> · **#1 `items` 캐시 (제일 큼)** — 지금은 문항 코드·문항 창고 화면을 열 때마다 564개를
>   다시 읽는다. `items` 는 교재를 올릴 때만 바뀌는 **거의 불변**이라, 버전 문서 1건만
>   확인하고 브라우저에 캐시하면 **564건 → 1건**이 된다. 문항이 5,000개가 돼도 1건이다.
> · **#2 `loadAll` 선별 캐시** — 앱을 한 번 열면 컬렉션 통째 읽기 13번 + 단일 문서 19번이
>   나간다(실측). 자주 안 바뀌는 것(problembank 등)만 골라야 한다 — 클리닉·질문까지
>   캐시하면 낡은 화면을 보게 된다.
> 🔵 **읽기 과금은 «요청 수»가 아니라 «돌려받은 문서 수»**다. 그래서 커지는 것이 곧
>   비용이다 — `problembank`(문항마다 1개 영구 누적) · `items`(564 → 수천) · 학생 기록(N명).
> 🔵 사용자는 **무료 유지 + 코드로 해결**을 골랐다(Blaze 전환은 안 한다).
> ⚠ **테스트하다 한도를 태운 것은 Claude 쪽이 컸다** — 새로고침 한 번에 600건 넘게 나간다.
>   앞으로 로컬 테스트는 새로고침을 아끼고, 값 주입(state 직접 세팅)으로 대신할 것.

> ### 🔵 **정해 둔 것 — 보충문제 창고는 «뒷길»로만 남긴다** (09-04)
> 사용자가 정했다 — 「앞으로 계속해서 코드를 통해서 문항을 쌓아갈 것 … 코드 없는 문항은
> 내가 선별해서 코드를 붙일생각이야」. 그래서 `DATA.unitBank` 와 `submitWrongAnswers` 의
> 뒷길은 **살려 뒀지만 넣고 빼는 화면은 내렸다**. 옛 시험지는 사용자가 다 지울 예정이라
> 그 길이 도는 일도 점점 없어진다. 완전히 걷어낼지는 그때 다시 정한다.

> ### 2026-09-06 아침에 여기서 시작한다
>
> ### 🔵 **그다음 — 모바일에서 상자를 다시 볼 것**
> 09-05 밤에 조건·보기 상자를 고쳤다(223/564 문항이 상자를 갖고 있었다). **사용자가 짚은 그 화면에서**
> 다시 봐야 한다 — 내가 본 것은 데스크톱이다.
> ⚠ 폰에서 표는 옆으로 넘칠 수 있다. 넘치면 `overflow-x:auto` 상자를 씌우는 것이 답인데,
>   **3절의 「자르는 상자도 position:relative 여야 한다」(KaTeX 함정)**를 같이 봐야 한다.

> ### 🔵 **그림 있는 문항 — 셌다. 70 / 564 (12%)**
> 09-05 에 사용자가 물었고(「캡쳐를 떠서 원본 형태 그대로?」), **글이 주력**이라는 데까지는 정해졌다.
> 까닭은 워커가 `content` 없이는 **아예 거절**하기 때문이다(`missing content/answer`) —
> **이미지만 담으면 그 문항으로는 변형을 못 만든다.** 창고를 만든 까닭이 변형인데 그게 막힌다.
> 🔵 **그래서 남은 일이 564장이 아니라 70장이 됐다.** 정할 것은 하나다 —
>   **그 70개를 이미지로도 담을 것인가.** 담는다면 드라이브 업로드(워커 `/upload`)가 붙고,
>   node 도구로는 못 구우므로 **웹에서 하는 수밖에 없다**(브라우저 캔버스가 유일한 길이다 — 7절 참고).
> ⚠ 세는 규칙은 「되풀이 쓰인 참조는 장식」이었다. **문항마다 번호 딱지가 붙어 있어** 안 가르면 100%로 나온다.
>
> ### 🔵 **사용자가 낸 다른 길 — 「파일 위치를 자동 인식」 (나)**
> 「매번 업로드하지 말고 파일 위치를 자동으로 인식해서 그때그때 가져올 수 없나」.
> **된다** — File System Access API(`showDirectoryPicker`). 교재 폴더를 한 번 고르면 브라우저가 기억한다.
> 파서는 이미 브라우저 안에 있으니 붙이기만 하면 된다.
> ⚠ 조건: **Chrome·Edge 데스크톱만** · 세션마다 허용 한 번 · 파일을 옮기면 다시 고르기.
> 🔵 **지금은 (가)가 맞다** — 564제가 한 번에 차면 그 뒤로 hwpx 를 안 만진다.
>   (나)가 값을 하는 때는 **교재가 계속 바뀌는 동안**이다. 그때 다시 꺼낼 것.
>
> ---


> ### 2026-09-05 아침에 여기서 시작한다
>
> ### 🔴 **첫 일 — 저장이 막혀 있다. 이게 안 풀리면 창고는 전부 헛돈다.**
> Firestore 규칙에 새 컬렉션 둘이 없다. **코드로는 못 푼다 — 사용자가 콘솔에서 게시해야 한다.**
> ```
> problembank  200   ← 기존 컬렉션은 열려 있다
> variants     403   ← 「창고에 넣기」가 쓰는 곳
> items        403   ← 원본 «본문»을 담을 곳
> ```
> 더할 것 (이미 있는 줄은 그대로 두고 두 덩이만):
> ```
> match /variants/{doc} { allow read, write: if request.auth != null; }
> match /items/{doc}    { allow read, write: if request.auth != null; }
> ```
> ⚠ **「게시」를 눌러야 걸린다** — 편집만 하고 나오면 그대로다.
> 🔵 확인은 **설정 › 관리자 › 「저장이 막힌 곳」 → 「지금 확인」** — 둘 다 「열림」이면 된다.
> ⚠ **그 전에는 「창고에 넣기」가 실패한다** (이제 «못 넣었다»고 말은 한다 — 예전엔 넣었다고 거짓말했다).
>
> ### 🔵 **그다음 이야기할 것 — 원본 문항을 «코드 심을 때 같이» 저장하기**
> 사용자가 말했다 — 「원본 교재를 **추후에 업로드 하는 게 아니라** 기존에 **코드 삽입할 때 같이**
> 원본문항을 **이미지 저장 또는 웹페이지에 저장**하고 싶다」.
>
> **지금 만들어 둔 것은 «나중에 올리는» 길이다** — 문항 코드 화면의 「교재에서 본문 채우기」.
> 사용자가 원하는 것은 **도구가 코드를 심는 그 순간에 함께 담는 것**이다. 한 번에 끝난다.
>
> ⚠ **먼저 물을 것 — «이미지»인가 «글»인가.** 갈리는 자리다:
> | | |
> |---|---|
> | **글(텍스트)** | 지금 `items` 에 담는 것. 검색되고 가볍다. ⚠ 그림이 든 문항은 그림이 빠진다 |
> | **이미지** | 보이는 그대로다. ⚠ 검색이 안 되고, **드라이브 업로드가 필요하다**(워커 `/upload`) |
> | **둘 다** | 글로 담고 그림 있는 것만 이미지도. ⚠ 두 벌이 된다 |
> 🔵 **문항 수를 먼저 셀 것** — 564제다. 이미지로 담으면 그 수만큼 드라이브에 올라간다.
>   `rptSaveOne` 처럼 한 장에 100~300KB 라면 **564장이면 100MB 안팎**이다. 저장용량은 Google One 을 쓴다(9절 2-B).
> ⚠ **도구(`item-code.mjs`)는 node 로 돈다 — 브라우저가 없다.** 이미지로 구우려면
>   한글 COM 으로 뽑거나(문항마다 잘라야 한다) 웹에서 하는 수밖에 없다. **이 제약을 먼저 말할 것.**
>
> ### 오늘(09-04) 끝낸 것
> 사용자가 실제로 쓰면서 짚은 것을 계속 고쳤다 — I-7 부터 I-13 까지. 자세한 것은 맨 위 09-04 묶음에.
> 🔴 **오늘 세 번 놓친 것 하나** — 고칠 자리를 셀 때 **«이미 저장된 것»을 세는 것**.
>   `[5.0점]`·`sqrt`·정답 속 해설 셋 다 «새로 들어오는 것»만 고쳤다가 다시 손봤다.
>   → **정리 도구**(설정 › 데이터 정리)가 옛 기록을 맡는다. 규칙이 풀리면 그것부터 한 바퀴 돌릴 것.
>
> ---
>
> ### 2026-09-02 아침에 여기서 시작한다
>
> ### 🔴 **첫 일은 «종이와 파일»이다 — R-2b·R-3을 아직 눈으로 못 봤다**
> 둘 다 **검사판으로는 다 맞았다.** 하지만 **실서비스에서 한 번도 안 돌렸다** —
> 09-01의 ①이 말하는 그 자리다(«검사로는 확인했다»는 «봤다»가 아니다).
>
> **① 인쇄(R-2b)** — 반 › 학생 › 아무 학생 › 「이번 달 리포트」 › 「인쇄」.
> 미리보기에서 셋만 묻는다: 문서만 나오나 · 막대 색이 살아 있나 · 둘째 장이 안 잘리나.
> ⚠ 자동화로 `window.print()`를 **부르지 말 것** — 대화상자가 세션을 막는다.
>
> **② 사진 묶음(R-3·R-3b)** — 반 › 학생에서 **두세 명만** 체크 › 발판의 **「⤓ 리포트 이미지」** ›
> **달을 고르고**(9월 1일이면 「2026년 8월 · 기록 있는 학생 N명」) › 「폴더 고르고 N명 저장」.
> ⚠ **단추 이름은 「리포트 이미지」다** — R-4에서 PDF를 걷어냈다. 코드의 주석과 `rptBatch`라는
>   이름에는 아직 「PDF」가 남아 있지만 **나가는 것은 JPEG 한 장**이다.
> 🔴 **묻는 것 하나가 다른 것보다 크다 — 파일이 «사람 수만큼» 왔나.** 09-01에 3명이라 해 놓고
> 한 개만 왔던 자리다.
>   ✅ **09-02에 세어 봤다 — 셋 다 맞았다** (가짜 폴더로 실제로 쓰인 파일을 셌다):
>   3명→3개 · 20명→20개 · **기록 없는 3명을 섞은 20명→17개**(건너뛴 사람 앞으로는 파일이 안 생겼다).
>   20명이 **6.4초 · 3.1MB**다. ⚠ **그래도 «봤다»는 아니다** — 진짜 폴더는 사용자만 고를 수 있다.
> 그다음: 이름이 맞나 · **글자가 견딜 만한가** · 「선생님 한마디」가 들어갔나.
> ⚠ **처음엔 두세 명으로** — 스무 명으로 시작하면 어디서 어긋났는지 모른다.
> 🔵 **크기 걱정은 끝났다 (R-4b)** — 한 장이 **107~269KB**(384dpi JPEG)다. 스무 명이 3.1MB고,
>   950KB 한도에 안 닿아 **품질 낮추기 사다리가 한 칸도 안 내려간다.** 옛 기록의 「3.6MB」는 PDF 시절 값이다.
>
> **③ 교재(T-1·T-1b)** — 반 › 진도 › 과목을 고른 뒤 「교재」 칸에 한 권씩 넣는다.
> 그다음 **네 곳에서 보이는지** 본다 — 학생 홈(다음 수업 카드) · 학생 진도 · 학생 상세 오른쪽 칸 ·
> **월간 리포트**(진도 위).
> 묻는 것: **과목마다 따로 기억하는 것이 맞나**(아니면 반 하나에 한 벌) · 여러 권이 쓸모 있나 ·
> **학부모께 나가는 리포트에 교재가 실리는 것이 맞나**(빼기 쉽다 — 눈금 줄 하나다) ·
> 지난 달 리포트의 「지금 쓰는 책」이라는 곁말이 어색하지 않나.
> ⚠ **명단의 「반 진도」 열에는 일부러 안 넣었다**(행 높이 40px). 거기도 필요하다고 하시면
>   열을 넓히거나 두 줄을 허용하는 «표의 규칙»부터 바꿔야 한다 — 별건이다.
> ⚠ **수업 › 진도에도 교재가 뜬다**(T-1c). 거기는 «보는 것»만이다 — 넣는 문을 두 곳에 두지 않았다.
>
> ### 🔴 **④ 특강(E-1) — 심었다. 실서비스에서 한 바퀴 돌려 볼 것.**
> **순서대로 볼 것** — ⓐ 설정 › 반 추가에서 **갈래를 「특강」**으로 하나 만든다
> ⓑ 설정 › 학생 › 계정 수정에서 **「추가로 듣는 반」**에 그 특강을 체크한다
> ⓒ 명단에서 그 특강 반을 고르면 **그 학생이 뜬다** ⓓ 수업 탭에서 **특강 출결을 찍는다**
> ⓔ 명단의 **출석률이 안 움직이는지** 본다 (이것이 「가르자」의 증거다)
> ⓕ 그 학생 **리포트에 특강 구역이 뜨는지** 본다
> ⚠ **정규와 특강이 같은 요일인 반**으로 한 번 해 볼 것 — 같은 날 두 줄이 이 일의 핵심이었다.
> 🔴 **아직 못 물은 것** — 「특강만 듣는 학생」을 뭐라 부를지(지금은 임시로 「특강만」).
>   `SPECIAL_ONLY_LABEL` 한 줄이다.
>
> ### 🔴 그다음도 «듣는 것»이다 — **A · B · C 세 화면이 아직 밀려 있다**
> **묻기 전에 다음 것을 만들지 말 것** — 08-26 이후 이 방식이 **스물한 번** 값을 했다.
> ⚠ **아래 「물어볼 것」이 스물 몇 줄이다. 한 번에 다 묻지 말 것** —
>   **화면을 열고 그 화면 것만** 묻는다. 08-31에 배운 것이다: **구조를 물을 때는 언제나 화면을 짚는다.**
>
> #### 09-01에 배운 것 다섯 (다음에도 그대로 쓸 것)
> ① 🔴 **«검사로는 확인했다»는 «봤다»가 아니다** (C-21). 08-31에 ⚠로 남겨 둔 자리를
>   눌러 보니 **정말 어긋나 있었다.** 노트에 ⚠로 남긴 자리는 반드시 화면에서 끝까지 눌러 볼 것.
> ② 🔵 **무엇을 옮기기 전에 이미 어디에 있는지부터 세어 볼 것** (C-22). 사용자가 세 갈래를
>   주었는데 **네 줄 중 셋이 이미 벨에 있어서** 하나만 옮기니 끝났다. 새 단추가 필요 없었다.
> ③ 🔵 **보이게 만들었으면 고칠 수 있게 해야 한다** (C-16→C-17). 열 머리를 날짜로 바꾸자
>   그 값이 매일 보이게 됐고 사용자가 곧장 「고칠 수 있게」를 요구했다.
> ④ 🔵 **만들지 말고 물어 두는 것**도 값을 한다 (C-19→C-20). 끝에 물어만 두었더니 답이 왔다.
> ⑤ 🔴 **내가 낸 흠 셋** — ⓐ 제약(폭)이 이미 사라졌는데 그 전제로 고쳤다(「대」 한 글자·C-11→C-12)
>   ⓑ 어긋남을 **알림 줄로 설명하려 했다**(C-12→C-13) — **어긋남은 설명할 것이 아니라 없앨 것이다**
>   ⓒ **정렬이 바뀌는 목록의 첫 항목을 기본값으로 삼았다**(C-19) — 누를 때마다 화면이 바뀌었다.
>
> #### 🔵 09-01에 사용자가 말해 준 «화면의 성격» — 한 번의 고침이 아니라 기준이다
> **수업 화면** — 「수업하면서 빠르게 체크하고 입력하고 넘어가야하는 것들이라 복잡하지
> 않았으면 좋겠고 한눈에 직관적으로 파악되고 … **작은 글들이 너무 많으면 좋지 않을 것 같아** …
> **출결쪽처럼 학생별로 큼직하게**」. 수업 화면에 무엇을 더할 때마다 이 문장에 비춰 볼 것.
>
> ### 🔴 물어볼 것 — **화면을 열고 그 화면 것만**
>
> **A. 수업 탭** (C-18~C-20 · 어제 가장 크게 바뀐 곳)
> | | |
> |---|---|
> | **과제 검사 세 버튼이 손에 맞나** | 제출 · 보완 필요 · 미제출. **20명 반에서 스무 번 누르는 손**이 진짜 시험이다 |
> | **시험 점수를 수업 중에 실제로 치시나** | 만든 자리에서 바로 친다. 안 치면 «만들기»만 남기고 점수판은 접는 것이 맞다 |
> | **넘어갈 때 저절로 저장되는 것이 손에 맞나** | 토스트가 뜬다. 성가시면 조용히 넣는 쪽도 된다. ⚠ **상단 메뉴로 수업 탭을 통째로 떠나는 문은 아직 안 막았다** |
> | **일곱 단계가 많지 않나** | 폰에서는 옆으로 미는 띠가 된다 — 성가시면 「지난 수업 확인」을 접는 것이 첫 후보 |
> | **시험이 없는 날에도 「4. 시험」을 지나가는 것이 성가시지 않나** | 「오늘 본 시험 없음」을 한 번 누르는 길이 답일 수 있다 |
> | **과제 검사에서 제일 급한 것이 자동으로 펴지는 것이 맞나** | 매번 접게 되면 접힌 채로 두는 것이 맞다 |
>
> **B. 반 탭** (C-3~C-16 · 서브탭이 열이 됐다)
> | | |
> |---|---|
> | 🔴 **반 › 학생**이 이제 안 튕겨 나가나 | 짚으신 그 자리다. **09-01에 물었으나 「아직 안 눌러 봤다」 — 다시 물을 것** (내가 눌러 본 것은 안 튕긴다) |
> | **서브탭이 열이 됐다** | 08-31에 아홉일 때 「괜찮다」고 하셨는데 **열은 어떤지** |
> | **반 › 상담을 실제로 쓰시나** | C-22. **«누구와 아직 이야기 안 했나»**를 보는 자리 — 한 번도 없는 사람이 맨 위다 |
> | **과제 열 머리가 «날짜»만인 것이 손에 맞나** | 이름은 마우스를 올리거나 열 머리를 눌러야 나온다. 자주 되묻게 되면 이름을 한 줄 더 얹는 것이 답 |
> | **마감 지난 미제출이 붉은 것이 손에 맞나** | 마감 전에는 «·»로 조용하다. **20건이 쌓이면 표가 붉어질 수 있다** |
> | **「보완 필요」를 실제로 쓰시나 · «한 것»으로 세는 것이 맞나** | 제출률·리포트·신호에 완료와 똑같이 들어간다. **리포트에서 숫자를 보고 나면 생각이 달라질 수 있다** |
> | **성적을 「문항수·정답수」로 넣는 것 · 달 고르개 · 날짜 열 눌러 고치기** | 셋 다 08-30에 만들고 아직 못 들었다 |
> | **「그날 시험/과제」가 없어진 것이 아쉽지 않나** | 각각 «반 › 성적»·«반 › 과제»로 갔다. 되돌릴 수 있다 |
>
> **C. 오답 › 시험지** (C-1·C-2 · 08-29~30에 만들고 **아직 한 번도 못 들었다**)
> | | |
> |---|---|
> | **왼쪽이 이제 안 갈라지나** | 짚으신 그 자리다. 복사해도 목록은 한 줄이어야 한다 |
> | **과목 폴더가 손에 맞나** | 몇 장 안 될 때는 성가실 수 있다. 아니라면 «전부 펼침»이 답 |
> | **「전체 ↔ 반」 오가는 것 · 「반별로 견주면」 표를 보나** | 「전체」의 값어치가 여기 걸려 있다 |
> | **반 화면에서 문항 내용이 빠진 것이 아쉽지 않나** | 이정표 한 줄만 남겼다(C-2b) |
> | **복사 날짜 칸 · 「2개 반이 함께 씁니다」 · 성적판(만점 하나+전체 저장)** | C-1의 넷이다 |
> | **점수만 있는 시험에 문항을 붙일 때 경고가 뜨는 것이 맞나** | C-21. 문항 수와 저장된 만점이 다를 때만 뜬다 |
>
> **D. 월간 리포트** (R-1·R-2 · **반 › 학생** › 학생 › 「이번 달 리포트」)
> ✅ **09-01에 다 물었다.** 다섯 줄(등수 포함) · 「그중 N건은 보완 필요」 · 글귀 말투 셋은
> **「이대로 좋다」**로 닫혔다. 색 기준만 일이 됐고 **설정으로 뺐다**(R-2a).
> | 남은 것 | |
> |---|---|
> | 🔴 **종이로 뽑아 보셨나** | R-2b. 위 「첫 일」이다. **셋만 묻는다** — 문서만 나오나 · 색이 살아 있나 · 둘째 장이 안 잘리나 |
> | 🔴 **여러 명 PDF가 손에 맞나** | R-3. **그림이라 흐린 것**을 알고 고르셨지만, 실제로 보시면 생각이 달라질 수 있다. 그러면 «한 명씩 인쇄»(ⓐ)나 서버(ⓒ)를 다시 꺼낸다 |
> | **파일 이름이 「이름_2026-08_월간리포트」로 맞나** | 학부모께 보낼 때 그대로 쓰는 이름이다. 학교·반을 넣고 싶으실 수 있다 |
> | **달 고르개 옆의 「기록 있는 학생 N명」이 도움이 되나** | R-3b. 이것 때문에 기본값을 안 찍었다. 성가시면 숫자만 빼면 된다 |
> | **파일 크기(3.6MB)가 견딜 만한가** | 메일로 보내기 버거우면 배율을 낮춘다 — 흐려지는 대신 1/3로 준다 |
> | **설정 > 신호 규칙의 「리포트 색 기준」이 눈에 띄나** | 그 줄만 «신호»가 아니라 이름과 설명으로만 갈라 뒀다. 헷갈리면 갈래를 나누는 것이 답 |
>
> ### 🔵 그다음 만들 것 — **3차 「보냄으로 표시」**
> 직접 보내는 방식이라 시스템은 보냈는지 모른다.
> 자리는 `report:{sid}:{ym}` 문서에 `sentAt` 하나. 명단에서 «이번 달 보낸 사람»이 보이게.
> ⚠ **먼저 물을 것** — 인쇄해서 손에 들려 보내시는지, PDF로 보내시는지에 따라 «보냄»의 뜻이 다르다.
>   09-01에 인쇄(R-2b)와 PDF 묶음(R-3)을 붙였으니 **그 쓰임을 보고 나서** 만드는 것이 순서다.
> 🔵 **R-3이 자리를 반쯤 만들어 놨다** — 「N명 저장」이 도는 그 순환이 곧 «N명에게 보냄»의 순환이다.
>   저장이 끝난 학생을 그대로 `sentAt`으로 찍을 수도 있다. ⚠ 다만 **«저장했다»와 «보냈다»는 다른 일이다** —
>   저장만 하고 안 보내는 날이 반드시 있다. 묻지 않고 엮지 말 것.
>
> ### 🔴 **바깥에서 막혀 있는 것 둘 — 재촉하지 말고 «물어만» 볼 것**
> ① **학부모 전화번호가 재원 4명 전부 비어 있다.** 무엇을 만들든 보낼 곳이 없다.
>   설정 > 학생 > 계정 수정에서 채운다. 리포트 화면 아래에 그 경고를 띄워 뒀다.
> ② **알림톡(N-1)은 사업자 등록부터다** (08-25). 채널도 없다. **며칠~몇 주 걸리는 바깥 절차다.**
>   안내서 → https://claude.ai/code/artifact/6fd1c846-ffac-4ed4-a6b4-d4b88fc2ae8a
>   ⚠ **다섯 값**(`apikey`·`userid`·`senderkey`·`tpl_code`·`sender`)을 받기 전에는 워커를 만들지 말 것.
>   ⚠ **`apikey`를 채팅에 붙이지 말라고 이미 안내했다.** 붙이시면 받지 말고 다시 안내하라.
>   ⚠ **알림톡이 열려도 리포트 표는 못 보낸다** — 심사받은 고정 문구뿐이다. 그때는
>     «리포트가 나왔습니다 + 링크»가 되고 **학부모 열람 페이지와 인증**이 새로 필요하다. 별건이다.
>
> ### 그 뒤에야 아래에서 고른다
> | 남아 있는 것 | |
> |---|---|
> | ✅ ~~**T-2** 채팅의 한글 조합 Enter~~ | **09-01에 고쳤다** — `chatSendKey(ev)` 하나로 모았다 |
> | ✅ ~~**달리기를 손으로 해 볼 것**~~ | **09-02에 했다.** 사용자가 직접 해 보고 여덟 가지를 짚었고 전부 고쳤다 (S-D-4~11). 🔴 **게임은 여기서 마감이다** — 사용자가 「게임은 여기까지 만들게」로 닫았다 |
> | ✅ ~~**특강 흐름을 끝까지**~~ | **09-02에 돌렸다.** 쓰는 길은 멀쩡했고 **보여 주는 곳 셋**이 안 갈라져 있었다 (E-5) |
> | ✅ ~~**리포트 사진이 몇 KB인지**~~ | **09-02에 쟀다 — 70~123KB였다.** 950KB 한도의 8분의 1이라 **품질 낮추기 사다리가 한 칸도 안 내려가고 있었다.** 384dpi로 올렸고 그래도 **309KB**다 (R-4b) |
> | 🔴 **리포트를 종이로 한 장** | R-2b. 인쇄 규칙은 검사판으로만 봤다 |
> | **등급컷을 «예상 등급»으로** | G-1에서 **기록만** 하기로 정했다(사용자 선택). 원하시면 학생 점수 옆에 「예상 2등급」을 붙인다 — **데이터는 이미 그 모양이다.** 읽는 곳만 더하면 된다 |
> | **시즌을 넘겨 볼 것** | G-1의 등급컷이 넘긴 뒤에도 살아 있는지. **node 검사로만 확인했다** — 실제로 넘기면 진짜 범위·날짜가 지워져서 못 해 봤다 |
> | **「특강만」이라는 이름** | 특강만 듣는 학생을 뭐라 부를지 아직 안 정하셨다. 정해지면 `SPECIAL_ONLY_LABEL` **한 줄**만 고치면 된다 |
> | **H-6** 과제가 20건일 때의 격자 | 지금은 전부 보여 준다. 쌓이면 「최근 8개만」 접기 — **사용자가 그렇다고 할 때** |
> | **M-2** 폰 — 출석 도장 점수 열 | 「마지막 기록」 열을 접는 것이 답일 듯 |
> | **`.mono` 안의 한글 두부** | 과제 쪽만 고쳤다. `12061`·`12135`가 남았다 — 찾는 법은 📌(H-3)에 |
> | **수업 탭을 통째로 떠날 때의 출결 저장** | C-20에서 안 막은 문 하나. `goTeacherTab`은 화면 어디서나 불려 위험하다고 봤다 |
>
> ### 🔵 값을 한 방법들 — 그대로 쓸 것
> ① **브라우저 확장이 안 붙으면 재시도하지 말고 곧장 사용자에게 말하라.** Chrome 재시작 한 번에 붙었다.
> ② **계산은 화면과 갈라 «순수 함수»로 두고 node로 검사한다.** 재료는 전부 `ctx`로 주입한다.
> ③ **확인은 `dbSetDoc`·`dbSet`·`saveRecord`를 막고 한다.** 09-01에도 실 DB에 한 줄도 안 썼다.
>   🔵 **한 걸음 더 — «무엇을 쓰려 했는지»를 배열에 적어 두면** 어느 문서를 건드리는지까지 보인다(C-21).
> ④ **창이 안 줄면 iframe에 같은 문서를 넣어** 폰 뷰포트를 만든다. 세션도 살아 있다.
>   ⚠ 그 안의 `state`는 `contentWindow.state`로 못 잡는다 — **`contentWindow.eval(...)`로 들어간다.**
> ⑤ **새 입력칸을 만들면 반드시 한글을 직접 쳐 본다**(W-3). 조합 중 칸이 교체되면 안 된다.
> ⑥ ⚠ **`confirm`은 세션을 막는다** — 확인할 때는 `window.confirm`을 가로채고 답을 넣는다(C-21).
⑦ **막는 대화상자를 여는 기능은 «사본에서 규칙을 화면으로 켜» 본다** (R-2b · 09-01).
  `@media print`→`@media screen`으로 바꾼 사본 + A4 폭(703px) + 조상 훑기. 위 📌에 자세히 적었다.
⑧ 🔴 **«저장했다»고 말하는 기능은 «저장된 것을 세어» 본다** (R-3b · 09-01).
  화면의 알림을 믿지 말고 **내려받기 폴더를 직접 열어 개수를 센다.** 3개라 했는데 1개였다.
  `ls -lt ~/Downloads` 한 줄이면 된다 — 이걸 안 했으면 사용자가 실 리포트를 보낼 때 알았을 것이다.
⑪ 🔵 **브라우저가 없어도 «계산»은 잴 수 있다** (S-D · 09-01). 게임 갈래를 `step`(순수 계산)과
  `draw`(캔버스)로 갈라 두었더니, Chrome이 안 켜지는 날에도 **node로 봇을 돌려 난이도를 쟀다.**
  ⚠ **검사가 틀렸을 가능성을 먼저 볼 것** — 처음 잰 6.4초는 게임이 아니라 봇의 셈이 틀린 것이었다.
⑩ 🔴 **«씨앗을 심고 눈으로 본다»** (E-2·E-3 · 09-01). 단위 검사 39건이 전부 통과한 뒤에도
  실 데이터를 심고 화면을 열자 **둘이 더 나왔다.** 검사는 «내가 생각한 자리»만 보고,
  씨앗은 **생각 못 한 자리**를 드러낸다. ⚠ 여러 «모양»을 심을 것 — 겹쳐 듣기 · 하나만 듣기 ·
  같은 날 두 수업 · 한 시험을 여럿이 보기(안 그러면 평균·등수가 «—»로 남아 확인이 안 된다).
⑨ **화면을 그리는 함수는 stub으로 감싸 node에서 돌려 본다** (R-3b · 09-01).
  `rptBatchDrawerHTML`을 떼어다 DATA·escHtml·iconSvg만 가짜로 주면 **네 가지 상태(준비·도는 중·
  끝남·옛길)의 HTML을 전부 눈으로 볼 수 있다.** 로그인도 DB도 필요 없다.
> ⚠ **화면에 얹어 볼 때 `rec`은 `state.allRecords`가 아니라 `DATA.records[sid]`에서 온다.**
>   09-01에도 리포트를 볼 때 이것 때문에 한 번 헛짚었다. **둘 다 얹어야 화면이 바뀐다.**
> 🔴 **«맞는 숫자»와 «학부모가 읽어도 되는 말»은 다른 것이다.** 계산이 다 맞아도 문장은 틀릴 수 있다.

H. ~~**과제 제출·승인 — 배포본에서 한 바퀴**~~ — **2026-08-16에 끝냈다.** → 위 ✅ 상자
   수락 · 반려(사유 필수 포함) · 현장 확인 · 완료 취소 · 반려 취소 · 벨 착지 ·
   조교 자리 · 옛 기록 흡수 · 사진 확대까지 전부 배포본에서 눈으로 봤다. **흠 둘이 남았다.**
   - ⚠ **사진이 쌓이는 속도를 지켜볼 것.** 한 과제 × 재원생 × 6장이다.
     과제를 지우면 같이 지워지지만, 안 지우면 드라이브에 계속 남는다
H-1. ~~**사진 확대에 닫는 문이 없다**~~ — **2026-08-19에 고쳤다.** × · ESC · 겹침 셋 다.
   닫는 일은 `closeHwPhoto()` 하나로 모았다 → 맨 위 📌 ①. 배포본 확인까지 끝났다
H-2. ~~**강사 연대기의 「(삭제된 시험지) n번」**~~ — **2026-08-19에 고쳤다.**
   `studentTimeline`도 `liveWrongHomework(rec)`를 쓴다 → 맨 위 📌 ②.
   **데이터 15건은 그대로 둔다** (08-14 결정). 배포본에서 0건 확인
H-3. **연대기의 「퀴즈」 줄에 같은 구멍이 남아 있다** — `dailyQuiz.log`(9248행 언저리)도
   문항이 없어지면 「(삭제된 시험지) ?번」으로 선다. **08-19에 일부러 안 고쳤다.**
   오답숙제는 «해야 할 일»이라 없어지면 그만인데, 퀴즈 로그는 «학생이 실제로 푼 기록»이다.
   막으면 학습 이력이 화면에서 사라진다. **어느 쪽인지 사용자에게 물어야 한다.**
   ⚠ 지금은 테스트학생의 퀴즈 로그가 0건이라 **화면에 안 보인다** — 급하지 않다
   - **2026-08-21에 사용자가 「지금은 보류」로 정했다.** 다시 꺼내지 말고, 퀴즈 로그가
     실제로 쌓여 화면에 보이기 시작하면 그때 다시 묻는다
S. ✅ **출석 도장 — D(똥피하기)로 확정하고 `index.html`에 심었다** (2026-08-21)
   시안은 `design-v1/21-checkin.html`(넷) · 프리뷰에도 걸려 있다(`preview.html`, `go5`).
   **사용자가 D 를 골랐고 실서비스에 이식했다. 아직 배포·커밋 전이다.**
   - **심은 것** — CSS 한 덩어리(`gm-` 접두사) · `#gamehost`(body, **`#app` 밖**) ·
     `ckState`/`weekStartOf`/`rankHTML`/`ckCardHTML`/`Dodge`/`openDodge`/`closeDodge`/`finishDodge` ·
     `emptyRecord()`에 `checkin` · `loadRecord()`에 `ckState()` 보정 ·
     `stuHomeHTML`에 카드 한 줄 · `render()` 끝에 `ckEnsureRank()`
   - **새 파일 둘** — `game-char.webp`(12KB) · `stamp.webp`(31KB). 배포에 포함해야 한다
   - **로컬에서 확인한 것** (⚠ **`dbSet`을 막아 놓고 했다 — 실 DB에 한 줄도 안 썼다**)
     · **게임을 켜 둔 채 `render()`를 여러 번 돌려도 캔버스가 살아남는다** — 이식의 핵심이 이것이다
     · 한 판 → `rec.checkin` 이 채워지고 **`rec.attendance` 는 그대로 0건**
     · 닫기 → 홈이 «닫힌 줄»로 바뀌고 주간 칸에 도장 · 다시 열기 시도는 막힌다
     · **화면 전부 정상** — 공개 홈 · 로그인 · 강사 3탭 · 조교 · 학생 12탭
     · 도장 카드는 **홈에만** 뜬다 · **콘솔 오류 0건**
   - ✅ **배포까지 끝났다** — 세 커밋(`015bd95` · `fb69113` · `2fa8444`) · push 완료.
     배포본에서 `index.html`(gamehost 5곳) · `stamp.webp` 31,358 · `game-char.webp` 11,892 를
     **파일 크기까지 대조**했다. 셋 다 HTTP 200. **사용자도 배포본에서 확인했다**
   - ✅ **실 DB에서 돌았다 (2026-08-24 확인)** — `rank:2026-08-17`이 **08-23에 처음 생겼고**
     다섯 줄이 겹쳐 쓰였다. 08-24(월)에 열쇠가 `rank:2026-08-24`로 넘어가 비어 있고
     「지난 주 1등」이 떴다 → 맨 위 📌 (2026-08-24)

M-1. ✅ **강사 화면의 폰 가로 스크롤을 없앴다** (2026-08-24 · **배포 완료**) → 맨 위 📌
   `@media (max-width:560px)` 한 블록. **13개 탭 전부 넘침 0px.** 데스크톱은 그대로다
   - ⚠ **상단바 높이 52px를 늘리지 말 것** — `.subnav{top:52px}`과 `calc(100vh - 92/134px)`가 물려 있다
   - ⚠ **`.app:not(.sap)`** — 학생 앱은 이미 폰 우선이라 건드리면 안 된다
   - **모바일 검사는 iframe으로 한다** — 확장은 창 리사이즈를 못 한다.
     페이지 안에 `<iframe width=414>`를 띄우면 미디어쿼리가 진짜로 걸리고
     같은 오리진이라 로그인 세션도 그대로 물려받는다. `contentWindow.eval('state…')`로 몰 수 있다
     (`state`가 `const`라 `contentWindow.state`로는 안 잡힌다)

M-2a. ✅ **수업 탭을 폰용으로 풀었다** (2026-08-24 · **배포 완료**) → 맨 위 📌
   사용자가 「폰으로 쓴다면 수업 탭일 것」이라고 해서 **여기부터** 골랐다
   - **순서가 핵심이다** — 수업 고르기 → 작업 칸 → 오늘 남은 일.
     그냥 쌓으면 출결 찍으러 올 때마다 「오늘 남은 일」 200px를 지나쳐야 한다
   - **`display:contents`로 aside를 녹여** 부모를 넘나드는 `order`를 걸었다.
     그 때문에 「오늘 남은 일」을 `.cls-rest`로 감쌌다(마크업 변경은 이것 하나)
   - ⚠ **`.cls-screen`은 M-1의 «가둠» 목록에서 빠져 있어야 한다** —
     `height:auto`인데 `overflow-y:hidden`이 남으면 본문이 잘린다
   - ⚠ **안쪽 스크롤러(`.cls-body`)를 폰에서 푼 이유** — 화면 안에 또 스크롤 영역이 있으면
     손가락이 어느 쪽을 미는지 알 수 없다. 페이지 하나만 스크롤하게 둔다

N-1. ⏸ **숙제 미제출 알림 — «발송 직전»까지** (2026-08-25에 **배포 완료 · 발송은 막힘**) → 맨 위 📌
   ⚠ **08-25 갱신** — 문안은 **지금 것 그대로 확정**, 대행사는 **알리고**로 정해졌다.
   그런데 **사업자 등록부터라 사용자 쪽에서 멈췄다.** 절차 안내서를 드렸다 →
   https://claude.ai/code/artifact/6fd1c846-ffac-4ed4-a6b4-d4b88fc2ae8a
   **알리고 API 모양은 확인해 뒀다** (맨 위 📌 · N-1 · 2026-08-25).
   화면·판정·문안은 다 됐다. **실제 발송만 막혀 있다.** 이어서 하려면 순서가 이렇다 —
   1. **원장님이 하실 것** — 카톡 채널 개설 + **비즈니스 인증**(사업자등록증) →
      **발송 대행사** 선택(솔라피·알리고·NHN 등) → **템플릿 심사** 제출
   2. **문안 확정이 먼저다.** 지금 `HW_NOTICE_TEMPLATE`은 **내가 지은 자리표시**다.
      승인 뒤에는 한 글자도 못 바꾼다(바꾸면 재심사)
   3. **전화번호를 채운다** — 지금 실데이터에 한 건도 없다
   4. **워커에 엔드포인트**를 만든다. 대행사가 정해져야 API 모양이 정해지므로 지금은 안 만들었다
   - 🔴 **워커의 「강사인지 모른다」를 먼저 풀어야 한다** — 돈이 나가고 학생에게 도달한다
   - 🔴 **중복 방지·발송 기록**을 같이 만든다 (`rank:<월요일>`과 같은 어법)
   - ⚠ **API 키는 워커 비밀로만.** 브라우저에 실으면 그 순간 공개된다
   - ⚠ 받는 사람을 학부모로 바꾸려면 `HW_NOTICE_TO` 한 곳만 고치면 된다

   #### 📎 내일 이어서 — 손댈 자리와 미리 정해 둔 것 (2026-08-24 밤에 적음)
   **이미 있는 것 (다시 만들지 말 것)** — `index.html`의 「숙제 미제출 알림」 블록 하나에 모여 있다.
   `hwReviewRowHTML` 바로 뒤다. `grep -n "숙제 미제출 알림" index.html` 로 바로 간다.
   | 이름 | 하는 일 |
   |---|---|
   | `HW_NOTICE_TO` | 받는 사람 고르기. 지금 `s.phone`(학생 본인) |
   | `HW_NOTICE_TEMPLATE` | **심사에 낼 원문.** `#{학생명}`·`#{과제명}`·`#{마감일}` |
   | `hwNoticeText(name, a)` | 위 템플릿을 실제 값으로 치환 |
   | `hwUnsubmitted(a, roster)` | 미제출자 = `hwNeedsAction` 인 학생 |
   | `hwNoticeBarHTML(a, roster)` | 펼친 과제 위의 「미제출 n명 · 미제출 알림」 줄 |
   | `hwNoticePanelHTML(a, miss)` | 명단 · 번호 없는 사람 · 문안 · **비활성 버튼** |
   | CSS `hw-nbar` `hw-npanel` `hw-nlist` `hw-nrow` `hw-nmsg` `hw-nact` | 그 화면 |

   **내일 제일 먼저 물을 것 둘** (이것부터 갈리면 뒤가 다 달라진다)
   1. **문안을 확정했는가.** 지금 것은 내가 지은 자리표시다. 승인 뒤엔 못 바꾼다
   2. **대행사를 정했는가.** 정해져야 API 모양이 정해진다 — 그전엔 워커를 못 쓴다

   **대행사 고를 때 볼 것** — ① 알림톡 **+ 실패 시 SMS/LMS 대체발송**을 한 번의 호출로 해 주는가
   ② REST API인가(SDK 강제면 워커에서 쓰기 나쁘다) ③ 단가 ④ 템플릿 등록을 웹에서 하는가
   ⚠ **대체발송은 어차피 필요하다** — 카톡을 안 쓰거나 차단한 학생이 있다

   **템플릿 심사에서 막히는 것** — 광고성 문구(할인·모집·홍보)가 한 줄이라도 있으면 반려.
   변수는 «값»만 넣는다(문장 통째로 변수로 만들면 반려). 채널 발신프로필이 먼저 있어야 한다

   **워커 엔드포인트 스케치** (아직 안 만들었다 — 대행사 미정이라)
   - `POST /notify/homework` ← `{ hwId }` **만** 받는다
   - 🔴 **클라이언트가 보낸 «보낼 사람 명단»을 그대로 믿으면 안 된다.**
     받으면 그 목록으로 아무에게나 쏠 수 있다. **서버가 Firestore를 다시 읽어
     «정말 미제출인지»를 스스로 판정한다.** 화면의 명단은 «미리보기»일 뿐이다
   - 상한을 서버에 건다(1회 n명 · 하루 n건). AI 한도를 KV로 세던 그 자리를 그대로 쓴다
   - **부분 실패를 그대로 돌려준다** — 「5명 중 4명 성공 · 1명 번호 오류」. 뭉뚱그리면 안 된다
   - ⚠ **강사 신원 문제(0-B의 «강사만 접근»)가 여기서 처음으로 진짜 문제가 된다.**
     AI 한도는 «남이 내 몫을 태운다»였지만 이건 «남이 우리 학생에게 문자를 보낸다»다

   **중복 방지 문서** — `notify:<ymd>` 에 `[{sid, hwId, at, by, result}]`.
   ⚠ **`rank:<월요일>`과 같은 어법으로 — 읽고 → 내 줄만 더하고 → 쓴다.**
   통째로 덮으면 같은 시각 다른 강사가 보낸 기록이 사라진다(08-24에 순위에서 확인된 함정)
   - 화면에서는 「오늘 이미 보냄」으로 표시하고 **기본 제외**한다

   **발송 시각** — 정보성이라 야간 금지 규정에 걸리지는 않지만, 밤 10시에 「숙제 안 냈다」가
   가면 민원이 온다. **시간 가드를 두고, 넘으면 «내일 아침에 보낼까요»를 묻는다**

   ⚠ **연결한 날은 반드시 «나에게 먼저 한 통»을 보내 본다.** 학생 명단으로 첫 발사하지 말 것

M-2b. ✅ **신호 탭을 폰용으로 접었다** (2026-08-24 · **배포 완료**) → 맨 위 📌
   `.sig-row`의 세 칸이 `flex-shrink:0`이라 591px를 요구했다(폭은 369px).
   `.sig-mid`가 폭 0이 되어 **학생 한 명이 763px** → **세 줄 카드로 접어 158px**
   - **`order`가 필요한 이유** — DOM은 who → mid → num → act 인데
     점수를 이름과 같은 줄에 올리려면 num을 mid보다 앞으로 당겨야 한다
   - 🔴 **폰에서 조치 버튼이 «없는 기능»이었다** — `.sig-act{opacity:0}` + `:hover`.
     폰에 hover가 없어 **주의·양호 학생은 연락·출결·오답·7일 해제에 손이 닿지 않았다.**
     ⚠ **데스크톱의 hover 절제는 의도된 것이니 건드리지 말 것**
   - ⚠ 이 «hover에서만 뜨는 조작»은 다른 화면에도 있을 수 있다.
     폰을 볼 때마다 **`opacity:0` + `:hover` 짝을 먼저 찾아볼 것**

M-2. **폰에서 남은 것 — 출석 도장 하나** (2026-08-24 · 열려 있음)
   M-1이 «페이지가 옆으로 밀리는 것»을, M-2a가 수업을, M-2b가 신호를 잡았다. 남은 하나다
   - **출석 도장** — 점수 열이 가로 스크롤 밖. 폰에서는 「마지막 기록」 열을 접는 것이 답일 듯하다
   - ⚠ **먼저 물을 것 — 폰으로 실제로 무엇을 하시는지.** 08-24에 사용자는
     「상단바 + 가로 스크롤」로 좁혔다가 그 뒤 **수업 → 신호를 하나씩 콕 집었다.**
     **이 방식이 잘 맞았다** — 전부 반응형으로 만드는 것보다 훨씬 적은 일로 쓸 수 있게 됐다

H-3. ✅ **과제 확인 명단을 «할 일» 순서로 재편했다** (2026-08-25 · **배포 완료**) → 맨 위 📌
   강사 화면 둘(수업 > 과제 · 반 관리 > 과제)이 **`hwRosterReviewHTML` 하나를 같이 쓴다.**
   확인 대기(폄) → 아직 안 냄(눕힘) → 완료(접음)
   - 🔴 **`.hw-who`에 `.hw-title`을 다시 겹치지 말 것** — `flex:1`이 이겨 배지가 오른쪽 끝으로 밀린다
   - 🔴 **한글을 `.mono` 안에 넣지 말 것** — JetBrains Mono라 **두부 상자**로 나온다
   - ⚠ **「아직 안 냄」은 `hwNeedsAction` 하나로만 가른다** — 미제출 알림과 «같은 집합»이어야 한다
   - ⚠ **학생 상세(`hwReviewRowHTML` 맨 인자)는 건드리지 않았다** — 거기는 배지가 유일한 단서다

H-5. ✅ **과제 탭의 «축»을 격자로 바꿨다 — 행=학생 × 열=과제** (2026-08-25 · **배포 완료**) → 맨 위 📌
   H-3(명단 정리) 뒤에도 **「아직도 보기가 불편하다」**고 해서 축 자체를 뒤집었다.
   `hwGridHTML` · `hwCellDrawerHTML` · `hwOpenCell` / `hwCloseCell` / `hwToggleCol` · `state.hwCell`
   - 🔴 **완료 칸을 다시 칠하지 말 것** — 칠하면 격자가 «성적표»가 되고 할 일이 묻힌다
   - 🔴 **열 머리를 «못 누르게» 만들지 말 것** — 미제출 알림(N-1)으로 가는 유일한 길이다
   - ⚠ **「수업 > 과제」는 격자로 안 바꿨다** — 거기는 «그날 낸 과제»를 묻는 자리다
   - ⚠ 상세의 줄은 명단과 같은 `hwReviewRowHTML`을 쓴다. 두 벌로 만들지 말 것

H-5b. ✅ **열 머리가 명단을 다시 펴지 않는다** (2026-08-26 · **배포 완료** · `09a3af1`) → 맨 위 📌
   H-5로 축을 뒤집고도 **「아직 불편하다」**고 해서 **«어디가»를 물었더니 한 줄로 답이 나왔다** —
   **「표에서 이미 내용이 있는데 미제출 알림을 보내기위해 과제를 누르면 또 똑같은 내용이
   반복적으로 펼쳐지고 그안에서 미제출 알림을 눌러야하는 것이 불편함」**.
   격자를 만들면서 **옛 명단 화면(`hwRosterReviewHTML`)을 격자 아래에 그대로 붙여 둔 것**이
   남아 있었다. 그것이 말하는 «확인 대기 · 아직 안 냄 · 완료» 셋은 **격자가 이미 하는 말**이다.
   - **역할을 갈랐다** — **칸** = 검토(수락·반려, `hwCellDrawerHTML`) · **열 머리** = 미제출 알림
   - 🔴 **여기에 명단을 다시 붙이지 말 것.** 그게 불편의 원인이었다
   - 🔴 **열 머리는 여전히 눌러야 한다** — N-1로 가는 유일한 문이다(H-5의 규칙 그대로)
   - ⚠ **「수업 기록 > 그날」의 목록은 그대로 뒀다** — 거기는 격자가 없어 명단을 대신할 것이 없다
   - ⚠ 미제출자가 **없을 때**와 기록을 **불러오는 중**일 때를 갈라서 말한다.
     `hwUnsubmitted`는 기록 전에는 빈 배열이라 «없음»과 구별이 안 된다
   - 🔵 **여기서 배울 것** — 「불편하다」로 끝내지 말고 **«어디가»를 되물어라.**
     08-25에는 안 묻고 축을 통째로 뒤집었다. 이번엔 물어서 **줄 하나**로 끝났다

A-1. ✅ **계정은 설정에서, 학업은 명단에서 — 둘로 갈랐다** (2026-08-26 · **배포 완료** · `62abf6e`) → 맨 위 📌
   사용자가 갈랐다 — **「계정관련 설정이나 정보입력 등을 전체다 설정탭으로 옮기고 명단에서는
   학업에 관련한 관리를 하는 것만 하도록 분리했으면 좋겠고, 빠른작업에 기본정보 탭은 지우고
   빠른작업의 네개는 다시 활용도가 높은것들로 재구성을 해야할 것 같아」**
   | | 전 | 후 |
   |---|---|---|
   | 정보·비밀번호·삭제 | 명단 > 열기 > 기본정보 | **설정 > 학생 > 계정 수정** |
   | 빠른 작업 | 채팅·**기본정보**·클리닉 배정·수업 화면 | 채팅·**상담**·클리닉 배정·수업 화면 |
   | 개인 보충 영상 | 기본정보 안 | 학생 상세 **우측 칸**(출결 아래) — 학업이라 명단에 남는다 |
   - `studentAccountFormHTML`(옛 `sdInfoHTML`) · `stuEdit` / `stuSearch` / `sdConsult` ·
     `state.stuEditId` / `stuQuery` · `teacherSettingsStudentsHTML`에 계정 목록
   - 🔴 **입력칸 id(`info-*`)를 바꾸지 말 것** — `updateStudentInfo`가 DOM에서 그 id로 읽는다.
     화면을 옮기면서 그 함수를 **한 글자도 안 고칠 수 있었던 이유**가 이것이다
   - 🔴 **한 번에 한 학생만 편다**(`state.stuEditId`). 같은 id가 화면에 둘이면
     `getElementById`가 위엣것만 집어 **엉뚱한 학생이 저장된다**
   - 🔴 **설정의 학생 목록은 한때 내가 «일부러 지웠던» 것이다** — 「같은 곳으로 가는 문이
     두 개면 어느 쪽이 최신인지 헷갈린다」. **그 말은 둘 다 «보는» 목록일 때 옳았다.**
     지금은 문이 아니라 **일이 갈렸다.** 그래서 이 목록에는 **성적·출결·신호를 한 칸도 안 둔다** —
     두는 순간 옛 문제로 돌아간다
   - 🔵 **빠른 작업의 나머지 셋은 안 건드렸다** — 사용자가 **「추후에 활용도가 높은걸 다시
     이야기하면 그때 수정하도록 하자」**고 했다. **먼저 꺼내지 말 것**
   - ⚠ `studentDetailTab`의 `'info'`는 **없는 값이 됐다.** 예전 세션은 연대기로 떨어진다
   - ⚠ 딸려 나온 것 둘 — ① 설정 안에서는 `.rst-fbar .field input` 규칙이 안 걸려 검색칸이
     «상자 속 상자»로 나왔다(`.st-fbar`용 규칙을 세웠다) ② 미제출 알림 패널의
     「명단 > 학생을 열어 전화번호를」이 **틀린 안내가 됐다** — 설정으로 고쳤다.
     **화면을 옮기면 그곳을 가리키던 문장을 같이 찾아야 한다**

W-1. ✅ **퇴원 처리 — 삭제와 갈랐다** (2026-08-26 · **배포 완료** · `636108c`)
   사용자: **「학생들 퇴원처리하는게 없는것 같아! 그냥 삭제랑 별도로 정보는 남겨두고
   퇴원처리만 하는 기능이 필요해」**
   ⚠ **«없다»는 아니었다 — 얇았다.** 퇴원은 있었는데 **출결에 얹혀** 있었고
   (`isWithdrawnRecord` = 마지막 출결이 「퇴원」인가), 하는 일은 **명단에서 감추기 +
   로그인 막기** 둘뿐이었다. 🔵 **「없는 것 같다」는 말을 들으면 먼저 «정말 없나»를 확인하라.**
   여기서는 «있지만 하는 일이 거의 없었다»가 답이었고, 그 사실이 설계를 정했다.
   - **판정을 학적으로 옮겼다** — `student.withdrawnAt`(+`withdrawnReason`).
     🔴 **옛 방식의 병** — 마지막 출결로 보면 **더 늦은 날짜의 출석이 한 줄 들어오는 순간
       조용히 재원으로 돌아간다.** node로 시험해서 실제로 그런 것을 확인했다.
       학적은 사건이지 출결의 부산물이 아니다
     ⚠ **`isWithdrawnRecord`는 안 지웠다** — 옛 데이터를 읽는 유일한 길이다. **새로 적지만 않는다**
     ⚠ `ATT_STATUSES`에서 「퇴원」을 뺐다 — **새로 적는 문만 닫은 것**이다
   - **어디까지 빼는가는 사용자가 정했다 — 「앞으로의 일에서만」**
     | | |
     |---|---|
     | **뺀다** (`classRoster`, 23곳) | 출석부 · 과제 격자 · 미제출 알림 · 반 인원 · 진도 · 퀴즈 · 영상 · 엑셀 · 클리닉 배정 |
     | **안 뺀다** | 지난 시험의 오답 집계·제출 현황 — 빼면 **그때 20명이 봤던 시험이 나중에 18명으로 보인다** |
     | **안 빼되 «퇴원»이라 적는다** | 학생 상세의 옆 학생 목록 · 채팅 목록 — 빼면 **퇴원생 본인이 목록에서 사라지거나** 지난 대화에 닿을 길이 없어진다 |
     🔴 **`DATA.students.filter(s=>s.classId===…)`를 새로 쓰지 말 것.** «앞으로의 일»이면
       `classRoster(classId)`다. 안 그러면 퇴원생이 딸려 온다 — 그게 이 기능이 얇았던 이유다
   - **화면** — 설정 > 학생 > 계정 수정 아래 「학적」 상자(`.stu-life`).
     🔴 **삭제를 같은 상자 안에 선 아래로 뒀다** — 무엇이 되돌아오고 무엇이 안 되돌아오는지
       **같은 자리에서 읽혀야** 급할 때 삭제를 안 누른다. 떼어 놓지 말 것
   - ⚠ **반 배정(`classId`)은 안 지운다** — 「어느 반이었나」가 기록이다(사용자가 정했다)
   - ⚠ **로그인 차단·세션 자동 로그아웃도 새 판정을 본다.** 옛 판정만 고치면
     퇴원시켰는데 계속 들어와진다
   - 🔴 **화면으로는 확인 못 하고 올렸다** — 브라우저 확장이 안 붙어서(다섯 번 시도)
     **판정 로직만 node로 따로 8가지 경우를 돌렸다.** 사용자가 「푸시해줘」로 정했다.
     **다음에 열면 배포본에서 눈으로 보는 것이 첫 일이다** → 「▶ 다음 차례」

W-2. ✅ **설정 > 학생 — 한 줄에 열을 맞추고, 퇴원은 아래로** (2026-08-26 · **배포 완료** · `4ad65b8`)
   ✅ **사용자가 W-1을 실제로 눌러 보고 나온 말이다** — 「퇴원한 학생은 아래로 정렬되었으면
   좋겠고 (…) 학교와 반이 이름 옆으로 쓰여지면서 보이게 하면 더 한눈에 여러 학생이
   정리되지 않을까 싶은데 **어떤게 더 나아보여?**」 → **의견을 물으셨다. 그림 셋을 그려 드렸고
   ①한 줄·열 맞춤 ②한 줄·이어 붙이기 ③지금(두 줄) 중 ①을 고르셨다.**
   - 🔴 **한 줄로 만드는 것만으로는 절반이다 — 열이 맞아야 값을 한다.**
     학교가 「광남고」·「건대부고」·「학교 미입력」로 길이가 달라서 그냥 이어 붙이면
     **「반」이 줄마다 다른 자리에 서고 세로로 훑을 수가 없다.**
     이 목록의 일은 «여러 명 중 한 명 찾기»고, 그때 눈은 세로로 내려간다
   - ⚠ **`table`을 쓰지 않았다** — 줄을 펴면 그 아래에 편집 폼이 들어와야 하는데
     **표의 칸 안에서는 폼이 열 폭에 갇힌다.** 고정폭 `flex`(`.stu-line`)가 같은 결과를 준다
   - 🔴 **폭은 `flex:0 1 <폭>`이다. `flex-shrink:0`으로 못 박지 말 것** —
     폰에서 줄이 통째로 밀려 나간다(M-1이 세 번 잡은 그 사고)
   - 🔴 **걸린 함정 — 이름이 `.stu-line` 안으로 한 단계 들어가면서
     `.st-row .t > b`(직계 자식 선택자)가 더는 안 걸린다.** 글꼴을 명시 안 하면
     **이름만 조용히 작아진다.** 🔵 **한 단계 감쌀 때는 `>` 선택자를 먼저 찾아볼 것**
   - **퇴원은 아래로 · 감추지는 않는다** — 계정 화면은 «되돌리러» 오는 곳이라 없으면 못 찾는다.
     재원과 퇴원 사이에 「퇴원 n명」 금(`.stu-sep`)을 긋는다.
     퇴원끼리는 **늦게 그만둔 사람이 위** — 되돌릴 일은 대개 최근 사람이다
   - ⚠ **여기도 화면은 못 봤다**(확장 여전히 안 붙음). 대신 **`teacherSettingsStudentsHTML`을
     node로 실제 실행해** 줄 순서·금 위치·태그 균형을 확인했다 → 아래 🔵

   #### 🔵 브라우저가 없을 때 쓴 방법 — «코드를 그대로 꺼내 돌린다»
   확장이 안 붙어도 **베낀 판이 아니라 파일 안의 진짜 함수**를 시험할 수 있다.
   `index.html`에서 함수 소스를 중괄호 균형으로 잘라 내 `new Function`에 먹이고,
   `escHtml`·`classNameOf` 같은 곁가지만 스텁으로 채운다.
   - **W-1**: `isWithdrawn`/`classRoster`를 8가지 경우로 돌려 **옛 방식이 늦은 출석에
     되살아나는 것**까지 재현했다
   - **W-2**: `teacherSettingsStudentsHTML`을 통째로 그려 **줄 순서·「퇴원 n명」 금 위치·
     `div` 여닫이 균형**을 봤다
   ⚠ **이걸로 못 보는 것은 «폭·간격·색»이다.** 구조는 잡아도 레이아웃은 못 잡는다 —
     열이 실제로 예쁘게 서는지는 **여전히 눈으로 봐야 한다.**

W-3. ✅ **설정 > 학생 검색 — 한글 조합이 끊기던 것** (2026-08-26 · **배포 완료** · `67a4417`)
   사용자: **「이름 검색하는 칸에 자음 하나 치면 끊기게 되어있어」**
   `stuSearch`가 `oninput`에서 **곧장 `render()`를 불렀다.** 화면을 다시 그리면 입력칸이
   통째로 새로 만들어지고 **IME가 조합하던 글자를 잃는다** — 「ㄱ」에서 끊기고 「김」이 안 된다.

   ### 🔴 화면을 다시 그리는 입력칸은 **반드시 `liveEdit`을 거친다**
   ```js
   function stuSearch(ev){ liveEdit(ev, v=>{ state.stuQuery = v; }); }   // ← 이렇게
   function stuSearch(e){ state.stuQuery = e.target.value; render(); }   // ← 한글이 깨진다
   ```
   `liveEdit`이 하는 일은 둘 — ① **조합 중(`isComposing`)이면 안 그린다**
   ② 그린 뒤 **포커스와 커서 자리를 되돌린다**. `rosterSearch`가 이미 쓰던 것이다.
   ⚠ 마크업도 짝이 있다 — `oninput="…(event)" oncompositionend="…(event)"` **둘 다** 건다.

   🔵 **왜 놓쳤나 — 「겉모양을 베끼면 속도 따라온다」고 여긴 것이다.**
   W-2에서 이 검색칸을 만들 때 **명단 것의 마크업(`.field` + 돋보기 + placeholder)은
   그대로 베끼고 «핸들러»는 새로 썼다.** 겉이 같으니 같은 장치를 쓰는 줄 알았다.
   **부품을 베낄 때는 그 부품이 «부르는 것»까지 따라가 볼 것.**
   ⚠ 이번 세션에 **비슷한 사고가 또 있었다** — W-2에서 이름을 `.stu-line`으로 감싸자
     `.st-row .t > b`가 안 걸리게 된 것. **둘 다 «옆에 있던 규칙»을 못 본 것이다.**

   ✅ **다른 `oninput` 14곳도 훑었다 — 전부 `liveEdit`을 쓰거나 `render`를 안 부른다.**
     (출결 초안·메모·수식 미리보기는 상태만 담고 안 그린다)
   ✅ **확인** — 파일 안의 진짜 `stuSearch`/`liveEdit`를 꺼내 IME 이벤트를 흉내 냈다.
     고친 판은 **조합 중 `render` 0회 · `compositionend`에 1회 · 커서 복원 O**.
     **옛 판으로 같은 시험을 돌리니 조합 중 3회 그렸다** — 원인이 이것임을 확인했다

P-1. ✅ **학생·조교가 스스로 비밀번호를 바꾼다** (2026-08-25 · **배포 완료**) → 맨 위 📌
   `myAccount()` · `pwChangeModalHTML()` · `submitPwChange()` · `state.pwModal`/`pwError`
   - 🔴 **저장이 성공한 «뒤에» 메모리를 고친다** — 먼저 고치면 실패 때 옛 비밀번호로도 못 들어온다
   - ⚠ **안내 문단은 사용자가 빼라고 해서 뺐다**(2026-08-25). 「올해는 테스트용 몇 명,
     내년에 본격 시작할 때 평문 비밀번호를 고치겠다」는 계획과 함께 말했다 → **P-2**
     ⚠ **P-2 전에 쓰는 사람이 늘면 이 안내를 다시 세우는 것을 «먼저» 물을 것**
   - ⚠ **감사 기록에 비밀번호를 남기지 않는다** · **바꿔도 로그아웃시키지 않는다**
   - ⚠ 학생·조교가 같은 덩어리를 쓴다. 갈리는 것은 컬렉션·열쇠뿐이다

P-3. ✅ **강사가 학생·조교 비밀번호를 «본다»** (2026-08-25 · **배포 완료**) → 맨 위 📌
   사용자가 물어서 시작됐고, **①그대로 ②보기 ③재설정 편하게 중 ②를 골랐다**(나는 ①을 권했다)
   `togglePwReveal` · `pwRevealHTML` · `state.pwReveal` · 학생 기본정보 + 설정 > 조교
   - 🔴 **P-2를 시작하면 이 기능부터 걷어낸다** — 서버도 원본을 모르게 되므로 «못 만드는 것»이다
   - ⚠ **본 것을 기록에 남기되 비밀번호는 안 적는다** · **한 번에 하나만** · **점은 12개까지만**
   - ⚠ 강사만 부를 수 있게 함수 안에서 막았다 — 조교·학생이 직접 불러도 안 열린다

M-2d. ✅ **폰 — 설정·시험지·창고를 풀었다** (2026-08-26 · **배포 완료** · `01a30de`) → 맨 위 📌
   `.ex-screen`(좌측 262px + 본문)이 M-1의 «가둠» 대상이라 **폰에서 본문이 122px**였다.
   **골격을 셋이 나눠 쓴다** — 설정(갈래) · 시험지(시험지 목록) · **창고(단원 목록)**.
   ⚠ 08-25 노트에는 「설정과 시험지」 둘만 적어 뒀는데 **창고도 같은 골격이었다.**
   반 관리(M-2c)와 **같은 어법**으로 위아래로 풀었다. **본문 122px → 369~384px.**
   - 🔴 **`.ex-screen`을 «가둠» 목록에서 먼저 뺐다** — 안 빼면 `overflow-y:hidden`이 남아
     본문이 잘린다 (M-2a·M-2c와 **똑같은 함정**이다. 이걸로 세 번째다)
   - 🔴 **`.ex-nosub`의 높이도 같이 풀어야 한다** — 설정이 그것을 쓴다.
     `.ex-screen`만 풀면 **뒤에 오는 `.ex-nosub{height:calc(…)}`가 높이를 다시 못 박는다**
   - ⚠ 목록은 **190px로 묶었다** — 시험지·단원은 개수가 정해져 있지 않다(반 목록 196px과 같은 판단)
   - ⚠ **딸려 나온 것** — 문항별 오답률 한 줄(`.ex-q`)이 고정폭 다섯으로 430px을 요구해
     **50px이 넘쳤다.** 전에는 `.ex-screen`의 `overflow-x:auto`가 가리고 있던 것이다.
     🔵 **가둠을 풀 때마다 «가려져 있던 것»이 드러난다 — 푼 뒤에 반드시 다시 재라**
   - **`.st-row`·`.st-h`는 이미 폰에서 접힌다**(H-5·P-3). 그만큼은 덜 해도 됐다
   - 데스크톱은 그대로다(목록 262px + 본문 1618px, `display:flex`) — 규칙 전부가
     `max-width:560px` 안에 있다

P-2. 🔴 **비밀번호가 «비밀»이 아니다 — 인증 구조 문제** (2026-08-25 · **내년으로 미뤘다**)
   `loadAll`이 **로그인 전에** `DATA.students`(평문 `pw` 포함)를 모두에게 내려준다.
   **사이트를 연 사람은 누구나 콘솔에서 전원의 비밀번호를 읽는다.** P-1이 만든 문제가 아니다
   - 🔵 **사용자가 «내년»으로 정했다** (2026-08-25) — **「올해는 테스트용으로 몇 명만 사용하고
     내년에 본격적으로 시작할 때 평문 비밀번호를 수정하겠다」.** 알리고 받은 답이다.
     ⚠ **그러니 재촉하지 말 것.** 대신 **전제가 깨지면 그때 다시 꺼낸다** —
     전제는 «쓰는 사람이 몇 명뿐»이다. **신입생을 받거나 반이 늘면 그 전에 물을 것**
   - ⚠ **P-3(강사가 보기)과 P-1의 «안내문 뺀 것»이 이 결정 위에 서 있다.**
     이걸 할 때 **P-3은 걷어내고 P-1의 안내문은 되살릴지 물어야 한다**
   - **고치려면 Firebase Auth로 옮겨야 한다** — 로그인 대조를 서버로 넘기고 `pw`를 안 내려보낸다
   - ⚠ **워커의 «강사인지 모른다»(0-B · N-1 🔴)와 같은 뿌리다.** 같이 풀면 둘 다 닫힌다
   - ⚠ **알림톡 발송(N-1)을 붙이기 전에 이쪽을 봐야 한다** — 돈이 나가고 학생에게 도달한다
   - ⚠ **사용자에게 먼저 알리고 «할지 말지»를 물을 것.** 로그인 방식이 바뀌면
     학생 전원이 한 번은 영향을 받는다. 조용히 시작할 일이 아니다

M-2c. ✅ **반 관리를 폰용으로 풀었다** (2026-08-25 · **배포 완료**) → 맨 위 📌 (H-5 안)
   M-2에 「원래 짜부라져 있다」고 적어 둔 화면. **어느 탭이든 본문 152px → 367px.**
   - ⚠ **`.chub-screen`을 M-1의 «가둠» 목록에서 뺐다** — 안 빼면 본문이 잘린다
   - ⚠ 반 목록은 `max-height:196px`로 묶었다 — 반이 열 개면 목록이 화면을 다 먹는다
   - **`.st-h`도 폰에서 접게 했다** — 「과제 등록」이 화면 밖 505px에 있었다.
     ⚠ **`opacity:0`+`:hover` 말고도 «한 줄 flex»가 폰에서 기능을 숨긴다.** 같이 찾을 것

D-1. ✅ **아래로 펴던 둘을 «오른쪽»으로 옮겼다** (2026-08-27 · 로컬 확인 끝 · **배포 전**)
   사용자의 말 — **「과제 위에 목록중 하나를 누르면 아래 뜨는게 아니라 미제출알림 보내는걸
   마치 학생 완료나 미제출을 누르면 옆에 새롭게 뜨는것처럼 그렇게 떴으면 좋겠어. 아래 또 뜨는게 불편해.
   이와 같은 방법으로 설정-학생 탭에서도 계정수정을 누르면 아래 펼쳐지는게 아니라
   오른쪽에 공간 많으니까 같은방법으로 새롭게 뜨는 식으로」**.
   🔵 **«참고할 모양»을 사용자가 직접 지목했다** — 격자의 «칸»을 누르면 열리는 `hwCellDrawerHTML`.
     새 모양을 지어내지 않고 **그 껍데기(`.hwd`)를 그대로 셋이 나눠 쓰게** 했다.
   - **① 과제 «열 머리» → `hwNoticeDrawerHTML()`** (격자 아래 묶음은 통째로 걷어냄)
   - **② 설정 > 학생 «계정 수정» → `studentAccountDrawerHTML()`** (줄 아래 폼은 걷어냄)
   - 폼 함수(`studentAccountFormHTML`)는 **한 글자도 안 고쳤다** — 08-26에 명단에서 설정으로
     옮길 때와 같은 이유다. `info-*` id로만 이야기하므로 껍데기만 갈면 된다
   🔴 **`state.homeworkDetailId`와 갈라 `state.hwNoticeId`를 새로 만들었다.**
     그 값은 **«수업 기록 > 그날»의 과제 목록도 쓴다** — 하나로 두면 거기서 목록을 편 순간
     오른쪽 드로어까지 같이 떠 버린다. 격자 열 머리는 새 자리만 본다.
   🔴 **드로어는 반드시 `.app` 안에서 그려야 한다 — 여기서 실제로 데였다.**
     이 프로젝트의 글자 크기·색은 거의 다 `.app`으로 스코프돼 있는데(`.app .st-note{font-size:11.5px}`),
     화면 셸이 `<div class="app">`이고 드로어는 그 **형제**로 붙는다. 감싸지 않았더니
     안내 문단이 **16px**(상속값)로 나왔다. `<div class="app hwd-layer">`로 감싸고
     `.hwd-layer{background:none;min-height:0;}`로 **`.app`의 배경과 100vh만 벗겼다** —
     안 벗기면 그 층이 종이색으로 화면을 통째로 덮는다.
     ⚠ **딤(`.hwd-back`)이 안 보이던 것도 같이 풀렸다.**
   ⚠ **드로어를 그릴 때 «화면»을 함께 본다** — `hwNoticeId`·`stuEditId`는 탭을 옮겨도 남는다.
     조건이 없으면 그 화면을 떠난 뒤에도 드로어가 따라다닌다.
   ⚠ **열 머리 드로어에서는 「미제출 알림」을 한 번 더 누르게 하지 않는다** — 열 머리를 누른 뜻이
     이미 「보내겠다」이므로 `hwNoticeBarHTML`(버튼 한 단계) 대신 `hwNoticePanelHTML`을 곧장 편다.
     08-26에 고친 «두 번 누르기»와 같은 이야기다.
   ✅ **로컬에서 눈으로 다 봤다** (`127.0.0.1:8777`) — 열림·같은 것 다시 눌러 닫힘·다른 것으로 갈아타기
     ·딤 클릭·ESC·**탭을 옮기면 안 따라옴** 다섯이 전부 통과. 저장 경로(`updateStudentInfo`)도
     `info-*`를 그대로 읽는 것을 확인했고, **폰(410px)에서도** 드로어 385px·가로 스크롤 없음.
     ⚠ 확인은 전부 **`dbSetDoc`을 막고** 했다 — 실 DB 무손상을 새로고침으로 확인했다.

R-1. ✅ **월간 리포트 — 1차(숫자 + 선생님 한마디)** (2026-08-27 · 로컬 확인 끝)
   사용자가 원한 것 — **「우리가 수집하는 자료들을 종합해서 학부모님께 월마다 전송하는
   시스템(직접 전송을 눌러서)」**. **«직접»이 핵심이다** — 자동 발송이 아니라 선생님이 보고
   가져가는 흐름이라, 1차의 일은 «만들어 보여 주는 것»까지다.
   자리 — **명단 > 학생 상세 > 빠른 작업 「이번 달 리포트」**(`sdMode('report')`).
   ⚠ 빠른 작업의 나머지 셋은 **갈아끼우지 않고 «더했다»** — 사용자가 아직 안 정한 자리다.

   **물어서 정한 셋** (2026-08-27) —
   | 물은 것 | 사용자의 답 |
   |---|---|
   | 가져가는 길 | **PDF로 저장** (텍스트 복사·이미지는 안 고름) |
   | 견주는 숫자 | **반 평균과 등수를 넣는다** — 내가 「넣지 않는다」를 권했으나 사용자가 정했다 |
   | 첫 판 범위 | **한 명씩** |
   ⚠ **등수는 분모를 반드시 함께 적는다**(「4명 중 2등」) — 「2등」만 있으면 실제보다 크게 읽힌다.

   🔴 **자동으로 «칭찬 문장»을 만들지 않는다.** 숫자는 시스템이, 사람의 말은 「선생님 한마디」에서
     선생님이 직접. 비우면 그 자리는 리포트에 아예 안 나간다 → **9절 8번의 그 규칙이다.**
     («있을 법한 말»을 지어내 공개 페이지에 실었던 2026-08-13의 일.)
   🔴 **그 달에 없던 항목은 «줄째로» 뺀다** — 빈 칸이나 `0/0`은 «안 한 것»으로 읽힌다.
   ⚠ **상담 일지·신호는 안 넣는다** — 상담은 선생님이 나중에 보려고 적은 메모라 학부모가 읽을 것을
     전제로 쓰이지 않았고, 신호의 「위험」은 내부 판단이다.

   🔵 **화면에서만 드러난 흠 둘 — 계산은 다 맞는데도 나왔다.**
   ① **「반 평균 92점 · 1명 중 1등」** — 그 시험을 이 학생만 봤을 때. **자기를 자기와 견준 것**이라
      정보가 0인데 학부모에게는 뜻이 있는 말로 읽힌다. → `peers.length < 2`면 평균·등수를 뺀다.
   ② **7월 리포트에 «8월 현재»의 시청률과 진도가 실렸다.** `videoProgress`는 편별 누적치고
      `progress.unit`은 «지금» 어디까지인지라 **달로 자를 방법이 아예 없다.**
      그대로 두면 달이 지날수록 **옛 리포트가 저절로 고쳐 쓰인다** — `examWrongCounts`·W-1과
      정면으로 어긋난다. → **지난 달 리포트에서는 통째로 뺀다**(`ctx.isThisMonth`).
      이번 달에는 넣되 **「지금까지」라고 밝혀서** 내보낸다.
   ⚠ **둘 다 node 검사에서는 안 걸렸다** — 숫자는 처음부터 맞았기 때문이다.
     **«맞는 숫자»와 «학부모가 읽어도 되는 말»은 다른 것이다.** 다음에도 눈으로 볼 것.

   ✅ **확인** — `monthlyReportData`를 **화면과 갈라 둔 순수 함수**로 만들어 node로 **36개**를 돌렸다
     (해를 넘는 달 이동 · 그 달만 자르기 · 없는 달 · 빈 기록 · 같은 시험 중복 기록 · 위 흠 둘).
     화면에서는 실데이터와 대조했다 — 8월 「출석 4 / 기록된 4일」, 7월 「출석 1 · 결석 2」가
     실제 `attendance`와 맞았고, 7월에만 시험이 있는 것도 맞았다.
     **새 입력칸이라 한글을 직접 쳐 봤다**(W-3의 교훈) — 조합 중 칸이 교체되지 않는다.
     저장·삭제도 `dbSet`을 막고 확인했다(비우면 문서에서 사라진다).

   **남은 것** — **2차 인쇄/PDF**(`.rp-doc`만 남기는 인쇄용 CSS. 쓰는 칸과 달 고르개를
   일부러 문서 «밖»에 둔 것이 이 때문이다) · **3차 「보냄으로 표시」**(직접 보내므로 시스템은
   보냈는지 모른다 — 없으면 매달 명단을 손으로 세게 된다).
   ⚠ **알림톡이 열려도 이 표는 못 보낸다** — 템플릿 심사를 받은 고정 문구뿐이라,
     그때는 «리포트가 나왔습니다 + 링크»가 되고 **학부모 열람 페이지와 인증**이 새로 필요하다.
   🔴 **학부모 전화번호가 재원 4명 전부 비어 있다** — 무엇을 만들든 보낼 곳이 없다.
     리포트 화면 아래에 그 경고를 띄워 뒀다.

R-1b. ✅ **리포트를 채우고 꾸몄다 — 오답·데일리퀴즈 · 예비 글귀 · %와 표** (2026-08-27 · 로컬 확인 끝)
   사용자의 말 — **「성적과 성적에 대한 오답 및 데일리퀴즈가 잘 이루어지는지 정도도 기록했으면
   좋겠고 선생님 한마디 입력을 지금처럼 할수 있게 남겨두되 예비 글귀들 몇개정도 만들어두고
   선택할 수 있도록 해주고, 조금더 성적이나 출결이나 과제 등에 대해서 %나 표 등을 이용하여
   있어보이도록 꾸몄으면 좋겠어」**

   **① 담은 것** — 출결(%·명세) · 과제(%) · 오답 숙제(%·정답 수) · **데일리퀴즈**(정답률·참여 일수)
     · 강의 시청 · 진도, 그리고 **시험 표**(점수·%·반 평균·등수·**그 시험의 오답**).
   🔵 **오답을 «시험 옆»에 붙인 이유** — 점수와 떼어 놓으면 두 숫자가 서로 남이 된다.
     한 줄에 나란히 놓아야 「몇 점이었고, 틀린 것을 어떻게 했나」가 한 이야기로 읽힌다.
   ⚠ **데일리퀴즈는 «푼 날 수»를 함께 센다** — 정답률만으로는 «꾸준히 했는가»를 말할 수 없다.
     한 날 몰아서 푼 것과 여덟 날에 걸쳐 푼 것은 학부모가 볼 때 다른 이야기다.
   ⚠ **성적과 시험지를 잇는 것이 도메인에 없다**(12818행) — **이름과 날짜가 둘 다 같을 때만** 잇는다.
     못 이으면 그 칸은 「—」다. 이름만으로 짐작해 이으면 그 줄이 거짓이 된다.
   ⚠ **% 옆에 «몇 건 중 몇 건»을 반드시 둔다** — 「50%」는 2건 중 1건일 때도 20건 중 10건일 때도
     같은 얼굴이라, 크기를 모르면 학부모가 잘못 읽는다.

   **② 예비 글귀** — 🔴 **«문장을 지어내지 않는다»와 부딪히지 않게 셋을 지켰다.**
     ① **고르는 사람은 선생님이다** — 누르면 «입력칸에 들어갈 뿐»이고, 저장은 따로 눌러야 한다.
        고른 뒤 고쳐 쓸 수 있고, 두 개를 고르면 이어 붙는다. 화면에 **「이것은 초안입니다」**를 적어 뒀다.
     ② **데이터가 뒷받침하는 것만 목록에 뜬다** — 결석이 있는 달에 「빠지지 않고」는 아예 안 나온다.
     ③ **권유·당부는 사실 주장을 하지 않는다** — 「~해 주시면 좋겠습니다」는 선생님의 뜻이다.
     ⚠ **조건 없이 늘 뜨는 «칭찬»은 만들지 말 것.** 그것이 9절 8번의 그 일이다.
       (지금 조건 없는 것은 마무리 인사 하나뿐이고, 그것도 사실 주장이 없다.)

   🔵 **잡은 흠 셋 — 셋 다 «만들면서» 나온 것이 아니라 «확인하다» 나왔다.**
   ① 🔴 **리포트가 고아 숙제를 세고 있었다.** 실데이터에서 **「9건 중 4건」인데 살아 있는 것은 3건**뿐.
      문항이 사라져도 학생 기록의 숙제는 남는다(3291행). 연대기·학생 화면은 `liveWrongHomework`로
      막고 있었는데 **리포트만 raw `wrongHomework`를 셌다.**
      **학부모에게 나갈 문서에 유령 숫자가 실릴 뻔했다.** → ctx.liveWrong 으로 거른다.
   ② **`liveHw`를 선언보다 먼저 썼다**(const TDZ) — 시험이 있는 달을 열면 **즉시 터진다.**
      **node 검사가 잡았다.** 브라우저로 8월(시험 없음)만 봤으면 못 봤을 것이다.
   ③ **글귀를 «인덱스»로 집고 있었다.** 목록은 그 달 데이터로 만들어지는데 `loadAllRecordsIfNeeded()`
      전후로 두 번 그려진다. 그 사이 목록이 길어지면 **같은 자리에 다른 글귀가 온다** —
      실제로 마지막 글귀를 눌렀는데 데일리퀴즈 문장이 들어갔다. → **`this.textContent`로 그 문장을 받는다.**
   ⚠ **화면을 다시 쓰면서 «진도» 줄을 흘렸다.** 눈금(%) 중심으로 짜다 보니 %가 없는 것이 빠졌다.
     되살렸다. **줄 목록을 바꿀 때는 전후를 견줘 볼 것.**

   ✅ **확인** — node 검사 **35개**(고아 제외 · 데일리퀴즈 · 시험별 오답 · 못 잇는 경우 ·
     퇴원 줄 제외한 출석률 · 글귀가 데이터를 따르는지). 화면에서는 실데이터로 대조했고,
     데일리퀴즈는 실기록이 0건이라 **`DATA.records`에 합성 로그를 얹어** 눈으로 봤다.
     ⚠ **`rec`은 `state.allRecords`가 아니라 `DATA.records[sid]`에서 온다**(11169행) —
       처음에 엉뚱한 쪽에 얹어 「안 나온다」고 헛짚었다. 다음에 얹어 볼 때 이 자리를 볼 것.

R-1c. ✅ **부족한 곳에 색 · 곁말 정리 · 오답을 둘로** (2026-08-27 · 로컬 확인 끝)
   사용자의 말 — **「부족한 것이나 미비한 것은 신호판과 비슷하게 붉은색이나 노란색을 이용해서
   표시해주고, 출석4 옆에 "기록된4일" 문구는 없는게 좋을것 같고, 오답숙제는 제출과 정답율에
   대해서 분리해주고, 강의시청 옆에 "지금까지"라는 표현, 진도 공통수학2 옆에 "지금"이라는
   문구도 의미 없는것 같아. … 학부모님께서 매달 받아보는 자료로 조금더 있어보이고
   조금더 이해하기 쉽게 했으면 좋겠어」**

   **① 색** — `rpGrade()`. 🔴 **임계값을 새로 지어내지 않았다.**
     신호 규칙(설정 > 신호 규칙)에 이미 있는 것은 **그대로 읽는다** — 같은 학생이 신호판에서는
     빨강인데 리포트에서는 멀쩡하면 둘 중 하나가 거짓말이 된다. **선생님이 설정에서 임계값을
     바꾸면 리포트 색도 함께 움직인다**(node로 확인했다).
     · 출결 → `R.attPct`(80) 미만 빨강 · 그 위 10점 안쪽 노랑
     · 강의 시청 → **규칙에 이미 3단계가 있다**(`vidHeavy` 50 · `vidWarn` 70). 그대로
     · 오답 «제출» → 남은 미제출이 `R.wrong`(3)건 이상이면 빨강 — 신호와 같은 잣대
     ⚠ **과제·오답 정답률·데일리퀴즈는 규칙에 없어 내가 60/80으로 정했다.**
       기준을 다투게 되면 **신호 규칙에 항목을 늘리고 여기서 그것을 읽는 쪽**이 옳다.
   🔴 **색은 «막대와 %»에만 준다** — 줄 전체를 물들이면 학부모 문서가 경고장처럼 보인다.
   🔴 **「위험」·「주의」라는 «말»은 안 쓴다.** 그건 선생님이 볼 내부 판단이다(R-1에서 신호를
     뺀 것과 같은 뜻). 대신 **범례를 학부모 말로 적었다** — 「함께 챙겨 주시면 좋은 부분」·
     「조금만 더 하면 되는 부분」. **색만 칠하고 뜻을 안 적으면 «무슨 뜻이지»에서 멈춘다** —
     사용자가 말한 「조금더 이해하기 쉽게」가 바로 이 자리였다.
     ⚠ 범례는 **색이 하나라도 있을 때만** 나온다(`anyWarn`). 다 좋은 달에 범례만 뜨면 군더더기다.

   **② 곁말을 뺐다** — 「기록된 n일」·「지금까지」·「지금」.
   🔵 **다만 「기록된 n일」은 분모였다.** 그냥 지우면 「출석 2일」이 며칠 중 2일인지 사라진다.
     그래서 **분모를 본문으로 옮겼다** — 출석·지각·**결석을 0이어도** 적어서 그 합이 곧 분모가 된다.
     「결석 0일」은 학부모가 가장 보고 싶은 줄이기도 하다. (조퇴만 드문 일이라 있을 때만 낀다.)
     ⚠ **곁말을 지울 때는 «그 말이 하던 일»이 무엇이었는지 먼저 볼 것.**

   **③ 오답을 «제출»과 «정답률»로 갈랐다** — 둘은 다른 이야기다.
     **다 냈는데 많이 틀린 것**과 **아예 안 낸 것**은 집에서 할 일이 다르다.
     ⚠ **정답률의 분모는 «낸 것»**(`done`)이다. 안 낸 것까지 넣으면 «틀렸다»와 «안 냈다»가 섞인다.
     ⚠ **하나도 안 냈으면 정답률 줄은 아예 뺀다** — 0/0은 0%가 아니라 «말할 수 없음»이다.

   ✅ **확인** — 등급 판정 node 검사 **23개**(신호 임계값을 그대로 쓰는지 · 경계값 ·
     임계값을 바꾸면 색이 따라 움직이는지 · 값이 없으면 색도 없는지). 기존 35개도 그대로 통과.
     화면에서는 **일부러 «부족한» 기록을 얹어** 빨강·노랑이 실제로 서는 것을 봤다.

H-4. **과제 화면에서 아직 안 본 곳 — 등록 폼** (2026-08-25 · **열려 있음 · 다음 차례로 유력**)
   H-3이 명단을, H-5가 축을, H-5b가 중복 펼침을 고쳤다. 남은 것은 **과제 등록 폼**이다.
   **08-26에 사용자가 「오늘 할 일」로 골랐다가 M-2d에 밀렸다** — 그날 계정 관리를 설정으로
   옮기면서 폰에서 설정이 못 쓸 화면인 것이 급해졌기 때문이다(사용자가 순서를 그렇게 정했다).
   - **자리** — `teacherClassDailyHomeworkHTML` 안의 `state.settingsForm==='dhw'` 블록.
     `dhw-title` · `dhw-due` · `dhw-desc` + `addDailyHomework(classId, date)`
   - ⚠ **먼저 눌러 보시게 하고 듣는다.** 격자로 충분한지가 갈리는 지점이다
   - 반 관리 쪽 등록 폼은 08-24에 «두 벌이던 것»을 한 벌로 합쳐 둔 상태다 — 그건 건드릴 것 없다

S-F. ✅ **강사 화면에 순위를 붙였다 — 「소통 > 출석 도장」** (2026-08-24 · **배포 완료**)
   사용자가 물어서 시작됐고, **「전원 명단」을 사용자가 골랐다** → 맨 위 📌 (2026-08-24 · 두 번째)
   - **읽는 것은 `rank:<월요일>` 하나뿐이다.** `record:학번`도 안 읽고 쓰지도 않는다
   - **주 넘기기가 된다** — 지난 주가 지워지지 않고 남는 설계 덕에 공짜로 얻은 것이다
   - ⚠ **「안 한 학생」 목록을 따로 세우지 말 것.** 전원 명단까지가 사용자가 고른 선이다
   - ⚠ **자리를 신호·명단으로 옮기지 말 것** — 놀이가 그 화면의 성격을 물려받는다
   - ⚠ **상품·가산점을 걸면 이 화면이 제일 먼저 문제가 된다** (점수는 학생 기기에서 만들어진다).
     화면 아래 주의문 두 줄이 그 말을 하고 있다 — 지우지 말 것

S-E. **도장 손보기 — 사용자가 말할 때만 한다** (열려 있음)
   ⚠ **사용자는 「확인했다」고만 했다. 손맛·난이도에 대한 평은 «듣지 않았다».**
   그러니 **「좋다고 했다」로 읽지 말고, 동시에 먼저 고치러 들어가지도 말 것.**
   말이 나오면 그때 만진다. 만질 자리는 이미 좁혀 뒀다 —
   | 증상 | 만질 곳 |
   |---|---|
   | 손가락을 따라오는 게 굼뜨다 / 미끄럽다 | `_step` 의 `dt * 14` (따라가는 비율) |
   | 닿지도 않았는데 죽는다 | `reach = it.r * .72 + 10` (판정 반지름 19.4) |
   | 너무 쉽다 / 어렵다 | 오르막 시각 `30 · 55 · 80` 과 생성 간격 하한 `.14` |
   | 캐릭터가 크다 / 작다 | `CHAR_H`(46) |
   - **재는 법** — 확장이 모는 탭은 rAF 가 안 도니 **`_step` 을 손으로 돌려** 봇으로 잰다.
     고치기 전 기준값: **봇 8판 중앙값 25.8초 · 최대 36.5초** (→ S-D 의 검증 항목)
   - ⚠ **러너로 확장하고 싶다면** 게임 셸(`Dodge` 의 mount/stop/입력/저장)은 그대로 쓰고
     `_step` 과 `_draw` 만 갈아 끼운다. **걸림돌은 코드가 아니라 그림이다** → S-D
   - ⚠ **이것은 출결이 아니다 — 사용자가 그렇게 정했다.** 강사가 「수업 > 출결」에 넣는
     출석·지각·조퇴·결석은 그대로 두고, **출석률에 한 숫자도 안 섞는다.**
     그래서 이름을 「출석 체크」가 아니라 **「출석 도장」**으로 쓴다 —
     학생이 「내가 눌렀으니 출석이 됐다」고 읽으면 안 된다. 화면에도 그 한 줄을 단다
   - **자리는 학생 홈** — 인사 아래, 「오늘 할 일」 위. 하루 한 번, 한 번 터치
   - ⚠ **그리기만 해도 저장하는 길을 따라가지 않는다.** `dqEnsureToday()`는 화면을 여는 것만으로
     오늘 뽑기를 박아 두지만, 도장은 **학생이 실제로 누른 순간에만** 쓴다.
     「연 것」과 「찍은 것」이 같아지면 도장이 아무 뜻도 못 갖는다
   - **기록은 `rec.checkin` 한 줄을 더한다** — `last`·`streak`·`best`는 셋이 공통이고
     `days[ymd]`의 모양만 안마다 다르다. 그래서 나중에 안을 갈아도 연속 일수는 이어진다
   - **연속은 수업 없는 날도 센다** — 학원 출결이 아니라 「앱을 연 날」이라 주말에 끊기면 아무도 안 한다
   - **강사 화면에는 안 붙인다 · 실물 보상도 없다**(기본). 강사가 보는 순간
     「도장 안 찍은 애」가 지표가 되고, 보상을 걸면 도장이 «점수»가 되어 부정이 생긴다
   - 넷 — **A 도장 찍기**(제일 가볍다 · 정할 것이 없다) · **B 카드 뽑기**(매일 다르다 ·
     ⚠ **스티커 여섯과 문구는 내가 임의로 채운 자리표시다** — 사용자의 말로 갈아야 한다) ·
     **C 오늘의 한 문제**(학원다움 · ⚠ 같은 화면에 데일리퀴즈가 이미 있어 문제가 둘이 된다) ·
     **D 똥피하기**(진짜 게임 · 아래)
   - **고를 때 같이 정할 것** — 이름 · 연속이 끊길 때 보여줄 것 · (B면) 스티커 · (C면) 난이도

S-D. **D 똥피하기 — 사용자가 요청해 프로토타입까지 만들었다** (2026-08-21)
   사용자가 「똥피하기나 러너 정도의 게임은 작업량이 높은가」를 물었다. 답은
   **똥피하기는 하루 + 조율 반나절, 러너는 3~4일**이고 **러너의 진짜 걸림돌은 코드가 아니라 그림이다**
   (이 프로젝트에 캐릭터 에셋이 없다. 똥피하기는 도형만으로 성립하지만 러너는 초라해진다).
   그래서 **똥피하기부터** 만들었다 — 여기서 만든 «게임 셸»이 러너에도 그대로 쓰인다
   - ⚠ **캔버스는 «`#app` 밖»에 둬야 한다.** `render()`가 `app.innerHTML`을 통째로 갈아치우므로
     (index.html:5712) 안에 두면 렌더 한 번에 캔버스가 사라진다.
     시안에서는 덮개를 `.ph`의 자식으로, 홈을 `.ph-body`로 갈라 **같은 조건을 흉내냈다** —
     실제로 결과 화면에서 `drawD()`가 홈을 다시 그려도 게임이 살아남는 것을 확인했다
   - ✔ **다행히 주기적으로 도는 렌더가 없다** — `setInterval`은 두 곳뿐이고 둘 다 게임과 무관하다.
     `render()`는 사용자가 뭘 눌렀을 때만 돈다
   - **셸이 지키는 것 셋** — ① `#app` 밖 ② 화면을 떠나면 `stop()`(루프·리스너 전부)
     ③ **점수는 판이 끝날 때 한 번만 저장한다**(루프 안에서 저장하면 초당 60번 쓴다)
   - **이미 밟아 본 함정** — `dt` 상한(1/30)이 없으면 화면이 잠겼다 돌아올 때 한 프레임이
     몇 초가 되어 똥이 «통과»한다(터널링) · `pointermove`를 캔버스에 걸면 손가락이 밖으로
     나갔을 때 놓친다(`window`에 건다) · `touch-action:none`이 없으면 끄는 손가락이 페이지를 스크롤한다 ·
     `canvas.width`를 넣는 순간 컨텍스트가 초기화되므로 `setTransform`은 그 뒤에 건다
   - ⚠ **난이도 — 2026-08-21에 사용자가 「더 어렵게 · 뒤로 갈수록 똥이 많이」로 고치라 했다.**
     첫 판은 **29초면 상한**이라 그 뒤로는 아무리 버텨도 똑같았다. 정확한 지적이었다.
     → **서로 다른 시각에 오르는 셋**으로 갈랐다 — ① 낙하 `min(155+9t, 540)`(~43초 상한)
     ② 생성 간격 `max(.14, .70-.016t)`(~35초 하한) ③ **한 번에 떨어지는 개수**(30·55·80초에 한 단씩)
     - ⚠ **③이 핵심이다. 속도만 올리면 화면은 오히려 «비어» 보인다** — 빨리 떨어질수록
       화면에 머무는 시간이 짧아지기 때문이다. 「쏟아진다」는 개수로만 만들 수 있다
     - ⚠ **한 번에 여럿을 낼 때 58px 안에는 겹쳐 놓지 않는다.** 붙여 놓으면 피할 수 없는 벽이 된다
     - ⚠ **처음 30초는 일부러 예전만큼 둔다.** 한 번 초반부터 조여 봤더니 봇이 28초에 다 죽어
       **③의 단계를 아무도 못 봤다** — 「뒤로 갈수록」이 도달 불가능한 약속이 된다
     - **판정은 그림보다 작게**(반지름 19.4) — 억울하면 안 한다
   - **검증** — ⚠ **확장이 모는 탭은 `visibilityState`가 hidden이라 `requestAnimationFrame`이 안 돈다.**
     그래서 루프 대신 `_step`을 손으로 돌렸다. **봇 8판: 중앙값 25.8초 · 최대 36.5초**
     (고친 뒤. 고치기 전에는 82·58·18초였다). 36초 시점에 **화면에 23개**가 떠 있다
     - **「죽는 순간 피할 자리가 있었나」까지 쟀다** — 초반에 죽은 판은 **안전폭이 140~200px**
       열려 있었고(봇이 못 피한 것이다), 33~35초에 죽은 판은 **위협 14개에 틈 24~48px**였다.
       즉 **초반은 공정하고 후반은 진짜로 어렵다.** 이 측정이 곡선을 잡아 줬다
     - 프레임을 손으로 그려 화면도 확인했다. **콘솔 오류 0건**
   - ⚠ **하루 한 판이다 — 2026-08-21에 사용자가 정했다.** 「한 번 더」를 없앴다.
     - **소모 시점은 «판이 끝날 때»다.** 열었다가 시작 전에 닫은 것은 소모되지 않는다 —
       그러면 잘못 눌러 하루를 잃는다
     - **끝난 뒤 홈의 줄은 «닫힌 자리»로 그린다**(`.gm-run.off`). 눌러도 안 열리는 버튼을
       남겨 두면 학생은 고장 난 줄 안다
     - **시안에는 「↺ 오늘 것을 되돌리기」를 뒀다** — 하루 한 판이면 시안을 한 번밖에 못 눌러 본다.
       ⚠ **시안 전용이다. 실서비스로 옮기지 말 것**
S-D-3. **도장 그림을 사용자가 준 것으로 바꿨다** (2026-08-21) — `stamp.webp` · **259×256 · 31KB**
   「참! 잘했어요」 도장. **판이 끝나면 결과 화면에 «쿵» 하고 내려앉는다**(0.45초).
   ⚠ **글로 「도장을 찍었습니다」라고 쓰는 것과 실제로 도장이 찍히는 것은 다른 일이다** —
     이 0.45초가 D 의 보상이다. 그래서 결과 화면의 주인공을 «점수»가 아니라 «도장»으로 뒀다
   - **세 자리에 쓴다** — 결과 화면(132px) · 일곱 칸의 채워진 날(26px) · 홈의 닫힌 줄(32px)
   - ⚠ **일곱 칸에 넣을 때는 칸의 테두리 원을 지운다**(`.gm-week.img`). 도장 자체가 원이라 두 겹이 된다
   - ⚠ **미리 받아 둔다**(`(new Image()).src`). 찍히는 순간에 늦게 뜨면 «쿵»이 죽는다
   - **흰 배경 그림은 처리 방법이 다르다** → `tools/cut-sprite.html?mode=white`.
     캐릭터(검은 배경)는 «가장자리에서 흘려보내»지만, **도장은 «닫힌 원»이라 흘려보내면 안 된다** —
     원 «안쪽» 흰색이 바깥과 이어져 있지 않아 흰 원반으로 남고, 종이 위에 허연 딱지가 붙는다.
     → **채도로 지운다.** 흰색(채도 0)은 지우고 옅은 보라 채움(채도 .15)은 남긴다.
     ⚠ **밝기만으로 가르면 옅은 보라 채움(밝기 212)까지 날아간다. 채도가 진짜 기준이다**
   - ⚠ **여기서도 세 번 헛짚었다** —
     ① **JPEG 잡티가 알파 5~20 짜리 «안개»로 온 화면에 깔린다.** 눈에는 안 보이는데
        «이어져 있는 것»으로 세어져 **외딴 얼룩이 본체와 한 덩어리가 되어** 부스러기 제거를 빠져나갔다
        → **바닥 알파를 잘라낸다**(0.10 미만은 0). 종이 위에 뜨는 희미한 때도 같이 없어진다
     ② **크기만으로 부스러기를 가르면 안 된다.** 「참!」의 **느낌표가 얼룩과 크기가 비슷해**
        문턱을 얼룩에 맞추니 느낌표가 같이 지워졌다. 둘을 가르는 것은 크기가 아니라 **자리**다 —
        얼룩은 원 «바깥», 느낌표는 «안». → `clip=main`(가장 큰 조각의 상자 밖을 지운다)
     ③ **그 「가장 큰 조각」을 픽셀 수로 고르면 안 된다.** 도장의 바깥 테두리는 «얇은 선»이라
        픽셀이 적어, 안쪽 캐릭터 덩어리가 뽑히고 **테두리가 통째로 잘려 나갔다**(상자가 597→300).
        → **상자가 가장 큰 조각**으로 고른다
     ⚠ `clip=main` 은 **캐릭터에 쓰면 안 된다** — 먼지 자국과 속도선이 몸통 상자 «밖»이라 같이 날아간다

S-D-2. **주간 순위를 붙였다 — 사용자가 정했다** (2026-08-21)
   **학원 전체 · 주마다 새로 · 상위 5명만.** 「하루 한 번뿐이니 가볍게 즐길 수 있겠다」가 사용자의 판단이었다.
   ⚠ **처음에 «반 안»으로 만들었다가 사용자가 «전체 통합»으로 바꿨다.** 반은 열쇠에서 빠졌다
   - **문서를 학생 기록과 분리한다** — `rank:<그 주 월요일>` → `[{sid, name, cls, score, at}]`
     ⚠ **`record:학번`에는 성적·출결이 다 들어 있다.** 순위를 그리자고 남의 기록을 읽으면 안 된다.
       **이름·반·점수만** 든 문서를 따로 둔다
   - ⚠ **전체로 세우면 이름만으로는 누군지 모른다** — 동명이인도 있고 모르는 이름이 대부분이다.
     그래서 **이름 옆에 반을 작게 적는다**(`고1 목`). 반 안일 때는 필요 없던 것이다
   - ⚠ **전체로 합치면서 잃은 것 둘** —
     ① **5위가 그만큼 멀어진다.** 한 반 10명이면 절반이 이름을 올려 보지만 전체 60명이면
        **대부분은 한 번도 못 올린다.** 그래서 **「이번 주 내 최고」 줄이 이 기능의 본체가 된다** —
        실제로 대부분의 학생이 매일 보는 것은 그 줄이다
     ② **«반 안이라 서로 아는 사이»라는 조작 억제력이 사라졌다.** 모르는 이름이 1등에 있으면
        조작인지 실력인지 아무도 모른다. 감수하는 근거가 «보상 없음 · 하루 한 판» 둘로 줄었다
   - ⚠ **열쇠에 주(週)가 들어 있어 «리셋»이라는 동작 자체가 없다.** 월요일이 되면 열쇠가 바뀌고
     새 문서는 그냥 비어 있다. 지우는 작업이 없으니 실패할 일도 없고, 지난 주 것이 그대로 남아
     **「지난 주 1등」**을 보여줄 수 있다. (지우는 배치를 만들 뻔했다 — 만들지 말 것)
   - ⚠ **쓸 때는 «읽고 → 내 줄만 고치고 → 쓴다».** 들고 있던 배열로 통째로 덮으면
     그 사이에 올라온 **남의 점수가 사라진다**
   - ⚠ **5위 밖이면 «몇 등»인지 말하지 않는다.** 매일 열 때마다 자기 이름이 아래쪽에 있는 것을
     보면 그만두게 된다. 대신 「이번 주 내 최고 500점」처럼 **자기 점수만** 보여준다.
     이 한 줄이 이 기능의 성격을 정한다 — 겨룰 대상이 남이 아니라 어제의 자기여도 된다
   - **자리는 둘** — 홈 카드(늘 보인다)와 결과 화면(막 끝낸 순간, 관심이 가장 큰 자리).
     홈 카드는 459px 가 된다 — 「오늘 할 일」이 첫 화면에서 아직 보인다
   - ✔ **이름은 실명으로 정했다 — 사용자가 2026-08-21에 정했다.** 명단의 이름을 그대로 쓴다.
     ⚠ 그래서 **이름과 반이 학원의 모든 학생에게 보인다.** 순위 문서에 들어가는 것은
     **이름·반·점수뿐**이고 성적·출결은 들어가지 않는다 — 이 경계를 넘기지 말 것
   - ⚠ **점수는 학생 폰 안에 있다 — 조작을 막지 못한다.**
     브라우저가 Firestore 를 직접 부르는 구조라(`index.html:1590`) 콘솔 한 줄로 1등이 되고,
     **남의 줄도 덮어쓸 수 있다.** 지금 감수하는 근거는 둘뿐이다 —
     **보상이 없고 · 하루 한 판이라 갈아 넣기가 막혀 있다.**
     (셋째였던 «반 안이라 서로 아는 사이»는 전체 통합으로 사라졌다)
     🔴 **상품이나 성적 가산점을 거는 순간 이 근거가 전부 무너진다.** 그때는 워커 검증(2~3일)이
     **먼저**여야 한다 — 살아있던 시간 대비 점수와 별 개수의 상한을 되짚는 방식.
     자리는 이미 있다(워커가 AI 한도를 uid별로 서버에서 센다). 다만 **완전히는 못 막는다**
   - ⚠ **D는 「가볍게」의 반대편이다.** A·B·C가 하루 3초인데 D는 계속 하게 된다.
     **공부 앱을 열어 10분 놀다 나가는 것이 괜찮은지**를 사용자가 정해야 한다
   - **그림체 스위치를 시안에 뒀다**(💩 그대로 / 점잖게 도형). 실서비스에는 없는 시안 전용이다 —
     둘 다 그려 보고 어느 쪽이 이 학원의 톤인지 사용자가 고른다.
     ⚠ 스위치는 **떨어지는 것에만** 걸린다. 캐릭터는 둘 다 같다

S-D-1. **캐릭터를 사용자가 준 그림으로 바꿨다** (2026-08-21) — `game-char.webp` · **179×176 · 12KB**
   - ⚠ **받은 원본은 검은 배경 위에 발광 테두리가 있는 1536×1024 · 1.16MB PNG였다.**
     그대로 쓰면 종이색 화면에 **검은 사각형**이 뜨고, 44px 로 그릴 그림을 1.16MB 로 들고 다니게 된다.
     → `tools/cut-sprite.html` 로 배경을 걷어내고 잘라 **12KB WebP** 로 줄였다.
     **원본은 저장소에 안 남겼다**(작업 뒤 지웠다)
   - ⚠ **두 번 헛짚었다. 같은 실수를 반복하지 말 것** —
     ① **축소한 뒤에 훑었다.** 검은 윤곽선(L≈30)과 흰 발광(L≈240)이 섞여 «중간 회색»이 되고
        그것이 «무채색·밝음» 조건에 걸려 윤곽선을 뚫는 다리가 됐다 → **머리카락이 통째로 먹혔다.**
        **원본 해상도로 훑는다** (1.5M 픽셀이라도 흘리기는 O(N)이라 금방 끝난다)
     ② **문턱을 70으로 잡았다.** 단면을 재 보니 배경은 **정확히 0**이고 거기서 **한두 픽셀 만에**
        윤곽선(27~36)이 온다 — 내가 가정한 «넓은 발광 그라데이션»이 없었다.
        70은 머리카락(55)·바지(78)까지 배경으로 삼았고, 발광 띠의 틈으로 새어 들어가
        **윤곽선 그물을 통째로 훑어** 색만 남은 그림이 됐다 → **문턱 16**
   - **먼저 픽셀을 재라.** 두 번 다 «그림이 이렇게 생겼겠지»로 짐작해서 틀렸고,
     단면(luma·채도)을 한 번 찍어 보니 5분 만에 끝났다
   - ⚠ **기준점은 상자 중앙이 아니다** — 오른쪽에 먼지 자국·속도선이 붙어 있다.
     **색이 있는 픽셀의 무게중심**(0.479)에 맞춘다. 먼지·속도선은 무채색이라 저절로 빠진다
   - ⚠ **그림의 기본 방향은 «왼쪽»이다. 오른쪽으로 갈 때 뒤집는다.**
     처음에 반대로 걸어 놔서 **왼쪽을 눌렀는데 오른쪽을 보고 있었다** — 사용자가 잡았다.
     **가리는 법**: 먼지 자국과 속도선이 **오른쪽**에 있다. 자국은 언제나 «가던 길 뒤»에 남으므로
     오른쪽에 자국이면 왼쪽으로 가는 그림이다. 얼굴만 보고 짐작하지 말고 자국을 볼 것
   - ⚠ 방향 판정에 **문턱 0.5px**을 뒀다 — 0이면 제자리에서 좌우가 깜빡인다
   - ⚠ **그림을 못 읽으면 예전 도형으로 떨어진다.** 그림 하나 때문에 게임이 안 열리면 안 된다
   - ⚠ **확장이 data URL 모양의 긴 문자열 반환을 막는다.** 그래서 결과를 꺼내 올 수 없었다.
     **일회용 서버**(scratchpad)를 띄워 브라우저가 곧장 POST 하게 하고 그 서버가 파일을 썼다.
     서버는 한 번 받고 스스로 닫는다 — 쓰기 구멍을 열어 둔 채 남기지 않는다.
     ⚠ 이 컴퓨터에는 **ImageMagick 도 Pillow 도 없다**(`convert`는 윈도우 것이고 `python`은 스토어 스텁이다).
     이미지 가공은 **브라우저 캔버스가 유일한 길**이다
C. **클리닉 — 배포본에서 남은 것 하나** (2026-08-14에 네 번 고쳤다) → 위 📌 ④⑤⑥
   - ~~시간대를 범위로 열기~~ · ~~학생이 여러 시간대를 골라 신청~~ —
     **사용자가 배포본에서 이미 해 봤고 데이터로 확인했다.**
     08-16에 2시~9시 여덟 자리가 서 있고, 두 학생이 «2시~4시 3개»·«2시~9시 8개»로 잡아
     겹치는 2·3·4시가 2/20, 나머지가 1/20으로 **자리마다 정확히 반영**돼 있다
   - **남은 것은 「희망 시간 범위 제안」을 승인해 보는 것뿐이다.**
     그때 **이미 열린 자리에 앉는지**(새 자리가 안 생기는지)를 본다 — 로컬에서는 확인했다
   - ⚠ 「마지막」 시각에도 자리가 하나 선다 — 뜻이 갈리는 자리다.
     쓰다가 어색하면 «끝나는 시각»으로 바꿀지 정하면 된다
   - ~~옛 표기(「저녁 7시」)~~ — **정리했다.** 실 DB에 자유 문장 시간은 없다
L. **랜딩 마무리 — 골격은 섰고 재료를 기다린다** (2026-08-13 재구성) → 6절 「랜딩을 다시 세웠다」
   - ~~교재 사진~~ — **2026-08-13에 사용자가 준 목업 PNG 세 권으로 넣었다.**
     ⚠ 개념서는 «스프링 제본으로 뽑으면 좋겠다»고 했다가 **그냥 가기로 정했다** — 다시 꺼내지 말 것.
     남은 것은 **펼친 내지** 사진뿐이다 — 있으면 「직접 선별한 문항」을 눈으로 보여줄 수 있다
   - **원칙 문장 셋을 사용자가 읽고 확정** — 큰 문장은 말씀을 한 겹 다듬은 것이고
     딸린 설명 세 줄은 내가 이은 것이다. **틀린 뉘앙스가 있으면 그 줄부터 고친다** (9절 8번)
   - **「생각의 흐름」에 부를 이름** — 메가스터디의 「3단계」·「3 in 1」에 해당하는 자리다.
     수업에서 이미 쓰시는 말이 있으면 그게 제일 좋다
   - **클리닉** — 「관리도 열심히, 클리닉도 많이」가 원칙 03의 절반인데 지금 페이지에 한 줄뿐이다.
     주 몇 회·누가 오는지·무엇을 하는지를 들으면 한 칸을 세울 수 있다
   - **장치 섹션의 화면 캡처 3장** — 지금은 글만 있다. 강사 검토 · 학생 오답숙제 · 데일리퀴즈를
     배포본에서 찍어 넣으면 「말이 아니라 실물」이 된다
   - **대상·지역** — 고1~고3 · 광진 일대로 보이는데 명시 범위를 정해야 한다. 학부모의 첫 필터다
0. ~~화면에서 (B) 한 바퀴~~ — **2026-08-11 밤에 끝냈다.** ①②③④ 전부 의도대로 돌았다.
   자세한 것은 맨 위 ✅ 상자. 아래 셋은 다음에도 그대로 쓸 수 있는 요령이라 남긴다.
   - **`figureSpec`이 시원찮으면** 검토 화면에 편집칸이 이미 있으니 강사가 직접 쓴다.
     프롬프트의 [figureSpec 작성 형식]만 손보면 된다 — 거기 한 곳이다
   - ⚠ **«세는 문제»의 정답은 검산해야 한다** — 지도 색칠이 세 번 다 다른 답을 냈다 (위 경고).
     그래프·도형처럼 **식으로 푸는 것**은 이번에도 두 번 다 맞았다
   - **워커를 화면 없이 눌러 보는 길이 있다** — `node tools/probe-worker.mjs cases.json`.
     익명 로그인으로 토큰을 받아 Node에서 부른다 (CORS는 «브라우저» 제약이라 안 걸린다).
     **`/quota`만 볼 때도 이 길이 제일 빠르다** — 한도를 안 쓰고 남은 건수를 알 수 있다.
     cases.json 만드는 법은 그 파일 머리말에 있다 (`.gitignore` — 시험지 본문이라 안 올린다)
0-E. ~~「AI로 다시 생성」 뒤에 빈 큐를 마주한다~~ — **2026-08-13에 고쳤다.**
   그림 업로드 쪽에 두 벌로 있던 같은 처리까지 **`followBankToQueue()` 하나로 모았다**
   → 위 「큐를 문항 따라 옮긴다」. 소스에서 떼어 여섯 경우로 확인했다.
   **화면 확인만 남았다** (위 시작 상자 ③).
0-F. ~~그림 초안을 기계가 낸다~~ — **2026-08-12에 1·2·3단계 다 끝났다. 배포·배포본 확인까지 완료.**
   `figure.js` · `tools/figure-preview.mjs` · `tools/probe-figure.mjs` · 워커 `/figure` ·
   검토 화면 「초안 그려 보기」 → 6절 「그림을 코드로 그린다」. **남은 것은 아래 둘이다.**
   - ⚠ **(B)가 싸지면 프롬프트의 억제력이 사라진다.** 지금 「(C)→(A)→(B) 순으로 따져라」가
     먹히는 이유의 절반은 «(B)를 고르면 사람이 그려야 한다»는 대가다. **갈래 분포를 다시 재 볼 것**
   - 다음에 넓힐 곳 — 스키마 v1은 `kind:'graph'` 하나다. **v2 후보는 «좌표평면 위의 도형»**
     (선분·원·각 표시). 지도·입체는 계속 사람 몫이다
0-G. ~~`dbGet`이 실패를 «비어 있음»과 구분하지 않는다~~ — **2026-08-12에 고쳤고 배포본에서 확인했다**
   → 6절 「기록을 지우는 두 가지 길을 막았다」. 학생으로 제출까지 해 봤고 차단 경고는 안 떴다
   (= 새 문이 정상 경로를 막지 않는다).
0-A. ~~조교 여러 반 — 배포본에서 눈으로~~ — **2026-08-14에 끝났다.**
   옛 형식(`classId`)은 **감사 로그가 증명한다** — 2026-08-11 「김하현 · **테스트반** →
   테스트반2 · 고20.5B」. 단일 `classId` 상태에서 읽혀 변경까지 됐다.
   조교 화면에 「담당 반 3개」·**「반」 열**·**칩 고르개**(전체 4 · 테스트반2 0 · 고20.5B 1 ·
   테스트반 3)가 서고 숫자도 실제 배정과 맞는다. 교사쪽 설정 > 조교도 정상.
0-B. **데일리퀴즈 전체 흐름 — 배포본에서 한 바퀴.** 로컬에서는 저장 경로를 막은 채 확인했다.
   순서 — **설정 > 시험 범위**에서 시즌 맞추기 → **반 관리 > 진도**에서 반 과목 정하기 →
   시험지 등록(**문항마다 단원**) → 학생이 틀린 번호 등록 → 쌍둥이 생성·승인 →
   **오답숙제 제출** → 그때 퀴즈에 뜬다.
   - **뒤쪽 절반은 2026-08-11 밤에 봤다** — 오답 등록 → 오답숙제 카드 → 제출 → **자동 채점**까지
     배포본 실데이터로 확인. 그 뒤 데일리퀴즈가 **「범위 밖으로 접어 둔 문제 2개」**로 접는 것도 봤다
   - **아직 못 본 것은 «범위 안»으로 들어왔을 때 실제로 세 문제가 뽑히는가**다.
     시즌을 그 단원이 든 것으로 맞추거나 **설정 > 시험 범위**에서 범위를 넓혀 놓고 다시 본다
   - **날짜가 바뀔 때 새로 뽑히는지**는 하루 지나야 안다 (로컬에서는 `todayStr()`을 갈아끼워 확인했다)
0-C. ~~표 모양 재확인~~ — **2026-08-14에 끝냈다.** 그림 쪽은 2026-08-11 밤에 봤고,
   표는 `test22`를 다시 올려 훑었다. **조건 상자 다섯(8·9·16·17·18)은 «상자»로 정상**,
   그런데 **9번 좌석 배치도가 무너져 있었다** → 파서를 고쳤다
   (6절 「자리표는 선지가 아니다」). ⚠ 배포본에 저장된 9번은 **아직 깨진 채다** —
   내용은 파싱 «시점»에 평문으로 박히므로 다시 올려야 고쳐진다
0-D. **단원 AI 판정** — 지금은 낱말 대조(`guessChapter`)다. 워커에 엔드포인트를 하나 더하고
   재배포하면 품질이 오른다. **그 함수 하나만 갈아끼우면 된다.** 로컬 검증이 안 되므로
   배포본에서 바로 확인해야 한다 (9절 1번)
1. ~~학생 화면 태블릿 대응~~ — **2026-08-12에 끝냈다** → 아래 「학생 화면을 태블릿 폭으로 푼다」.
   남은 것은 **실제 기기로 한 번 보는 것**뿐이다 (측정은 진짜 뷰포트로 했지만 손가락은 못 재 봤다).
2. **비밀번호 평문 저장** (9절 3번) — **지금 남은 것 중 제일 큰 구멍이다.**
   학생 `pw` · 조교 `pw` · `teacher-pw`가 전부 Firestore에 그대로 있다.
   강사는 아이디도 없이 비밀번호 하나뿐이라 «누가 시도했는지»도 안 남는다(4-D).
   워커 인증 때 미룬 「강사만 접근」(커스텀 클레임) 문제와 맞닿아 있어서
   **손대기 전에 설계부터 이야기해야 한다** — 화면 여러 개가 같이 움직인다
3. ~~워커 재배포~~ · ~~AI 생성 배포본 확인~~ — **2026-08-10에 둘 다 끝났다.**
   워커는 그날 두 번 배포했고(그림 프롬프트 · `/quota`), AI 경로는 실제로 불러
   **429까지 받아 봤다** (그래서 한도 어긋남을 찾았다). 남은 것은 «생성 품질»뿐 → 0번
4. 9절의 나머지 — `href` 없는 `<a onclick>`의 키보드 접근 · state만 갱신하는 입력칸의 커서 튐
5. 태블릿 다음에 볼 만한 것 — 학생앱 홈·성적을 **태블릿에서만 2열**로 (1번을 실제 기기로 본 뒤 판단).
   같이 볼 것: **폰에서 그림 눈금이 9.5px**다. 캔버스를 480으로 좁히고 `width:100%`로 올려 쓰면
   11px가 되는데, **이름표 자리 규칙을 다시 재야 한다** (`figure-preview.mjs`로 배포 없이 볼 수 있다)
6. **옛 시험지는 표가 안 살아난다** — 파서를 고치기 «전»에 올린 것은 본문이 이미 뭉개져
   저장돼 있고 원본 hwpx를 보관하지 않는다. **지우고 다시 올리는 것이 유일한 방법이다**

> **시안에 있으나 못 만드는 것** (물어보면 이렇게 답한다)
> - 공지 「읽음 현황」 — 읽음이 학생 브라우저 localStorage에만 남는다
> - 조교 권한 토글 — assistant 레코드에 권한 필드가 없다
> - AI 「유사도 하한」 — 유사도 점수라는 것이 아예 없다 (시안에서 지어낸 수치)
> - 학생 상세의 「과제 추가」·「보충영상 배정」 — 과제는 반 단위로만 배포되고,
>   개인 영상은 결석을 찍을 때 함께 등록된다 (경로를 두 벌로 만들지 않는다)
>
> ~~학생 「고정 메모」~~ — **2026-08-08에 만들었다.** 도메인에 없으면 못 만드는 것이 아니라,
> «컬렉션을 늘리지 않고 kv 문서 하나로 되는가»를 먼저 물으면 되는 것이 있다 (수업 메모가 그 선례였다).
>
> ~~데일리퀴즈 정답률~~ — **2026-08-09에 만들었다.** 퀴즈에 정답이 없던 것이 문제였는데,
> **문제를 내는 곳을 바꾸니**(교사 작문 → 학생 오답) 정답이 딸려 왔다.
> 못 만드는 것처럼 보이는 지표가 «데이터가 없어서»가 아니라 «엉뚱한 데서 뽑고 있어서»일 수 있다.

**리디자인은 여기서 끝났다.** 이 다음부터는 «디자인 이식»이 아니라 제품 개선이다.

> **수업 화면과 반 관리 > 수업 기록의 출결 중복은 그대로 둔다.** 겹쳐 보이지만 역할이 다르다 —
> 수업은 «지금 하는 수업을 4버튼으로 빨리 찍는 곳», 수업 기록은 «지난 날짜를 5상태로 고치고
> 성적·시험까지 넣는 곳». 시안도 둘을 따로 그렸다.

화면 하나를 옮기는 절차는 명단·신호·수업에서 확정됐고, 끝까지 그대로 반복했다.
이제 «옮길 화면»은 없지만 **새 화면을 만들 때도 같은 순서**다.
1. `teacherXxxHTML()`를 새로 만들고 `teacherTabBodyHTML()`의 해당 분기를 갈아끼운다
2. 화면별 스타일은 `.app` 블록에 **접두사를 붙여** 둔다 (명단은 `rst-`). ds.css 클래스 이름을 다시 정의하지 않는다
3. 브라우저에서 **행 높이 · 잘림 · 넘침**을 DOM 기하로 잰다. 스크린샷만 보면 놓친다
4. 같은 일을 하던 옛 진입점을 이쪽으로 모은다

학생 화면도 같은 절차를 따랐다. 다른 것은 두 가지뿐이다 —
`STUDENT_TAB_SECTION`이 «화면 → 탭»을 되찾아 주고(그래서 옛 탭 id를 안 바꿔도 된다),
스타일 접두사가 `sap-`이며 셸은 `.app.sap`이다.


> ### ✅ **닫힌 것 — 다시 꺼내지 말 것**
> · **리포트 다섯 줄의 구성 · 「등수」 열** → 「이대로 좋다」(09-01). **등수도 그대로 둔다.**
> · **「그중 N건은 보완 필요」 곁줄** → 「그대로 내보낸다」(09-01). 제출률 숫자는 계속 안 건드린다.
> · **예비 글귀의 말투** → 「이대로 초안으로 쓸 만하다」(09-01). ⚠ **내 문장이라는 사실은 남는다** —
>   사용자가 실제로 써 본 뒤 다시 말할 수 있다. 먼저 고치지는 말 것.
> · **리포트 색 기준 60/80** → 설정으로 뺐다(09-01 · R-2a). 이제 «내가 정한 값»이 아니다.
> · **검색 고르개(학교 ▾/학년 ▾)** → 「검색은 그대로두고」(09-01). 띄어쓰기 검색(C-14)으로 충분.
> · **「영상」을 「자료」에 넣기** → 「괜찮다, 그대로 둔다」(09-01).
> · **「문항 등록」의 끝까지** → 09-01에 눌러 봤다(C-21). 08-31부터 열려 있던 것이 닫혔다.
> · **옛 복사본 둘**(`ex1788011969268`·`ex1788011972941`) → 「지우고 다시 하면 되니까 무시해줘」.
>   ⚠ 다만 «옛 복사본은 저절로 안 이어진다»는 사실은 남는다 — 실 데이터에서 나오면 **지우고 다시 복사**하시라고 안내.
> · **P-2(평문 비밀번호)** → «내년». 전제는 **«쓰는 사람이 몇 명뿐»** — 신입생·반이 늘면 먼저 물을 것.
>

> ### 📁 **끝난 일 상자 25개를 기록으로 옮겼다** (2026-09-07)
> ⚠ **버린 것이 아니다** — [`docs/기록-2026-09.md`](docs/기록-2026-09.md) 맨 아래에 그대로 있다.
> 여기 남긴 것은 **규칙·함정·아직 할 일**뿐이다(🔴 🔵 ▶). 옮긴 것은 이것들이다:
>
> · ✅ 워커를 올렸고 되돌려주기가 돈다 — 실측 (2026-09-06)
> · ✅ 동그랑땡(D) 27제를 창고에서 걷어냈다 (2026-09-06 · 사용자 요청)
> · ✅ 가형/나형을 코드가 안다 — 사용자가 답을 주었다 (2026-09-06)
> · ✅ D 끝 — 「데이터 정리」는 «비어 있다»가 맞았다. 그래서 도구가 아니라 거짓말을 걷어냈다 (2026-09-06)
> · ✅ C 끝 — 폰에서 한 바퀴. 레이아웃은 멀쩡했고, 대신 «검은 그림»을 찾았다 (2026-09-06)
> · ✅ B 끝 — 사용자가 워터마크 2문항을 고쳐 주어 71제가 다 담겼다 (2026-09-06)
> · ✅ A 끝 — 브라우저에서 실제로 돌려 봤다 (2026-09-06 · 사용자가 크롬을 열어 주었다)
> · ✅ 2026-09-06 — 웹 「hwpx로 올리기」가 ②꼴을 알게 됐다 (1번 완료)
> · ✅ 창고 수식을 «교재를 다시 안 올리고» 고친다 (2026-09-06 · 사용자 질문에서 나왔다)
> · ✅ 빨갛게 뜨는 수식을 «세어서» 잡았다 — 18개 → 1개 (2026-09-06 · 사용자가 짚었다)
> · ✅ 그래서 지금 빨갛게 뜨는 수식은 0개다 (2026-09-06 · 실측)
> · ✅ AI 가 남는 한도로 창고를 채운다 — 만들었다 (09-06 · 사용자 요청)
> · ✅ 정답 564개를 회수했다 — 이게 없으면 위의 것이 아예 못 돈다 (09-06)
> · ✅ #2 첫 덩이 — `auditlog` 를 «통째로» 안 읽는다 (2026-09-06 · 사용자가 이 쪽을 골랐다)
> · ✅ #1 끝났다 — 문항 본문 읽기가 «564건 → 1건»이 됐다 (2026-09-06)
> · ✅ 09-04에 한 일 — 시험지 등록 리뉴얼 · 오답숙제를 코드 위로 · 문항 창고
> · ✅ 클리닉 화면 재구성 — 만들어서 푸시까지 끝났다 (09-05 · 커밋 `22579b2`)
> · ✅ 후속 — 「이 주에 처리된 신청」을 통째로 없앴다 (09-04 · 사용자 질문에서 시작)
> · ✅ 끝난 것 둘 — 규칙도 열렸고 564제도 창고에 들어갔다 (09-05 밤)
> · ✅ 그림 올리기 — 끝났다. 54개가 다 들어갔다 (2026-09-04 밤)
> · ✅ 데일리퀴즈를 코드 위에 다시 세웠다 — 배포 완료 (09-05)
> · ✅ 오류 신고 단추 — 만들고 배포했다 (09-05)
> · ✅ 씨앗이 실 DB에 있다 — 이제 학생을 새로 안 만들어도 된다 (09-01)
> · ✅ C-1~C-22 + R-2 · R-3 · T-1까지 심었다. ⚠ `git status -sb`로 먼저 볼 것.
> · ✅ 리포트 인쇄(2차)는 09-01에 만들었다 (R-2b) — 남은 것은 종이로 한 장 뽑아 보는 것뿐.

### 시안대로 신설할 화면은 남지 않았다
「신호」와 「수업」이 마지막이었다. 상단바 7메뉴가 전부 제 화면을 갖고 있고,
학생 하단 탭 4개도 마찬가지다.

`ds.js`는 **실서비스에 로드하지 않는다.** `fitCanvas`와 «가짜 학생 22명»이 들어 있어 그대로 쓰면 예시 데이터가 제품에 섞인다. 메뉴 정의는 `index.html`의 `TEACHER_NAV`/`TEACHER_SUBNAV`/`STUDENT_NAV`/`STUDENT_SUBNAV`에 따로 있다. 아이콘도 `ICON_PATHS`에 따로 두고, ds.js에서 쓰는 것이 생기면 그때 옮겨 적는다.

---

## 1. 결정 기록 — 막힌 결정은 없다

**2026-08-06에 막혀 있던 결정 3건이 전부 해소됐다.**

| | 결정 | 결과 |
|---|---|---|
| ① | 랜딩 「전환」 문구 | **「짐작으로 가르치지 않습니다」** — 반영 완료 |
| ② | 로고 차콜 지정값 | **`#3F3537`** — 매뉴얼북에서 직접 확인, 반영 완료 |
| ③ | 다크 테마 | **라이트 유지** — 5절 권고대로 확정 |
| 2-C | 구현 방식 | **기존 SPA에 이식** — 아래 참고 |

### 이식과 별개로 남아 있는 것
구현 순서는 **「▶ 다음 차례」**가 갖고 있다. 여기 있는 것은 그 바깥의 일이다.

1. 2-C에 남은 두 가지 — 렌더링 구조(전체 재렌더링 유지 여부) · 관리자 모바일 대응 수준.
   **이식을 끝까지 «전체 재렌더링 유지»로 했고 막히지 않았다.** 다만 그 구조 때문에 화면마다
   대비를 해 둬야 했다 — 채팅은 열릴 때 맨 아래로 보내고(9절 아님, 0-B), 랜딩은 한 번 드러난 뒤
   다시 페이드인하지 않게 막고, 검색창은 `focus()`로 손보고 있다(9절 4번).
   관리자 모바일은 여전히 미정이다
2. 랜딩 5안 기각 사유 청취 → 필요하면 랜딩 재검토.
   **「하루」로 합류를 끝냈으므로 사실상 닫혔다** — 다시 꺼낸다면 그때 듣는다
3. ~~`AI_SITE_TOKEN` 회전~~ → **3단계만 남았다.** 「▶ 다음 차례」

> **①·② 반영 결과는 확인했다** — 랜딩 `h2`가 데스크톱 62px / 모바일 29px에서 «짐작으로 / 가르치지 않습니다» 두 줄로 정확히 접힌다. 잘림·부모 침범·가로 스크롤 0건.

---

## 2. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 저장소 | `github.com/khhyun827-lang/hahyunmath` (main) |
| 로컬 | `C:\Users\hahyun\Desktop\claude work` |
| 추적 파일 | 시안·로고·도구까지 전부 추적 중 (2026-08-06 커밋). 예전엔 `index.html` 등 3개뿐이었다 |
| index.html | **22,825줄 / 1,482KB** (gzip 479KB). CSS·JS·HTML·데이터가 전부 한 파일 |
| | ⚠ 2026-09-07에 재 보니 여기 **4,875줄 / 322KB** 라 적혀 있었다 — **4.7배 낡은 값**이었다. |
| | 🔵 그중 **36%(535KB)가 주석**이다(1,680덩이). 학생 폰이 한 번 열 때 **551KB**(gzip)를 받는다. |
| | 🔴 그 주석은 **이 프로젝트의 기억**이라 지우면 안 된다. 떼려면 «배포할 때만» 떼는 빌드가 답인데, |
| | 그건 아래 「빌드 도구 없음」을 깨는 일이다 — **사용자가 「느리다」고 하기 전에는 건드리지 않는다.** |
| 빌드 도구 | 없음 (package.json / 번들러 / 프레임워크 전무) — **일부러 없다.** `hwpx.js` 가 웹에서는 `<script src>`, node 에서는 `new Function` 으로 읽혀야 해서 `export` 를 못 붙인다 |
| 노트 | `CLAUDE.md` 2,258줄 / 111KB + `docs/` 기록 셋 (2026-09-07에 갈랐다) |

### 2-A. 기술 스택
- **렌더링**: 프레임워크 없는 순수 JS SPA. 템플릿 리터럴 → `app.innerHTML = html` 로 **전체 화면 통째로 재렌더링** (`render()`, index.html:2925)
- **인증**: Firebase Auth (compat SDK v10.12.2)
- **DB**: Firestore를 SDK가 아닌 **REST + fetch**로 직접 호출 (index.html:497-501). WebChannel 차단 이슈 회피 목적이라 주석에 명시됨
- **수식**: KaTeX 0.16.11 (`$...$` 델리미터)
- **파일**: SheetJS(xlsx), JSZip 지연 로딩 + HWPX 파서 직접 구현 (index.html:2424-2721)
- **AI**: Cloudflare Worker(Gemini) 프록시로 유사문항 자동 생성, 일 20건 제한
- **이미지**: Google Drive 업로드 프록시 + 클라이언트 압축
- **라우팅**: 해시 기반 (`#teacher/home`, `#student/quiz`, `#assistant/students`)

### 2-B. 데이터 구조 (리디자인해도 반드시 보존)
Firestore `kv/` 하위 **문서-당-레코드 컬렉션**. 구버전 blob은 `migrateBlobToCollection()`이 자동 이관.
```
students / classes / notices / consults / quizzes / clinics / clinicslots
qnas / videos / assistants / exams / problembank / assignments / auditlog
kv/teacher-pw          단일 blob
kv/signal-mutes        {학생id: 'YYYY-MM-DD까지'} — 신호 화면의 «해제»
kv/classnote:{반id}    {날짜: 글} — 수업 화면의 «메모»
kv/record:{studentId}  scores, attendance, assignments, progress,
                       quizAnswers, wrongHomework, videoProgress, assignmentsDone
kv/chat:{studentId}    메시지 배열
```
- `student`: studentId, name, pw, grade(고1/고2/고3), school, parentPhone, phone, classId
- `class`: id, name, schedule(`"월,수,금 7시~9시"`), period, status
- 출석 상태 5종: **출석 / 지각 / 조퇴 / 결석 / 퇴원** (퇴원 시 로그인 차단)
- 오답 검토 3단계: **pending → review1 → approved**
- 클리닉 상태: 대기 / 승인 / 완료 / 취소
- 상수: `UNITS`(7과목), `UNIT_CHAPTERS`(과목별 단원 체크리스트) — index.html:849-861

**유지해야 할 핵심 기능** — 오답 파이프라인(시험지 등록 → 학생 오답 체크 → AI 쌍둥이 문항 생성 → 3단계 검토 → 오답숙제 배포) · 영상 시청률 실시간 트래킹(YouTube IFrame API) · 반별 진도 체크리스트 · 출석 5상태 · 감사 로그 · HWPX/엑셀 일괄 등록 · 조교 권한 분리

### 2-C. 구현 전에 정해야 할 것
- [x] 리디자인을 **기존 SPA 안에 이식**할지, 별도 파일로 새로 만들지
      → **기존 `index.html`에 이식 (2026-08-06 확정).** Firestore REST · HWPX 파서 ·
      오답 파이프라인 · 영상 트래킹이 전부 이 파일에 있고, 그게 이 제품에서 제일 비싼 부분이다.
      화면을 하나씩 갈아끼우면 기능 유실 위험이 가장 낮다
- [ ] 전체 재렌더링(`innerHTML`) 구조를 유지할지, 부분 렌더링으로 바꿀지
- [ ] 관리자 화면의 모바일 대응 수준 (태블릿·폰에서 실제로 쓸 것인지)

---

## 3. 디자인 시스템 — 확정

**기준: 관리자 시안 A3 「학생 도시에」.** 색은 **자체 로고**에서 나온다.
상세 문서는 `design-v1/00-system.html`. 아래는 요약이다.

### 3-A. 색 — 새 색은 만들지 않는다
```
--paper   #FAF9F6   앱 배경 (순백 안 씀)
--card    #FFFFFF   패널·카드
--ink     #22221E   본문
--sub     #5C5C54   보조
--dim     #96968C   흐림
--line    #E7E5DE   패널 경계
--line2   #F1EFE9   내부 구분

--accent      #3F3537   주조 = 로고 차콜. 선택·주요 액션·버튼 (매뉴얼북 지정값)
--accent-d    #2A2325
--accent-bg   #ECE9E6
--accent-line #D5D0CA

--brand       #BA5054   브랜드 = 로고 와인. UI 강조에 쓰지 않는다
--brand-d     #9E4246
--brand-gray  #A8A4A4   로고 워드마크 KIM HA HYUN MATH

상태색 (의미 있을 때만)
--ok    #3F7A4D 출석   --late  #9A7000 지각   --leave #6B46A8 조퇴
--no    #B03A2E 결석   --gone  #96968C 퇴원   --blue  #39607F 성적

--note #FDFAEF / --note-line #EDE3C6   «사람이 적은 것»에만
```

> **와인을 UI 강조색으로 쓰지 않는 이유** — 위험 `#B03A2E`와 색상이 가까워
> 고밀도 표에서 «선택된 행»과 «위험한 학생»이 구분되지 않는다. 취향이 아니라 사용성 문제다.

### 3-B. 서체
- **Pretendard** 본문 (관리자 13px / 학생앱·랜딩은 더 큼)
- **Noto Serif KR** — «그 화면이 다루는 대상의 이름»에만. 그 외 어디에도 쓰지 않는다
- **JetBrains Mono** — 숫자·시각·ID·전화 전부. `tabular-nums`

### 3-C. 반경 · 모션 · 인터랙션
```
--r-xs 5 / --r-sm 7 / --r-md 9 / --r-lg 12 / --r-xl 14 / --r-pill 20
--t 140ms cubic-bezier(.4,0,.2,1)
```
- 선택 = **accent 배경 + 좌측 3px accent 바**. 리스트·표·큐 어디서나 같은 모양
- hover = 웜 그레이 틴트 한 단계. 그림자·확대 없음
- 포커스 = accent 링 3px @ 12%. 절대 지우지 않는다

### 3-D. 로고
`로고/` 폴더. 2024-01 PAPERFIT 제작, 매뉴얼북 있음.
- 가로형/세로형 × 3종 — (1) 컬러 (2) **흰색 녹아웃** (3) 단색
- 심볼은 **곡선 + 붉은 점** (삼차함수 그래프처럼 보인다). 랜딩 척추의 점이 이 점의 반복이다
- **브랜드 이름은 로고로만 말한다.** 같은 이름을 서체로 또 쓰지 않는다

---

## 4. 만들어진 화면

### 4-A. 관리자 16개 (`design-v1/`, 1440×900)
메뉴 구조 — 상단바·서브탭은 `ds.js`의 `NAV`/`SUBNAV` 한 곳에서 생성된다.

| 메뉴 | 화면 |
|---|---|
| **명단** | `01-roster` → `03-student`(학생 상세) |
| **신호** | `05-signals` |
| **반** | 학생 명단(→01) · `14-log`(수업 기록) · `06-classes`(진도) · `15-quiz`(데일리퀴즈) · `16-videos`(영상) |
| **수업** | `02-class` — 출결 → 진도 → 과제 → 메모 |
| **오답** | `04-review`(검토) · `07-exams`(시험지) · `08-bank`(창고) |
| **소통** | `09-clinic` · `10-notice` · `11-qna` · `12-chat` |
| **설정** | `13-settings` |

각 화면의 «역할»과 «무엇을 흡수/버렸는가»는 `preview.html`과 `00-system.html`에 적혀 있다.

### 4-B. 학생 마이페이지 11개 (`20-student-app.html`, 모바일 390×812)
**기존 11개 탭을 4개로 묶었다. 기능은 하나도 빼지 않았다.**
```
홈    오늘 할 일 · 다음 수업 · 이번 주 요약
학습  데일리퀴즈 · 오답숙제 · 진도·과제 · 강의시청
기록  성적 · 출석
소통  공지사항 · 질의응답 · 클리닉 신청 · 채팅
```
하단 탭바에 들어가는 것은 4~5개가 한계다. 11개를 가로로 밀게 하면 뒤쪽은 아무도 안 누른다.

**실서비스 이식 완료 (2026-08-07).** 시안과 다르게 간 곳은 [`docs/기록-2026-08-이식.md`](docs/기록-2026-08-이식.md) 의 「학생 마이페이지 이식 결과」에 있다.

### 4-C. 랜딩 (`landing-v2/index.html`) — 컨셉 「하루」
**이 학원이 일하는 하루를 방문자가 목격한다.**
기존 5안이 전부 «수학»을 소재로 썼다면, 이번은 «서비스»를 소재로 쓴다.

```
도착   로고 + 오늘 날짜(자동) + 첫 로그가 접힌 채로 보임
하루   16:00 수업 / 19:00 수업 / 21:10 수업 / 21:14 검토 / 23:41 질문
       ──── 다음 날 ────
       09:12 답변          ← 날짜가 넘어가는 이 한 칸이 핵심
전환   차콜 풀블리드 + 흰 세리프 한 문장   ← 유일하게 큰 소리 「짐작으로 가르치지 않습니다」
사람   메모 종이 + 인물 컷아웃
행동   상담 신청
```
- 시각·행위 종류만 쓰고 **수치를 넣지 않았다** — 공개 페이지의 숫자는 방문자가 사실로 읽는다
- 16시·19시·21시10분은 **실제 반 시간표**다
- 콘텐츠는 `CONTENT` 객체 한 곳에 모여 있다. 문자열만 갈면 그대로 돌아간다
- **경력·성과 자리는 주석으로 미리 잡아 뒀다** (`CONTENT.credits`). 나중에 끼워 넣으면 레이아웃이 깨진다

**실서비스 합류 완료 (2026-08-07).** 다섯 장면에 **들은 말 · 기록 · 공지**를 더하고 커리큘럼은 뺐다.
합류하면서 시안에서 찾은 버그 둘(히어로 척추 어긋남 · mono 한글 벌어짐)은 **시안에도 같이 고쳤다** —
여기가 「따라 그릴 기준」이라 시안이 틀린 채로 남으면 다음에 그걸 또 베낀다. 상세는 0-B.

### 4-D. 로그인 (`17-login.html`) — 상태 5개 · **2026-08-08 신설**
**역할: 누구로 들어갈지 고르고 들어간다. 그 이상은 하지 않는다.**
1440 캔버스로 그리지 않았다 — 폰과 데스크톱에서 «같은 한 칸»이라 캔버스를 씌우면 거짓이 된다.

- **문 셋을 동등하게 두지 않았다.** 지금은 학생/조교/강사가 같은 크기의 탭 셋이다.
  그런데 오는 사람은 거의 전부 학생이고, 조교는 계정이 아직 없고, 강사는 한 명이다.
  → **학생 폼이 곧 화면**, 조교·강사는 아래 작은 글. 공개 홈이 이미 같은 비중이다
- **오류는 칸 «위»에 온다.** 지금은 버튼 아래라, 다시 입력하려고 올라가면 안 보인다
- **「최초 비밀번호 설정」을 제 상태로 그렸다.** 지금은 힌트 한 줄로 지나가는데,
  여기서 정한 값은 되찾을 수 없다. 「한 번 더」 확인 칸을 **새로 넣었다** — 순수 클라이언트 검증이라
  도메인에 없는 기능을 만드는 것이 아니다
- **강사 로그인에는 아이디가 없다.** 비밀번호 하나뿐이고, 그게 지금 제품의 사실이라 그대로 그렸다.
  다만 약점이다 — 누가 시도했는지가 남지 않는다. 9절 3번과 함께 다룰 일이다
- ⚠ **지금 로그인 폼은 Enter가 안 먹는다** (`<form>`도 keydown도 없다). 옮길 때 같이 고친다

### 4-E. 조교 (`18-assistant.html`) — 화면 2개 · **2026-08-08 신설**
**역할: 담당 반 하나의 성적·출결·과제·오답을 넣는다.**

> **핵심 판단 — 조교 화면은 «따로 만드는 화면»이 아니라 «권한으로 좁힌 관리자 화면»이다.**
> 지금 구버전이 남아 있는 이유가 «DS 학생 상세는 기본정보·삭제까지 열어 두기 때문»인데,
> 그건 화면의 문제가 아니라 **권한의 문제**다. 화면을 두 벌로 두면 앞으로 모든 변경을
> 두 번 해야 하고, 반드시 한쪽만 고쳐진다 (신호 규칙을 `signalOf()` 하나로 합친 것과 같은 이유).

- **상단바에 메뉴가 없다.** 갈 곳이 하나뿐인데 메뉴를 만들면 그게 거짓말이다.
  대신 「조교」 뱃지와 담당 반 이름을 둔다 — 자기가 누구로 있는지가 늘 보여야 한다
- **권한을 화면이 먼저 말한다.** 「성적·출결·과제·오답을 넣을 수 있습니다. 학생 등록·삭제, 반 이동,
  개인정보 수정은 강사만 할 수 있습니다.」 눌러 보고 나서야 «안 되는구나»를 알게 하지 않는다
- **명단** — 01-roster에서 조교가 손댈 수 없는 열을 뺐다.
  반·반 진도(반이 하나다) · 신호 등급(05가 전담) · 시청률(영상은 강사가 올린다).
  남은 열은 전부 조교가 «넣는» 것이다. 일괄 작업도 없다
- **학생 상세** — 03-student의 연대기를 그대로 쓴다. 갈래는 일곱이 아니라 넷(전체·성적·출결·과제·오답).
  우측 «빠른 작업» 칸은 통째로 없다 (연락처·기본정보·삭제·채팅이 전부 강사의 것이라 빈 칸이 된다) → 3열이 아니라 2열
- **신호는 보이되 «해제»가 없다.** 상태를 아는 것은 조교에게도 필요하지만,
  규칙을 다루는 것은 강사의 판단이다

---

## 5. 다크 vs 라이트 — **라이트 유지로 확정 (2026-08-06)**

**결정: 라이트 유지. 다크는 «화면별»이 아니라 «제품 전체 테마»로만 — 지금은 하지 않는다.**
아래는 그 근거이며, 나중에 다크를 다시 꺼낼 때 읽으면 된다.

1. **오답 검토는 흰 배경 콘텐츠를 다루는 화면이다.** 시험지 스캔·HWPX·수식·학생 답안이 전부 흰 바탕이다. 어두운 UI에 흰 패널이 박히면 대비가 극단으로 벌어져 오히려 눈이 더 피로하다. 다크가 편한 것은 «화면 전체가 어두울 때»뿐이다.
2. **하루에 명단 → 수업 → 검토를 계속 오간다.** 한 화면만 다크면 이동할 때마다 명순응·암순응이 일어난다.
3. 사용자가 정한 최우선 원칙이 «하나의 제품처럼 보이는 것»이다.

전체 테마로 간다면 `:root` 색 값만 갈아끼우면 되어 구조적으로 싸다. 다만 **상태색 5종과 메모 종이색의 다크 대응은 새로 설계**해야 한다. **우선순위는 낮게.**

---

## 6. 결정 기록

### 6-A. 확정
- ~~**`index.html` 수정 금지**~~ — 설계 기간의 규칙이었다. 2026-08-06 이식 착수와 함께 해제됐다 (0-B)
- 기존 디자인을 개선하지 않는다. 완전히 새 방향
- 브랜드 성격: 학원이 아니라 **개인 브랜드**. Premium / Modern / Minimal / Trustworthy / Sophisticated
- 피할 것: 흔한 학원 홈페이지, 과도한 카드 나열, AI가 만든 것 같은 랜딩, 복잡하고 촌스러운 디자인
- **관리자 시안 A3를 디자인 시스템의 기준으로 확정** — 버튼·카드·색·폰트·간격·아이콘·인터랙션 전부
- 다른 시안은 화면을 합치지 않고, **화면별 역할을 먼저 정의한 뒤 그 역할에 맞는 UX만** A3 언어로 번역해 흡수
  - 전체 학생 관리 → **A1**(고밀도 표·저장된 뷰·일괄 작업) / 수업 진행 → **A2**(단계형 워크플로·큰 터치)
  - 학생 상세 → **A3** 그대로 / 오답 검토 → **A4**(큐·2열 비교·키보드) / 신호 → **A5**(근거 문장)
- **버린 것**: A4의 다크 테마, A5의 3열 칸반 보드, A1의 파랑 선택색·IBM Plex, 이모지 아이콘
- **주조색을 로고 차콜로 교체** (원래 올리브 `#4E5A42`였다 — 로고를 못 보고 정한 내 실수)
- **로고 지정색 2개는 매뉴얼북 PDF에서 직접 확인함 (2026-08-06)** — 차콜 `#3F3537` · 와인 `#BA5054`
  - 매뉴얼북은 **텍스트가 전부 아웃라인 처리**돼 있어 문자열 추출이 안 되고, Chrome PDF 뷰어는 이 파일에서 렌더러가 멈춘다(두 번 겪었다). 뷰어로 읽으려 하지 말 것
  - 대신 **PDF 콘텐츠 스트림을 zlib로 풀어 «칠해진 사각형»의 색과 면적을 쟀다.** 스크립트는 스크래치패드의 `pdfswatch.js` / `pdfrects.js`
  - p6이 컬러 지정 페이지 — 같은 높이(219.5)의 색면 두 개가 나란히 있다. 이게 지정값이다
  - p7의 `#9E9E9E`는 **배경 사용 규정 페이지의 타일**이지 워드마크 회색이 아니다. `--brand-gray #A8A4A4`는 그대로 둔다
  - 검증: 이 방법으로 뽑은 와인이 사용자가 정정해 준 `#BA5054`와 정확히 일치했다
  - `--accent-d`는 기존 비율(×0.667)을 그대로 적용해 `#2A2325`로 재계산. `--accent-bg`/`--accent-line`은 accent의 틴트가 아니라 **paper 계열 웜 뉴트럴**이라 손대지 않았다
- **랜딩 「전환」 문구 = 「짐작으로 가르치지 않습니다」 (2026-08-06 확정)**
  - 위의 로그가 «증거», 이 문장이 «주장», 아래 메모가 «이유» — 셋이 서로 다른 일을 한다
  - 기각: 「무엇을 틀렸는지 기억합니다」·「가르친 것은 전부 남습니다」는 아래 메모의 «전부 적습니다»와 겹친다.
    「한 명도 같은 방식으로…」는 62px에서 두 줄로 접히며 앞머리가 약해진다.
    「틀린 문제는 두 번 만납니다」는 차점이었으나 오답 시스템 하나만 말한다
  - 후보 5개와 각각의 찬반은 `landing-v2/copy.html`에 그대로 남겨 뒀다
- **AI 유사문항의 정답 형태를 못박았다 (2026-08-07)** — «999 이하 자연수» 아니면 «객관식» 둘 중 하나
  - 학생 답안칸이 받는 것은 숫자 입력과 ①~⑤ 보기 버튼 둘뿐이다. 분수·무리수·음수·문자식이
    정답이면 학생은 풀고도 제출할 방법이 없다. 예전 프롬프트는 «깔끔한 정수 **또는 간단한 기약분수**»를
    허용했는데 기약분수가 바로 그 사례였다
  - 워커 프롬프트(`answerFormRule`) + 받는 쪽 안전망 셋. 상세는 `worker/README.md`
  - **기존 데이터에 이미 어긋난 것이 있었다** — 생성된 7개 중 5개, 그중 2개는 공개 상태였다.
    제일 나쁜 것은 «객관식인데 정답이 숫자 4» — 학생이 ④를 누르면 «④»가 저장되어
    문자열 비교에서 오답 처리된다. **맞혀도 틀린 것으로 채점된다**
- 조교 계정은 **없음**. 필요하면 설정에서 새로 만들어야 함
- 강사 비밀번호는 전달받았으나 **이 파일에 기록하지 않음** (공개 저장소)

### 6-B. 랜딩 5안 — 전부 기각 (보관: `mockups/`)
| | 컨셉 | 히어로 |
|---|---|---|
| 01 | 정리와 증명 | 「정리 1」 한 문장, 이미지·버튼 없음 |
| 02 | 좌표계 | 화면 전체가 좌표평면, 성적 곡선 실시간 드로잉 |
| 03 | 한 사람 | 인물 컷아웃 + 극단적 여백 |
| 04 | 관측소 | 카피 대신 실제 기록 대시보드 |
| 05 | 한 문제 | 실제 문제 1개, 선택지를 누르면 풀이 시작 |

**기각 사유는 아직 못 들었다.** 사용자 말로는 "예쁘지 않아서가 아니라 서비스의 첫인상과 브랜드 경험이 부족해서".
내 진단 — 1·2·5는 «수학»을 소재로 썼고 3은 인물 포트폴리오였다. 아무 수학 강사나 쓸 수 있는 소재라 브랜드가 없었다.
04 관측소는 「하루」와 인접하지만 **대시보드는 성과 자랑, 로그는 태도**라는 점이 다르다.

### 6-C. 현행 index.html 디자인 (참고 — 계승하지 않음)
컨셉 "채점된 답안지". 종이 질감 + 접힌 모서리 + 빨간 첨삭펜.
```
--paper #F5F1EB  --ink #3D3335  --blue-deep #AE4B53(실제는 와인)  --red-pen #6E2A2F
```
> `--blue-deep`이 와인색이었던 것 = **토큰 이름에 색 이름을 넣으면 안 되는** 실제 사례.
> 그리고 이 값이 로고 와인과 같은 계열이다 — 원래 사이트는 로고에서 색을 가져왔던 것이다.

---

## 7. 파일 위치

```
claude work/
├─ index.html                     ← 실제 서비스. 화면 이식 완료 (기록: docs/기록-2026-08-이식.md)
├─ profile.png / profile.webp     ← 인물 컷아웃. 공개 홈이 webp 99KB를 쓰고 png 2.2MB가 폴백이다
├─ profile.jpg                    ← 84KB. **컷아웃이 아니라 폴백으로 못 쓴다** (JPEG에 알파가 없다). 지금은 쓰는 곳이 없다
├─ CLAUDE.md                      ← 이 파일. **지금 할 일 · 규칙 · 함정 · 결정만** 남긴다
├─ docs/                          ← 끝난 일의 기록 (2026-09-07에 갈랐다)
│   ├─ 기록-2026-09.md              9월 · ⚠ 본문에 「0-B」라 적힌 것은 아래 «이식» 파일을 말한다
│   ├─ 기록-2026-08.md              8월
│   └─ 기록-2026-08-이식.md          리디자인 이식(옛 0-B절) — 끝났다. 구버전은 한 줄도 안 남았다
│
├─ hwpx.js                        ★ hwpx 문항 파서 — **웹과 도구가 같이 쓴다**. 평범한 스크립트로 둘 것(모듈 X)
│                                   웹: <script src>  ·  node: 글자로 읽어 new Function (tools/hwpx-node.mjs)
│
├─ ds.css                         ★ 유일한 디자인 시스템. 토큰은 :root. 실서비스·시안이 같이 쓴다
├─ ds.js                          ★ 아이콘 34종 · NAV/SUBNAV · 피팅 · 예시 데이터 — 시안 전용, 실서비스는 로드하지 않는다
│
├─ design-v1/                     ★ 관리자 16 + 학생앱 11 + 로그인 + 조교 (시안)
│   ├─ 00-system.html             ← 디자인 시스템 문서
│   ├─ 01-roster / 02-class / 03-student / 04-review / 05-signals
│   ├─ 06-classes / 07-exams / 08-bank / 09-clinic / 10-notice
│   ├─ 11-qna / 12-chat / 13-settings
│   ├─ 14-log / 15-quiz / 16-videos          ← 반 관리 서브탭
│   ├─ 17-login / 18-assistant               ← 상단바 메뉴 «밖»의 화면 둘 (4-D · 4-E)
│   ├─ 20-student-app.html                   ← 학생 마이페이지 11개 (모바일)
│   └─ preview.html                          ← 통합 프리뷰. 카드 목록은 관리자 16개,
│                                              나머지는 위쪽 링크 넷(시스템 문서·학생앱·로그인·조교)
│
├─ landing-v2/                    ★ 랜딩페이지 (시안 — 실서비스는 homeHTML로 합류했다)
│   ├─ index.html                 ← 「하루」. 실서비스와 같이 고친다 (여기가 따라 그릴 기준이다)
│   ├─ preview.html               ← 데스크톱 1440 + 모바일 390 동시
│   ├─ copy.html                  ← 전환 문구 후보 5개 (실제 크기)
│   └─ concept.html               ← 컨셉 보드 (로고 반영 전, 보관)
│
├─ 로고/                          ★ 자체 로고 (2024-01 PAPERFIT)
│   ├─ 가로형(1~3).png            ← (1) 컬러 (2) 흰색 녹아웃 (3) 단색
│   └─ 세로형(1~3).png
│   ※ 매뉴얼북 원본: Documents/카카오톡 받은 파일/[김하현T] 로고 매뉴얼북.zip
│
├─ mockups/                       ← 랜딩 5안 (기각, 보관)
├─ mockups-admin/                 ← 관리자 5안 (선정 완료, 보관)
├─ worker/                        ★ Cloudflare 워커 — 코드 완성, 배포 대기 (9절 1번)
│   ├─ gemini-proxy.js            ← 배포본 + Firebase ID 토큰 검증 · 서버 쪽 할당량
│   ├─ verify-test.mjs            ← 토큰 검증 테스트. node worker/verify-test.mjs
│   └─ README.md                  ← 왜 회전이 아니라 교체인지 · 3단계 배포 순서
│   ※ 손댄 곳은 CORS · 인증 게이트 · 할당량 셋뿐. 드라이브·Gemini 로직은 배포본 그대로다
└─ tools/                         ★ 개발 도구 (배포와 무관)
    ├─ serve.js                   ← 로컬 정적 서버. node tools/serve.js
```

관리자 목업은 **1440×900 캔버스를 화면 크기에 맞춰 축소**하는 구조(`fitCanvas()`).
표시된 학생 이름·수치는 전부 **구조 확인용 예시**이며 실제 데이터가 아니다.

**검증 상태** — 관리자 16 + 학생앱 11 + 로그인 5상태 + 조교 2화면 + 랜딩(6폭) 전부 **0건**
(캔버스 이탈 / 텍스트 잘림 / 부모 침범 3종. 스크롤 컨테이너 안쪽과 KaTeX 내부는 제외)
`mockups/`(랜딩 5안)에는 `word-break:keep-all`이 안 들어갔다. 보관용이라 두었다.

---

## 8. 작업 규칙

### 만들 때
- **시안을 만들 때**는 별도 폴더에. **실서비스에 이식할 때**는 `index.html`을 직접 고친다 (0-B)
- **이식하다 시안의 버그를 찾으면 시안도 같이 고친다.** 시안이 「따라 그릴 기준」이라
  틀린 채로 두면 다음에 그걸 또 베낀다 (랜딩의 히어로 척추가 그 사례다)
- **`.mono`에 한글 «낱말»을 넣지 않는다.** JetBrains Mono에 한글이 없어 고정폭 대체 글꼴로
  떨어지면서 「오늘」이 「오 늘」처럼 벌어진다. mono는 `YYYY-MM-DD`·숫자·퍼센트에만
- **새 화면은 루트 `ds.css`를 링크해서 만든다.** 화면별 `<style>`에는 «배치»만 두고 색·서체·반경·간격을 새로 정하지 않는다
- **«칸»에는 배경(`--card`)을 준다.** 목록·문맥 칸은 시안에서 흰 면이고, 경계선 하나만으로는
  «칸»이 아니라 «여백을 띄운 목록»으로 읽힌다. 화면 가장자리에 닿는 칸은 본문 패딩(20px)을
  음수 마진으로 되먹인다. **단, 시안이 종이색으로 둔 칸은 그대로 둔다** (수업 시간표 · 오답 큐)
- **카드를 쓰지 않는다.** 흰 상자를 종이 위에 띄우는 것은 구버전(`.lg-panel`)의 어법이다.
  구역은 `.st-sec`+`.st-h`(제목 아래 실선), 그 안은 `.st-row`(얇은 선으로 나눈 줄)로 짓는다
- **만드는 폼은 접어 둔다.** 「추가」·「새 ○○」 버튼을 눌러야 열린다 (`.st-form`).
  상시 펼쳐 둔 폼은 화면에서 제일 큰 자리를 차지하면서 제일 가끔 쓰인다
- **입력칸은 `.fm-field` / `.cls-form`** 을 쓴다. 화면마다 다시 그리지 않는다 —
  그렇게 51곳으로 늘어난 것이 구버전 `lg-field`였다
- **후손 선택자로 `display:block`을 걸지 않는다.** `.t span{display:block}`은 줄 안에 넣은
  `<span class="mono">`까지 블록으로 만들어 한 줄을 두 줄로 갈라 놓는다. 직계 자식(`> span`)만 건다
- **상단바·서브탭을 손으로 적지 않는다.**
  - 시안: `<nav id="nav">` / `<div class="subnav" id="subnav">`만 두고 `renderNav()` / `renderSubnav()`를 부른다. 메뉴가 바뀌면 `ds.js`의 `NAV`/`SUBNAV`만 고친다
  - 실서비스: `index.html`의 `TEACHER_NAV` / `TEACHER_SUBNAV` 한 곳만 고친다. **둘은 따로다** — 시안은 href로, 실서비스는 탭 id로 움직이기 때문이다. 메뉴를 바꾸면 양쪽을 같이 고쳐야 한다
- **두 화면에서 쓰이는 컴포넌트는 ds.css로 올린다.** 로컬에 두면 한쪽만 고쳐진다
- 새 HTML의 리셋에 **`word-break:keep-all; overflow-wrap:break-word;`**. 없으면 한글이 음절 단위로 갈라진다("목표"→"목/표")
- CSS 커스텀 속성은 **`:root`에.** 특정 클래스 안에 두면 그 클래스를 안 쓰는 문서 페이지에서 색이 조용히 사라진다

### ⚠ 클래스 이름 — 같은 사고를 여섯 번 냈다
**화면별 `<style>`에서 ds.css의 클래스 이름을 다시 쓰지 않는다.**
**그리고 `index.html` 자신의 `.app` 블록에 이미 있는 이름도 쓰지 않는다** (여섯 번째가 그것이다).
`.stage` · `.ev` · `.tag` · `.row` · `.body` · `.ph` 를 로컬에서 재정의했다가 매번 조용히 망가졌다.
(`.stage`는 목업 뷰어의 검은 배경, `.body`는 앱 셸의 `absolute; top:52px` — 장면이 통째로 헤더 위로 올라갔다)
→ **랜딩처럼 접두사를 붙이는 쪽이 안전하다** (`l-` 등).

> **다섯 번째는 `.ph`였다 (2026-08-08 · 조교·로그인 시안).** 「placeholder」의 줄임으로 썼는데
> ds.css의 `.ph`는 **학생앱 폰 액자(`width:390px; height:812px`)**다.
> 입력칸 하나가 390px로 부풀어 캔버스를 111px 넘어갔다.
> **두 글자짜리 약어가 제일 위험하다** — 겹칠 확률이 높은데 눈에는 안 띈다.
>
> **여섯 번째는 `.body`였다 (2026-08-08 · 학생 상세 연대기).** 이번엔 ds.css가 아니라
> **`index.html` 자신의 앱 셸**과 부딪혔다 — `.app .body{padding:20px}`가 연대기 한 줄의
> 본문에 그대로 걸려 행 높이가 1.6배가 돼 있었다. ds.css만 봐서는 안 잡힌다.
>
> **새 클래스 이름은 ds.css «와 index.html»에서 한 번 찾아보는 것**이 유일한 방어다
> (`grep -n "\.이름" ds.css index.html`).
> 이걸 잡아 주던 `check-css-collisions.js`는 이식이 끝나면서 지웠고, 애초에 `index.html`만
> 보던 도구라 `design-v1/`·`landing-v2/`의 시안은 검사 대상도 아니었다.

### 색을 정할 때
- **브랜드 자산(로고·매뉴얼)이 있는지 먼저 묻는다.** 로고를 못 보고 주조색을 정했다가 24개 화면을 만든 뒤 갈아엎었다
- **토큰 이름에 색 이름을 넣지 않는다.** `--olive`가 차콜을 담는 순간 거짓말이 된다
- **브랜드색을 UI 강조색으로 바로 쓰지 않는다.** 상태색과 겹치는지 먼저 본다

### 판단할 때
- **통일해야 하는 것은 색·서체·간격·버튼이지 레이아웃이 아니다.** 질의응답은 세로 스택, 채팅은 말풍선 — 글의 성격이 다르면 형태는 달라야 한다. 형태를 억지로 맞추는 것은 일관성이 아니라 게으름이다
- **같은 곳으로 가는 문은 하나만 만든다.** (명단의 「위험 신호」 뷰 제거, 반의 「학생 명단」 → 01로)
- **학생 화면은 모바일 기준으로 그린다.** 누르는 것은 최소 46px
- 실제 도메인 값을 쓴다: 7과목(공통수학1·2, 대수, 미적분Ⅰ, 확률과통계, 기하, 미적분Ⅱ), 출석 5상태, 오답 3단계, 실제 학교명(건대부고·광남고·대원고·동대부여고·가람고·광양고·대원여고)
- 실존 인물에 대한 사실(경력 연수 등)을 **지어내지 않는다.** 수치는 예시임을 명시한다
- **공개 페이지에는 검증 불가능한 수치를 넣지 않는다.** 방문자는 사실로 읽는다
- 비밀번호·토큰을 문서나 코드에 기록하지 않는다

### 검증할 때
- 목업을 만들면 **반드시 브라우저로 렌더링을 확인한다.** 축소된 스크린샷만 보면 놓친다
- 캔버스를 `scale(1)`로 되돌린 뒤 **DOM 기하를 재는 편이 확실하다** — 1440×900 이탈 / `scrollWidth>clientWidth` 텍스트 잘림 / `overflow:hidden` 부모 침범 3종
- **스크롤 컨테이너 안쪽 요소는 제외**해야 한다 (안 그러면 오탐이 쏟아진다). KaTeX 내부(`.katex`)도 의도적 클리핑이라 제외
- **잘림·넘침 3종으로는 «어긋남»을 못 잡는다.** 랜딩의 히어로 척추가 아래 본문과 256px 어긋나 있었는데
  검사는 전부 0이었다 — 넘친 게 아니라 «틀린 자리에 있던» 것이라서.
  같은 것이 반복되는 요소(척추·열 시작점·라벨 폭)는 **`getBoundingClientRect().left`를 모아 비교**한다
- **반응형은 창 크기로 확인한다.** 창이 안 줄면 같은 문서를 **iframe에 넣어** 실제 뷰포트를 만든다 —
  미디어쿼리는 iframe 폭을 따른다. 규칙만 손으로 흉내 내면 중단점 자체는 확인되지 않는다
  (`let`/`const` 최상위 선언은 `window`에 안 붙으므로 `iframe.contentWindow.eval()`로 들어간다)
- **IntersectionObserver·requestAnimationFrame은 렌더링 프레임에 묶여 있다.** 자동화로 볼 때
  백그라운드 탭에서는 콜백이 아예 안 온다 — `setTimeout`이 아니라 `rAF`를 기다려야 실제 동작이 보인다
- ⚠ **입력칸이 있으면 «한글로» 쳐 본다.** 기하만 재고 넘어가면 못 잡는 것이 있다 —
  한글은 조합으로 들어와서, 매 글자 `render()`가 돌면 「안녕하세요」가 「ㅇㅏㄴㄴㅕㅇ…」이 된다.
  실제로 명단 검색이 이식 때부터 계속 깨져 있었고, 영문 아이디로만 쳐 봐서 못 잡았다.
  자동으로 볼 때는 `compositionstart` → `input{isComposing:true}` → `compositionend` 순으로
  이벤트를 흘려 넣고, **조합 중 `render()` 호출이 0회인지**를 센다

---

## 9. 리디자인과 별개로 처리해야 할 이슈

1. ~~**`AI_SITE_TOKEN`**~~ — **해결됨 (2026-08-06 3단계 완료 · 2026-08-07 확인).**
   유출된 값은 죽었다. 아래는 «왜 회전이 아니라 교체였는가»의 기록이다.
   - ~~토큰 회전 필요~~ — **회전은 효과가 거의 없다.** 정적 클라이언트라 토큰이 모든 방문자
     브라우저로 내려간다. 저장소에서 지워도 배포본에는 남아야 하고, 갈아도 새 값이 똑같이 공개된다.
     **처음부터 비밀이 될 수 없는 값이었다**
   - **진짜 위험은 그 뒤에 방어가 없다는 것** — 일 20건 제한이 클라이언트에만 있다(731·758).
     토큰만 있으면 워커를 루프로 호출해 Gemini 비용을 태울 수 있다
   - 해법: 워커가 **Firebase ID 토큰을 검증**하고 **할당량을 서버에서 uid별로** 강제한다.
     `getAuthToken()`이 이미 있고 Firestore REST가 그걸 쓰고 있다 — 재료는 다 있다
   - ⚠ **「강사만 접근」은 이걸로 안 된다.** 5401이 `signInAnonymously()`라 Firebase 신원이
     «사이트를 연 사람»까지만 말한다. 학생/강사 구분은 Firestore 대조라 토큰에 안 실린다.
     커스텀 클레임을 심으려면 권한 있는 백엔드가 따로 필요하다 — 별건
   - **순서를 지켜야 한다. 클라이언트를 먼저 바꾸면 실서비스가 죽는다** —
     새 `Authorization` 헤더가 프리플라이트를 바꾸는데 워커의 `Access-Control-Allow-Headers`에
     `authorization`이 없으면 브라우저가 요청을 통째로 막는다
   - 상태: **1·2·3단계 전부 완료**
     - 1단계 — 워커 배포 (사용자). 프리플라이트로 확인함
     - 2단계 — `index.html`에서 `AI_SITE_TOKEN` 상수와 `X-Site-Token` 헤더 **4곳**을
       `getAuthToken()`으로 교체. `deleteFromDrive()`는 await가 필요해 async가 됐다
       (부르는 곳 8곳은 전부 결과를 안 기다리는 호출이라 그대로 둬도 된다)
     - **3단계 — Cloudflare에서 `SITE_TOKEN` 비밀 삭제 (2026-08-06, 사용자).**
       이때 유출된 값이 죽었다
   - **2026-08-07 배포본에 직접 물어 확인했다** — 커밋 이력의 옛 토큰으로 부르면 **401**.
     인증 없음·틀린 Bearer도 401. 프리플라이트 200에 `Authorization` 허용, Origin은 배포 도메인 그대로.
     (`/delete`는 할당량을 안 태우는 경로라 이 확인은 비용이 들지 않는다)
   - 소스에서도 옛 경로를 지웠다 (CORS 헤더의 `X-Site-Token` · `authenticate()`의 분기).
     **반영하려면 워커를 재배포해야 하지만 안 해도 동작은 같다** — 비밀이 없어 이미 닫혀 있었다
   - **배포본에서 AI 생성·이미지 업로드 둘 다 정상 동작을 확인했다 (2026-08-07, 사용자).**
     막힌 쪽(옛 토큰 401)과 통하는 쪽(새 경로) 양쪽이 다 확인됐으므로 이 건은 끝났다
   - ⚠ **`ALLOW_ORIGIN`을 걸어서 로컬(`127.0.0.1:8777`)에서는 워커를 못 부른다.**
     AI 생성·이미지 업로드 확인은 **배포된 사이트에서** 해야 한다.
     다만 CORS는 브라우저의 규칙이라 **curl로는 그냥 닿는다** — 인증만 보는 확인은 로컬에서 된다
   - 커밋 이력의 옛 토큰은 회수되지 않는다. 무효화가 유일한 해결이었고, 그게 끝났다
2. ~~**홈 히어로 이미지 깨짐**~~ — **해결됨.** 세 파일 전부 추적 중이고 배포본에서 열린다.
   랜딩 합류 뒤로 **공개 홈은 `profile.webp`(99KB) + `profile.png`(2.2MB) 폴백**을 쓴다.
   `profile.jpg`(84KB)는 **컷아웃이 아니라 폴백으로 못 쓴다** (JPEG에 알파가 없다) — 지금은 쓰는 곳이 없다
2-B. ~~**드라이브 업로드가 죽어 있다**~~ — **해결됨 (2026-08-09, 사용자).**
   OAuth 동의 화면을 **「프로덕션」으로 게시**하고 **클라이언트 보안 비밀 + 리프레시 토큰을
   새로 발급**해 워커 비밀(`DRIVE_OAUTH_CLIENT_SECRET` · `DRIVE_REFRESH_TOKEN`)을 갈아 끼웠다.
   배포본에서 **문서·이미지 두 경로 모두 정상**을 확인했다 (강의자료 업로드 → 학생 화면 →
   삭제까지, 그리고 `uploadImageToDrive`로 질의응답·시험지가 쓰는 경로까지).
   - **다시 이 증상이 나오면 제일 먼저 게시 상태를 보라.** 「테스트」면 리프레시 토큰이
     **7일**마다 죽는다. 증상은 `/upload` 502 + `invalid_grant`이고, 코드는 멀쩡하다
   - **저장용량은 Google One 그대로 쓴다** — API로 올린 파일도 토큰을 내준 계정이 소유하고
     그 계정 용량에서 차감된다. «API 전용 저장공간»은 없고 드라이브 API 호출 자체도 무료다.
     ⚠ **서비스 계정으로 바꾸면 안 된다** — 자체 용량이 0이라 결제한 용량을 못 쓰고 업로드가 막힌다
   - 클라이언트 보안 비밀을 새로 만들 때 **클라이언트 ID는 그대로 둔다.** 새 클라이언트로 갈면
     `drive.file` 범위에서 **예전 클라이언트가 올린 파일에 접근하지 못해** 삭제가 조용히 깨진다
   - 아래는 당시 진단 기록이다.

   배포본에서 `/upload`가 **502**를 돌려줬다. 워커가 남긴 이유는 분명했다:
   ```
   auth failed — token refresh failed: 400
   invalid_grant · "Token has been expired or revoked."
   ```
   - **내 코드나 새 기능의 문제가 아니다.** 기존 «이미지» 경로(질의응답 사진 · 시험지 이미지 ·
     창고 이미지)도 **똑같이 502**다. 1×1 PNG로 확인했다
   - 즉 **지금 이 순간 사진 첨부와 이미지 업로드가 전부 실패한다.** 2026-08-07에는 됐다
   - **가장 그럴듯한 원인 — OAuth 동의 화면이 「테스트」 상태다.** 그 상태의 리프레시 토큰은
     **7일 뒤 만료**된다. 8/7에 되고 8/9에 죽은 것이 정확히 그 주기다
   - 고치는 길 둘 (둘 다 구글 계정 소유자만 할 수 있다)
     1. **동의 화면을 「프로덕션」으로 게시한다** → 7일 만료가 사라진다. 근본 해결
     2. 리프레시 토큰을 새로 발급받아 워커의 `DRIVE_REFRESH_TOKEN` 비밀을 갈아 끼운다
        → 지금은 살지만 테스트 상태로 두면 7일 뒤 또 죽는다
   - **AI 생성은 별개다** — 그쪽은 `GEMINI_API_KEY`를 쓰고 드라이브 토큰과 무관하다
3. **비밀번호 평문 저장** — 학생 `pw`, 조교 `pw`, `teacher-pw` 전부
4. **전체 재렌더링으로 인한 포커스 유실** — 「치면서 화면이 따라오는」 입력칸은
   전부 `liveEdit()`을 쓴다. 조합 중에는 안 그리고, 커서는 있던 자리로 되돌린다 (0-B).
   - ~~한글이 「ㅇㅏㄴㄴㅕㅇ」으로 입력되던 것~~ — **고쳤다 (2026-08-08).** 네 곳 전부
   - **남은 것** — state만 갱신하는 칸(출결 메모·수업 메모·일별 성적명)은 다른 이유로
     `render()`가 돌면 커서가 끝으로 튄다. 자주 겪는 길은 아니라 그대로 뒀다
6. ~~**`todayStr()`이 UTC 기준이다**~~ — **고쳤다 (2026-08-07).** `ymdLocal()`을 만들어
   「오늘」을 로컬 날짜로 통일했다. `new Date().toISOString().slice(0,10)`을 쓰던 12곳이
   전부 `todayStr()`을 부른다. 이미 저장된 기록의 날짜는 그대로 뒀다 — 고쳐 봐야 어느 쪽이 맞는지 모른다
   - 미룰 수 없었던 이유: 반 관리 > 수업 기록의 날짜 목록이 **요일은 로컬로 판정하고
     날짜는 UTC로 기록**해서 통째로 하루씩 밀려 있었다. 월·수·금 반이 **일·화·목**으로 보였고,
     그 상태로 출결을 넣으면 하루 전 날짜에 저장된다
   - 수업 화면도 같은 이유로 그 시간대에 「이 날짜에 수업이 없습니다」를 띄우고 있었다
   - `shiftYmd()`는 문자열 산술이라 이 변경과 무관하게 그대로 맞다
   - **한 곳을 놓쳤었다 — 채팅 시각.** `sendChatMessage()`가 `toISOString()`으로 «분»까지 찍어
     KST에서 보낸 말이 9시간 전으로 남고 있었다. 학생 채팅 화면이 시각을 띄우면서 드러났다.
     `nowStamp()`(로컬 `'YYYY-MM-DD HH:MM'`)로 고쳤다 (2026-08-07)
7. **SEO/접근성 공백** — **공개 홈은 해결됐다 (2026-08-07).** 랜딩 합류와 함께
   meta description·OG 태그가 `<head>`에 들어갔고, skip link·focus-visible·reduced-motion도 같이 왔다.
   자동으로 흐르던 후기 마퀴(`prefers-reduced-motion` 미대응)는 통째로 사라졌다
   - **로그인도 해결됐다 (2026-08-08)** — `<form>` + `<button type="submit">`이라 Enter와 탭 이동이 먹는다.
     구버전은 버튼 onclick뿐이라 **Enter를 쳐도 아무 일이 없었다**
   - **남은 것** — 화면 곳곳의 `href` 없는 `<a onclick>`(`.pill`·서브내비·문 링크 등)은
     여전히 키보드로 못 간다. DS 화면은 대부분 `<button>`을 쓰지만 전부 검증하진 않았다.
     한 번에 훑어야 할 일이지 화면마다 고칠 일이 아니다
8. ⚠ **«있을 법한 말»을 지어내서 공개 페이지에 실었다** (2026-08-13에 드러남 · 고침)
   랜딩 시안 작업 중에 쓴 두 문장이 실서비스에 실렸고, **사용자가 한 말이 아니었다.**
   더 나쁜 것은 그 뒤에 내가 그것을 «사용자의 교육철학»으로 읽고 다음 판을 설계했다는 것이다.
   사진 `alt`의 「원장」도 사실이 아니었다(**개인 강사**다).
   - **«수치를 지어내지 않는다»는 지켜 왔는데 «태도를 나타내는 문장»에는 같은 규칙을 안 걸고 있었다.**
     공개 페이지에서 방문자는 둘 다 사실로 읽는다
   - **규칙** — 실존 인물의 생각·태도·이력을 나타내는 문장은 **사용자의 말에서 나온 것만** 쓴다.
     재료가 없으면 그 자리를 비워 두고 «필요하다»고 말한다. 자리를 채우려고 짓지 않는다
   - 지금 랜딩의 원칙 셋과 시연 흐름은 전부 사용자가 직접 말한 것에서 나왔다 → 6절
