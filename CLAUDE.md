# 김하현수학연구소 — 작업 노트

마지막 갱신: **2026-08-19** — 리디자인 이식은 끝났고, 여기서부터는 제품 개선이다.

> ### 📌 2026-08-19 — 08-16에 찾아 둔 흠 둘을 고쳤다 (**H-1 · H-2 완료**)
> **두 커밋. push 완료. 배포본에서 눈으로 확인했다.** 워커는 안 건드렸고 AI 한도도 안 썼다.
> 실 DB에는 **한 줄도 안 썼다** — 사진을 열어 보고 닫기만 했다.
>
> **① 사진 확대에 닫는 문을 냈다 (H-1)** — `7c26736`
> 겹침 아무 데나 눌러 닫히긴 했으나 **그 길이 화면에 안 보였다.**
> → **×**를 오른쪽 위(40×40)에 세우고 **ESC**도 듣는다.
> 닫는 일은 **`closeHwPhoto()` 한 곳**에 모았다 — 세 길이 어긋나지 않게.
> ⚠ **ESC는 사진이 떠 있을 때만 듣는다.** 안 그러면 검토 화면 키보드(J·K·E·X·R) 옆에
> 또 하나의 전역 키가 생긴다. `state.hwPhotoView`가 비어 있으면 그냥 통과시킨다.
>
> **② 강사 연대기에서도 고아 오답숙제를 막았다 (H-2)** — `b6d18ed`
> 08-14에 «읽는 쪽에서 막은 것»은 **학생 화면뿐**이었다. 강사의 학생 상세 연대기
> (`studentTimeline`)는 `rec.wrongHomework`를 **날것으로** 훑고 있었다.
> → 거기서도 **`liveWrongHomework(rec)`**를 쓴다. 같은 문 하나를 두 화면이 나눠 쓴다.
> ⚠ **데이터는 안 건드렸다** — 08-14에 사용자가 「그냥 두라」고 정했다. 15건은 그대로 있다.
>
> | 배포본에서 본 것 | 결과 |
> |---|---|
> | 사진 확대 (실제 제출물) | ×·**ESC**·겹침 **세 길 다 닫힌다** · 닫아도 **펴 둔 과제가 그대로** 남는다 |
> | ESC를 사진 없이 누르기 | **아무 일도 안 일어난다** (해시·화면 그대로) |
> | 테스트학생 연대기 | **「(삭제된 시험지)」 0건** · 전체 14건 · 갈래는 출결 7 · 성적 1 · 과제 2 · 질문·클리닉 4 |
> | 데이터 | `wrongHomework` **15건 그대로** · 화면에 뜨는 것만 0 |
>
> ⚠ **같은 자리에 하나 더 있다 — 안 고쳤다.** 연대기의 **「퀴즈」 줄**(`dailyQuiz.log`)도
> 문항이 없어지면 「**(삭제된 시험지) ?번**」으로 선다. 08-16에 보고된 것은 «오답» 줄이었고
> 테스트학생은 **퀴즈 로그가 0건**이라 지금은 눈에 안 띈다. 다만 «막는다»와 «학습 이력을
> 지운다»의 뜻이 갈리는 자리라 **혼자 정하지 않았다** → 「▶ 다음 차례」 **H-3**

> ### ✅ 2026-08-16 — 과제 제출·승인을 배포본에서 한 바퀴 돌았다 (**H 완료**)
> **코드는 안 고쳤다.** 배포본(`https://khhyun827-lang.github.io/hahyunmath/`)에서
> 실데이터로 눈으로 본 기록이다. AI 한도는 안 썼다.
>
> ⚠ **시작할 때 이미 「ㅁㄴㅇ」이 수락된 상태였다** — 08-14 노트는 「확인 대기」로 적혀 있었으나
> 그 뒤 사용자가 직접 수락했다(`by 김하현T · 2026-08-14`). 그래서 **「완료 취소」로 되돌려**
> 대기를 만든 뒤 한 바퀴를 돌렸다.
>
> | 본 것 | 결과 |
> |---|---|
> | 완료 취소 | 「확인 기다리는 중」 · **사진은 그대로 남는다**(판단만 지운다) · 수락/반려 버튼 |
> | 대기 표시 | **벨 «1»** · 반 목록 「과제 1」 · 머리말 「확인 대기 1」 · 완료 1/3→0/3 — **네 곳이 같이 움직인다** |
> | **벨 착지** | 오답 화면·테스트반2를 보던 중에 눌렀는데 **반 관리 → 테스트반(제일 밀린 반) → 과제 탭 → 「ㅁㄴㅇ」이 펴진 채** 착지 |
> | 벨이 안 지워진다 | 눌러도 배지 1 그대로 · **수락하니 사라졌다** — `keepUnseen` 설계대로 |
> | 수락 | 「완료」 · `김하현T · 2026-08-16` · **화면이 즉시 갱신됐다** = `setHwState`의 «되꽂기»가 산다 |
> | 사유 없이 반려 | **토스트로 막힌다** · 상태 그대로 |
> | 반려 | 「다시 해야 해요」 + **붉은 사유 상자에 적은 글 그대로** · 버튼이 「현장 확인 · 반려 취소」로 |
> | 반려 취소 | `submitted` 복귀 · 사유 삭제 · **사진·낸 날 유지** |
> | 현장 확인 | 미제출 학생 → 「완료」 + **「현장에서 확인했습니다 (온라인 제출물 없음)」** |
> | **조교 자리** | 학생 상세 > **「과제·오답」 갈래**를 고르면 「과제 확인」 폼이 붙는다 — 반 관리와 같은 줄 |
> | **옛 기록 흡수** | `adsf`가 「**예전 방식으로 완료 표시된 과제입니다 (제출물 없음)**」로 뜬다 — 마이그레이션 없이 읽는 쪽에서 흡수하는 결정이 화면에서 확인됐다 |
> | 사진 | 썸네일·확대 둘 다 **드라이브 원본이 뜬다** |
>
> ⚠ **찾은 흠 둘 (안 고쳤다)**
> · **사진 확대에 닫는 문이 화면에 없다** — `.hw-view` 아무 데나 누르면 닫히지만
>   **×도 없고 ESC도 안 먹는다.** 처음 여는 사람은 막힌 줄 안다 → 「▶ 다음 차례」 **H-1**
> · **강사의 학생 상세 연대기에 「(삭제된 시험지) n번」이 그대로 뜬다** — 08-14에 막은 것은
>   **학생 화면**이었다. 신호에는 안 잡히지만 연대기가 지저분하다 → **H-2**
>
> ✔ **「반 관리 > 과제」가 명단 순서 그대로인 것은 설계대로다** — 「안 낸 사람이 위로」는
> **수업 > 과제**에만 있다(8766행). 반 관리는 «반이 어떻게 돌아가나»라 명단 순서가 맞다.
>
> ⚠ **실데이터에 남은 것** — `decidedAt`이 **2026-08-14 → 2026-08-16**으로 바뀌었다
> (오늘 다시 수락했으므로 사실 그대로다. 되돌리면 감사 로그와 어긋난다).
> **그 외에는 전부 원상복구했다** — 전수 대조로 «남은 차이 1건»만 확인했다.
> 감사 로그에는 오늘 항목이 남는다(수락 2 · 반려 1 · 현장 확인 1) — 지울 수 없다.

> ### 📌 2026-08-16 — 시험 달력을 «학교로 훑을 수 있게» 만들었다
> **세 커밋. 웹 쪽은 08-15에 아무것도 안 했다** (그날은 project2 교재 압축본 쪽이었다).
> AI 한도는 안 썼고 워커도 안 건드렸다.
>
> **① 줄을 고정했다 · ② 띠를 잇고 이름을 주마다 다시 쓴다 · ③ 칸 높이를 줄였다.**
> ①이 «어느 학교인가»를 자리로 알려 주고, ②가 그 자리를 눈으로 따라갈 수 있게 만들고,
> ③이 ①이 늘려 놓은 세로를 되돌린다.
>
> ---
> **① 줄 고정**
>
> 설정 > 시험 일정 > **달력**에서 «시험기간 띠»와 «수학 시험일»이 **행 구분 없이 쌓이고 있었다.**
> 원인은 한 곳이다 — 그날 걸린 것을 **순서대로** 쌓았고, 수학 시험일 딱지를 기간 딱지보다
> **먼저** 그렸다. 그래서 같은 학교가 날마다 다른 높이에 떴고, **하필 수학 시험일에만
> 그 학교가 맨 위로 튀어 띠가 두 동강** 났다. 학교로 훑을 수가 없다.
>
> → **학교·학년마다 줄(lane)을 고정한다.** 한 학교는 그 달 내내 같은 줄에 서고
> **수학 시험일도 그 줄 위에** 뜬다. 기간이 안 겹치는 학교끼리는 한 줄을 나눠 쓴다.
> 그 학교가 없는 날은 **투명한 빈 자리**(`.exc-chip.void`)를 남긴다 —
> **이게 없으면 아랫줄이 위로 당겨져 행이 도로 어긋난다.**
>
> ⚠ **줄을 고정하면 대가가 하나 생긴다 — 검증에서 잡았다.**
> 예전에는 «그날 걸린 것 중 셋»을 골랐으니 어느 학교든 언젠가는 보였는데,
> 줄이 고정되면 **높은 줄로 밀린 학교는 한 달 내내 한 번도 안 보인다.**
> 셋으로 두고 6개 학교를 돌려 보니 자양고가 9월에 한 번도 안 떴다 → **셋 → 여섯.**
> 겹치는 것끼리만 줄을 더 쓰므로 한산한 주는 칸 높이가 그대로다.
>
> ⚠ **빈 줄이 남는 것은 그대로 둔다 — 사용자가 정했다. 다시 꺼내지 말 것.**
> 높은 줄에 혼자 있는 학교는 위로 빈 줄이 그대로 비어 있다(9월 13~19일의 자양고).
> **«주마다 줄을 다시 짜서» 없앨 수 있지만 그러면 주가 바뀔 때 같은 학교가 다른 높이로 옮겨 간다** —
> 줄을 고정한 이유(「눈이 한 줄만 따라가면 된다」)가 주 단위로 깨진다.
> 빈 자리보다 **한 달 내내 같은 높이인 것**이 크다고 판단했다.
>
> · 줄을 잡는 범위에 **수학 시험일까지 넣는다** — 기간 밖에 적힌 경우가 있다
> · **검증** — 소스에서 함수만 떼어(`node`) 겹침·안 겹침·수학일만 있는 것 6개로
>   전수 확인: **줄 어긋난 곳 0건.** 로컬 화면에서도 눈으로 봤다
>   (⚠ 실 DB에 시험 일정이 한 줄도 없어서 **지어낸 날짜를 메모리에만 꽂아** 그렸다)
>
> ---
> **② 띠를 한 덩어리로 잇고, 이름을 주마다 다시 쓴다**
>
> ⚠ **이름을 «기간 첫날 한 번»만 쓰고 있었다** — 주가 넘어간 칸은 이름이 없고
> **지난달에 시작한 시험은 이 달에 이름이 아예 안 나온다.** 줄만으로는 못 되짚는다
> (눈을 왼쪽 위로 한참 올려야 하고, 지난달 것은 올려도 없다).
> → **기간 첫날 · 주가 바뀔 때(일요일) · 달 첫날**에 다시 쓴다. 닷새 내내 반복되지는 않는다.
>
> ⚠ **띠가 닷새면 다섯 조각으로 읽혔다** — 날마다 둥근 알약이 따로 서서
> «이어지는 기간»이 아니라 «매일 뭔가 있음»이 됐다. 셋을 고쳤다 —
> **칸의 좌우 패딩(7px)만큼 늘려 격자선까지 닿게** · **모서리는 첫날 왼쪽·마지막날 오른쪽만**
> (주 경계는 양쪽 다 열어 둬야 «다음 줄로 이어진다»가 보인다) ·
> **첫날과 기간 중의 색을 나누지 않는다** (시작은 모서리와 이름이 알려 준다. 범례도 셋→둘).
> 수학 시험일은 모서리를 «기간 안의 위치»가 정하므로 **띠 한가운데면 각진 채로 색만 바뀐다.**
>
> ⚠ **글자 왼쪽 자리가 어긋날 뻔했다** — 늘린 쪽(margin −7)과 막힌 쪽(margin 0)의
> padding-left를 13/6으로 갈라 **둘 다 칸에서 13px**이 되게 맞췄다.
>
> · **학교마다 색은 안 준다** — 이 시스템은 색이 뜻을 갖는다(`--ok` 출석 · `--no` 결석 ·
>   `--blue` 성적 · `--brand` 수학 시험일). 여섯 색을 뿌리면 그 뜻이 흐려지고
>   **수학 시험일이 «여러 색 중 하나»로 내려앉는다.** 정체성은 줄 자리와 이름이 맡는다
> · **검증** — 위 6개에 «지난달에 시작한 시험»(8/28~9/2)을 더해
>   **모서리 0건 · 이름 0건 · 이름 없는 주 0.** 화면에서도 기하로 쟀다 —
>   기간 시작 7px 막힘 · 가운데 0(이어짐) · 끝 7px 막힘 · 하루짜리는 양쪽 7px
>
> ---
> **③ 칸 높이를 줄였다 — 달력 전체 633px → 555px (−12%)**
>
> ①로 줄이 고정되면서 빈 줄이 자리를 먹어 칸이 세로로 길어졌다. 그만큼 되돌린다.
> `.exc .d` 패딩 `7 7 9` → `6 7 7` · `min-height` 104 → 88 · 딱지 22px → 20px
> (`margin-top` 4→3 · 세로 `padding` 3→2). 주별 높이 104·139·139·104·104 → **88·124·124·88·88**.
>
> ⚠ **세로만 줄인다. 좌우 7px는 건드리면 안 된다** — ②의 «띠를 칸 가장자리까지 늘리는»
> 규칙(`.exc-chip`의 `margin −7` · `padding-left 13`)이 그 7px에 물려 있다.
> 바꾸려면 두 곳을 같이 고쳐야 한다. 바꾼 뒤 **가로 기하와 글 왼쪽 13px가 그대로임을 다시 쟀다.**

> ### 📌 2026-08-14 — 클리닉을 다시 세웠다 · 과제 확인으로 가는 길 (`058ad17`)
> **다섯 커밋. 전부 push 완료.** 위 과제 흐름을 만든 뒤 이어서 한 것이다.
>
> **① 과제 확인으로 가는 길이 다섯 단계였다** — 반 → 수업 기록 → 날짜 → 과제 → 확인하기.
> 게다가 과제 목록이 «그 날짜 것»만 보여서 다른 날 낸 것은 「전체 보기」를 눌러야 나왔다.
> → **서브탭에 「반 > 과제」를 세웠다.** 날짜에 묶이지 않는 상시 자리다.
> **그리는 함수는 그날 상세와 같은 것 하나**를 쓴다 — 묻는 것만 다르다
> (저기는 「그날 무슨 과제를 냈나」, 여기는 「이 반 과제가 어떻게 돌아가나」).
>
> **② 알림 벨에 「과제 제출 확인 대기」를 올렸다.** 누르면 제일 밀린 반의 과제 자리로
> 곧장 가고 대기가 있는 첫 과제를 펴 둔다. 반 목록·과제 머리말에도 대기 수가 뜬다.
> ⚠ **상담·질문처럼 «본 개수»로 셀 수 없다.** 그것들은 늘기만 하는 목록이라 watermark가
> 맞지만 **과제 제출은 수락하면 줄어든다.** 채팅과 같은 길로 갔다(`unseen`+`keepUnseen`) —
> 벨을 눌러서가 아니라 **처리해야** 사라진다.
>
> **③ 수업 > 과제에서 과제 줄을 누르면 학생들이 펴진다.** 안 낸 사람이 위로 온다.
> 줄은 반 관리와 **같은 것**(`hwReviewRowHTML`)이라 **그 자리에서 「현장 확인」**이 된다 —
> 수업 중에 노트를 들고 오는 학생을 바로 체크하는 자리다.
>
> **④ 클리닉 시간대를 «범위»로 연다** — 시작·마지막·간격·정원. 「2시부터 10시까지 1시간」이면
> **아홉 개가 한 번에** 선다. 예전에는 칸에 「오후 2시」를 쳐 넣고 추가를 아홉 번 눌러야 했다.
> **「마지막」은 그 시각에도 자리가 하나 선다는 뜻이다** (끝나는 시각이 아니다 — 화면에도 적어 뒀다).
>
> **⑤ 클리닉을 여러 시간 이어서 신청한다** — 열린 시간대는 **여러 개를 골라** 한 번에 내고
> (같은 날 안에서만), 희망 시간 제안에는 **마지막 시각**이 생겼다(비우면 한 시간).
> 승인하면 그 사이 시각마다 자리를 잡되 **이미 열어 둔 자리가 있으면 거기 앉는다.**
> 신청 하나가 여러 자리를 차지하게 되어 `slotIds`를 뒀다 — 옛 기록은 `slotId` 하나뿐이라
> `clinicSlotIds()`가 둘 다 읽고, `clinicWhenLabel()`이 「오후 2시 ~ 오후 4시 · 3개」로 묶는다.
>
> **⑥ 처리된 신청이 주가 지나도 계속 쌓였다** — 위 주간 판은 주 단위로 넘겨 보는데
> 아래 목록만 전 기간이었다. **보고 있는 그 주의 것만** 남긴다.
> 「승인 대기」는 옆 칸에 주와 무관하게 그대로 둔다 — 처리 안 된 것을 숨기면 그대로 잊힌다.
>
> ⚠ **이날 클리닉에서 겪은 함정 셋** (전부 검증에서 잡았다)
> · **시각을 문자열로 맞추고 있었다.** 열린 자리를 `HH:MM`으로 바꾸자 학생이 제안한
>   「오후 4시」가 그 자리의 `16:00`과 다른 것으로 읽혀, 승인해도 정원이 안 줄고
>   정원 1짜리 자리가 하나 더 생겼다. → `slotTimeKey()`로 «시각»을 맞춘다.
>   자유 문장 입력 둘(학생 희망 시간 · 교사 직접 배정)도 시간 고르개로 바꿨다
> · **정원 세기가 `slotId`만 믿었다.** 승인이 따로 만든 자리를 나중에 중복으로 보고 지우면
>   그 학생이 어느 자리에도 안 앉아 있게 된다. 확정된 것(승인·완료)은 자리가 사라졌으면
>   **날짜·시각으로 센다.** 대기·취소는 안 센다
> · **`'cs'+Date.now()`가 한 밀리초 안에 겹쳤다.** 범위를 승인하면 슬롯을 여러 번 만드는데
>   id가 같아져 `find()`가 늘 첫 번째를 돌려줬고, **세 시간이 전부 한 자리에 붙었다**
>
> ⚠ **정렬도 거짓말을 하고 있었다** — 시간이 자유 문장이라 문자열 비교로
> **「오후 10시」가 「오후 2시」보다 앞에 섰다.** `slotTimeKey()`로 맞췄고, 표시는
> `slotTimeLabel()`이 「오후 2시」로 되돌린다.
> **옛 표기로 남아 있던 둘(시간대 하나 · 신청 하나)은 그날 `HH:MM`으로 정리했다** —
> 쓰기 전에 전수 조사로 «못 읽는 것 0 · 바꾼 뒤 겹치는 것 0»을 확인하고 적용했다.
> 이제 실 DB에 자유 문장 시간은 하나도 없다. 읽는 쪽은 그래도 둘 다 읽는다.
>
> ⚠ `.mbtn`에 `[disabled]`가 없어 «못 누르는데 멀쩡해 보이는» 버튼이 됐다 → `ds.css`에 넣었다.

> ### 📌 2026-08-14 — 과제를 «내고 · 확인받는» 흐름으로 바꿨다 (`d387f84`)
> **새 기능이다. 확인 작업(아래 상자)이 끝난 뒤에 이어서 했다.**
>
> 과제는 오래 **«학생이 스스로 누르는 참/거짓» 하나**였다. 눌렀다는 말 말고는 아무 근거가
> 없었고, 강사에게는 체크할 문조차 없었다(「완료 현황」은 보기 전용이었다).
> 이제 **학생이 노트를 찍어 올리고(최대 6장) 강사·조교가 보고 수락**해야 완료가 된다.
> **온라인으로 안 낸 학생도 현장에서 보여 주면 「현장 확인」 한 번으로 완료**가 된다.
>
> | 상태 | 누가 움직이나 |
> |---|---|
> | 미제출 | 학생이 사진을 올려 제출 · **또는 강사가 「현장 확인」** |
> | 확인 기다리는 중 | 강사·조교가 **수락** 또는 **반려(사유 필수)** |
> | 완료 | 되돌리려면 「완료 취소」 |
> | 다시 해야 해요 | 학생이 사유를 보고 **다시 제출** |
>
> **① 학생에게 «완료로 만드는 버튼»이 없다.** 그 문은 강사·조교 쪽에만 있다.
> **② 낸 것은 «할 일»에서 빠진다** — 공이 강사에게 넘어가 있는데 학생 할 일에 남아 있으면
> 학생은 뭘 더 해야 하는 줄 안다. 안 냈거나 반려된 것만 할 일이다.
> **③ 승인 자리는 둘** — **반 관리 > 수업 기록 > 과제**(반 전체 · 「확인 대기 n」이 뜬다) ·
> **학생 상세 > 과제**(조교도 접근한다). **한 줄(`hwReviewRowHTML`)을 둘이 같이 쓴다.**
>
> ⚠ **옛 기록은 마이그레이션하지 않았다.** `assignmentsDone[id]===true`가 이미 있는데,
> 기록을 통째로 다시 쓰는 것은 이 앱에서 제일 위험한 작업이다(2026-08-12).
> **읽는 쪽에서 «수락됨»으로 흡수한다** — `hwState()` · `hwIsDone()` · `hwNeedsAction()` 셋.
> 같은 날 오답숙제 신호가 «조건문 네 벌 중 한 곳만 빠져서» 샜기에 **처음부터 한 곳에 뒀다.**
>
> ⚠ **강사 화면은 `state.allRecords`를, 저장은 `DATA.records`를 본다 — 다른 객체다.**
> `setHwState`에서 되꽂지 않으면 **수락을 눌러도 강사 화면이 그대로다**(버튼이 안 먹는 것처럼
> 보인다). 실제로 그렇게 만들었다가 검증에서 잡았다. `deleteExam`도 같은 이유로 되꽂는다.
>
> · 과제를 지우면 **제출 사진도 드라이브에서 지운다**(몇 장인지 미리 알린다)
> · `.mbtn`에 `[disabled]`가 없어 «못 누르는데 멀쩡해 보이는» 버튼이 됐다 → `ds.css`에 넣었다
>
> **⚠ 아직 못 한 것 — 배포본에서 «진짜 사진»으로 한 바퀴.** 드라이브 업로드는 로컬에서
> 확인이 안 된다(`ALLOW_ORIGIN`, 9절 1번). 폰으로 찍어 올리는 것은 눈으로 봐야 한다.
> **제출되면 강사에게 알리는 것도 아직 없다** — 지금은 그 화면을 열어야 「확인 대기」가 보인다.

> ### 📌 2026-08-14 — 배포본을 눈으로 봤다. 0-A·0-C·0-E 완료, 버그 셋 (`509981b`)
> **두 커밋, 전부 push 완료. 워커는 안 건드렸다.** AI 한도는 **3/20**만 썼다.
> **브라우저 확장이 붙었다** — 어제까지 못 보던 것들이 전부 풀렸다.
>
> **① ⚠ 시험지가 배포본에 하나도 없었다** — 감사 로그를 보니 **2026-08-13 새벽 2시에
> 사용자가 `test`·`test22`를 직접 지웠다** (작업 노트에 안 적혀 있었다).
> 0-C·0-E·0-B가 전부 그 문항들을 전제로 한 확인이라 한 개도 못 볼 뻔했다.
> → 로컬의 hwpx로 **`test22`(광남고 20문항)를 다시 올렸다.** 파싱 20/20, 정답 20/20 자동.
>
> **② 0-A 조교 여러 반 — 확인 완료.** 옛 형식(`classId`)은 **감사 로그가 증명한다**
> (2026-08-11 「김하현 · **테스트반** → 테스트반2 · 고20.5B」 — 단일 classId에서 읽혀 바뀌었다).
> 조교 화면에 「담당 반 3개」·**「반」 열**·**칩 고르개**(전체 4 · 테스트반2 0 · 고20.5B 1 · 테스트반 3)가 선다.
>
> **③ 0-E 화면 확인 — 통과.** 「공개됨」에서 R을 누르니 큐가 **「1차 검토 대기」로 따라갔고**
> 문항 선택도 유지됐다. `approved`→`pending`, 공개됨 1→0 · 1차 0→1. **빈 화면이 아니다.**
> 쌍둥이 정답도 두 번 다 검산 통과(63 · 45).
>
> **④ 0-C에서 버그를 찾아 고쳤다 — 좌석 배치도가 «보기 표»로 오판됐다**
> `isChoiceTable`이 «칸 하나가 딱 ①»을 선지로 봐서, **기호의 위치가 곧 문제인 표**를
> 글로 펴고 `.filter(Boolean)`으로 **빈 칸(운전석 자리)까지 지웠다.**
> 9번은 좌석 위치가 전부라 **학생이 못 푸는 문제**가 된다. → 6절 「자리표는 선지가 아니다」
> 조건 상자 다섯(8·9·16·17·18)은 원래대로 «상자»로 잘 섰다.
>
> **⑤ 일괄 생성에 「멈춤」을 달았다** — 「AI로 채우기」가 **큐 전체 일괄**인데
> 시작하면 끊을 방법이 화면에 없었다. 실제로 한 건인 줄 알고 눌렀다가 20건이 돌기 시작해
> **페이지를 새로고침해서** 끊었다. → 6절 「일괄을 멈춘다」
>
> **⑥ 고아 오답숙제를 «읽는 쪽»에서 막았다** — 없는 문항을 가리키는 숙제 15건이
> 테스트학생 기록에 남아 있었다(2026-08-09 «전부 지우고 새로 시작»의 잔재.
> 어제 삭제와 무관하다 — 7월 27~29일자를 가리킨다). 그게 **거짓 「위험」 1순위**를 만들고,
> 학생 화면에는 **「삭제된 시험지 0/15」 유령 구획**을 띄우고 있었다.
> **데이터는 안 건드렸다** — 지금 것은 전부 테스트용 학생이라 사용자가 그냥 두라고 했다.
> 앞으로 같은 일이 안 생기게 **읽는 쪽**만 고쳤다. → 6절 「고아 오답숙제를 읽는 쪽에서 막는다」
>
> ⚠ **손대지 않고 남긴 것** — **단원 자동 채우기 15/20**(5개 미지정 — 0-D의 근거 수치) ·
> 신호가 **「검토 중」인 숙제를 미제출로 세는 것**(뜻이 다른 이야기라 따로 정한다) ·
> 저장된 `test22`의 9번은 **아직 깨진 채다**(내용은 파싱 «시점»에 평문으로 박히므로
> 다시 올려야 고쳐진다. 사용자가 「그대로 둔다」로 정했다 — 다음 진짜 시험지부터 적용된다).

> ### 📌 2026-08-13 — 여덟 커밋, 전부 push 완료 (`e27a83f`)
> 큰 줄기가 셋이다. **AI 한도는 한 건도 안 썼고, 워커도 안 건드렸다.**
>
> | | |
> |---|---|
> | **0-E** | 「AI로 다시 생성」 뒤의 빈 큐 — 큐가 문항을 따라간다. 같은 처리가 세 곳에 흩어질 뻔한 걸 `followBankToQueue()` 하나로 모았다 |
> | **랜딩** | 「하루」 → **「생각의 흐름」** 전면 재구성. 주인공이 시스템에서 **수학강사 김하현**으로. 교재 목업 세 권. 공지 제거 |
> | **시험 일정** | 노션 표를 옮겼다 → 표로 세웠다 → **수학 시험일 · 달력 · 지난 시즌 보관**까지 |
>
> ⚠ **이날 드러난 제일 중요한 것** — 내가 지어낸 문장이 공개 페이지에 넉 달 가까이 떠 있었고,
> 그걸 다시 「사용자의 교육철학」으로 읽고 다음 판을 설계하고 있었다. → **9절 8번**
>
> ### 📌 2026-08-13 — 랜딩을 통째로 다시 세웠다 「하루」 → **「생각의 흐름」**
> **공개 홈의 주인공이 시스템에서 «수학강사 김하현»으로 바뀌었다.**
> 목표가 「학습 시스템 홍보」가 아니라 **「개인 강사 브랜딩」**임이 확인됐다.
>
> **① ⚠ 사실이 아닌 것 셋이 공개 페이지에 있었다 — 전부 내렸다**
> 「한 학생을 이해하는 데는 한 학기가 걸립니다」·「왜 그 풀이가 성립하는지를 설명하는」은
> **사용자의 말이 아니라 시안 작업 중에 내가 지어낸 문장**이었다. 사진 alt의 「원장」도 틀렸다 —
> **학원에 소속된 개인 강사**다. 「김하현수학연구소」는 학원 이름이 아니라 **개인 브랜드명**이다.
> → 6절 「랜딩을 다시 세웠다」 · **9절 8번**
>
> **② 뼈대는 사용자가 직접 말한 원칙 셋이다** — 개념(원리 이해) · 문제(떠올리는 순서) ·
> 반복(몸에 붙을 때까지). **오답·변형문항·데일리퀴즈는 «원칙 03의 도구»로 70% 지점에 선다.**
>
> **③ 히어로에서 «말하지 않고 보여준다»** — 항등식 문항 하나의 생각 흐름 6단계.
> 사용자가 수업에서 실제로 쓰는 순서를 그대로 옮겼고, **답은 끝까지 안 나온다.**
>
> **④ 확장 없이 재는 길을 만들었다** — `tools/measure-landing.html` + 헤드리스 크롬.
> **여섯 폭(390·560·760·900·1180·1440) 전부** 잘림 0 · 침범 0 · 가로 넘침 0 · 척추 정렬 OK.
>
> **⑤ 교재 목업 — 두 번 갈아엎었다** 받은 AI 목업은 글씨가 깨져 있어서 인쇄용 PDF로
> 다시 만들었고(pdf.js), 곧 사용자가 **투명 PNG 목업**을 올려 줘서 그걸로 갈아끼웠다.
> **엔딩크레딧 · 주기나(기출변형) · 개념서 세 권**이 서고,
> 교재 섹션은 그에 맞춰 «넓은 사진 한 장 + 아래 3열»로 바뀌었다.
>
> **⑥ 공지를 랜딩에서 뺐다** — 재원생에게 자주 내보내는 글이라 흐름을 끊는다.
> 앵커(`sec-notice`)까지 지웠다. **부르는 곳이 하나도 없었다** — 그걸 부르던 구버전
> 로그인 헤더는 2026-08-08에 이미 사라졌는데 **주석만 남아 있었다.**
>
> ⚠ **원칙 문장 셋의 «딸린 설명»은 내가 이은 것이다.** 큰 문장은 말씀 그대로에 가깝지만
> 아래 세 줄은 아니다. 사용자가 push를 지시해 올렸으나 **다음에 한 번 읽고 확정할 것** (9절 8번).
>
> ### 📌 2026-08-13에 한 일 — 큐가 문항을 따라간다 (「▶ 다음 차례」 0-E **완료**)
> **한 커밋. AI 한도는 안 썼다.**
>
> 「AI로 다시 생성」을 **「공개됨」 큐에서 누르면 빈 화면**을 마주하던 것을 고쳤다.
> 그림 업로드 쪽에 **같은 처리가 두 벌** 있던 것까지 **`followBankToQueue()` 하나로 모았다** —
> 가는 곳을 `status`로 계산하지 않고 **큐의 조건 함수에 직접 물어본다.** 줄이 하나 늘어도 따라온다.
> 소스에서 두 조각을 떼어 **여섯 경우**로 확인했다(옛 동작이 그대로인 것 둘 포함).
> ⚠ **화면에서는 못 봤다** — 브라우저 확장이 안 붙어 있었다.
> → 6절 「큐를 문항 따라 옮긴다」 · 「▶ 다음 차례」 시작 상자 ③
>
> ### 📌 2026-08-12에 한 일 — 그림을 기계가 그리게 됐고, 기록을 한 번 날렸다
> **여섯 커밋(`a458dec` `765e392` `d35fe88` `35f640b` `c97d092` `0558276`), 전부 push 완료.
> 워커도 배포 완료.** AI 한도는 5/20만 썼다.
>
> **① (B)의 그림을 기계가 그린다 — 3단계 전부 섰고 배포본에서 한 바퀴 돌았다**
> AI에게 그리게 하지 않는다. **«무엇을 그릴지»를 장면(scene) JSON으로 받아 `figure.js`가 그린다.**
> 그래야 「정말 x=2에서 x축을 만나는가」를 **검산**할 수 있고, 통과 못 한 초안은 버린다.
> 실측에서 모델이 **손으로 유도한 것과 같은 식**을 냈고(`-(x-3)*(x-9)` 등, 검산 12건 통과),
> 지도에는 「못 그린다」로 물러났다. 검토 화면 「초안 그려 보기」 → 붙이기 → 공개 →
> **학생 오답숙제에서 채점까지** 배포본에서 확인했다.
> → 6절 「그림을 코드로 그린다」 · 「▶ 다음 차례」 0-F
>
> **② ⚠ 테스트학생의 기록을 날렸다 (복구 못 함)** — 메모리의 기록을 갈아끼우고 학생 화면을
> 그렸는데, **이 앱은 학생 화면을 «그리기만 해도» 저장한다**(`dqEnsureToday`).
> 그 김에 원래 있던 구멍도 드러났다 — `dbGet`이 실패를 삼키고 `emptyRecord()`를 돌려주므로
> **학생이 앱을 열기만 해도 자기 기록이 지워질 수 있었다.**
> 저장하는 쪽에 문을 둘 달았다(`dbReadFailed` · `recordLoaded`).
> → 6절 「기록을 지우는 두 가지 길을 막았다」
>
> **③ 학생 화면 태블릿 대응** — `.app.sap` 480 고정을 폭 구간으로 풀었다
> (폰 480 · 세로 620 · 가로 680). 전용 레이아웃은 안 만들었다. iframe에 실서비스를 띄워
> **진짜 뷰포트로** 쟀고, 가로 모드에서 탭바에 가리는 요소는 0개였다.
> → 6절 「학생 화면을 태블릿 폭으로 푼다」 · 「▶ 다음 차례」 1번 **완료**

> ### ✅ 2026-08-11 밤 — 배포본에서 (B) 한 바퀴를 다 돌았다 (「▶ 다음 차례」 0번 **완료**)
> **코드는 안 고쳤다.** 배포본(`https://khhyun827-lang.github.io/hahyunmath/`)에서
> 실데이터로 눈으로 본 기록이다. AI 한도 **2건**을 썼다.
>
> | 본 것 | 결과 |
> |---|---|
> | (B) 생성 | `test·1번`(두 이차함수)이 **(B)로 갔다.** figureSpec 네 줄 형식대로 |
> | 정답 검산 | f절편 2·6 · 교점 1·5 → 해 $1<x<2$ → $\alpha+\beta=3$. **AI 답과 일치** |
> | 구조 유지 | 원본(f 위로 볼록·x절편 −1,2 / g 아래로 볼록 / 교점 −2,2 → 해 −2<x<−1 → −3 = 원본 정답 ②)과 **같은 구조** |
> | ① 그림 필요 큐 | 0→1 · 붉은 지침 상자 · 업로드 문 · 「공개할 수 없습니다」 문구 · 한도 20→19 |
> | ② 그려 붙이기 | 큐 1→0, **문항을 따라 「1차 검토 대기」로 큐가 옮겨갔다** · 「1차 검토 완료」 버튼이 회색→활성 |
> | 공개 | 1차→2차→공개. `status=approved`, `variantImgKind='var'` |
> | ③ **학생 화면** | 오답숙제 카드에 **강사가 그린 그림**이 떴다 (원본 그림은 안 붙는다) · 답 3 제출 → **자동 채점 「정답」** · 1/1 |
> | 데일리퀴즈 | 「지금 시험 범위에 해당하는 문제가 없습니다 · **범위 밖으로 접어 둔 문제 2개**는 방학에 다시 나옵니다」 — 단원(여러가지 부등식)이 시즌 범위(공통수학2 5단원) 밖. **설계대로 접혔다** |
> | ④ 다시 생성 | 붙여 뒀던 그림을 **버렸다** (`hasVariantImage=false` + `pbvimg:` 문서 삭제) · `approved→pending` · 새 (B)의 정답도 검산 통과 (f절편 3·9 · 교점 1·7 → $1<x<3$ → 4) |
> | **0-C** 시험지 탭 | 원본 그림·변형 그림 **둘 다** 뜬다. 2번(지도) 원본 그림도 정상 — 2026-08-11 `examDetailHTML` 수정 확인 |
>
> **찾은 흠 하나 → 「▶ 다음 차례」 0-E.** 「AI로 다시 생성」 뒤 **큐가 「공개됨」에 그대로 남아
> 빈 화면**을 마주한다. 그림 업로드에는 큐를 문항 따라 옮기는 처리가 있는데(`uploadVariantFigure`
> 안의 `state.reviewQueue==='nofig'`) **다시 생성 쪽에는 그 처리가 없다.** 같은 종류의 사고다.
>
> ⚠ **③을 보려고 `test` 시험지를 잠깐 「테스트반」(`c_test`)으로 옮겼다가 되돌렸다** —
> 그 시험지가 재원 0명인 「테스트반2」 소속이라 학생에게 닿을 길이 없었다
> (`stuCtx()`가 `DATA.exams.filter(e=>e.classId===classId)`). **`classId`는 원래 값으로 복구했다.**
> 남아 있는 것은 **테스트학생(`test`)의 오답숙제 1건** — 그 문항이 지금 `pending`이라
> 학생 화면에는 「검토 중」으로 뜬다.

> ### 📌 2026-08-11에 한 일 — 그림을 어떻게 다룰지가 두 건, 그리고 조교
> **네 커밋(`111fe6e` `170bb1c` `e1c04d7` `bfef2a4`), 전부 push 완료.
> 워커도 배포 완료(네 번).** ~~남은 것은 **화면에서 (B) 한 바퀴**뿐이다~~ → **위에서 끝냈다.**
>
> **① 그림 문항 변형을 다시 갈랐다** — 전날 못박은 「무조건 그림 없이 푸는 문제로」가
> 반대쪽으로 넘어가 **원본보다 길고 어려운 문제**를 냈다. 갈래를 되돌리되
> **고를 기준을 못박고 쉬운 쪽에 대가를 붙였다** — (B)를 고르면 «그리는 지침»을 써 내야 하고
> **강사가 그려 붙이기 전에는 공개되지 않는다.** 검토 큐에 「그림 필요」가 늘었다.
> **배포본으로 다섯 문항을 돌려 두 번 고쳤고, 결국 갈래가 셋(A/B/C)이 됐다.**
> → 「그림 문항 — 갈래를 되돌리고, 그리는 일을 사람에게 넘겼다」
>
> **② 문항 그림이 시험지 탭에서만 안 보이던 것** — `examDetailHTML`만 `pbimg:{bankId}`를
> 안 불러오고 있었다. 그림을 늦게 부르는 코드 네 벌을 `pbImageHTML()` 하나로 모았다.
> → 「문항 그림이 시험지 탭에서만 안 보였다」
>
> **로컬에서 확인한 것** — 실데이터 시험지의 그림 3장이 시험지 탭에 뜬다 · 「그림 필요」 큐·
> 지침 상자·업로드 문·공개 잠금(버튼 + `advanceBankStatus` 둘 다) · `variantImgKind` 다섯 경우.
> **AI 생성 품질은 배포해야 본다** (`ALLOW_ORIGIN`, 9절 1번).
>
> **③ 조교가 여러 반을 맡는다** — `assistant.classId` 하나였고 바꿀 문도 없었다(계정을 지우고
> 다시 만들어야 했다). 읽는 곳 다섯을 `assistantClassIds()` 하나로 모으고, 설정에 「담당 반 변경」을
> 달았다. → 「조교가 여러 반을 맡는다」

> ### 📌 2026-08-09 ~ 10에 한 일 — 오답 파이프라인을 통째로 다시 세웠다
> **열한 커밋, 전부 push 완료.** 큰 줄기 하나에 그때그때 드러난 버그가 딸려 나왔다.
>
> **① 데일리퀴즈를 갈아엎었다** (`cdeca99` `73a06ee`)
> 「교사가 문제를 써서 반에 낸다」 → **「학생이 틀린 것(원본+AI 변형)이 하루 3문제씩
> 랜덤 로테이션으로 돌아온다」.** 간격 반복(틀리면 다음날 · 맞히면 3→7→14일 → 은퇴),
> problembank에 정답이 있어 **자동 채점된다.**
> 들어오는 문은 **오답숙제를 끝낸 문항**, 나가는 문은 **지금 시험 범위**.
> 범위는 **시즌 × 학교 × 학년**으로 갈리고, 범위 밖에서는 복습 시계가 멈춘다.
>
> **② 시험지 등록·검토를 쓸 만하게** (`365dbed` `7fcca80` `a138aa1`)
> hwpx 파싱 수정(39개→20개) · 등록 화면 수식 미리보기 · 문항 전체 삭제 ·
> **시험지 자리에서 쌍둥이 일괄 생성**(한도까지, 끝나면 1차 검토 대기로) ·
> J/K 뒤집기 · 문항 내용 좌우 2열 · **시험지 삭제가 문항·오답숙제까지 함께 턴다**
>
> **③ 표·선지·그림이 화면에서 깨져 있던 것** (`5323a62` `8b4f366` `4e2d6d7`)
> 학생 화면 그림이 `[object Object]`였다 · 표가 글 뭉치로 뭉개졌다 · 선지가 다닥다닥 붙었다.
> 되살린 표를 **«상자»(1열)와 «격자»(2열+)**로 갈라 그린다.
>
> **④ 그림 문항 변형** (`9686337`) — 워커 프롬프트를 「그림 없이 푸는 문제로 다시 쓰라」로.
> **배포는 했으나 품질 확인은 못 했다** (그날 AI 한도 소진). 「▶ 다음 차례」 0번.
>
> **⑤ AI 한도가 거짓말을 하고 있었다** (`bd421f7`) — 화면 15/20, 실제 20/20.
> 워커에 `/quota`를 열고 **워커를 단일 진실로** 삼았다.
>
> **데이터** — 2026-08-09에 시험지·문항·오답숙제를 전부 지우고 새로 시작했다.
> ⚠ **컬렉션을 비우면 구버전 blob에서 되살아난다** (아래 「⚠ 컬렉션만 비우면」).

> ### 📌 2026-08-08 ~ 09에 한 일 (열 커밋, 전부 push 완료 · 배포 확인까지)
> **① 배포본 확인** — 공지 서식(세 곳 같은 HTML) · 채팅 알림 · 한글 입력 네 자리 전부 정상
> **② 제품 개선 넷**
> `8c736a9` 신호 임계값을 설정으로 (일곱 개 전부) → `8600044` 연대기 레일·점 +
> `.body` 이름 충돌 버그 → `a2146ca` 학생 고정 메모 · 클리닉 배정 문 →
> `45a09f7` 화면의 «칸» 배경 아홉 (+ 공지·설정 높이 버그 둘)
> **③ 시안 전수 대조** — 열여섯 개를 한 쌍씩 봤다
> `a72867b` 질의응답 두 단 · 검토 선 맞춤 · 설정 부품 (+ ds.css `.q` 충돌) →
> `b86b003` mono 안의 한글 아홉 곳 → `f286f92` 수업 기록 출결 도트·월 머리 ·
> 시험지 KPI · 클리닉 날짜별 시간대
> **④ 새 기능 둘** — `4ef2ef4` **상담 일지**(학생 상세 연대기) · **강의자료**(반 관리 > 자료 ·
> 학생앱 학습 > 자료)
> **⑤ 드라이브 업로드 복구** — `673fad5` 진단 → `72e8d01` 확인.
> 토큰이 만료돼 **사진 첨부가 전부 죽어 있었다**. 사용자가 동의 화면을 프로덕션으로 게시하고
> 토큰을 재발급했고, 문서·이미지 두 경로 다 배포본에서 확인했다 (9절 2-B)
>
> **다음에 이어서 할 것은 아래 「▶ 다음 차례」 하나만 보면 된다.**

---

## 0. 지금 상태 — 여기부터 읽으면 된다

**2026-08-06에 시작한 이식이 2026-08-08에 끝났다.**
`index.html`에 **구버전 스타일이 한 줄도 남아 있지 않다** — `.legacy` 스타일 블록(476줄)도,
`body.legacy`도, `lg-` 클래스도 전부 사라졌다. `design-v1/`·`landing-v2/`는 «따라 그릴 기준»으로 남는다.
남은 일과 진행 기록은 **0-B**에 있다.

> 그래서 `tools/check-css-collisions.js`도 지웠다 — 두 CSS가 공존하는 동안만 필요한 도구였다.

| | 상태 |
|---|---|
| 디자인 시스템 | **확정** — 루트의 `ds.css` + `ds.js`. 로고 기준 색까지 정리 완료 |
| 관리자 화면 | **16개 완성 · 실서비스 이식 16/16 완료** — 렌더링 검증 전부 0건. 설정에 갈래가 둘 늘었다 (시안 밖) — 「신호 규칙」 · 「시험 범위」 |
| 학생 마이페이지 | **11개 완성 · 실서비스 이식 11/11 완료** (4탭) — 검증 전부 0건. 학습 탭에 「자료」가 늘었다 (2026-08-09). **데일리퀴즈는 오답 간격 반복으로 다시 만들었다** (2026-08-09~10). **태블릿 대응 완료** — 폭 구간 셋 (2026-08-12) |
| 시험 일정 | **2026-08-13 신설** — 노션에서 손으로 정리하던 표를 「설정 > 시험 일정」으로 옮겼다. 기존 «시험 범위»에 **날짜와 교재**를 더한 것이고 표를 새로 만들지 않았다. **D-day가 학생 홈과 명단에 뜬다** |
| 오답 파이프라인 | 시험지 등록(문항마다 **단원**) → 오답 등록 → **쌍둥이 일괄 생성** → 3단계 검토(+**그림 필요**) → 오답숙제 → **데일리퀴즈 간격 반복**. 2026-08-10에 한 바퀴를 다시 세웠고, 2026-08-11에 그림 문항의 갈래를 갈랐고, **2026-08-12에 (B)의 그림을 기계가 그리게 됐다** |
| 기록 저장 | ⚠ **저장하는 쪽에 문이 둘 있다** (2026-08-12) — 못 읽은 문서에는 안 쓰고(`dbReadFailed`), 저장되는 기록은 «읽어 온 그 객체» 하나뿐이다(`recordLoaded`). **학생 화면은 그리기만 해도 저장한다** |
| 파일 업로드 | **정상** — 드라이브(워커 `/upload`). 2026-08-09에 토큰 만료로 죽었다가 복구했다 (9절 2-B) |
| 워커 | **최신** — 2026-08-11에 네 번 배포해 갈래를 셋(A/B/C)으로 맞췄고, **2026-08-12에 `/figure`(그림 장면)를 열어 배포했다.** 배포본에서 전부 확인 완료. **AI 한도의 진실은 워커 하나뿐이다** |
| 랜딩페이지 | **2026-08-13에 전면 재구성** — 컨셉 「하루」 → **「생각의 흐름」**. 주인공이 시스템이 아니라 **수학강사 김하현**이다. 원칙 셋이 뼈대, 시스템은 70% 지점. 여섯 폭 실측 0건. ⚠ **교재는 표지 시안(목업)이라 실물 사진 대기 중** · `landing-v2/`는 이제 기준이 아니다 |
| 로그인 · 조교 | **시안 2개 완성 · 이식 완료** (2026-08-08). **조교는 여러 반을 맡는다** — 설정에서 담당 반을 바꿀 수 있다 (2026-08-11) |
| 실제 구현 | **구버전 화면도 스타일도 하나도 없다.** 「legacy」라는 낱말이 파일에 0회 |

### 이어서 시작하는 절차
```bash
cd "C:\Users\hahyun\Desktop\claude work"
git log --oneline -8              # 어디까지 왔는지
git status -sb                    # main == origin/main 이어야 정상
node tools/serve.js               # 127.0.0.1:8777
```
`file://`로 열면 iframe 썸네일이 막힌다. Windows Store 파이썬 스텁은 동작하지 않으므로 **node**를 쓴다.

그 다음 **0-B의 «다음 차례»**를 읽고 이어가면 된다.

**`tools/`에 있는 것 넷** — 전부 node로 곧장 돌린다.
| | |
|---|---|
| `serve.js` | 로컬 서버 (127.0.0.1:8777) |
| `test-json-repair.mjs` | 워커의 JSON 복구를 8건으로 시험한다. **배포 없이 돌아간다** |
| `probe-worker.mjs` | 배포된 워커를 화면 없이 부른다. 쌍둥이 «갈래»와 figureSpec만 볼 때. ⚠ 한 번에 하루 한도 하나를 쓴다 |
| `figure-preview.mjs` | 장면(scene)을 검산해 SVG로 그리고 대조표를 낸다. **배포도 한도도 안 쓴다** → 6절 「그림을 코드로 그린다」 |
| `probe-figure.mjs` | 배포된 워커의 `/figure`를 눌러 받은 장면을 그 자리에서 검산·렌더한다. **클라이언트를 배포하기 전에 모델 품질부터 볼 수 있다.** ⚠ 한 건에 한도 하나 |
| `measure-landing.html` | 공개 홈을 진짜 뷰포트 iframe에 띄워 **잘림·침범·넘침·척추 정렬**을 잰다. **브라우저 확장 없이 헤드리스 크롬으로 돈다** → 6절 「확장 없이 재는 길」 |
| `render-pdf-cover.html` | 인쇄용 표지 PDF를 캔버스로 그린다 (poppler 대신 pdf.js). ⚠ 본체는 `.gitignore` — 파일 머리말의 curl 두 줄로 받는다 |
| `make-book-mockup.html` | 투명 목업 PNG(`tools/mock-*.png`)를 `--paper` 위에 앉힌다 → `book-set.jpg`. **책이 늘면 여기만 고쳐 다시 굽는다** |
| `screen-shot.html` | **로그인한 화면**(관리자·학생)을 진짜 뷰포트로 찍는다. 확장 없이 헤드리스로 돈다. ⚠ `?pre=`에 저장 함수를 넣지 말 것 — 실 DB에 쓴다 |

### 바로 열어볼 곳
```
http://127.0.0.1:8777/index.html                     실서비스 (공개 홈이 첫 화면)
http://127.0.0.1:8777/design-v1/preview.html         시안 전체 (관리자 16 + 아래 넷 링크)
http://127.0.0.1:8777/design-v1/00-system.html       디자인 시스템 문서
http://127.0.0.1:8777/design-v1/20-student-app.html  학생앱 11개
http://127.0.0.1:8777/design-v1/17-login.html        로그인 5상태
http://127.0.0.1:8777/design-v1/18-assistant.html    조교 2화면
http://127.0.0.1:8777/landing-v2/preview.html        랜딩 (데스크톱+모바일 동시)
```
관리자·학생·조교 화면은 로그인이 필요하다. 브라우저 콘솔에서 곧장 들어갈 수 있다.
```js
// 관리자
state.currentUser={type:'teacher'}; state.view='teacher'; state.teacherTab='roster'; render();
// 학생 (기록이 있어야 화면이 찬다)
state.currentUser={type:'student',studentId:'test',name:'테스트학생'};
state.view='student'; loadRecord('test').then(render);
// 조교 (계정 없이도 화면은 볼 수 있다)
state.currentUser={type:'assistant',name:'김하현',classId:'c_test',taId:'t',id:'a'};
state.view='assistant'; loadAllRecordsIfNeeded();
```
> ⚠ **`ALLOW_ORIGIN` 때문에 로컬에서는 워커를 못 부른다.** AI 생성·이미지 업로드는
> 배포된 사이트에서만 확인된다 (9절 1번).

---

## 0-B. 구현 진행 상황 (2026-08-06 착수)

**`index.html` 이식이 시작됐다.** 아래 «이행 발판»을 먼저 읽어야 이 파일의 나머지가 이해된다.

### 이행 발판 — 두 CSS의 공존 장치 · **2026-08-08 전부 철거됨**
> 아래는 «이 제품이 어떻게 옮겨졌는가»의 기록이다. **지금은 하나도 남아 있지 않다** —
> `body.legacy` · `MIGRATED_VIEWS` · `TEACHER_TABS_DS` · `CLASSHUB_TABS_DS` · `lg-` 클래스 ·
> 구버전 `<style>` 블록(476줄)이 전부 사라졌다. 남은 것은 `.app` / `.app.sap` / `.lp` 셋뿐이다.

새 디자인은 `ds.css`, 구버전은 `index.html`의 `<style>`이었다. 화면을 하나씩 옮기려면 둘이 한동안 공존해야 하는데, **두 쪽이 클래스 이름을 10개 공유하고 있었다.**

> 이름이 같으면 캐스케이드는 «합집합»을 취한다. 구버전 규칙이 선언하지 않은 속성은 ds.css 것이 그대로 얹힌다.
> `.field`가 특히 심했다 — ds.css의 `display:flex`·`height:32px`·`border`가 구버전 폼 래퍼 102곳에 들어와 레이아웃이 무너진다.
> **스코프를 아무리 좁혀도 이건 못 막는다. 이름을 갈라야 한다.**

그래서 **버려질 쪽인 구버전** 클래스에 `lg-` 접두사를 붙였다 (`btn field badge hint mono who panel tag`).

| 장치 | 하는 일 |
|---|---|
| `body.legacy` | 구버전 `<style>` 전체가 이 클래스 안에 갇혀 있다. `:root`도 `.legacy`로 내려 `--ink`/`--paper` 충돌을 막았다 |
| `MIGRATED_VIEWS` | 여기 든 뷰는 `body`에서 `legacy`를 떼고, 공개 헤더·푸터도 붙이지 않는다 (자기 셸을 갖기 때문). **다섯 뷰가 전부 들어갔다** — 이제 `body.legacy`가 붙는 화면이 없다 |
| `TEACHER_TABS_DS` | 관리자 탭 중 새 디자인으로 옮긴 것. 없는 탭은 `<div class="legacy">`로 감싸 구버전 스타일로 그린다. **지금은 16개 전부 들어 있어 사실상 항상 참이다** |
| `.app` (index.html `<style>`) | 실서비스 셸. 목업의 절대배치(1440×900 캔버스)를 스크롤 문서에 맞게 **position만** 바꾼다 |
| `.app.sap` | 그중 «학생 열». 폭을 480으로 묶어 가운데 세우고 하단 탭바를 뷰포트에 고정한다 |
| `.lp` | 공개 홈(랜딩). `.app`을 안 쓴다 — 본문 16px에 전폭이라 밀도가 완전히 다르다 |

**그리고 실제로 그렇게 끝났다** — `<style>` 블록 · `body.legacy` · 두 집합 · `lg-` 클래스가 통째로 사라졌다.
그 자리에 온 것이 `.fm-*`(폼·표 부품)이다 → 아래 「legacy 폼 정리」.

### 지금까지 옮긴 것
- [x] `ds.css`/`ds.js` 루트 승격 (`design-v1/`은 `../ds.css`를 본다)
- [x] 이행 발판 설치 — 공개 홈 문서높이 4151px·버튼 122px로 이전과 동일함을 확인
- [x] **관리자 셸** — 좌측 사이드바 5그룹 → 상단바 7메뉴 + 서브탭. 13개 탭 전부 동작 확인
- [x] **명단** (`01-roster`) — 첫 DS 화면. 진입점 통합까지 완료 → 아래
- [x] **신호** (`05-signals`) — 홈 대시보드를 대체했다 → 아래
- [x] **수업** (`02-class`) — 출결→진도→과제→메모 단계형 → 아래
- [x] **관리자 16개 전부** → 아래
- [x] **학생 마이페이지 11개 → 4탭** (`20-student-app`) → 아래
- [x] **랜딩 합류** (`landing-v2/` → 공개 홈) → 아래
- [x] **로그인 · 조교** (`17-login` · `18-assistant`) → 아래. **이로써 구버전 화면 0**
- [x] **감싸 둔 legacy 폼 전부** → 아래. **이로써 구버전 «스타일» 0**

### 명단 이식 결과 (2026-08-06)
`teacherRosterHTML()` 신설, `TEACHER_TABS_DS = {'roster'}`. 검증 — 행 높이 전부 40px 정확,
머리말·헤더 잘림 0건, 문서 가로/세로 넘침 0건, `check-css-collisions` 새는 속성 0.

**실데이터로 계산한 열** — 신호 / 반 진도 / 성적 추이 / 출결 최근 8회 / 출석률 / 과제 / 오답 / 시청률.
전부 `state.allRecords`와 `getClassProgress()`에서 나온다. `rosterStats()` 한 곳에 모여 있다.

- **출석률 = 「퇴원」을 뺀 기록 중 「결석」이 아닌 비율.** 지각·조퇴는 «온 것»으로 센다.
  제품에 출석률 정의가 없어서 여기서 처음 정했다. 다른 화면에서도 이 정의를 쓴다
- **뷰(누구를) / 필터(어떤 상태를) 두 축으로 갈랐다.** 목업은 반 하나를 고른 상태만 그렸지만
  실서비스는 반이 여러 개고 미배정·퇴원이 있다. 반을 고르면 「반」·「반 진도」 열이 사라진다
- **퇴원 학생은 「퇴원」 뷰에서만 보인다.** 재원 명단에 섞이면 모든 숫자가 흐려진다
- 신호 임계값은 잠정이다. 규칙을 **편집**하는 것은 `05-signals`가 맡는다

**넣지 않은 것** — 열 선택(15/22) · 사용자 저장 뷰 · 과제 일괄 배포 · 학부모 문자 · 클리닉 배정.
전부 도메인 레이어에 없는 신규 기능이라 «눌러도 아무 일 없는 버튼»이 된다.
(과제는 **반 단위로만** 배포되게 돼 있어 «선택한 학생에게»가 데이터 구조상 성립하지 않는다.)
일괄 작업으로 실제로 넣은 것은 **반 이동**(감사 로그 남김)과 **선택 학생 엑셀 내보내기** 둘이다.

#### 진입점을 하나로 모았다
| 예전 | 지금 |
|---|---|
| 설정 > 학생 «전체 계정 목록» 표 | 지웠다. 설정은 «계정을 만드는 곳»만 남았다 (추가 · 엑셀 일괄 등록) |
| 반 > 학생 명단 (`classRosterHTML`) | 함수째 지웠다. 반 관리 머리말의 「학생 명단 →」이 명단의 반 뷰로 보낸다 |
| 반 관리의 성적·출결 내보내기 | 반 관리 머리말로 옮겼다 (`exportClassExcel`은 그대로) |

`state.classHubTab` 기본값이 `'students'` → `'sessions'`로 바뀌었다.

#### 이 화면에서 배운 것 — sticky 헤더와 가로 스크롤은 같이 못 쓴다
고밀도 표는 1440을 전제로 그려서 좁은 화면에서 가로 스크롤이 필요하다.
그런데 **`overflow-x:auto`를 주는 순간 그 상자가 새 스크롤포트가 되어**
`thead th{position:sticky;top:52px}`의 기준이 뷰포트에서 그 상자로 바뀐다.
헤더가 52px 내려앉아 **첫 행을 덮는다.** (`overflow-y:visible`로는 못 막는다 — 브라우저가 auto로 바꾼다.)

→ 목업과 같은 구조로 갔다. `.rst-screen`을 `height:calc(100vh - 92px)` 플렉스 칼럼으로 잡고
머리말은 고정, 표만 자체 스크롤(`flex:1;min-height:0;overflow:auto`), `th`는 `top:0`.
**92 = 상단바 52 + `.body` 상하 패딩 40.** 서브탭 있는 화면으로 이 구조를 옮기면 +42다.

### 신호 이식 결과 (2026-08-06)
`teacherSignalsHTML()` 신설, `TEACHER_TABS_DS = {'roster','signals','home'}`.
검증 — 텍스트 잘림 0 · 부모 침범 0 · 문서 가로/세로 넘침 0 · 콘솔 오류 0 · `check-css-collisions` 새는 속성 0.
40행을 넣어 스크롤시켜도 sticky 등급 머리가 첫 행을 덮지 않는다(명단에서 겪은 사고의 재발 확인).

**규칙을 `signalOf()` 하나로 합쳤다.** 명단(01)의 신호 열도 이제 이 함수만 본다.
규칙이 두 벌이면 화면끼리 숫자가 어긋나고, 그러면 두 화면 다 못 믿게 된다.
실데이터 3명으로 «명단 등급 == 신호 등급»을 대조해 확인했다.

- **등급은 셋.** 위험=무거운 신호 2개 이상 / 주의=무거운 1개 또는 시청률<70 / 관찰=경미한 것만
- 시안은 위험을 «출석률<80 **그리고** 시청률<50»으로 못박았는데, 그러면 결석이 잦아도
  영상만 잘 본 학생이 어디에도 안 걸린다. «무거운 신호 2개 이상»으로 바꿨다
- **점수(0~99)는 등급을 정하지 않는다.** 등급 «안에서 줄 세우는» 값이다.
  점수로 등급까지 정하면 임계값이 두 벌이 되어 어느 쪽이 진짜인지 알 수 없게 된다
- **해제**를 넣었다. 지울 수 없는 트리아지 목록은 곧 아무도 안 보게 된다.
  새 컬렉션 대신 `kv/signal-mutes` 문서 하나에 `{학생id: 'YYYY-MM-DD까지'}`. 기간이 지나면 저절로 풀린다

**넣지 않은 것** — 신호 규칙 «편집» UI · 클리닉 배정 · 메모.
규칙을 고치는 것은 아직 `signalOf()`를 고치는 일이다. 클리닉은 «학생이 신청하고 강사가 승인»하는
구조라 강사가 배정하는 문이 없고, 메모는 도메인에 아예 없다.

#### 홈 대시보드를 지웠다
상단바에 「홈」이 없다. 그런데 로그인 직후 기본값이 `home`이라 **메뉴는 「신호」가 켜진 채
반 목록이 그려지고 있었다.** `TEACHER_TAB_SECTION`도 home을 「신호」로 잡고 있었으니 원래 의도가 그거였다.
→ `home`·`signals` 둘 다 신호로 보낸다. `teacherHomeHTML()`은 함수째 지웠고, 담고 있던 것은 제 자리로 갔다.

| 홈에 있던 것 | 간 곳 |
|---|---|
| 과제 빠른 등록 | 반 관리 > 수업 기록 > 과제 (보는 곳과 만드는 곳을 붙였다. 반·마감일이 그 화면 값으로 미리 채워진다) |
| 시험지 빠른 등록 | 오답 > 시험지 |
| 강의 업로드 | 반 관리 > 영상 |
| 새 상담 알림 | 알림 벨 · 설정 > 상담 |
| 반별 진도 현황 | 반 관리 > 진도 |

> **남겨 뒀으면 `hw-title`·`video-url` 같은 입력 id가 두 화면에 동시에 살아 있게 된다.**
> 둘이 같이 그려지는 날 `getElementById`가 엉뚱한 칸을 읽는다. 지운 뒤 각 id가 하나씩만 남은 것을 확인했다.

### 수업 이식 결과 (2026-08-07)
`teacherSessionHTML()` 신설, `TEACHER_TABS_DS`에 `session` 추가.
검증 — 5단계 전부 텍스트 잘림 0 · 부모 침범 0 · 문서 넘침 0 · 콘솔 오류 0 · 새는 속성 0.

**1차 축이 반이 아니라 날짜와 시간이다.** 명단·신호가 «훑는» 화면이라면 이건 «하는» 화면이라,
왼쪽에 그날의 시간표를 세우고 거기서 지금 하는 수업을 고른다. 오른쪽에서 단계를 순서대로 끝낸다.

- **출결 저장을 새로 쓰지 않았다.** 기존 `attDraft`/`ensureAttDraft`/`setAttStatus`/
  `saveClassAttendance`를 그대로 쓴다 — 결석 보충 영상 자동 배정까지 붙어 있는 로직이고,
  저장 경로가 두 벌이 되면 한쪽만 고쳐진다
- **출결 버튼은 4개다.** 「퇴원」은 수업 중에 누를 일이 없고 잘못 누르면 학생 로그인이 막힌다.
  5상태 전부와 일괄 변경은 **반 관리 > 수업 기록 > 출석**에 그대로 남아 있다 — 기능 손실이 아니다
- **0단계 「지난 수업 확인」을 실제로 만들었다.** 시안은 `fin` 표시만 있었는데,
  지난 수업의 출결 집계·그날 낸 과제·그날 메모를 보여 준다. 「저번에 어디까지 했더라」가 이 화면의 첫 물음이다
- 데일리퀴즈는 이 흐름에 없다 (학생이 제출하고 자동 채점된다) — 시안의 판단을 그대로 따랐다

#### 메모는 새로 만들었다
도메인에 수업 메모가 없었다. 신호 「해제」와 같은 방식으로 **`kv/classnote:{반id}` 문서 하나에
`{날짜: 글}`**로 둔다. 컬렉션을 늘리면 loadAll·마이그레이션까지 건드려야 하는데 이 정도 데이터엔 과하다.
저장 → 다시 읽기 → 다음 수업의 「지난 수업 확인」에 뜨는 것까지 실제 Firestore로 확인했다.

> 글자마다 `render()`를 부르면 전체 재렌더링이라 커서가 튄다. 초안(`sessionNoteDraft`)만 담아 두고
> 저장할 때 읽는다. 그래서 단계를 옮겼다 와도 쓰던 글이 남아 있다.

#### ⚠ 날짜 산술 — `toISOString()`으로 하면 안 된다
`'YYYY-MM-DD'`는 **UTC 자정**으로, `'YYYY-MM-DDT00:00:00'`은 **로컬 자정**으로 읽힌다.
후자를 만든 뒤 `toISOString()`으로 되돌리면 KST(+9)에서 하루가 깎인다.
실제로 **▶ 버튼이 제자리걸음**을 했고 「지난 수업」이 하루 더 옛날(08-05 대신 08-04)을 가리켰다.
→ `shiftYmd()`를 만들어 **양쪽 다 UTC로 맞춰** 문자열 산술만 한다.

> **`todayStr()`은 UTC 기준이다** (`new Date().toISOString()`). KST 00:00~09:00 사이에는
> «오늘»이 하루 전을 가리킨다. 이건 이 화면만의 문제가 아니라 출결 날짜·과제 마감 전부에 걸린
> **기존 문제**라 여기서 고치지 않았다 — 한쪽만 로컬로 바꾸면 수업 화면이 저장한 날짜와
> 반 관리가 보여 주는 날짜가 어긋난다. 고친다면 앱 전체를 한 번에 고쳐야 한다. (9절 6번)

#### 진입점 정리
| | |
|---|---|
| `teacherAttendanceHTML`의 반·날짜 선택기 | 지웠다. 이걸 쓰던 유일한 화면이 수업 탭이었고, 이제 왼쪽 시간표와 날짜 네비가 그 일을 한다. 함수는 `(classId, date)`를 반드시 받는다 |
| `state.attendanceClassId` · `attendanceDate` | 위와 함께 지웠다 |
| `addHomework()` | `addHomework(prefix)`로 일반화했다. 과제 폼이 두 화면(반 관리 > 과제 · 수업 > 과제)에 있는데, **폼을 복제하는 대신 id 접두사만 받는다** — 등록 로직이 두 벌이 되면 한쪽만 고쳐진다. 접두사가 다르니 id도 안 부딪친다 |

> **반 관리 > 수업 기록 > 출석과 겹친다.** 지금은 둘 다 남겨 뒀다 — 저쪽은 5상태·일괄 변경·
> 성적·시험까지 있고 아직 DS로 옮기지 않았다. `14-log`(수업 기록)를 옮길 때 정리한다.

### 반 관리 이식 — 완료 (2026-08-07)
서브탭이 넷이라 **하나씩** 옮긴다. `CLASSHUB_TABS_DS`에 든 것만 DS로 그리고
나머지는 `.chub-body` 안에서 `<div class="legacy">`로 감싼다 — 탭 단위 `TEACHER_TABS_DS`와 같은 장치다.

- [x] **셸 + 반 목록** — 좌측 상시 목록. 예전엔 «목록 화면»과 «상세 화면»이 따로여서
      반을 바꿀 때마다 「← 반 목록으로」로 되돌아가야 했다
- [x] **진도** (`06-classes`) — KPI 6개 + 단원 체크리스트
- [x] **수업 기록** (`14-log`) — 되짚는 목록 + 그날 상세 2단
- [x] **데일리퀴즈** (`15-quiz`) — 회차 스트립 + 제출 답안
- [x] **영상** (`16-videos`) — 이탈 지점 + 학생별 진행

**화면 안의 서브탭 막대를 지웠다.** 상단 서브내비가 이미 같은 넷을 그리고 있어 두 줄로 겹쳐 있었다.

**시안과 다른 점** — 시안은 단원을 「미분/적분」 대단원으로 묶고 각 단원에 진행 날짜·회차를 달았다.
도메인에 그 둘이 다 없다 (`UNIT_CHAPTERS`는 과목별 평평한 목록이고 회차 개념이 없다).
없는 것을 지어내지 않고 **«다음에 나갈 단원»만** 표시한다 — 체크 안 된 첫 단원이라 실제로 도출되는 값이다.

지운 것 — `teacherClassesHTML`(반 목록 표) · `teacherClassProgressHTML`(구버전 진도) ·
`teacherClassSessionsHTML`(날짜 버튼 줄 + 서브탭 막대).
반 목록 표에 있던 「종료/재개」·「삭제」는 반 관리 머리말로 옮겼다.

#### 수업 기록은 «읽는 화면»이 아니라 두 단이다
시안(`14-log`)의 역할은 «되짚는다»라 읽기 전용으로 그려져 있다. 그런데 **여기서만 되는 입력이 셋**
있다 — 퇴원 지정 · 일별 성적 · 시험 등록. 시안대로 만들면 그게 통째로 사라진다.

| 단 | 무엇 | 상태 |
|---|---|---|
| 목록 | 한 행 = 한 번의 수업. 출결 요약 · 메모 · 그날 낸 과제 · 성적 입력 인원 | DS (새로 만듦) |
| 상세 | 행을 누르면 그날의 출석 · 성적 · 시험 · 과제 입력 | 구버전 그대로 (`.legacy`) |

- 필터 4개(전체 · 메모 있음 · 결석 발생 · 과제 배포)는 전부 실데이터에서 센다
- **메모는 수업 화면과 같은 `kv/classnote:{반id}`를 본다.** 수업에서 적은 것이 여기 목록에 그대로 뜬다
- **시안의 「회차」와 「단원」은 넣지 않았다.** 도메인에 둘 다 없다 — 기존 코드도
  «차시 번호 없이 날짜만 다룬다»고 못박아 두었고, 진도는 반 단위지 날짜 단위가 아니다
- 옛 화면의 **임의 날짜 입력칸**은 필터 줄에 남겼다. 목록은 반 일정에서 만든 날짜만 담으므로
  보충 수업처럼 일정 밖의 날짜로 들어갈 문이 필요하다

#### 데일리퀴즈 — 2026-08-09에 통째로 갈아엎었다 (학생별 오답 간격 반복)
**이식 당시의 데일리퀴즈는 「교사가 문제를 하나 써서 반에 낸다」였다** (`quizzes` 컬렉션,
`{id, classId, title, content, date}`). 정답 필드가 없어 **채점이라는 개념 자체가 없었고**,
반 전체가 같은 문제를 풀었다. 그래서 시안의 「정답률」 막대를 못 만들고 **제출률**로 대신했었다.

**지금은 학생이 틀린 것에서 뽑는다.** 풀(pool)은 그 학생의 `wrongHomework`이고,
한 문항이 **카드 둘**을 낳는다 — «원본»(그때 틀린 그 문제)과 «변형»(AI 쌍둥이).
둘 다 `problembank`에 정답이 이미 있으므로 **자동 채점된다.**
→ 「데일리퀴즈 정답률은 못 만든다」가 여기서 닫혔다. **없던 것은 정답이었지 채점 능력이 아니었다.**

| | |
|---|---|
| 저장 | 컬렉션을 늘리지 않았다 — `kv/record:{학생id}`의 `dailyQuiz` 필드 하나 (수업 메모·학생 메모와 같은 판단) |
| 들어오는 문 | **오답숙제를 끝낸 문항만** (`h.done`) — 아래 |
| 나가는 문 | **지금 시험 범위 안의 단원만** — 아래 「시험 시즌과 범위」 |
| 하루 | `DQ_PER_DAY = 3`문제 |
| 간격 | `DQ_STEPS = [3,7,14]`일. 맞히면 멀어지고 네 번째 정답에 **은퇴**한다. 틀리면 **다음날** 다시 |
| 뽑기 | 복습 날짜가 된 카드 중 랜덤. 같은 문항의 원본·변형이 같은 날 겹치지 않게 막는다 |

**오답숙제와 데일리퀴즈를 가르는 선이 `h.done`이다.**
오답숙제는 «틀린 것을 한 번 제대로 푸는 곳», 데일리퀴즈는 «그 뒤에 잊지 않게 하는 곳».
이 선이 없으면 아직 손도 안 댄 숙제가 퀴즈로 먼저 나와 **학생 눈에 같은 문제가 두 곳에 뜬다.**
덤으로, «숙제를 끝냈다»는 곧 그 변형을 실제로 제출했다는 뜻이라 **낼 수 없는 문항은 자연히 빠진다.**
(처음엔 `wrongHomework`에 들어오는 즉시 넣었다가 2026-08-09에 이 조건을 달았다.)

- **오늘 것은 «한 번만» 뽑아 기록에 박아 둔다** (`dailyQuiz.day`·`picks`).
  `render()`가 전체 재렌더링이라, 뽑기를 그릴 때마다 하면 **화면을 그릴 때마다 문제가 바뀐다**
- 뽑기 보장은 `stuCtx()` 한 곳에서 한다 — 홈의 할 일 줄·서브내비 숫자·퀴즈 화면이 **같은 세 문제**를 봐야 한다.
  **교사 화면은 이걸 부르지 않는다** (남의 기록을 렌더 중에 저장하게 된다).
  그래서 교사에게는 「안 열어 봄」이라는 상태가 따로 있다 — 학생이 들어와야 문제가 뽑히기 때문이다
- **원본은 검토 상태와 무관하다** (교사가 시험지에 직접 넣은 문제다). 변형만 `approved`를 본다

> ### ⚠ 못 치는 답을 가진 문제는 로테이션을 **영구히 막는다**
> 채점이 문자열 비교(`===`)라, 학생이 입력할 수 없는 정답을 가진 문제를 내면 영원히 오답이 된다.
> 그리고 **오답은 다음날 다시 나오므로** 그 카드가 자리를 잡고 안 비킨다.
> 실제 시험지에 `$0 \leq a <1$ 또는 $3<a \leq 5$` 같은 정답이 들어 있었다.
> → `dqAnswerable()`이 «칠 수 있는가»를 보고 못 치는 것은 **풀에서 아예 뺀다.**
>
> **`answerFormProblem()`을 쓰지 않은 이유** — 저쪽은 AI에게 시킨 «999 이하» 약속이 지켜졌는지 보는
> **검수**용이라 더 엄하다. 정답 `1512`는 검수에선 걸리지만 **학생은 칠 수 있다.**
> 물음이 다르므로 함수를 따로 뒀다. `normalizeTwinAnswer()`는 여기서도 쓴다 —
> hwpx 파서도 숫자에 `$`를 두른다 (`$100$` → `100`).

**교사 화면은 축이 바뀌었다.** 학생마다 다른 문제를 풀게 되면서 «회차»가 사라졌다.
회차별 막대 → **날짜별 막대**, 세로축은 제출률 → **정답률**. 「이 반에 퀴즈 내기」 폼은 없앴다 —
낼 것이 없다. 그 자리에 **학생별 현황**(오늘 n/3 · 복습 대기 · 다 익힘 · 누적 정답률)이 왔다.
이 화면의 물음이 「누가 밀리고 있나」로 바뀌었다.

지운 것 — `DATA.quizzes`(로드도 안 한다) · `addQuiz` · `deleteQuiz` · `submitQuizAnswer` ·
`quizClassLabel` · `rec.quizAnswers` 사용처 전부 · `stuCtx().quizzes`.
`state.quizSelectedId`(회차 id) → `state.quizStudentId`(학생 id)로 뜻이 바뀌었다.
**Firestore의 `quizzes` 문서는 지우지 않았다** — 되돌릴 일이 생기면 그대로 있다.

> **빈 기록의 «모양»을 `emptyRecord()` 하나로 모았다.** 같은 리터럴이 다섯 군데에 복제돼 있어서
> 필드를 하나 늘리려면 다섯 곳을 다 고쳐야 했고, 한 곳이 빠지면 **그 경로로 만들어진 학생만**
> 조용히 필드 없이 돌아다닌다.

> **CSS 순서가 곧 우선순위다.** `.qz-b.low`를 `.on` 뒤에 뒀더니 «지금 보고 있는 것»이
> 노란색(낮음)에 덮여 어디를 보고 있는지 알 수 없었다. 선택이 상태보다 위여야 한다.

> ### ⚠ KaTeX 함정의 **가로** 판 — 자르는 상자도 `position:relative`여야 한다
> 「오늘의 문제」 목록의 한 줄 요약을 `overflow:hidden;text-overflow:ellipsis`로 잘랐는데,
> KaTeX가 넣는 `position:absolute`인 `.katex-mathml`의 기준이 그 `<b>`가 아니라
> **`.app.sap`(가장 가까운 위치 지정 조상)**이 되어 클립을 그냥 빠져나갔다.
> **문서가 521px 가로로 늘어났다.** `position:relative` 한 줄로 0이 됐다.
> 세로 스크롤 상자에 걸었던 규칙과 같은 것이다 — **자르는 상자에도 똑같이 걸린다.**

#### 시험 시즌과 범위 — «지금 준비하는 시험»만 나가게 했다 (2026-08-10)
간격 반복만으로는 **시험이 끝난 단원이 계속 돌아온다.** 그래서 범위라는 문을 하나 더 달았다.

| 층 | 무엇 | 어디 |
|---|---|---|
| ① 시즌 | 전역. `SEASONS` 6개를 순환한다 (겨울방학·1학기 중간·1학기 기말·여름방학·2학기 중간·2학기 기말) | `kv/season` |
| ② 프리셋 | 과목의 단원 목록을 시즌으로 자른다. `SEASON_SPLIT[과목]` **숫자 하나** | 상수 |
| ③ 학교·학년별 수정 | 프리셋을 **갈아끼운다** (더하지 않는다 — 확장도 축소도 되어야 한다) | `kv/exam-ranges` |

> ⚠ **범위는 학교만으로 갈리지 않는다 — 학년이 같이 들어가야 한다.**
> 처음엔 `bySchool[학교][과목]` 두 겹이었는데, 한 학교의 고1과 고2가 같은 과목을 들으면
> 범위가 하나로 합쳐져 한쪽이 틀린 범위를 받는다. `bySchool[학교][학년][과목]` 세 겹으로 고쳤다
> (2026-08-10). 학교·학년이 비어 있는 학생은 «빈 값»끼리 한 줄로 묶인다 —
> 화면이 그 사실을 경고한다.

- **날짜로 자동 전환하지 않는다.** 관리자가 「시즌 종료」를 누르면 다음으로 간다.
  학교마다 시험 날짜가 다르지만 **먼저 끝난 학교는 휴강·휴식기로 간다**는 것이 현장 판단이다.
  이걸 몰라서 「전역 시즌은 못 쓴다」고 판단했었다 — 사용자가 알려 준 뒤 접었다
- **과목은 학년에서 유추하지 않는다.** 학생 → 반 → `progress.subject`.
  고2·고3은 선택과목이 갈려 학년만으로는 안 정해지는데 **반은 이미 과목을 안다.**
  이 덕분에 「학년×시즌×과목」 표(12~18개)가 **과목당 숫자 하나(7개)**로 줄었다
- **범위를 모르면 거르지 않는다** (`studentRange`가 null). 반이 과목을 안 정했거나 학교가 비었을 때다.
  모른다고 다 막으면 화면이 통째로 비어 «고장난 줄» 안다
- 시즌을 넘기면 **학교별 수정은 지운다.** 시험범위는 매 시험마다 새로 받는 것이라
  지난 시즌 수정이 남아 있는 쪽이 더 위험하다

> ### ⚠ 범위 밖에서는 **복습 시계를 멈춘다**
> 안 그러면 시즌이 넘어가 있던 동안에도 `due`가 흘러, 방학에 범위를 다시 열 때
> **수십 장이 한꺼번에 밀린 상태로 쏟아진다.** 그러면 «3일 뒤»였던 것과 «14일 뒤»였던 것이
> 구분되지 않아 간격 반복이 통째로 무너진다.
> → `dqRoll`이 마지막으로 뽑은 날(`dailyQuiz.day`)과의 차이만큼 **범위 밖 카드의 `due`를 같이 민다.**
> 20일 뒤 검증: 범위 밖 `04-08 → 04-28`, 범위 안 `04-08` 그대로.

**문항 단원 태그** — 시험지에 여러 단원이 섞이는 일이 많아 **문항별**로 붙인다.
- `exam.questions[].chapter` + **`problembank.chapter`에도 복사한다.**
  ⚠ 시험지를 지우면 태그를 잃는데 범위 판정은 그 뒤에도 돌아야 한다
- 초안은 `guessChapter()`가 낱말 대조로 만든다 (LaTeX 명령어 `\log`·`\int`·`\lim`도 본다).
  실측 9개 중 7~9개를 맞혔다. **강사가 훑고 이상한 것만 고친다** — 22문항을 하나씩 고르게 하면
  등록이 무거워지고 hwpx 일괄 등록의 이점이 사라진다. hwpx 경로도 같이 채운다
- **단원이 빈 문항은 어느 범위에도 안 들어 데일리퀴즈에 영영 안 나온다.**
  저장을 막지는 않는다(급할 때 등록 자체를 못 하게 된다). 대신 등록 화면이 그 사실을 미리 말해 준다
- ⚠ **AI 판정은 아직이다.** 워커(`gemini-proxy.js`)에 엔드포인트를 더하고 재배포해야 하는데
  `ALLOW_ORIGIN` 때문에 **로컬에서 검증이 안 된다.** `guessChapter()` 하나만 갈아끼우면 되게 해 뒀다

#### hwpx 파싱 — 한 문항이 12조각으로 갈라지고 있었다 (2026-08-10)
실제 파일(광남고 1-1b, 20문항)을 올려 보니 **39개로 인식되고 정답이 19개 비어 있었다.**

원인은 `hwpParseBlocks`가 **최상위 표를 만나면 진행 중이던 문항을 끊고**(`flushPlain`)
셀 하나하나를 별개 문항으로 만든 것이었다. 시험지 양식이 두 가지인데 한쪽만 맞추고 있었다.

| 양식 | 표의 뜻 |
|---|---|
| ① | 표 한 칸이 **문항 하나** (원래 이 파서가 맞춘 것) |
| ② | 문항은 본문에 흐르고, 표는 «다음 조건을 만족시킨다» 상자·보기 격자 |

②를 ①로 읽으면 한 문제가 조각난다. 좌석 번호 표(①~⑧)가 든 문항 하나가 **12조각**이 났다.

> **양식을 알아맞히려 들지 않는 것이 답이었다.** 알아맞히는 규칙은 다음 양식에서 또 깨진다.
> → **양쪽으로 다 읽어 보고 미주(정답) 개수와 맞는 쪽을 고른다.**
> 문항 하나에 정답 미주가 하나씩 달리므로 미주 수가 곧 «문항 수»의 근거다.
> 동점이면 기존 해석(①)을 유지한다 — 그 양식으로 이미 쓰던 문서가 있다.

**결과 — 39개 → 20개, 정답 누락 19개 → 0개.**

같이 고친 것: 한글 수식 문법 넷이 LaTeX로 안 바뀌고 있었다.
`LEFT |`·`LEFT {`(괄호만 처리하고 있었다) · `CDOTS` · `!=` · `pile{a#b}`(세로 쌓기).
**수식 275개가 전부 KaTeX로 렌더된다**(`renderToString`으로 전수 확인, 실패 0).
- ⚠ 중괄호는 `\left\{`로 이스케이프해야 한다. `\left{`는 KaTeX가 못 읽는다
- 짝이 안 맞는 `LEFT`/`RIGHT`가 남으면 **수식 전체가 안 그려진다** → 못 알아본 것은 지운다

> **테스트에 쓴 시험지는 저장소에 넣지 않는다** — 남의 저작물이고 공개 저장소다.
> `.gitignore`에 `*.hwpx`를 넣었다. 폴더에 두고 쓰는 것은 괜찮다.

#### ⚠ AI 한도를 클라이언트가 따로 세고 있었다 — 「남았다고 떠 있는데 429」 (2026-08-10)
화면엔 **15/20**으로 떠 있는데 누르면 실패했다. 워커에 물으니 **`{"error":"quota exceeded","used":20}`**.
파일 머리의 경고(«어긋나면 남았다고 떠 있는데 429가 된다»)가 그대로 현실이 됐다. 어긋난 이유가 셋이다.

| | 워커 | 클라이언트(옛) |
|---|---|---|
| 언제 세나 | 요청을 받을 때마다 (**실패해도**) | 성공했을 때만 |
| 누구별로 | 사용자(uid)별 + 전체 | 공용 문서 하나 (`kv/ai-quota`) |
| 날짜 | **UTC** | **로컬**(`todayStr()`) — KST 00~09시에 하루 어긋남 |

→ 워커에 **`/quota`**(세지 않고 보기만)를 열고 **클라이언트는 그 숫자를 그대로 쓴다.**
생성 응답에도 `quotaUsed`를 실어 보내 매번 맞춘다. `bumpAIQuotaUsed()`는 지웠다.
- 워커에서 `quotaDay()`로 날짜 만드는 곳을 하나로 모았다 (peek와 bump가 어긋나면 안 된다)
- `peekQuota`는 개인·전체 중 **더 많이 찬 쪽**을 돌려준다 — 실제로 막는 쪽이 그것이다
- 워커를 못 부르는 곳(로컬)에서는 **0으로 보고 그린다.** 진짜 한도는 어차피 워커가 막는다
- **429는 «실패»와 다르게 다룬다** — 일괄 생성 중이면 그 자리에서 **멈춘다.**
  계속 두드려도 전부 429인데 13초씩 쉬며 헛돌기만 했다

#### 시험지를 지우면 거기서 나온 것도 같이 지운다 (2026-08-10)
예전에는 시험지만 지우고 **문항 창고는 남겼다** («관련 오답 문제 DB는 유지됩니다»라고 물었다).
그러면 검토 큐에 **「(삭제된 시험지) N번」이 영영 남는다** — 원본이 없어졌으니
쌍둥이를 만들 수도, 판단할 수도 없는데 큐에서 빠지지도 않는다. 비울 수 없는 줄이 된다.

이제 `deleteExam`이 넷을 함께 턴다 — **문항 창고 · 문항 그림(드라이브 포함) ·
학생들의 오답숙제 · 그 카드의 데일리퀴즈 상태(cards·picks·log)**.
- **지우기 «전»에 몇 건인지 말해 준다** — 학생 기록을 먼저 실어 세고 나서 묻는다
- 학생 기록의 오답숙제를 안 털면 **«화면에 아무것도 안 뜨는 숙제»**가 남는다
  (카드 렌더가 `if(!bank) return ''`이라 조용히 빈다)
- `state.allRecords`와 `DATA.records`는 **서로 다른 객체다.** 고칠 때는 `loadRecord()`로
  받아 고치고 `saveRecord()`한 뒤 `state.allRecords`에도 같은 객체를 꽂아야 화면이 따라온다

#### 그림 있는 문항의 «변형»은 변형이 아니었다 (2026-08-10, 네 번째)
20번 원본이 「축구 대진표」, 변형이 「피구 대진표」였다 — **소재만 갈리고 수학은 그대로.**

원인은 워커 프롬프트였다. 그림 문항에 「그림은 그대로 두고 숫자만 바꾸거나,
**또는** 그림에 의존하지 않는 형태로 조정하라」고 **두 갈래를 열어 줬다.**
모델은 당연히 쉬운 쪽을 골랐다. 그림을 새로 그릴 수 없는 이상
«그림을 남기는 변형»은 원본과 같은 문제일 수밖에 없다.

→ 갈래를 없애고 한 길로 못박았다: **그림 문항은 «그림 없이 글만으로 풀 수 있는 문제»로 새로 쓴다.**
그림에서만 알 수 있는 값(좌표·길이·각도·배치)을 본문에 글로 옮겨 적게 하고,
「다음 그림과 같이」류 표현을 금지했다.

- 워커가 `figureFree`를 돌려주고, 클라이언트가 `bank.variantFigureFree`로 저장한다
- ⚠ **변형에는 원본 그림을 붙이지 않는다.** 필요한 값이 이미 글에 다 있는데
  상관없는 그림이 같이 뜨면 학생이 그걸 보고 풀려다 틀린다.
  그림은 «카드마다» 다르다 — 원본 카드는 자기 그림을 쓰고 변형 카드는 안 쓴다
- 검토 화면의 「↑ 왼쪽과 같은 그림을 그대로 씁니다」를 지웠다.
  그건 «같은 그림»이 아니라 사실상 **«같은 문제»**라는 뜻이었다.
  대신 「본문에 값이 다 적혀 있는지 확인하라」고 적는다

> **⚠ 워커를 다시 배포해야 반영된다** (`worker/gemini-proxy.js`).
> 그리고 `ALLOW_ORIGIN` 때문에 **로컬에서는 이 경로를 검증할 수 없다** — 가짜 응답으로
> 플래그·그림 분기만 확인했다. 실제 생성 품질은 배포본에서 봐야 한다.
>
> **이 규칙은 하루 만에 반대쪽으로 넘어갔다** → 바로 아래 「그리는 일을 사람에게 넘겼다」.

#### 그림 문항 — 갈래를 되돌리고, 그리는 일을 사람에게 넘겼다 (2026-08-11)
위의 「무조건 그림 없이 푸는 문제로 다시 써라」를 실제로 돌려 보니 **반대쪽으로 넘어가 있었다.**
그림 하나에 든 배치·좌표를 전부 문장으로 풀어 쓰니 **원본보다 길고 어려운 문제**가 나왔다
(사용자 확인). 애초에 **그림을 읽어 내는 것 자체가 물음인 문제**는 글로 옮길 수 없다.

**그래서 갈래를 둘로 되돌렸다. 다만 판단 기준을 못박고, 쉬운 쪽에 대가를 붙였다.**
처음(2026-08-10 이전) 실패한 이유는 «갈래가 둘이어서»가 아니라 **«고를 기준도, 고른 대가도 없어서»**였다.

| | 무엇 | 그림 | 강사가 할 일 |
|---|---|---|---|
| **(A) `figureFree`** | 그림 없이 글만으로 풀 수 있게 새로 쓴다 | 없음 | 확인만 |
| **(B) `needsFigure`** | 그림도 수도 바뀐다 (구조는 유지) | **직접 그려 붙인다** (`pbvimg:{id}`) | **그린다** |
| **(C) `reuseFigure`** | 그림은 배경이고 **본문의 수만** 바뀐다 | **원본 그림을 그대로** (`pbimg:{id}`) | 확인만 |

> **(C)는 처음 설계에 없다가 배포본 확인에서 «빠진 칸»으로 드러났다** (아래 2차 확인).
> 지도·구역도·배치도·대진표처럼 **그림에 수치가 안 적혀 있는** 문제가 여기로 온다.
> 조건이 둘이다 — ① 그림에 원본의 수치가 적혀 있지 않을 것 ② 바꿀 수가 본문에만 있을 것.
> ⚠ 그림에 수치가 하나라도 적혀 있는데 본문의 수를 바꾸면 **그림과 본문이 모순된다** → 그건 (B)다.

- **(B)를 고르는 기준을 프롬프트에 셋으로 적어 뒀다** — 글로 옮기면 조건이 다섯 줄을 넘거나 ·
  그림을 읽는 것이 곧 물음이거나 · 옮기면 난이도가 눈에 띄게 달라지거나
- **(B)에는 대가가 따른다.** 모델이 «그리는 지침»(`figureSpec`, 네 줄 형식)을 써 내야 하고,
  **그림이 붙기 전에는 공개되지 않는다.** 그래서 (B)가 «쉬운 도망길»이 되지 않는다
- (B)에서도 **소재만 바꾸기는 여전히 금지**다 — 구조를 남기되 수는 반드시 바꾼다

> ### ⚠ 배포본 1차 확인 — (B)가 도망길로 쓰였다 (2026-08-11, 워커 배포 후)
> 세 문항을 실제로 돌렸다. **둘은 의도대로, 하나가 뚫렸다.**
>
> | 문항 | 갈래 | 결과 |
> |---|---|---|
> | 두 이차함수 그래프 | **(B)** ✓ | 지침이 구체적이다 (x절편 4·10 / -2·6, 교점 2·7, 그리는 범위까지). **정답 6도 검산으로 맞다.** 객관식 → 주관식으로 정리됨 |
> | 삼각형 ABC (값이 본문에 다 있음) | **(A)** ✓ | AD 6→3, BD 4→2. **원본보다 짧다.** 「다음 그림과 같이」도 사라졌다 |
> | 광진구 7개 동 색칠 (지도) | **(B)인데 뚫림** ✗ | figureSpec이 「원본 그림과 동일함 (변형 없음)」. 본문도 글자 하나 차이. **그런데 정답만 432→240** |
>
> **뚫린 이유** — (B) 지시가 「**숫자**(계수·길이·좌표값·눈금·각도)만 바꾸라」였다.
> 지도·인접 관계도처럼 **그림에 바꿀 숫자가 없는** 문제에서는 그 지시가 «바꿀 것이 없다»로 읽힌다.
> 「축구→피구」 실패가 (B) 안에서 모양만 바꿔 되살아난 것이다.
>
> → 프롬프트를 고쳤다: **그림이 반드시 달라져야 한다**(「동일」·「변형 없음」 금지) ·
> 수가 없는 그림은 **개수·인접 관계·배치 자체를 바꾼다** ·
> **자기 점검** 한 줄 — «그림도 본문도 그대로인데 정답만 달라졌다면 그 문제는 틀린 것이다».
>
> 그리고 **지나간 뒤에 잡는 문**도 달았다 — 검토 화면이 figureSpec에서 「동일/변형 없음」을 찾아내면
> 「다시 생성」을 권한다 (`variantFigureNoteHTML`). 프롬프트 하나만 믿으면 검토자가 눈으로 놓친다.

> ### ⚠ 2차 확인 — 갈래가 둘로는 모자랐다. (C)를 열었다 (2026-08-11)
> 고친 프롬프트로 다시 돌렸다.
>
> | 문항 | 갈래 | 결과 |
> |---|---|---|
> | 평행한 두 선분 위 12개 점 | **(A)** ✓ | 그림이 「같은 간격 점 6개씩 두 줄」 이상을 안 담아 글로 옮겨도 안 길어진다. 점 6→5개, **정답 130도 검산으로 맞다**(원본 280과 같은 셈법) |
> | 광진구 7개 동 색칠 | **(B)인데 여전히 어긋남** | 이번엔 **수가 실제로 바뀌었다**(4색→5색, 선지·정답 전부 새로). 문제로서는 제대로 된 쌍둥이다. **그런데 (B)라서 강사에게 「똑같은 지도를 손으로 다시 그려라」를 시킨다** |
>
> **빠진 칸이 있었다.** 바뀌는 수가 **본문에만** 있고 그림은 그대로여도 되는 문제가 있다 —
> 지도·구역도·배치도·대진표. 갈래가 (A)/(B) 둘뿐이라 이것이 갈 곳이 없어
> (B)로 몰렸고, 그 대가로 **없어도 될 손그림 작업**이 생겼다.
> → **(C) `reuseFigure`를 열었다.** `variantImgKind`가 이미 `'orig'`를 돌려주고 있었으므로
> (옛 쌍둥이용 길) **학생 화면 쪽은 새로 만들 것이 없었다.**
>
> **(C)가 «제일 쉬운 도망길»이 되지 않게 막은 것** — 본문의 수를 반드시 바꿔야 하고,
> 그림에 수치가 적혀 있으면 (C)를 쓸 수 없다(그 순간 그림과 본문이 모순되므로 (B)다).
> 소재 바꾸기는 (C)에서 특히 금지다 — 그림을 그대로 쓰는데 소재를 바꾸면 둘이 어긋난다.
>
> **갈래가 (B)로 쏠리는 것도 같이 잡았다** — 삼각형 문항이 (A)에서 (B)로 넘어가 있었다.
> (B)도 틀린 선택은 아니지만 **강사 손이 더 간다.** 프롬프트에
> 「**(C) → (A) → (B) 순으로 따지고, 앞의 것이 성립하면 뒤를 고르지 마라**」를 넣어 되돌렸다.
> 조건 자체는 그대로 두었으므로 「무조건 (A)」(2026-08-10의 실패)로 되돌아가지는 않는다.

#### ⚠ 모델이 낸 JSON이 «터지거나 조용히 깨지거나» 했다 — LaTeX 역슬래시 (2026-08-11)
502 `parse failed`가 났는데 `finishReason`은 `STOP`이고 raw는 `"}`로 **끝까지 와 있었다.**
잘린 것이 아니었다. **모델이 LaTeX의 역슬래시를 JSON 규칙대로 두 번 쓰지 않은 것**이다.
간헐적이고 수식이 많은 문항일수록 자주 걸린다. 결과가 두 갈래인데 **뒤쪽이 더 나쁘다.**

| 모델이 쓴 것 | 무슨 일 |
|---|---|
| `"$\alpha$"` | `\a`는 JSON에 없는 이스케이프다 → **그 자리에서 터진다** (502로 눈에 보인다) |
| `"$\frac{}{}$"` | `\f`가 **폼피드**로 읽힌다 → **안 터지고 글자만 조용히 깨진다** |

→ `parseModelJson()`을 뒀다. 먼저 그냥 읽어 보고, 터지면 **문자열 «안»의 역슬래시만**
한 번 더 이스케이프해서 다시 읽는다. 통과했더라도 **제어문자(백스페이스·폼피드·탭·CR)가
섞여 있으면** 같은 방법으로 다시 읽는다 — 그게 조용히 깨진 흔적이다.
- 제대로 쓴 응답(`\\alpha`)은 첫 파싱에서 통과하므로 **손대지 않는다**
- ⚠ `\n`은 살려 둔다 — `figureSpec`의 네 줄을 나누는 것이 그것이다.
  (`\neq`가 줄바꿈으로 읽힐 수 있지만 우리가 줄바꿈을 시켜 놓았으므로 그쪽이 압도적으로 흔하고,
  부등호는 프롬프트가 `\leq`/`\geq`로 쓰게 해 두었다.) `\uXXXX`는 뒤 네 자리가 16진수일 때만 살린다 —
  `\underline`과 갈라야 한다
- **배포 없이 검증했다** — 워커에서 함수만 떼어 내 8건을 돌렸다
  (`node tools/test-json-repair.mjs`). 「조용히 깨지던 것」의 옛 동작까지 재현해서 확인했다 —
  안 터지는 사고라 «옛 동작을 재현하는 줄»이 없으면 시험이 무엇을 지키는지 알 수 없다
- 실데이터의 기존 문항을 훑어 제어문자 0건. 다만 그때 변형이 2건뿐이라 표본이 작다

> **같이 고친 것 — `maxOutputTokens`를 안 걸고 있었다.** 갈래를 셋으로 늘려 프롬프트가 길어지자
> 기본값에 걸려 **JSON이 `solution` 중간에서 잘렸다.** 이 모델은 «생각한 것»도 출력 예산에서 쓴다.
> `16384`로 명시하고, 실패 응답에 **`finishReason`을 같이 실어 보낸다** —
> 「잘린 것」과 「모델이 JSON을 못 쓴 것」은 고치는 법이 다른데 그때는 둘 다 `parse failed`로만 보였다.
> (그 구분이 없었으면 이번 역슬래시 원인을 못 찾았다.) 풀이도 「여섯 단계·각 두 줄 이내」로 묶었다.

#### ✅ 최종 확인 — 세 갈래가 배포본에서 다 돈다 (2026-08-11)
같은 세 문항으로 마지막 배포본을 돌렸다. **502가 사라졌고 갈래도 전부 의도대로 갔다.**

| 문항 | 갈래 | 검산 |
|---|---|---|
| 두 이차함수 그래프 (수치가 그림에 있음) | **(B)** | f의 x절편 -2·4, 교점 -3·3 → 해가 $-3<x<-2$ → $\alpha\beta=6$ **맞다.** 물음도 α+β→αβ로 바뀌었고 figureSpec이 구체적이다 |
| 삼각형 ABC (값이 본문에 있음) | **(A)** | AD=3, BD=2 → AC=6, AB=4 → 합 10 **맞다.** 원본보다 짧다 |
| 광진구 지도 | **(C)** | 그림 그대로, 4색→5색. 그릴 것 없음 |

> ### ⚠ (C)로 가는 «세는 문제»는 정답을 믿으면 안 된다
> 지도 문항을 세 번 돌리는 동안 정답이 **240 → 5220 → 5760**으로 매번 달랐다.
> 인접 관계를 그림에서 읽어 세는 일이라 모델이 흔들린다. **문제와 그림 자체는 제대로 만들어진다** —
> 흔들리는 것은 답뿐이다. 검토 화면의 「AI 풀이」가 접힌 채로 있는 것이 이런 경우를 위한 것이다.
> **이 유형은 강사가 반드시 검산한다.** (그래프·도형처럼 식으로 푸는 것은 세 번 다 맞았다.)

**공개를 막는 문이 셋이다** (「답 형태 문제」와 같은 방식) —
검토 큐에 **「그림 필요」**가 새로 생겼고, 공개 버튼이 잠기며, `advanceBankStatus`가 마지막에 한 번 더 막는다.
키보드 `E`로도 들어올 수 있어 **마지막 문은 반드시 함수 쪽**이어야 한다.

- 그림을 올리면 그 문항이 「그림 필요」 큐에서 빠진다. **그때 큐를 따라 옮겨 준다** —
  안 그러면 방금 올린 사람이 빈 큐를 마주한다 (올린 그림을 보고 공개까지 이어서 해야 한다)
- **다시 생성하면 붙여 뒀던 그림을 버린다** (`applyTwinToBank`). 본문의 수가 새로 바뀌었으니
  옛 그림은 이제 «다른 문제의 그림»이다. 남기면 수는 새것·그림은 옛것인 카드가 학생에게 간다
- 그림을 지우면 공개 중이던 것을 `review1`로 되돌린다 (같은 이유)
- 시험지·문항을 지울 때 `pbvimg:`도 같이 턴다 — 안 그러면 드라이브에 주인 없는 파일이 쌓인다

> **변형 카드가 쓸 그림은 `variantImgKind()` 하나가 정한다.** 오답숙제·데일리퀴즈·시험지 탭이
> 전부 이 함수만 본다 — 규칙이 두 벌이면 화면마다 다른 그림이 뜬다.
> 셋으로 갈린다: `'var'`(강사가 그린 것) · `null`((A)이거나 아직 안 그려진 것) ·
> `'orig'`(**2026-08-10 이전에 만든 옛 쌍둥이**. 그때는 «같은 그림을 쓴다»가 규칙이었으므로 지킨다).

> **「그림도 AI로 생성」은 여전히 안 한다** — 지금 엔드포인트(`gemini-3.6-flash:generateContent`)는
> 텍스트만 내고, 기하 도형은 좌표·각도가 틀리면 문제 자체가 거짓이 된다.
> 그래서 «그리는 일»만 사람에게 넘기고 **«무엇을 그릴지»는 AI가 적어 준다.**

#### 그림을 코드로 그린다 — 장면(scene) 스키마와 `figure.js` (2026-08-12, 1단계)

**(B)의 대가가 「강사가 손으로 그린다」인데, 그중 다수는 기계가 그릴 수 있는 그림이다.**
2026-08-11 밤에 (B) 한 바퀴를 돌면서 그 그림을 실제로 그려 봤더니, 쓸 만하게 나온 이유가
그림 실력이 아니라 **산수**였다 — 지침을 읽고 $f(x)=-(x-2)(x-6)$을 세우고,
교점 조건에서 $g=f+k(x-1)(x-5)$를 유도하고, 좌표를 계산해 찍은 것뿐이다.

> **그러니 AI에게 그리게 하지 않는다. «무엇을 그릴지»를 데이터로 받아 우리 코드가 그린다.**
> 이건 「그림도 AI로 생성은 안 한다」를 **뒤집는 게 아니라 우회하는 것**이다 —
> 모델은 여전히 그리지 않는다. 달라진 것은 그 «무엇»을 사람이 읽는 네 줄이 아니라
> **기계가 실행할 수 있는 형태**로 받는다는 것뿐이다.

| | |
|---|---|
| `figure.js` (루트) | 장면 → SVG. 브라우저·node 양쪽에서 `globalThis.Figure`로 선다 |
| `tools/figure-preview.mjs` | 붙박이 장면 다섯을 검산해 그리고 `tools/figure-preview.html`을 낸다 |
| 워커 `/figure` | 원본 그림 + figureSpec + 변형 본문 → **장면 JSON**. 그림이 아니다 (2026-08-12, 2단계) |
| 검토 화면 「초안 그려 보기」 | 장면을 받아 **검산하고** 그려 보여 준다. 통과해야 「이 그림으로 하겠습니다」가 뜬다 |

**장면 스키마 v1은 `kind:'graph'` 하나다** (함수 그래프). 자세한 것은 `figure.js` 머리말에 있다 —
`curves[].expr` · `points[]`(점·내린 점선) · `xTicks` · `axis` · 그리고 **`checks[]`**.

- **`checks`가 이 설계의 값어치 전부다.** 「곡선0이 x=2에서 x축을 만난다」·「곡선0과 1이 x=1에서
  만난다」·「곡선0은 위로 볼록」을 **수치로 확인**한다. 통과 못 하면 그 초안은 **쓰지 않는다.**
  `xTicks`·`points`가 창 안에 들어오는지는 **적지 않아도 자동으로 본다** —
  「반드시 표시할 값」이 화면 밖이면 지침을 안 지킨 것이다
- ⚠ **`eval`도 `Function`도 쓰지 않는다.** 모델이 낸 식을 그대로 실행하면 **강사 브라우저에서
  임의 코드가 도는 문**이 열린다. 재귀 하강 파서를 직접 뒀고, 함수·상수는 **화이트리스트**다
  (`abs sqrt sin cos tan exp ln log log10` · `pi e`). 그 밖의 이름은 「모르는 이름」으로 거부된다.
  `2x`·`3(x-1)` 같은 **생략된 곱셈**은 받아 준다 — 모델이 실제로 그렇게 쓴다
- **SVG로 낸다.** 드라이브를 안 타므로 업로드·압축·`deleteFromDrive`·주인 없는 파일 청소가
  통째로 없어진다. 태블릿에서 안 뭉갠다(「▶ 다음 차례」 1번과 맞물린다).
  **강사가 좌표 하나를 고쳐 즉시 다시 그릴 수 있고, 그때 AI 한도를 안 쓴다** — 이게 제일 크다

> ### ⚠ 어려운 것은 산수가 아니라 **창 잡기와 이름표 자리**다
> 두 번 다 여기서 못나게 나왔고, 둘 다 규칙을 눈에 보이게 고쳤다.
>
> | 사고 | 왜 | 고친 규칙 |
> |---|---|---|
> | 창이 서너 배로 벌어져 봉우리가 안 보였다 (y[−28, 26]) | 곡선의 **끝값**까지 담으려 했다. 가파른 포물선 하나가 나머지를 납작하게 만든다 | **국소 극값**(봉우리·골)으로 창을 잡고, 끝값은 그 폭의 0.8배까지만 따라간다. 나머지는 **잘린 채로 둔다** — 원본 시험지 그림도 거기서 잘려 있다 |
> | 이름표가 곡선 위에 앉았다 | 그리면서 놓아 **아직 안 그려진 곡선**을 몰랐다 | 이름표는 **두 번째 바퀴**에서 놓는다 |
> | 이름표가 눈금 숫자 위에 앉았다 | 곡선만 «잉크»로 셌다 | 축선·**눈금 숫자·축 이름·원점 표시**까지 잉크다. 후보마다 글자 상자를 만들어 **잉크가 하나라도 들어오면 버린다** |
>
> 남은 규칙은 「자기 곡선에는 가깝고 남의 이름표와는 먼 쪽」이다 —
> 너무 멀리 두면 어느 곡선의 이름인지 알 수 없게 된다.

**붙박이 장면 다섯은 전부 실제로 있었던 (B)다** — 원본(α+β=−3) · 1차 (B)(3) · 2차 (B)(4) ·
창 잡기 시험 · **일부러 틀린 것 하나.** 마지막 것은 「교점이 1·5」라고 적어 놓고 식은 1·4에서
만나게 해 뒀다. **통과하는 것만 모아 두면 검문이 실제로 잡는지 알 수 없다**
(`test-json-repair`가 «옛 동작»을 재현해 두는 것과 같은 이유). 지금 **4 ok · 1 fail**이 정상이다.

- **원본 그림부터 재현시켰다.** 변형을 그리기 전에 «원본과 같은 그림이 나오는가»를 먼저 본다 —
  못 그리면 변형은 볼 것도 없다
- 아직 못 미치는 것 — **눈금 숫자와 내린 점선이 겹칠 때가 있다.** 원본 시험지 그림도
  같은 자리에서 겹쳐 화살표로 빼냈다. 급하지 않아 두었다
- **되는 것은 함수 그래프까지다.** 지도·구역도·사진은 못 그린다 — 그런데 그건 이미 **(C)로 간다**
  (원본 그대로). 겹치지 않는다. 입체도형·자유 배치도가 사람 몫으로 남는다

##### 2단계 — 워커가 장면을 내고, 화면이 검산한다 (2026-08-12)

`/figure` 하나가 붙었고 검토 화면에 「초안 그려 보기」가 생겼다. **⚠ 아직 배포하지 않았다.**

| 어디 | 무엇 |
|---|---|
| `worker/gemini-proxy.js` `handleFigureScene` | 원본 그림·figureSpec·변형 본문 → **장면 JSON**. 못 그리면 `{unsupported, reason}` |
| `index.html` `fetchFigureSceneViaAI` | 그 호출. 돌아온 장면을 **`Figure.verifyScene`으로 검문** |
| `requestFigureDraft` / `acceptFigureDraft` | 초안을 받고, 눌러야 붙는다 |
| `figureDraftHTML` | 통과(초록)·실패(붉음)·못 그림 세 모양 |
| `pbImageHTML` | 저장된 그림이 **사진이냐 SVG냐**를 가르는 유일한 자리 |

- **검산은 화면에만 있다.** 워커에도 두면 둘이 다른 판정을 내리는데, 막는 쪽이 화면이므로
  진실도 거기 하나여야 한다. 워커는 «못 그리겠다»만 스스로 말한다
- **«못 그린다»는 실패가 아니다** — 워커가 200으로 돌려준다. 502로 돌리면 화면이
  «워커가 고장났다»와 구분할 수 없다. 지도·입체도형은 여기로 와서 사람에게 넘어간다
- **초안은 저장하지 않는다.** 「이 그림으로 하겠습니다」를 눌러야 붙는다 —
  검산을 통과했다는 것과 «이 문제에 맞다»는 것은 다른 판단이다.
  검산에 걸리면 **그 버튼 자체를 안 그린다** (그림은 보여 준다 — 무엇이 어긋났는지 봐야 정할 수 있다)
- 붙일 때 **손으로 올린 것과 같은 문서(`pbvimg:`)·같은 플래그(`hasVariantImage`)**를 쓴다.
  그래서 「그림 필요」 큐·공개 잠금·다시 생성 시 버리기가 **하나도 안 고치고** 그대로 돈다.
  `imgFileIdOf()`가 SVG 문서에 null을 주므로 `dropVariantFigure`의 드라이브 삭제도 저절로 비켜 간다
- ⚠ **SVG와 장면을 «둘 다» 저장한다.** 장면만 두고 볼 때마다 다시 그리면, 나중에 렌더러를
  손보는 날 **검토자가 이미 승인한 그림이 조용히 달라진다**
- ⚠ **이 경로도 하루 한도를 하나 쓴다.** 그래서 «일괄 생성»에서는 부르지 않는다 —
  검토자가 누를 때만 부른다. 안 그러면 (B) 하나가 한도를 둘씩 먹는다

> ### ⚠ SVG를 innerHTML로 넣는다 — 지켜야 할 불변식 하나
> **모델이 낸 SVG는 절대 저장하지 않는다.** 저장하는 것은 `renderScene()`이 «장면»에서
> 만들어 낸 것뿐이고, 거기서 글자는 전부 이스케이프된다. 식도 `eval`이 아니라 파서를 거친다.
> 이 불변식이 깨지는 순간 그 자리가 곧 스크립트 주입 구멍이다.
> (그래서 워커가 SVG를 돌려주게 만들지 않았다. 돌려받으면 «검사해서 넣고 싶은» 유혹이 생긴다.)

> ### ⚠ 캔버스 크기가 «글자가 읽히는가»를 정한다
> 처음에 720×540으로 잡았더니 **학생 앱에서 눈금 숫자를 못 읽었다.** 폭 480px 열의
> 카드 안이 약 400px이라 0.56배로 줄고, 18px 글자가 10px이 된다.
> → **560×430**으로 좁혔다. 0.72배라 13px로 읽힌다 — 벡터라 폭을 좁혀도 선은 그대로 선명하다.
> 눈금 숫자만 16→17px로 올렸다.
> 그리고 `.pb-fig`의 `max-height:260px`를 **푼다** — 안 풀면 상자에 세로 스크롤바가 생겨
> 그림이 잘린 채로 뜬다. 사진에 높이를 묶는 이유는 «원본이 얼마나 클지 모르기» 때문인데,
> 여기는 우리가 그린 것이다.
> ⚠ **`.pb-svg`로는 못 푼다.** `(0,1,0)`이라 `.app .pb-fig` `(0,2,0)`를 이기지 못한다 —
> 배포본에서 그림이 260px로 잘려 나오고 나서야 알았다. **`.app .pb-svg`로 무게를 맞춰야**
> «뒤에 온 쪽이 이긴다»가 성립한다. (ds.css가 아니라 이 파일 «안»의 클래스와 부딪힌 사고다.)

**로컬에서 확인한 것** (워커는 `ALLOW_ORIGIN` 때문에 못 부르므로 가짜 응답으로 태웠다) —
검산 통과(초록·「이 그림으로 하겠습니다」 뜸) · **검산 실패(붉음·이유 목록·그 버튼 없음)** ·
못 그림(「직접 그려서 올려주세요」) · 저장된 SVG가 **검토 화면과 학생 오답숙제 카드**에
뜨는 것 · 가로 넘침 0.

> ### ✅ 워커 배포 후 실측 — 모델이 «유도해서» 쓴다 (2026-08-12)
> `node tools/probe-figure.mjs figure-cases.json`으로 화면 없이 두 건을 눌렀다 (한도 2건).
>
> | 넣은 것 | 나온 것 |
> |---|---|
> | 실제 (B) 지침 (f절편 3·9, 교점 1·7) | `-(x-3)*(x-9)` · `-(x-3)*(x-9)+2*(x-1)*(x-7)` — **손으로 유도한 것과 글자까지 같다.** xTicks `[1,3,7,9]`, 교점 두 곳에 `dropTo:'axis'`, checks 6건. **검산 12건 전부 통과** |
> | 지도 (인접 관계를 바꾸라는 지침) | `unsupported` — 「지도 및 구역도는 좌표평면 위의 함수 그래프가 아니므로 지원하지 않습니다」 |
>
> **배포본 화면에서도 한 바퀴 돌았다 (2026-08-12)** — 「초안 그려 보기」 → 같은 식이 나오고
> 검산 통과 → 「이 그림으로 하겠습니다」 → `pbvimg:`에 `kind:'svg'` 저장 → **큐가 문항을 따라
> 이동** → 1차·2차 → 공개 → 시험지 탭에도 뜬다. 한도는 초안 1건만 썼다.
> **학생 화면만 배포본에서 못 봤다** (로컬에서는 확인했다) — 그날 기록 사고가 나서 멈췄다.
>
> **프롬프트에서 값이 있었던 것은 「식을 유도하세요」 한 대목이다** —
> 「f와 x=m, n에서 만나는 g는 `(f의 식) + k*(x-m)*(x-n)` 꼴로 두고 k를 정하라(k>1)」.
> 이걸 안 적으면 모델이 숫자를 찍어 넣고 맞기를 바라는데, 그러면 checks가 거의 다 깨진다.
> **못 그리는 것을 «못 그린다»고 말하는 것도 그대로 지켜졌다** — 여기가 뚫리면
> 그럴듯한 가짜 지도가 학생에게 간다.

#### 학생 화면을 태블릿 폭으로 푼다 (2026-08-12)

`.app.sap`이 **480px 고정**이라 태블릿에서 가로의 30~60%가 빈 여백이었다.
깨지는 곳은 없었지만 **손해 보는 화면이 분명했다** — 강의·출석 달력·성적·오답숙제 풀이.

**전용 레이아웃(좌측 레일 + 본문)은 만들지 않았다.** 화면이 11개라 유지비가 두 배인데
얻는 것이 그만큼 크지 않다. **한 열은 그대로 두고 폭만 단계로 풀었다.**

| | 폭 |
|---|---|
| 폰 | 480 (그대로) |
| 태블릿 세로 `≥760px` | **620** |
| 가로 `≥1100px` | **680** |

- **기기를 알아맞히지 않는다.** 학생들이 쓰는 기기가 섞여 있어(사용자 확인) «아이패드 몇 인치»가
  아니라 **폭 구간**으로 갈랐다. 알아맞히는 규칙은 다음 기기에서 또 깨진다 (hwpx 양식 때와 같다)
- ⚠ **데스크톱과 «태블릿 가로»는 폭으로 구분되지 않는다 — 구분하려 들지 않았다.**
  데스크톱에서도 680이 된다. 480보다 낫다
- ⚠ **폭은 `--sap-w` 한 곳에서만 정한다.** 셸(`.app.sap`)과 하단 탭바(`.sap-tabs`)가 같은 폭이어야
  하는데 숫자를 두 곳에 적으면 한쪽만 고치는 날 탭바가 본문과 어긋난다.
  탭바는 `position:fixed`라 흐름에서 빠지지만 **`.app.sap`의 자손**이라 커스텀 속성이 상속된다
- ⚠ **SVG 그림에 `max-width:100%`만 두면 열이 넓어져도 560px에서 멈춘다** (SVG의 `width` 속성).
  `width:100%`로 바꿔 열을 채우게 했다 — 벡터라 늘려도 안 뭉갠다

> ### 실측 — 진짜 뷰포트로 쟀다
> `resize_window`가 이 창에는 안 먹어서(`outerWidth`가 0) **iframe에 실서비스를 띄워** 쟀다.
> iframe은 진짜 뷰포트라 미디어쿼리가 그대로 동작한다.
> ⚠ `DATA`·`state`는 `const`라 `window`에 안 붙는다 — **iframe 안에서 `eval`로** 봐야 한다.
> ⚠ 그리고 **남의 기록을 볼 때는 `saveRecord`를 막아 두고 본다** (학생 화면은 그리기만 해도 저장한다).
>
> | | 폰 390 | 태블릿 세로 820 | 가로 1180 |
> |---|---|---|---|
> | 열 폭 | 390 | **620** | **680** |
> | 오답숙제 그림 | 312px (눈금 9.5px) | **542px (16.5px)** | **560→600px (17px)** |
> | 달력 칸 | — | **75×75** | 83×83 |
> | 탭바 맞물림 · 가로 넘침 · 글자 잘림 | 전부 정상 · 0 · 0 | 〃 | 〃 |
>
> **「가로 모드에서 하단 탭바가 본문을 가리는가」 — 안 가린다.** 끝까지 스크롤한 상태에서
> 탭바 영역에 걸치는 요소가 **0개**였다 (`.body`의 `padding-bottom:78px` > 탭바 58px).
> 네 화면(홈·오답숙제·강의·출석)을 세 폭에서 전부 봤다.
>
> **여백은 따로 안 다듬었다** — 계획에는 있었지만 실측에서 깨지거나 어색한 곳이 없었다.
> 달력은 칸이 커져 오히려 잘 읽히고, 강의·목록은 카드가 열을 채운다.
> **고칠 근거 없이 손대면 위험만 는다.**

> **폰에서는 그림 눈금이 9.5px다** (390px 폭 → 카드 안 312px → 560 캔버스가 0.56배).
> 태블릿 일이라 여기서는 건드리지 않았다. 고친다면 **캔버스를 480으로 좁히고 `width:100%`로
> 올려 쓰는** 쪽인데, 그러면 이름표 자리 규칙을 다시 재야 한다 → 「▶ 다음 차례」 5번 옆에 둔다.

#### 랜딩을 다시 세웠다 — 「하루」에서 「생각의 흐름」으로 (2026-08-13)

**주인공을 시스템에서 사람으로 옮겼다.** 목표가 「학습 시스템 홍보」가 아니라
**「수학강사 김하현이라는 개인을 브랜딩하는 것」**임이 확인됐기 때문이다.

> ### ⚠ 내가 지어낸 문장이 공개 페이지에 넉 달 가까이 떠 있었다
> 「수업은 두 시간이지만, 한 학생을 이해하는 데는 한 학기가 걸립니다」와
> 「공식을 외우는 수업이 아니라 왜 그 풀이가 성립하는지를 설명하는 수업을 지향합니다」는
> **사용자가 한 말이 아니다.** 시안(`landing-v2`) 작업 중에 «있을 법한 말»로 쓴 것이 그대로
> 실서비스에 실렸고, 그 뒤 내가 그것을 «사용자의 교육철학»으로 읽고 다음 판을 설계했다.
> 인물 사진 `alt`의 「김하현 **원장**」도 사실이 아니었다 — **학원에 소속된 개인 강사**다.
>
> **문장을 지어내면 그것이 다음 판의 전제가 된다.** 수치를 안 지어내는 규칙은 지켰는데
> «태도를 나타내는 문장»에는 같은 규칙을 적용하지 않고 있었다. → 9절 8번

**뼈대는 사용자가 직접 말한 원칙 셋이다.** 큰 문장은 그 말을 한 겹만 다듬은 것이고,
딸린 설명도 그 말이 담고 있는 범위를 넘지 않는다.

| | 원칙 | 사용자의 말 |
|---|---|---|
| 01 개념 | 개념은 외우는 것이 아니라, 원리를 이해하는 것입니다 | 「개념수업땐 개념의 원리를 정확히 이해하며 개념을 학습하고」 |
| 02 문제 | 풀이법을 알려주지 않습니다. 떠올리는 순서를 만들어 드립니다 | 「생각의 흐름을 잡아줘서, 스스로 문제의 발문으로부터 생각을 스스로 떠올려서 정답에 도달하는 흐름을 만들어주는 게 내 수업의 원칙」 |
| 03 반복 | 그 흐름이 몸에 붙을 때까지 따라갑니다 | 「좋은 교재나 컨텐츠로 수업의 질을 높이려 엄청나게 노력하는거고, 관리도 열심히하려 클리닉도 많이해」 |

- **셋이 흩어진 규칙 목록이 아니다** — 한 학생 안에서 순서대로 일어나는 일이라
  **척추 하나에 그대로 꿰인다.** 원칙 구간에서만 차콜 풀블리드를 쓴다 (페이지에서 한 번)
- **02가 심장이다.** 대부분의 수학 강사가 파는 것은 «유형별 풀이법»이라 이 원칙은 그 정반대고,
  **「무엇을 포기하면서까지 지키는가」가 들어 있는 유일한 원칙**이다 — 풀이법을 그냥 주면 진도가 빠르다
- **시스템은 원칙 03의 도구로 70% 지점에 선다.** 「AI가 이런 기능을 합니다」로 시작하면
  이 페이지의 순서가 통째로 뒤집힌다

#### 히어로에서 «말하지 않고 보여준다» — 항등식 한 문항

메가스터디 강사 페이지 넷을 봤는데 **방법론을 전부 «말로»만 한다**(「문제 풀이 3단계」·「3 in 1」).
이 사이트에는 KaTeX가 이미 있어 **실제로 시연할 수 있다.**

```
발문   임의의 실수 x에 대하여 등식이 성립할 때, f(3)의 값을 구하시오
01     「임의의 실수 x에 대하여 성립한다」 → 항등식이다
02     항등식에서 이어지는 길은 둘 — 수치대입 또는 계수비교
03 ★   계수를 비교하려면 식을 세울 수 있어야 한다. 이 문제는 아니다 → 계수비교 탈락
04     남은 길은 수치대입
05     구해야 하는 것이 f(3)이니 → x=3
06     정답에 도달한다
```
- **03이 이 페이지에서 제일 중요한 한 칸이다** — 답을 «고르는» 것이 아니라 아닌 것을 «지우는» 자리라
  메모 종이색으로 따로 세웠다. 이 수업의 핵심이 거기 있다
- **답은 끝까지 나오지 않는다.** 이 페이지가 파는 것은 답이 아니라 순서다
- 닫는 문장 — 「여기까지 오는 동안, 제가 알려 드린 풀이는 하나도 없습니다」
- 흐름은 **사용자가 수업에서 실제로 쓰는 순서를 그대로 옮긴 것**이다. 지어내지 않았다

#### 메가스터디 강사 페이지에서 가져온 것과 버린 것
사용자가 참고로 준 네 페이지(영어·국어·수학 프로모션)를 분석했다. 골격이 거의 같다 —
히어로(얼굴+캐치프레이즈) → 방법론 → 후기 → 교재 → 커리큘럼 → 이벤트.

| 가져온 것 | 버린 것 |
|---|---|
| **얼굴이 첫 화면에 크게 온다** — 전판에는 사람이 없었다(로고뿐). 개인 브랜딩에서 제일 큰 결함이었다 | **상품 판매 구조** — 가격표·패키지·할인·경품. 여기서 파는 것은 「나에게 배우는 것」 하나뿐이라 빈 껍데기가 된다 |
| **방법론에 이름과 단계를 붙인다** — 「생각의 흐름」 01~06 | **이미지 한 장으로 만든 페이지** — 저쪽은 텍스트가 거의 없어 검색에도 낭독기에도 안 걸린다. 이미 해결해 둔 것을 되돌리지 않는다 |
| **교재를 실물로 크게** — 전판은 교재를 한 번도 말하지 않았다 | **과장된 캐치프레이즈**(「마법을 부리듯 쉬워진다」류) |
| — | **커리큘럼 카드 나열** — 한 번 뺐던 것이고 이유(「아무 강사나 쓸 수 있는 소재」)는 지금도 유효하다 |

> **가장 큰 차이 — 저쪽은 «강의»를 팔고 여기는 «가르치는 방식»을 보인다.**
> 메가 페이지는 이미 그 강사를 아는 사람이 **결제하러** 오는 곳이라 방법론이 짧고 상품이 길다.
> 여기는 **모르는 사람에게 처음 알리는** 곳이라 비중이 반대여야 한다.

#### 교재 목업을 인쇄용 표지에서 다시 만들었다 (2026-08-13)

받은 `책목업.png`는 **AI가 만든 목업이라 작은 글씨가 전부 깨져 있었다** —
「기출은 **문석해, 재대요 준새았오**니다」·「UI헝 1듬글 새-쌍S…」.
큰 제목만 깨끗해서 처음엔 그 부분만 잘라 썼는데, 사용자가 **인쇄용 표지 PDF 세 장**을 줘서
**진짜 표지로 다시 만들었다.**

> **「짐작으로 가르치지 않습니다」를 내건 페이지에 깨진 글씨를 실을 수는 없다.**
> 원본이 있으면 목업을 «다시 만드는» 쪽이 «가리는» 쪽보다 언제나 싸다.

**그리고 곧 한 번 더 갈아엎었다** — 사용자가 목업 도구로 뽑은 **투명 PNG**를 올려 줘서
CSS로 책의 몸을 그리던 판을 **통째로 버렸다.** 지금 `make-book-mockup.html`이 하는 일은 **배치뿐**이다.

| 단계 | 무엇 | 도구 |
|---|---|---|
| ① 목업 | 사용자가 뽑는다 (`책표지/mockupea_export*.png`, **알파 있음**) | 외부 목업 도구 |
| ② 오려내기 | 책 상자 + 그림자 몫 여백만큼. **알파를 살려야 하므로 `Format32bppArgb` + `SourceCopy`** | System.Drawing → `tools/mock-*.png` |
| ③ 배치 | `--paper` 위에 세 권을 세운다 | `tools/make-book-mockup.html` |
| ④ 굽기 | 헤드리스 크롬 2배 → 1760×929 JPG (88KB) | `book-set.jpg` |

- ⚠ **바탕은 페이지와 «똑같은» `--paper` 단색이어야 한다.** 바닥 그라디언트를 깔았더니
  **이미지의 네 변이 사각 테두리로 드러났다.** 그림자는 목업 PNG에 이미 구워져 있다
- ⚠ **목업마다 «책이 이미지에서 차지하는 비율»이 다르다.** 같은 `width`를 주면 책 크기가 어긋난다.
  책 상자를 재서 **책 높이가 같아지도록 폭을 역산**했다 (셋 다 519px)
- ⚠ **책 상자를 잴 때 문턱을 절대값으로 두면 안 된다.** 이 PNG는 배경이 «투명(rgb 0,0,0)»이라
  「250보다 어두우면 내용」 같은 규칙은 이미지 전체를 잡는다. **네 귀퉁이 색을 배경으로 삼아
  거기서 벗어난 정도**로 재야 한다
- 인쇄용 PDF에서 앞표지를 뽑는 길은 그대로 남겼다 (`tools/cover-*.png`) —
  **저장소에 있는 유일한 표지 원본**이라 목업이 없는 책을 그릴 때 쓴다
- 교재가 여러 권이라 300px 칸에 넣으면 제목이 안 읽힌다 → 이 섹션만 **«넓은 사진 한 장 + 아래 3열»**이다

교재를 직접 만드는 이유는 사용자의 말 그대로다 — **흐름에 맞는 문제만 직접 선별** ·
**지역과 학교 특색** · **학생 수준**.

> **pdf.js를 헤드리스에서 돌릴 때 두 번 막혔다.**
> ① `.mjs`가 `serve.js`의 MIME 표에 없어 **모듈로 거절당했다**(「Failed to fetch dynamically
> imported module」). 한 줄 더했다.
> ② `--virtual-time-budget`은 **워커의 시간을 돌리지 않아** `getDocument()`에서 영영 멈춘다.
> **`window.Worker`를 지워** 가짜 워커(메인 스레드)로 돌리면 가상 시간이 그대로 먹는다.

#### 확장 없이 재는 길 — `tools/measure-landing.html`
브라우저 확장이 안 붙어 있어도 **헤드리스 크롬으로 진짜 뷰포트 기하를 잴 수 있다.**
실서비스를 지정한 크기의 iframe에 띄우고 `--dump-dom`으로 결과를 읽는다.

- 재는 것 여덟 — 문서 가로 넘침 · 텍스트 잘림 · 부모 침범 · **척추 x좌표** · **점 x좌표** ·
  **첫 화면에 이름/문장/사진이 다 보이는가** · KaTeX 렌더 · 섹션과 앵커
- ⚠ **`.l-rise`는 `opacity:0`이라 재기 전에 등장을 강제로 끝낸다.** 안 그러면 전부 0으로 나온다
- ⚠ **척추 x좌표를 따로 재는 이유** — 2026-08-07에 히어로 척추가 284px 어긋나 있었는데
  잘림·넘침 검사가 그걸 못 잡았다. **어긋난 것이지 넘친 것이 아니라서** 축을 따로 재야 한다
- `?y=N`을 붙이면 그 위치로 스크롤해 둔다 → `--screenshot`으로 구간별 확인
- **결과 — 여섯 폭(390·560·760·900·1180·1440) 전부 0건.** 문서 높이 데스크톱 7616 / 모바일 8959

> **눈으로 봐야 잡히는 것도 있었다.** 측정은 전부 통과했는데 스크린샷에서 셋이 걸렸다 —
> ① 원칙 눈금의 「원 칙 01 개 념」 자간 벌어짐(**mono에 한글이 없다 — 세 번째로 당했다**),
> ② 「지운다」 카드가 다른 줄보다 왼쪽으로 나가고 폭이 제한 없이 늘어난 것,
> ③ 교재 설명 첫 줄이 리드 문장과 **같은 말을 반복**하던 것.
> **숫자와 눈은 서로 다른 것을 잡는다.**

#### 시험 일정 — 노션 표를 옮겼다 (2026-08-13)

사용자가 시험기간마다 **노션에서 손으로 정리하던 표**를 사이트로 옮겼다.
열은 «학교 · 학년 · 시험유형 · 과목 · 날짜 · 시작 단원 · 끝 단원 · 메모»였다.

> ### 표를 새로 만들지 않았다 — **그 표가 이미 있는 「시험 범위」다**
> 「7. 여러가지 방정식 ~ 10. 행렬」은 `공통수학1`의 7~10번 단원을 켠 것과 **같은 값**이다.
> 새 표를 세우면 시험 범위가 두 벌이 되고, 이 저장소가 반복해서 겪은 사고
> (`signalOf`·`assistantClassIds`·`pbImageHTML`를 하나로 합친 그것)에 그대로 걸린다.
> → **없던 것 둘(날짜·교재)만 기존 화면에 더했다.** 「설정 > 시험 범위」가 **「시험 일정」**이 됐다.

| 노션 열 | 어디로 |
|---|---|
| 학교 · 학년 · 과목 | 이미 있다 — 줄은 **학생 명부에서 나온다**(학교를 따로 등록하지 않는다. 이름을 두 곳에서 관리하면 오타 하나로 «다른 학교»가 된다) |
| 시험유형 | 이미 있다 — 전역 시즌 |
| 시작 단원 ~ 끝 단원 | 이미 있다 — **「이 구간으로」 두 칸을 새로 달았다.** 노션의 입력 방식 그대로다 |
| **날짜** | `kv/exam-ranges`의 `dates` — **새로** |
| **교재 · 메모** | `kv/school-books` — **새로** |

- ⚠ **저장되는 곳을 일부러 갈랐다. 이게 이 기능의 핵심 판단이다.**
  **날짜**는 시험마다 새로 받는 값이라 `exam-ranges` 안에 두고 **시즌을 넘길 때 범위와 함께 비운다** —
  지난 시험 날짜가 남아 있으면 학생 홈에 **끝난 시험의 D-day**가 뜬다.
  **교재**(「미래엔」·「천재홍」)는 학교의 «고정 속성»에 가까워 매 시즌 다시 입력하게 하면 안 된다.
  그래서 **따로 둔다** — 시즌 초기화에 휩쓸리지 않는다
- ⚠ **날짜는 학교 × 학년 단위, 범위는 학교 × 학년 × 과목 단위다.**
  시험기간은 학교가 정하므로 그 학교 그 학년이면 과목이 무엇이든 같다.
  줄은 과목별로 서므로 **한 줄에서 날짜를 고치면 같은 학교·학년의 다른 과목 줄에도 반영된다** —
  화면이 그 사실을 미리 말해 준다
- **구간 선택은 거꾸로 골라도 받는다.** 사람이 순서를 바꿔 고르는 일이 잦아 막기보다 뒤집는 편이 낫다
- ⚠ **날짜·교재 칸은 `onchange`(칸을 떠날 때) 저장이다.** `oninput`으로 하면 `render()`가
  글자마다 돌아 커서가 끝으로 튄다 (9절 4번)

#### 강사도 «한눈에» — 목록을 표로 세웠다

처음엔 기존 화면 그대로 «펼치는 줄»의 목록이었는데, 그걸로는 **어디가 먼저인지가 안 보였다.**
노션 표가 표였던 이유가 그것이다. → `table.grid`로 세웠다.

```
학교 · 학년 · 과목 · 인원 · D-day · 시험 기간 · 범위 · 교재 | 펼치기
```
- **가까운 시험이 위로 온다.** 노션은 학교순이었지만 그건 «적는» 순서지 «보는» 순서가 아니다.
  날짜 없는 줄은 맨 아래로 내리고 거기서만 학교 → 학년 → 과목으로 세운다
- 머리말에 **「2주 안 n곳 · 날짜 정함 n/m」** — 세어 보지 않아도 알아야 한다
- 줄을 누르면 **그 자리 아래**가 펼쳐진다(`<tr><td colspan=9>`). 화면을 옮기지 않는다
- **시험 기간은 연도를 뗀다**(`08-17 ~ 08-22`). 두 날짜를 온전히 쓰면 이 폭에서 잘린다 —
  시즌마다 비우는 값이라 해가 헷갈릴 일이 없고, 온전한 날짜는 `title`과 펼친 칸에 있다
- ⚠ **`.cb`를 `<tr>`에 걸었다가 줄이 통째로 찌그러졌다.** 그건 «체크박스 네모»의 클래스라
  `width`·`height`가 박혀 있다. 행에는 인라인 `cursor:pointer`만 준다
- ⚠ **`.app .rst-wrap table.grid`의 `min-width`는 명단(1464px)의 것이다.** 이 표는 1108px라
  인라인 `style`로 눌러 준다 — 인라인이 시트 규칙을 이긴다
- ⚠ 펼친 칸 안의 입력·버튼은 전부 **`event.stopPropagation()`**을 단다.
  안 그러면 날짜를 고르는 순간 줄의 `onclick`이 같이 터져 칸이 접힌다
- sticky 헤더는 안 걸었다 — 줄이 적고, **가로 스크롤과 같이 쓰면 헤더가 첫 행을 덮는다**(0-B)

#### 날짜가 셋이다 — 시작 · 끝 · **수학 시험일**

시험기간은 닷새인데 **수학은 그중 하루**다. 이 사람에게 중요한 날은 그 하루다.
→ `dates`가 `{start, end, math}` 셋이 됐고, **D-day의 기준은 `math`**다(없으면 `start`로 물러난다).

- 표에 **「수학」 칸**이 따로 선다 — `08-19 수`처럼 요일까지. 이 화면에서 **브랜드 와인을 쓰는 곳은 여기뿐**이다
- 학생 홈도 바뀌었다 — 제목이 「**수학 시험**」이 되고 **수학 시험일이 크게**, 시험기간은 아래 작은 줄로 내려간다
- ⚠ 수학 시험일이 시험기간 «밖»이어도 **막지 않는다.** 보충 시험처럼 기간 밖에 치르는 경우가 실제로 있다.
  알려만 준다 (끝<시작은 되돌린다)

#### 달력 — 표가 못 하는 것을 한다

**표는 「어디가 먼저인가」에 답하고, 달력은 「이번 달이 어떻게 생겼나」에 답한다.**
시험기간이 서로 겹치는 학교가 몇인지는 표로는 안 보인다. 머리말에서 **표 / 달력**을 전환한다.

- 학생 마이페이지의 출석 달력(`.cal`)은 **ds.css의 «모바일 부품»이라 폰 크기**다. 강사 화면은
  칸이 훨씬 크고 안에 학교 딱지가 들어가므로 **따로 세웠다** (`exc-` 접두사)
- ⚠ **한 학교·학년이 과목 줄을 여럿 갖지만 날짜는 하나**다. 그대로 그리면 같은 딱지가
  과목 수만큼 겹친다 → **학교·학년으로 먼저 묶는다**
- **시험기간 첫날만 학교 이름을 온전히 쓰고 가운데 날은 「·」 띠**로 둔다 —
  닷새 내내 같은 이름이 반복되면 달력이 글자로 덮인다
- **수학 시험일은 기간 딱지에서 빼고 와인으로 따로 세운다** — 같은 학교가 한 칸에 두 번 나오면 안 된다
- 시작 달은 «오늘»이 아니라 **가장 이른 시험이 있는 달**이다. 오늘 달에서 시작하면 빈 달을 마주한다
- ⚠ **마지막 주의 남는 칸도 `.off`로 채운다.** 안 채우면 격자 배경(회색)이 통째로 드러난다
- 오늘은 칸을 칠하지 않고 **번호만 감싼다** — 칸을 칠하면 그날의 딱지가 안 읽힌다

#### 지난 시즌을 보관하고 되돌아본다

「시즌을 넘기면 범위·날짜를 비운다」는 판단은 그대로다(지난 시험 범위가 남아 있는 쪽이 더 위험하다).
다만 **비우기 전에 통째로 떠서 보관한다** — 작년 이맘때 그 학교 시험이 언제였고 범위가
어디까지였는지는 다음 해에 그대로 쓸모가 있다.

- `kv/exam-history` **문서 하나에 배열**로 쌓는다. 컬렉션을 늘리지 않는다 (이 저장소의 기존 판단)
- **스물넷(4년치)까지만** 남긴다
- ⚠ **교재 스냅숏을 같이 넣는다.** 교재는 시즌과 무관해 «지금 값»이 계속 살아 있지만,
  나중에 바뀌면 **지난 기록이 거짓이 된다**
- ⚠ 보관 이름에 **연도**가 들어간다 — 같은 시즌이 해마다 돌아오므로 「1학기 기말」만으로는 구분이 안 된다
- **적어 둔 것이 없으면 보관하지 않는다** — 빈 기록이 목록을 채우면 찾기만 어려워진다
- 화면 맨 위 고르개에서 「지금」과 지난 것들을 오간다. **지난 기록은 읽기 전용**이고
  표·달력 둘 다 그대로 볼 수 있다
- ⚠ **지난 기록의 줄은 «그때 적힌 것»에서 뽑는다** — 지금 명단에서 뽑으면 그동안 나간 학생의
  학교가 통째로 사라진다. 그래서 인원 칸은 「—」다 (그때의 인원은 남아 있지 않다)
- ⚠ 「지금이냐 지난 것이냐」를 **접근자 셋**(`getDates`·`getBook`·`getChaps`)에 가뒀다.
  표와 달력은 어느 쪽을 보고 있는지 몰라도 된다 — 이 갈림이 화면 곳곳에 흩어지면 반드시 한쪽만 고쳐진다
- 이걸 위해 `presetChapters(subject, seasonName)`에 **시즌을 넘길 수 있게** 했다

**D-day가 두 곳에 붙었다.**
- **학생 홈** — 「오늘 할 일」과 「다음 수업」 사이. `D-11 · 8월 24일(월) ~ 8월 30일(일) ·
  2학기 중간 범위 · 공통수학2 — 평면좌표 ~ 집합`. **끝난 시험은 안 그린다** —
  남겨 두면 학생이 «아직 안 끝났나» 하고 헷갈린다
- **명단** — 열을 새로 세우지 않고 **「학교 · 학년」 칸 안에** 붙였다. D-day는 그 둘의 값이기 때문이다.
  ⚠ 다만 124px에서는 **뱃지가 잘렸다**(`td{text-overflow:ellipsis}`). 170px로 넓히고
  표 최소폭 셋을 전부 +46 했다 (1418→1464 · 1214→1260 · 1004→1050)

#### 공지를 랜딩에서 뺐다 (2026-08-13) — 앵커까지 함께

**재원생에게 자주 내보내는 글**이라 브랜드 페이지의 흐름을 끊는다. 학생은 마이페이지에서 본다.
처음에는 앵커(`sec-notice`)만 남겨 두려 했는데 — **부르는 곳이 하나도 없었다.**

> 「구버전 로그인 헤더가 `scrollToId('sec-notice')`로 이걸 부른다」는 주석이 남아 있었지만,
> **그 헤더는 2026-08-08 이식 때 이미 사라졌다.** 지금 `scrollToId`를 부르는 곳은
> `lpScroll` 하나뿐이고 넘기는 값은 `lp-main`·`sec-contact` 둘뿐이다.
> **주석이 코드보다 오래 살아남아 «지우면 안 되는 것»처럼 보이게 만들었다** —
> 지우기 전에 실제 호출부를 grep으로 확인했다.

남은 앵커는 셋이다 — `sec-about`(사람) · `sec-review`(들은 말) · `sec-contact`(상담).
`measure-landing.html`의 필수 목록도 9섹션·앵커 3으로 같이 고쳤다.
문서 높이가 8020 → **7388px**로 줄었다.

#### `landing-v2/`는 이제 «따라 그릴 기준»이 아니다
시안은 「하루」 컨셉이고 실서비스는 「생각의 흐름」이라 **둘이 완전히 갈라졌다.**
시안을 고쳐 맞추지 않았다 — 기각된 컨셉의 기록으로 남긴다.
**다음에 랜딩을 손볼 때 `landing-v2/`를 베끼면 안 된다.**

#### 큐를 문항 따라 옮긴다 — 같은 사고를 세 번째로 막았다 (2026-08-13)

**「AI로 다시 생성」을 「공개됨」 큐에서 누르면 빈 화면을 마주했다.** 문항은 `pending`으로
내려가는데 **큐는 「공개됨」에 그대로 남는다.** 방금 만든 것을 검토하려면 큐를 손으로 찾아가야 했다.

같은 사고를 그림 업로드에서 이미 두 번 겪었고(`uploadVariantFigure` · `acceptFigureDraft`),
거기엔 `state.reviewQueue==='nofig'`를 보는 처리가 **같은 코드로 두 벌** 있었다.
세 번째가 나온 김에 **`followBankToQueue(b)` 하나로 모았다.**

```
지금 큐의 조건에 아직 걸리면 → 그대로 둔다
안 걸리면 → REVIEW_QUEUES 순서상 처음 걸리는 줄로 옮기고 그 문항을 고른다
```

- **가는 곳을 `status`로 계산하지 않는다.** 옛 코드는 `b.status==='review1' ? 'review1' : 'pending'`
  이라 큐 조건이 늘면 또 어긋난다. **큐의 조건 함수(`REVIEW_QUEUES[i][3]`)에 직접 물어본다** —
  줄을 하나 더 세우면 이 함수가 저절로 따라온다
- **REVIEW_QUEUES의 순서가 곧 「먼저 손봐야 하는 것」**이라, 첫 번째로 걸리는 줄이 갈 곳이다
- ⚠ **한 문항을 손보는 «버튼 핸들러»에서만 부른다.** 일괄 생성에서 부르면 스무 건을 도는 동안
  화면이 튀고, 애초에 어느 것을 따라갈지 정할 수 없다. 그래서 `applyTwinToBank`(둘이 공유하는
  자리)가 아니라 `regenerateOneWithAI` 쪽에 달았다
  - 일괄 생성이 끝난 뒤 「1차 검토 대기」로 보내는 것(`generateTwinsForExam`)은 그대로 둔다 —
    그건 «따라가기»가 아니라 **끝난 뒤 갈 곳을 정해 주는 것**이다
- **소스에서 두 조각을 떼어 여섯 경우로 확인했다** (배포·한도 안 씀) — 공개됨에서 다시 생성
  (그림 필요/아님) · 1차 검토 대기에서 다시 생성(그대로) · **그림 붙임 pending·review1**(옛 동작과
  같은 결과) · 공개됨에서 그림 갈아끼움(그대로). 큰 `<script>` 두 블록 문법 검사도 통과
- ⚠ **화면에서는 아직 못 봤다** — 브라우저 확장이 안 붙어 있었다. 다음에 검토 화면을 열면
  「공개됨」에서 R을 한 번 눌러 볼 것

#### ⚠ 기록을 지우는 두 가지 길을 막았다 (2026-08-12) — **실제로 한 번 지웠다**

**테스트학생의 출결·성적·영상 시청·오답숙제 15건이 날아갔다.** 백업이 없어 복구하지 못했다.
(테스트 계정이라 실제 학생 피해는 없었다. 사용자 확인 후 그대로 두기로 했다.)

**어쩌다 그랬나** — 학생 카드의 렌더 경로를 보려고 `DATA.records['test']`를 새 `emptyRecord()`로
갈아끼우고 학생 화면을 `render()` 했다. `saveRecord`는 부르지 않았다. 그런데—

> ### ⚠ **이 앱은 학생 화면을 «그리기만 해도» 저장한다**
> `stuCtx()` → `dqEnsureToday()`가 「오늘 뽑기」를 기록에 박으면서 `saveRecord()`를 부른다.
> 파일 어딘가에 「교사 화면은 이걸 부르지 않는다 — 남의 기록을 렌더 중에 저장하게 된다」고
> 적혀 있었는데, **그 말의 뒤집힌 뜻(학생 화면을 그리면 저장된다)을 놓쳤다.**
> «메모리만 바꾸고 화면만 보는 것»이 여기서는 성립하지 않는다.

**그리고 원래 있던 구멍이 하나 더 드러났다** — `dbGet(key, fallback)`이 타임아웃·네트워크·
HTTP 오류를 **전부 삼키고 fallback을 돌려준다.** `loadRecord`의 fallback이 `emptyRecord()`라
**읽기 한 번 실패하면 그 학생이 «기록 없는 학생»이 되고**, 위의 렌더-저장이 그 빈 것을
진짜 기록 위에 덮는다. 즉 **학생이 앱을 열기만 해도 자기 기록이 지워질 수 있었다.**

**문을 둘 달았다. 둘 다 «저장하는 쪽»에 있다** — 읽는 쪽은 실패를 숨길 수밖에 없고
(부르는 곳이 40군데다), 막아야 하는 것은 쓰기 하나다.

| 문 | 무엇 |
|---|---|
| `dbReadFailed` | **못 읽은 열쇠로는 `dbSet`이 쓰지 않는다.** 다시 읽어 성공하면 풀린다. ⚠ **404는 «실패»가 아니다** — 아직 없는 문서에 처음 쓰는 길까지 막으면 안 된다 |
| `recordLoaded` | **저장해도 되는 기록은 «읽어 온 그 객체» 하나뿐.** 다른 객체로 갈아끼운 뒤 저장하면 막힌다. 제자리에서 고치는 기존 경로는 객체가 그대로라 전부 통과한다 |

- 새 학생을 만드는 세 곳(`seedTestAccountIfEmpty` · `addStudent` · 엑셀 일괄 등록)은
  **`adoptRecord()`로 등록하고 나서** 저장한다 — 안 그러면 새 학생 기록이 안 만들어진다
- 막힐 때 **화면에 말해 준다** (`warnDbBlocked`). 열쇠마다 한 번만 — 조용히 실패하는 것이
  이 사고의 본질이었으므로 조용히 막기만 하면 절반만 고친 것이다
- **로컬에서 여섯 가지를 확인했다** — 정상 저장 통과 · **객체를 갈아끼우면 차단** ·
  읽기 실패 표시 · **그 열쇠로 저장 차단(PATCH가 아예 안 나감)** · 다시 읽어 성공하면 풀림 ·
  404로도 풀림

> **왜 «읽기 실패를 부르는 쪽에 알려 주기»로 안 갔나** — `dbGet`이 실패를 구분해 돌려주게 하면
> 부르는 곳 40군데를 전부 고쳐야 하고, 한 곳만 빠뜨려도 그 경로만 조용히 옛 동작으로 남는다.
> **위험한 것은 «쓰기»뿐이므로 쓰기 앞에 문을 세우는 쪽이 좁고 확실하다.**

#### ⚠ 문항 그림이 시험지 탭에서만 안 보였다 (2026-08-11)
그림 있는 시험지를 올리면 **검토 탭에는 뜨는데 시험지 탭에는 안 떴다.**

문항 그림은 **문항 «안»에 없다.** `exam.questions[]`에는 그림 필드가 없고,
`pbimg:{bankId}` 문서에 따로 있으며 **있다는 사실만 `bank.hasImage`가 안다.**
검토 탭·학생 화면 셋은 그 사실을 알고 늦게 불러왔는데, **`examDetailHTML`만 빠뜨렸다.**

같이 정리한 것 — 그림을 늦게 부르는 코드가 네 군데에 복제돼 있어 `pbImageHTML()` 하나로 모았다.
- ⚠ **부르는 함수 안에서 `render()`를 곧장 부르지 않는다.** 이 함수는 화면을 «그리는 도중»에 불린다.
  옛 `togglePbImage`는 그 자리에서 `render()`를 불러 **그리는 중에 또 그리고** 있었다
- 시험지 탭은 그림을 **스무 장씩** 부른다. 도착할 때마다 그리면 전체 재렌더링이 스무 번 돈다
  → 한 박자(60ms) 모아서 한 번만 그린다 (`pbImgArrived`)
- 검증 — 실데이터 시험지(그림 3문항)에서 세 장 다 뜨고, 문서 가로 넘침 0

#### 문제 본문을 «보이게» 그린다 — problemHTML() (2026-08-10, 세 번째)
내용은 평문으로 저장된다(HTML을 넣을 수 없다). 그 평문을 화면에 올릴 때 둘을 되살린다.

| 되살리는 것 | 왜 필요했나 |
|---|---|
| **표** | 파서가 셀을 공백으로 이어 붙여 대진표·조건표가 테두리도 칸도 없는 글 뭉치가 됐다 |
| **객관식 선지** | hwpx는 선지 사이를 탭으로 두는데 그 탭이 파싱에서 사라져 `① $4$② $12$③ $64$`로 붙었다 |

- 파서가 표 한 행을 **`| 칸 | 칸 |` 한 줄**로 적는다. 글로 봐도 읽히고 화면에서는 진짜 표가 된다
  (평문 저장이라 HTML을 넣을 수 없어서 이 표기를 쓴다). 표 안의 그림·미주는 표 밖으로 빼서 살린다
- 선지는 «내용 뒤에 바로 붙은» 기호 앞에서만 줄을 바꾼다 — 줄 맨 앞의 기호는 그대로 둔다
- ⚠ **수식은 여기서 건드리지 않는다.** `$…$`를 그대로 남기면 `render()` 끝의
  `renderMathInElement`가 알아서 그린다. 두 곳에서 손대면 어느 쪽이 그린 건지 알 수 없게 된다
- 검증 — 20문항·정답 누락 0 유지, 좌석 배치표가 3×3 격자로 보존됨

> ### 되살린 표를 «상자»와 «격자»로 갈랐다 (2026-08-10)
> 표를 되살려 놓고 보니 그리는 법이 틀려 있었다. 넷을 고쳤다.
> | 무엇이 잘못됐나 | 왜 |
> |---|---|
> | 첫 행만 색이 달랐다 | 첫 행이 «머리줄»인지 알 방법이 없다. 조건 (가)(나)(다) 상자의 첫 줄까지 칠해져 그 줄만 특별해 보였다 → **칠하지 않는다** |
> | 줄마다 선을 그었다 | 한 덩어리인 글이 표처럼 잘려 보인다 → **1열 상자는 바깥 테두리만** |
> | 가운데 정렬 | 문제 글은 왼쪽 정렬이다 |
> | 한 줄로 쭉 늘어남 | `white-space:nowrap`이라 긴 칸 하나가 가로로 뻗었다 (증명 상자 한 칸에 **361자**가 든 문항) → **폭에 맞춰 접는다** |
>
> `problemHTML`이 열 수를 보고 `.pb-tbl.box`(1열) / `.pb-tbl.grid`(2열 이상)를 붙인다.
> 상자는 `width:100%;table-layout:fixed`라 긴 글이 제 자리에서 접히고, 격자만 칸을 그린다.

> ### ⚠ 문항마다 표로 감싼 시험지가 한 줄로 뭉갰다 (2026-08-10, `test용hwpx.hwpx`)
> **한글에서 문항 하나를 표 한 칸에 넣는 양식**은 흔하다. 그런데 그 시험지를 올리니
> 선지까지 포함해 **문항이 통째로 한 줄**이 됐다. 원인이 셋이었고 전부 내가 만든 것이다.
>
> **① 미주 매칭을 점수 매긴 «뒤»에 하고 있었다** — 이게 핵심이다.
> 정답이 문제 칸 «밖»에 있는 양식이라 «표=문항» 해석은 정답을 하나도 못 붙여 0점이 됐고,
> 문항 전체가 한 셀에 든 «표=상자» 해석이 이겼다.
> → **보정까지 마친 «최종 모습»끼리 견줘야 한다.** 그래야 옳은 쪽이 뽑힌다.
> (동점이면 «표=문항»을 택한다 — 그쪽이 셀 안의 줄바꿈을 지킨다.)
>
> **② 셀 안의 `break`를 공백으로 바꾸고 있었다** — 줄바꿈이 아예 안 생겼다.
>
> **③ 셀에서 `|`를 그냥 지웠다** — 수식의 절댓값이 이미 `\left|…\right|`로 바뀌어 있어서
> 막대를 지우면 `\left `만 남아 **수식 전체가 안 그려진다.**
> → 수식 안에서는 `\vert`로 바꾸고 평문에서만 지운다.
>
> **표라고 다 «격자»가 아니다 — 셋으로 갈린다.**
> | | 무엇 | 어떻게 |
> |---|---|---|
> | 보기 표 | 칸 하나가 딱 `①`~`⑩` | **글로 편다.** 격자로 두면 10칸짜리 한 줄이 된다 |
> | 상자 | 열이 하나뿐 (조건 (가)(나)) | **줄마다 한 행**으로 편다 |
> | 진짜 격자 | 열이 둘 이상 | 칸을 지킨다. 줄바꿈만 공백으로 접는다 |
>
> 검증 — 두 파일 다 통과. `test용hwpx` 3문항·정답 3/3·수식 실패 0·선지 한 줄에 하나,
> 광남고 20문항·정답 누락 0·**수식 실패 0**(③ 때문에 1건 나던 것)·조건 상자 3행.

> ### ⚠ 표는 «파싱 시점», 선지는 «표시 시점»이다 — 옛 시험지에는 표가 안 살아난다
> 고친 뒤에도 표가 안 보인다는 말이 나왔다. 둘이 걸리는 시점이 다르기 때문이다.
> - **선지 줄바꿈** — `problemHTML()`이 그릴 때마다 넣으므로 **이미 올린 시험지에도 걸린다**
> - **표** — 파서가 `|` 표기로 «적어 둬야» 살아난다. 그 전에 올린 시험지는 본문이
>   이미 공백으로 뭉개진 채 저장돼 있어 **되살릴 근거가 없다**
>
> 원본 hwpx를 보관하지 않으므로(파싱된 글만 저장한다) **재업로드가 유일한 방법이다.**

> ### ⚠ `.cmp .q-body`에 `white-space`가 아예 없었다 (ds.css)
> 검토 화면에서는 **줄바꿈이 통째로 사라지고 있었다.** 선지가 다닥다닥 붙어 보이던
> 주된 원인이 파싱이 아니라 이 CSS 한 줄이었다. `pre-wrap`을 넣었다.
> **문제 본문을 담는 상자를 새로 만들 때마다 확인할 것** — 줄바꿈이 뜻을 갖는 글이다
> (보기가 줄마다 하나씩 서고 조건 (가)(나)도 그렇다).

> ### ⚠ 드라이브 그림은 «객체»다 — `imgSrcOf()`를 반드시 거친다
> 학생 화면에서 문항 그림이 안 떴다. `pbimg:{id}`에는 `{url, fileId}`가 들어 있는데
> 학생 쪽 두 곳(데일리퀴즈·오답숙제)이 그걸 그대로 `src`에 넣어 **`"[object Object]"`**가 됐다.
> 교사 검토 화면은 `imgSrcOf()`를 거치고 있어서 멀쩡했다 — **한 곳만 빠지면 그 화면만 조용히 깨진다.**
> 옛 기록에는 base64 문자열도 섞여 있어 두 형태를 다 받아야 한다.

#### 오답 파이프라인을 손봤다 (2026-08-10, 두 번째)
- **쌍둥이를 시험지 «그 자리»에서 한꺼번에 만든다.** 예전에는 등록 → 검토 탭으로 이동 →
  문항마다 「생성」을 눌러야 했다. 시험지 화면에 「쌍둥이 전부 생성 (N문항)」을 뒀다.
  - **오늘 한도까지만 만들고 남은 것은 손대지 않는다** — 내용이 빈 채로 남으므로
    다음날 다시 누르면 이어진다. 몇 개가 남는지 누르기 «전»에 말해 준다
  - **status를 건드리지 않는다.** 등록 때 `pending`이므로 내용이 채워지는 순간
    저절로 「1차 검토 대기」 큐로 옮겨 간다 (그 큐의 조건이 `status==='pending' && 내용 있음`)
  - 끝나면 **검토 화면의 «1차 검토 대기»로 데려간다** — 다음에 할 일이 그것이다
  - ⚠ 한 건에 13초씩 쉬어 가므로 20건이면 4분이 넘는다. **걸리는 시간을 미리 말해 준다**
  - `processAIQueue`(전체 일괄)와 같은 로직을 `runTwinBatch(목록, 라벨)`로 합쳤다
- **J/K를 뒤집었다** — J=이전 · K=다음. vi식(J=아래)이었는데 쓰는 사람 손에 안 맞았다.
  화면의 힌트도 「J 이전 K 다음」으로 함께 고쳤다 (한쪽만 고치면 화면이 거짓말을 한다)
- **시험지의 「문항 내용」을 좌우 2열로 바꿨다** (`.exq-*`). 원본과 변형을 세로로 이어 붙이니
  문항 경계가 안 보이고 글만 쭉 나왔다. **«이 변형이 저 원본의 쌍둥이가 맞나»를 보려면
  둘이 나란해야 한다** — 오답 검토 화면을 2열로 만든 것과 같은 이유다.
  900px 아래에서는 위아래로 접힌다. 문항별 오답률 줄도 과목 대신 **단원**을 보여 준다

#### 시험지 등록 화면 셋 (2026-08-10)
- **문항마다 «그려진 판»을 뒀다.** `textarea`·`input` 안의 `$…$`는 KaTeX가 건드리지 않는다
  (입력칸이기 때문이다). 단원을 판단하려면 수식이 보여야 해서 옆에 미리보기를 붙였다.
  ⚠ **글자마다 `render()`를 부르면 안 된다** — 전체 재렌더링이라 커서가 튀고 한글 조합이 깨진다.
  `updateEqPreview(i)`가 그 판 하나만 직접 그린다 (수업 메모·학생 메모와 같은 판단)
- **「문항 전체 삭제」**를 넣었다. hwpx를 잘못 올렸을 때 22개를 하나씩 지우면 다시 올리는 것보다 오래 걸린다
- **「단원 자동 채우기」**와 **「전부 한 단원으로 지정」** 둘 다 둔다 — 섞인 시험지와 한 단원짜리가 다 있다

> ### ⚠ 인라인 `onclick`에 사람이 친 값을 넣을 때 — 제어문자가 키를 깨뜨린다
> 설정 > 시험 범위의 「펼치기」가 안 먹었다. 키를 `학교 + \u0000 + 과목`으로 만들어
> `onclick` 속성에 넣었는데 **HTML 파서가 NUL을 U+FFFD로 바꿔** 키가 영영 안 맞았다.
> → `jsAttr()`로 «먼저 JS 이스케이프, 그다음 HTML 속성 이스케이프» 순서로 감싸고
> 제어문자는 지운다. 구분자도 `\n`으로 바꿨다.
>
> **곁들여 배운 것** — 소스에 진짜 제어문자가 들어가면 `grep`이 파일을 **binary로 보고 침묵한다.**
> 정규식은 `\u0000` 같은 **이스케이프 표기로** 쓸 것.
> (PowerShell `WriteAllLines`로 고치다 줄바꿈이 통째로 CRLF가 되어 diff가 10288줄이 된 일도 있었다.
> 이 저장소 작업본은 **LF**다.)

> **`unit`은 «과목»이지 «단원»이 아니다.** 시험지 문항의 `unit`에는 `UNITS`(과목 7개)가 들어가는데
> 화면에는 「단원」이라고 적혀 있었다. 진짜 단원(`UNIT_CHAPTERS`)을 새로 들이면서
> 한 폼에 같은 이름이 둘이 되므로 hwpx 쪽 라벨을 「이 파일의 과목」으로 고쳤다.

> **구분자에 `<s>`를 쓰면 안 되는 자리가 있다.** `text-decoration:none`을 지우는 규칙이
> `table.grid .nm s`에만 있어서, `td.sub` 안에서는 취소선이 그대로 그려져
> 「광남고 ~6단원」처럼 보였다. `.sap-range s`처럼 규칙을 같이 두거나 `<span>`을 쓴다.

> **mono 안의 한글이 또 둘 나왔다** — 「5/10단원」·「2026-03-02부터」. 숫자만 mono로 감쌌다.
> 이 함정은 새 화면을 만들 때마다 나온다. **숫자와 한글을 한 `.mono`에 넣지 말 것.**

#### 시험지·문항을 전부 지우고 새로 시작했다 (2026-08-09)
「못 치는 답」 문제를 `dqAnswerable()`로 막았으니 그동안 쌓인 것은 정리하기로 했다.
지운 것 — **시험지 1개**(복습테스트) · **문항 창고 9개** · **학생 오답숙제 15개**와 `dailyQuiz` 상태.
성적·출결·과제·영상·공지·질문·클리닉은 **그대로 뒀다**.

> ### ⚠ 컬렉션만 비우면 **구버전 blob에서 되살아난다**
> 지우고 새로고침했더니 **시험지 1개와 문항 7개가 그대로 다시 있었다.**
> `migrateBlobToCollection()`은 «컬렉션이 비어 있으면» 옛 `kv/{oldKey}` blob에서 다시 이관하는데,
> 그 blob 문서를 **일부러 영구 보존한다** (이관하다 실패해도 원본이 안 사라지도록 · `index.html:1403-1410`).
> 그래서 컬렉션을 비우는 순간 **되살리기 조건이 켜진다.**
> - 되살아난 7개는 **9개의 옛 스냅샷**이었다 — 개수가 안 맞아서 되살아난 것을 알아챘다
> - `kv/exams`·`kv/problem-bank`까지 지우고 나서야 0으로 유지됐다
> - **어떤 컬렉션이든 통째로 비울 때는 같은 이름의 blob도 같이 지워야 한다**
>   (`students`·`classes`·`notices`·`consults`·`quizzes`·`clinics`·`clinic-slots`·`qnas`·
>   `videos`·`assistants`·`exams`·`problem-bank`·`assignments`)
> - 반대로 **한 건씩 지우는 것은 안전하다** — 컬렉션이 비지 않으면 이관 조건이 안 켜진다.
>   지금까지 이 함정을 못 만난 이유가 그것이다

> **`.qz-sh .r`에서 mono를 걷어냈다** — 담기는 글이 「재원 3명 기준」이라 한글이
> 「재 원 기 준」으로 벌어져 있었다 (2026-08-08에 아홉 곳을 고친 그 문제가 한 곳 더 있었다).
> **`.qz-subs`의 중단점도 1080 → 1000으로 낮췄다.** 330px 고정이던 칸에 «수식이 든 문제 전문»이
> 들어오면서 `flex:1;min-width:360px`로 바꿨는데, 1080 화면(실제로 쓰는 크기다)이
> 이유 없이 접히고 있었다. 두 단이 서려면 742px이면 되고 그 화면의 본문은 786px이다.

#### 영상 — 「시청률」이 아니라 「이탈 지점」
시안이 못박은 역할을 그대로 따랐다. 몇 %가 봤나가 아니라 **어디서 멈췄나**가 다음 수업을 바꾼다.

**이 히스토그램은 지어낸 것이 아니다.** `videoProgress[영상id] = {completed, percent}`가
학생마다 «어디까지 보고 멈췄는지»를 이미 담고 있다. 그걸 10구간 + 「완주」 칸으로 묶은 것이 이탈 분포다.
(시청 «시점»의 시계열은 없다. 있는 것은 최종 도달 지점 하나라 그 분포까지가 한계다.)

- 동점이면 **가장 이른 구간 하나만** 빨갛게 칠한다 — 넷이 동시에 빨개지면 신호가 아니라 소음이다.
  수업이 학생을 놓치기 시작한 지점이 제일 먼저 손볼 곳이기도 하다. 나머지 인원은 tooltip에 있다
- 「완주」 칸은 이탈이 아니므로 최고점 계산에서 뺐다. 색도 초록이다
- **영상 재생(iframe)은 뺐다.** 강사가 자기 영상을 이 화면에서 볼 이유가 없고,
  목록 수만큼 iframe을 띄우면 그만큼 유튜브를 부른다. 학생 화면(`videoEmbedHTML`)은 그대로다

지운 것 — `videoByClassHTML` · `state.videoByClassId` · `state.videoStudentDetailId`.
시청 현황 표는 이탈 히스토그램과 학생별 진행 목록이 흡수했다.

> 시안은 값 라벨을 `left/right:-14px`로 막대보다 넓게 잡았는데, 실서비스에서는 그만큼
> `.vd-bars`의 `scrollWidth`가 커져 **가로 넘침으로 잡힌다.** 막대 폭이 충분하므로 제 박스 안에 뒀다.

> ⚠ **PowerShell로 index.html을 고치지 말 것.** `Get-Content`가 기본 인코딩으로 읽어
> 한글이 통째로 깨진다 (2026-08-07에 한 번 겪었다 — `git checkout`으로 되돌리고 다시 했다).
> 파일 편집은 Read/Edit/Write 도구로만 한다.

### 오답 검토 이식 (2026-08-07)
`teacherReviewHTML()` 전면 재작성. A4에서 흡수한 것 셋 — **큐 · 2열 비교 · 키보드**. 다크 테마는 버렸다.

예전 화면은 카드를 세로로 나열했다. 한 건을 판단하려면 원본과 쌍둥이를 나란히 봐야 하는데
카드가 길어 스크롤하는 동안 원본이 화면 밖으로 나갔다. 2열로 고정하면 그 왕복이 사라진다.

**큐는 시안과 다르다.** 시안은 「재생성 요청 · 난도 확인 필요 · 보류함」을 그렸는데 도메인에 그런 상태가 없다.
실제 상태는 `pending → review1 → approved` 셋뿐이다. 실제로 갈라지는 축으로 다시 짰다.

| 큐 | 뜻 |
|---|---|
| 내용 없음 | `variantContent`/`variantAnswer`가 비어 있다 — AI로 채우는 것이 첫 일 |
| 1차 검토 대기 | 내용은 있고 아직 `pending` |
| 2차 검토 대기 | `review1` |
| **답 형태 문제** | `answerFormProblem()`이 걸린 것 — 학생이 제출할 수 없다 |
| 공개됨 | `approved` |

- 「보류」는 넣지 않았다. 상태를 만들려면 필드를 더해야 하는데, `pending`에 머무는 것이
  이미 «아직 안 봤다»와 같은 뜻이라 층을 하나 더 둘 이유가 없다
- 키보드 — `J`/`K` 이동, `E` 다음 단계, `X` 되돌리기, `R` 재생성.
  **입력칸에 커서가 있으면 아무 것도 하지 않는다** (문항을 고치다 `J`를 치면 다음 문항으로 튄다)
- 승인·반려 뒤에는 **다음 것으로 넘어간다.** 큐를 비우는 화면이라 손이 멈추면 안 된다.
  지금 항목이 큐에서 빠지므로 빠지기 «전»에 다음 대상을 정해 둔다
- 「학생이 답을 제출할 수 없는 형태입니다」 경고가 `.legacy`에서 `.app .rv-warn`으로 올라왔다.
  2차 승인 버튼도 그 조건이면 비활성이다

지운 것 — `bankCardHTML` · `.legacy .bank-warn` · `state.reviewShowAll`/`reviewFocusMode`/`reviewFocusIndex`.

> **⚠ 플렉스 항목에 `min-height:0`을 빠뜨리면 안에 든 KaTeX가 상자를 밀어낸다.**
> `min-height`의 기본값이 `auto`(=내용 크기)라, 화면 높이는 고정인데 문서가 50px 세로로 스크롤됐다.
> 수식을 감추면 넘침이 0이 되는 것으로 원인을 잡았다. `.rv-work`·`.rv-list`에 `min-height:0`을 넣어 해결.
> **세로로 쌓는 플렉스 칸을 만들 때마다 확인할 것.**

### 학생 상세 이식 (2026-08-07)
**디자인 시스템의 기준 화면.** 이 시스템 전체가 그 사상 위에 있다 —
**탭을 없애고 하나의 연대기로 합친다.** 출결·성적·과제·오답·퀴즈·질문·클리닉이
종류를 가리지 않고 날짜 순으로 한 줄기에 쌓인다.
「이 학생에게 무슨 일이 있었나」는 원래 시간 순 질문이지, 탭을 일곱 번 눌러 맞춰 볼 질문이 아니다.

3열 — 좌: 같은 반 학생 목록(명단에서 들어와도 맥락을 잃지 않는다) /
중: 머리말 + KPI 5 + 신호 + 연대기 / 우: 연락처 · 반·진도 · 빠른 작업 · 출결 8회.

#### 탭을 없애면서 잃을 뻔한 것 — 입력
예전 7개 탭에는 **성적 추가/삭제 · 출결 추가/삭제 · 과제 완료 토글 · 기본정보 수정 ·
학생 삭제 · 개인 영상 삭제 · 채팅**이 들어 있었다. 전부 살렸다.

| | 어디로 |
|---|---|
| 성적·출결 추가 | 연대기 갈래를 고르면 그 갈래의 **입력 폼이 연대기 위에 붙는다** |
| 성적·출결 삭제 | 연대기 항목마다 삭제 버튼. **배열 인덱스를 사건에 실어 보낸다** — 연대기는 날짜순으로 다시 세우므로 |
| 과제 완료 토글 | 「과제·오답」 갈래의 폼 |
| 기본정보 · 학생 삭제 · 개인 영상 | 우측 「빠른 작업」의 **기본정보 모드** |
| 채팅 | 우측 「빠른 작업」의 **채팅 모드** (말풍선이라 연대기와 성격이 다르다) |

- 신호는 별도 보드를 만들지 않고 **근거 문장으로 화면 안에 앉혔다** (A5에서 흡수).
  「7일 해제」도 여기서 된다 — 신호 화면과 같은 `signalMutes`를 본다
- `rosterOpen(sid, tab)`이 쓰던 옛 탭 이름은 `ROSTER_OPEN_MAP`으로 **«어느 갈래로 열 것인가»**가 됐다.
  명단·신호·수업에서 「출결」·「성적」·「오답」·「연락」을 누르면 그 갈래가 열린 채로 들어온다
- **조교는 구버전 탭 화면을 그대로 쓴다** (`teacherStudentDetailLegacyHTML`).
  권한이 달라 DS 화면은 기본정보·삭제까지 열어 두기 때문이다. 그 함수가 스스로 `.legacy`를 두른다

> **`teacherHTML()`의 `onDs` 예외를 지웠다.** 「`studentDetailId`가 있으면 DS 취급을 푼다」는
> 학생 상세가 구버전이던 동안의 임시 조건이었다.

#### 연대기의 레일과 점을 되살렸다 (2026-08-08) — 그리고 거기서 나온 버그 둘
사용자가 «시안(03-student)의 기록과 많이 다르다»고 물어서 대조했다. 시안은 ds.css의
`.tl`/`.day`/`.ev` 부품이라 **세로 레일 위에 색 점**이 찍히고 날짜가 레일 왼쪽 108px 밖에 선다.
실서비스는 `.sd-day`/`.sd-ev`를 새로 만들면서 그 둘이 빠져 있었다.

| 시안과 다른 것 | 되살렸나 | 왜 |
|---|---|---|
| 세로 레일 + 색 점 | **되살렸다** | 빠질 이유가 없었다. «한 줄기»라는 인상이 여기서 나온다 |
| 날짜가 레일 왼쪽 108px 밖 | 아니다 | 그 여백은 1440 고정 캔버스의 장치다. 스크롤하는 목록에서는 **sticky 머리줄**이 «지금 보고 있는 날»을 위에 붙여 두는 쪽이 낫다 |
| 종류가 뱃지 | 아니다 | 실데이터에는 **상태**가 따로 있다(출석/지각/결석 · 완료/미완료 · 정답/오답 · 승인/취소). 둘 다 뱃지면 한 줄에 뱃지가 둘 서서 어느 쪽이 상태인지 흐려진다 → 종류는 왼쪽 52px 글자 열 |
| 항목마다 삭제 버튼 | 실서비스에만 있다 | 탭을 없애면서 성적·출결 삭제가 갈 곳이 연대기밖에 없다 |

- **레일은 `.sd-ev`가 한 줄씩 제 몫을 그린다** (`::after`). 스크롤 상자에 `position:absolute`로
  한 줄을 세우면 그 줄이 «보이는 높이»만큼만 생겨 첫 화면을 지나면 끊긴다
- **점 색은 넷뿐이다** — 출결(출석 초록 / 지각·조퇴 노랑 / 결석 빨강) · 성적 파랑 · 클리닉 강조색.
  과제·오답·퀴즈·질문은 회색이다. 전부 칠하면 한 화면이 알록달록해져 아무것도 눈에 안 띈다.
  그 항목들의 상태는 오른쪽 뱃지가 이미 말한다
- **「퇴원」은 칠하지 않는다** — 빨간 점은 «결석했다»로 읽히는데 퇴원은 그런 사건이 아니다

> ### ⚠ 여섯 번째 클래스 이름 사고 — 이번엔 ds.css가 아니라 «이 파일 안»이었다
> 연대기 한 줄의 본문이 `<div class="body">`였다. 앱 셸의 **`.app .body{padding:20px}`**가
> 그대로 걸려 줄마다 20px이 붙었다 — 종류와 제목이 세로로 어긋나 보이고
> **행 높이가 47px이어야 할 자리에 83px**(1.6배)이었다. 이식(`0caff13`) 때부터 그랬다.
> → `.sd-ev .tx`로 갈랐다. `class="body"`를 쓰는 곳은 이제 앱 셸 셋뿐이다.
>
> **`.ph`(다섯 번째)와 다른 점은 부딪힌 상대다.** ds.css만 `grep`해서는 안 잡힌다 —
> `index.html` 자신의 `.app` 블록도 같이 봐야 한다.

> **`.mono`에 한글이 또 있었다** — 우측 「수업」의 `월,수,금 7시~9시`가 `<b class="m">`이라
> 「월 , 수 , 금」처럼 벌어져 있었다. mono를 걷어냈다. **시안(03-student)도 같이 고쳤다** —
> 시안이 「따라 그릴 기준」이라 틀린 채로 두면 다음에 그걸 또 베낀다.

#### 고정 메모를 만들었다 · 빠른 작업을 손봤다 (2026-08-08)
시안 우측의 「고정 메모」가 이식 때 빠져 있었다 — 도메인에 학생 메모가 없었기 때문이다.
**수업 메모와 같은 방식으로 만들었다**: 컬렉션을 늘리지 않고 `kv/studentnote:{학생id}` 문서 하나에
`{text, at}`. 이걸로 «시안에 있으나 못 만드는 것» 목록에서 메모가 빠진다.

- **날짜별로 쌓지 않고 «한 덩어리»다.** 연대기가 이미 «언제 무슨 일이 있었나»를 답한다.
  여기 필요한 것은 그 위에 얹히지 않는 «늘 참인 한 가지»다 (형제 학원 · 통화 요지 · 주의사항).
  그래서 날짜를 손으로 적지 않는다 — **마지막 저장 시각만 자동으로 남는다**
- **초안은 학생별로 담는다**(`state.sdNoteDrafts[sid]`). 옆 학생을 봤다 돌아와도 쓰던 글이 남는다.
  글자마다 `render()`를 부르지 않으므로 커서도 한글 조합도 안전하다 (조합 중 render 0회 확인)
- **변경 이력에 «무엇을 적었는지»는 남기지 않는다** — 메모에는 사적인 말이 들어가고
  변경 이력은 조교도 본다. 「학생 메모 저장」·「학생 메모 지움」과 학생 이름까지만 남는다
- **조교에게는 없다.** 우측 칸이 통째로 없는 화면이라 메모도 함께 빠진다
- 비우고 저장하면 지워진다. 모드를 따로 만들지 않았다 — 「수정」 버튼을 눌러야 쓸 수 있는 칸은
  결국 아무도 안 쓴다

**빠른 작업** — 시안의 넷 중 실제로 만들 수 있는 것은 하나뿐이었다.

| 시안 | 지금 | 왜 |
|---|---|---|
| 클리닉 배정 | **「클리닉 배정」으로 고쳤다** | 이식 당시엔 배정하는 문이 없었는데 2026-08-07에 「학생 직접 배정」이 생겼다. **폼을 복제하지 않고** 클리닉 화면으로 보내되 그 학생을 미리 골라 둔다 (`state.clinicAssignSid`, 한 번 쓰고 지운다) |
| 과제 추가 | 없다 | 과제는 **반 단위로만** 배포된다 — 「이 학생에게」가 데이터 구조상 성립하지 않는다 |
| 보충영상 배정 | 없다 | 개인 영상은 **결석을 찍을 때 링크를 같이 넣으면 자동 등록**된다. 여기 버튼을 두면 등록 경로가 두 벌이 된다 |
| 학부모 연락 | 없다 | 바로 위 「연락처」에 번호가 있고 웹에서 더 할 수 있는 일이 없다 |
| — | 채팅 · 기본정보 | **탭을 없애면서 갈 곳이 없어진 것들**이다 (기본정보 수정 · 학생 삭제 · 채팅) |

### 소통 4종 이식 (2026-08-07)
**네 화면의 형태를 통일하지 않았다.** 통일해야 하는 것은 색·서체·간격·버튼이지 레이아웃이 아니다 —
글의 성격이 다르면 형태는 달라야 하고, 억지로 맞추는 것은 일관성이 아니라 게으름이다.

| | 형태 | 왜 |
|---|---|---|
| 클리닉 (`09`) | 주간 판 + 승인 대기 패널 | 시간이 자원이라 «어디가 비었나»가 보여야 한다 |
| 공지 (`10`) | 폼 + 목록 | 가장 단순하다. 쓰는 곳이 위, 읽는 곳이 아래 |
| 질의응답 (`11`) | 세로 스택 | 질문 하나가 한 덩어리이고 답이 그 아래 «붙는 글»이다 |
| 채팅 (`12`) | 좌 학생 목록 + 말풍선 | 이건 «주고받는 말»이다 |

- **클리닉 — 시안의 시간축 격자는 만들 수 없다.** 슬롯의 `time`이 「저녁 7시」 같은 자유 텍스트라
  시각으로 정렬·배치할 수 없다. 세로축(시각) 대신 **날짜별 열 안에 그날 슬롯을 쌓았다.**
  「어디가 비었나」는 그대로 읽힌다. 승인 대기는 **신호 있는 학생을 위로** 올린다
- **질의응답 — 미답변이 기본 갈래다.** 이 화면에 오는 이유가 그것이다
- **채팅 — 학생 드롭다운을 목록으로 바꿨다.** 누구와 이야기 중인지, 다음에 누구에게 가야 하는지가
  화면에 남는다. `studentSelectorHTML`은 그래서 지웠다

지운 것 — 구버전 클리닉 화면(시간대 표 + 신청 목록) · `studentSelectorHTML`.

#### 클리닉 신청 경로를 셋으로 늘렸다 (2026-08-07)
원래 있던 것은 **①교사가 시간대를 열고 학생이 고른다** 하나뿐이었다 (정원 마감·중복 방지 포함).
여기에 둘을 더했다. **컬렉션은 늘리지 않았다** — `clinic` 레코드에 `day`·`time`이 이미 있고,
`slotId`를 비우면 «슬롯 밖»이 된다.

| | 누가 | `slotId` | 시작 상태 |
|---|---|---|---|
| ① 열린 시간대에서 고르기 | 학생 | 있음 (정원에 반영) | 대기 |
| ② **희망 시간 직접 제안** | 학생 | 없음 | 대기 — 교사 승인이 있어야 확정 |
| ③ **교사 직접 배정** | 교사 | 고르면 있음 / 아니면 없음 | **승인** (교사가 넣은 것이므로) |

- 열린 시간대가 하나도 없으면 학생 화면은 **②로 시작한다** — 빈손으로 돌려보내지 않는다
- ②는 지난 날짜·같은 날짜+시간 중복을 막는다
- **슬롯 없이 잡힌 것도 주간 판에 세운다** (점선 · 「시간대 밖」).
  판에 안 보이면 교사가 이미 잡아 둔 시간을 모르고 이중으로 앉힌다
- 승인 대기 패널에서 ②는 「직접 제안」 뱃지로 구분된다

> **승인된 클리닉은 슬롯을 갖는다.** ②를 승인하면 그 자리에 **정원 1짜리 시간대를 만들어 붙인다**
> (`ensureClinicSlot`). 안 그러면 다른 학생이 같은 시간에 또 제안할 수 있고, 주간 판에서도
> 점선으로만 떠서 «자리가 잡혔다»고 읽히지 않는다. ③의 직접 입력도 같은 규칙을 따른다.
> - 이미 있는 시간대가 꽉 찼는데 교사가 승인하면 **정원을 한 자리 늘리고 그 사실을 말해 준다** —
>   조용히 넘기면 «승인했는데 왜 안 들어가 있지»가 된다
> - 학생이 이미 찬 시간을 또 제안하면 막는다
> - 취소하면 그 자리는 다시 열린다
> - ⚠ `time`이 자유 텍스트라 **「저녁 7시」와 「저녁7시」는 다른 시간으로 본다.**
>   형식을 강제하는 곳이 없어 정규화하려 들면 오히려 엉뚱한 것을 합친다

### 시험지 이식 (2026-08-07)
역할은 «어느 문항이 무너졌는지 본다» — 오답 파이프라인의 입구다.

**순서가 거꾸로였다.** 예전 화면은 «등록 폼»이 위에 길게 깔리고 시험지 목록이 그 아래 있었다.
등록은 가끔 하고 «보는» 일은 매번 하는데. → 목록 + 분석을 앞에 세우고 등록은 「새 시험지」 모드로 넣었다.

**문항별 오답률은 지어낸 수치가 아니다.** 학생 기록의 `wrongHomework`에 `{examId, questionNo}`가
그대로 있어서, 그 문항을 오답으로 올린 학생 수를 세면 된다. 분모는 그 반의 재원 수다.
25% 이상이면 노랑, 50% 이상이면 빨강.

머리말에 **그 시험지로 만들어진 쌍둥이 문항의 검토 단계**를 함께 띄운다 — 파이프라인의 다음 칸이다.

등록 폼(hwpx 파싱 · 이미지 업로드 · 동적 문항 행)은 **손대지 않고 `.legacy`로 감쌌다** —
이 화면에서 제일 비싼 부분이고, 형태를 바꾼다고 나아지는 곳도 아니다.
저장하면 등록 모드에서 나와 방금 만든 시험지를 보여 준다.

> ### ⚠ 스크롤 상자는 반드시 `position:relative`여야 한다
> KaTeX는 수식마다 `position:absolute`인 `.katex-mathml`을 넣는다. 조상 중에 위치 지정된 것이
> 없으면 그 요소의 기준이 **문서**가 되어 `overflow:auto`가 잡아 주지 못한다.
> 시험지 화면에서 **문서가 3736px 세로로 늘어났다** — 수식이 상자 «밖»에 있는 것으로 계산된다.
> `position:relative` 한 줄로 0이 됐다.
> **세로로 스크롤하는 상자를 새로 만들 때마다 이 줄을 같이 넣을 것.**
> 지금까지 만든 스크롤 상자 17개에 한꺼번에 걸어 뒀다 (`.app .rst-wrap, .sig-list, …`).

### 창고 · 설정 이식 (2026-08-07) — **관리자 16개 완료**

**보충문제 창고 (`08`)** — 학생이 어떤 단원을 틀리면 같은 단원의 문항이 자동으로 검색되어
오답숙제로 나간다. 그래서 **단원이 1차 축**이고, 좌측에 단원 목록을 세웠다.
시험지와 같은 문제가 있었다 — 「추가하는 방법 세 가지」(직접·hwpx·엑셀)가 위에 길게 깔리고
창고 목록이 맨 아래였다. **쌓는 일은 가끔, 보는 일은 매번이다.** 추가는 「문항 추가」 모드로 접었다.
업로드 미리보기가 떠 있으면 그것을 먼저 보여 준다 — 확인해서 등록해야 다음으로 갈 수 있다.

**설정 (`13`)** — 여섯 갈래가 서로 닮은 데가 없다 (조교 계정 · 학생 계정 · 반 만들기 ·
상담 신청 · 변경 이력 · 관리자 비밀번호). 하나의 형태로 묶을 이유가 없어 **껍데기만 DS로 바꿨다.**
갈래를 왼쪽에 세우면 여섯 개가 한눈에 보이고 지금 어디에 있는지가 남는다.
각 갈래의 본문은 폼과 표가 전부라 형태를 바꿔도 나아지지 않으므로 `.legacy` 그대로다.

### 「제출 현황」을 시험지에 흡수 (2026-08-07)
시안 07의 우측 칸이 그 자리였다. 따로 있던 화면은 **모든 시험지 × 모든 학생을 한 표에** 담았는데,
그렇게 보면 어느 시험지 이야기인지가 흐려진다. 시험지를 고른 채로 그 시험지의 제출을 본다.

- 덜 낸 학생이 위로 온다 — 이 칸을 보는 이유가 그것이다
- 「문항별」을 누르면 문항마다 낸 답과 정오가 펼쳐진다
- **«이 학생이 전체적으로 얼마나 밀렸나»는 명단의 오답 열과 학생 상세 연대기가 답한다** —
  그래서 «모든 시험지» 표가 없어져도 잃는 것이 없다

`TEACHER_SUBNAV['오답']`에서 서브탭이 하나 빠졌고 `teacherSubmissionsHTML`은 지웠다.
예전 세션이 `submissions` 탭을 들고 있으면 `teacherTabBodyHTML`이 시험지로 보낸다.

> **이로써 `.legacy`로 남은 관리자 «화면»은 없다.** 남은 legacy는 DS 화면 «안»에 감싸 둔
> 폼과 표뿐이다 (시험지 등록 · 창고 추가 · 설정 6갈래 · 수업 기록 상세 ·
> 학생 질의응답의 사진 첨부칸). ※ 「조교용 학생 화면」은 2026-08-08에 통째로 지웠다 — 아래.

### 학생 마이페이지 이식 — 완료 (2026-08-07) · **탭 11 → 4**
`studentHTML()` 전면 재작성. 화면 11개는 `stuXxxHTML()` 열한 함수로 갈라졌고
`.app.sap` 셸이 그 위에 앉는다. 검증 — 11화면 전부 텍스트 잘림 0 · 부모 침범 0 ·
열 가로 넘침 0 · 콘솔 오류 0 · `check-css-collisions` 새는 속성 0.

**탭 id는 예전 이름을 그대로 뒀다.** `state.studentTab`은 여전히 «화면 하나»를 가리키고,
그것이 어느 탭에 속하는지는 `STUDENT_TAB_SECTION`이 되찾아 준다 (관리자 셸과 같은 장치다).
덕분에 `#student/quiz` 해시 · `goStudentPage('clinic')` · 알림 드롭다운의 `gotoCode`가
**한 줄도 안 고치고 그대로 동작한다.** 넷을 새로 만들었으면 저 셋을 전부 따라 고쳐야 했다.

| 탭 | 안에 든 화면 |
|---|---|
| 홈 | 오늘 할 일 · 다음 수업 · 요약 |
| 학습 | 데일리퀴즈 · 오답숙제 · 진도·과제 · 강의 |
| 기록 | 성적 · 출석 |
| 소통 | 공지 · 질의응답 · 클리닉 · 채팅 |

- **탭 안에 «목록 화면»을 한 겹 더 두지 않았다.** 시안은 하위 화면마다 뒤로가기 화살표를 그렸는데,
  넷을 한 줄(서브내비)에 세울 수 있는데 굳이 한 번 더 누르게 할 이유가 없다.
  서브내비는 관리자와 **같은 `.subnav`**를 쓴다 — 학생이라고 다른 부품을 만들지 않는다
- 서브내비 숫자는 **«아직 안 한 것»만** 센다 (미제출 퀴즈·안 푼 오답·미완료 과제·안 읽은 공지·미답변 질문).
  전체 개수는 화면 안에 이미 있다
- 데스크톱에서 열면 480px 한 칸이 가운데로 모인다. 시안이 못박아 둔 그대로다

#### 홈에서 덜어낸 것 — 그리고 그 김에 사라진 id 충돌
예전 홈에는 «질문 폼 · 반 진도 링 · 빠른 변형 문제 요청 · 영상 카드»가 함께 있었다.
전부 제 화면이 따로 있는 것들이라 그리로 보냈다. 남은 것은 «할 일»과 그 주변뿐이다.

> **덜어내니 실제 버그가 둘 사라졌다** — 관리자 홈에서 겪은 것과 같은 사고다.
> - `qna-question` textarea가 홈과 질의응답 **두 곳에 동시에** 살아 있었다.
>   `submitQuestion()`이 `getElementById`로 읽으니 둘이 같이 그려지면 엉뚱한 칸을 읽는다
> - 「빠른 변형 문제 요청」과 오답숙제가 **같은 위젯**(`wrongSubmitWidgetHTML`)을 둘 다 그렸다.
>   `state.wrongExamId`·`wrongChecked`를 공유해서 한쪽에서 고른 것이 다른 쪽에 비쳤다
>
> 지운 것 — `wrongSubmitWidgetHTML` · `progressRingSVG` · `videoEmbedHTML` · `videoProgressGaugeHTML`.
> 「틀린 문제 등록」은 이제 오답숙제 화면 하나뿐이다.

#### 시안대로 하지 않은 것 — 도메인에 근거가 없어서
| 시안 | 실제 | 왜 |
|---|---|---|
| 데일리퀴즈 «10문항 중 3번» | 회차 하나를 크게 + 지난 회차 목록 | 퀴즈 하나가 **한 문항**이고 날짜별로 따로 선다. 묶음이라는 개념이 없다. 진행 표시는 «제출한 회차/전체 회차»로 바꿨다 |
| 성적의 «반 평균» | 넣지 않았다 | 그 값을 내려면 **같은 반 전원의 기록을 이 학생 브라우저로** 내려받아야 한다. 화면에 안 보여도 이미 유출이다 |
| 결석의 «사유» | 상태만 | 출결 레코드에 사유 필드가 없다 |
| 클리닉 시간 격자 | 날짜별 목록 | `time`이 자유 텍스트라 시각으로 정렬할 수 없다 (관리자 클리닉과 같은 이유) |

데일리퀴즈에는 **자동 채점이 안 된다는 사실을 화면에 적어 뒀다.** 감추면 «채점된 줄» 알게 된다
(관리자 15-quiz에서 내린 것과 같은 판단이다).

#### 강의는 «누른 하나»에만 iframe을 붙인다
예전에는 목록 수만큼 iframe이 떠서, 영상이 열 개면 화면을 열자마자 유튜브를 열 번 불렀다.
이어보기 카드를 맨 위에 두고, 카드를 누른 것 하나만 임베드한다.

> **⚠ 그 과정에서 시청 시간 추적이 «한 번 다시 그리면 끊기는» 버그를 찾았다.**
> `mountVideoTrackers`가 «`state.ytPlayers[id]`가 있으면 건너뛴다»라, `render()`가
> `app.innerHTML`을 갈아 iframe이 새로 만들어져도 **죽은 예전 iframe을 가리킨 채로 남아**
> 새 것에 다시 붙지 못했다. 그때부터 시청 시간이 **조용히** 기록되지 않는다.
> 예전 화면은 전부 임베드해 두고 거의 다시 그리지 않아 잘 안 드러났는데,
> 영상을 하나씩 열고 닫게 되면서 매번 걸리는 길이 됐다.
> → 붙어 있던 `iframe` 요소를 같이 들고 있다가, 다른 것이면 **정산·destroy 후 다시 붙인다.**

#### ⚠ `.mono`에 한글 낱말을 넣지 말 것
JetBrains Mono에는 한글 글리프가 없다. 브라우저가 «고정폭 한글 글꼴»로 떨어뜨리는데,
그러면 **「오늘」이 「오 늘」처럼 벌어진다.** 자유 텍스트 시간(「7시~9시」·「저녁 7시」)도 같다.
→ **mono는 `YYYY-MM-DD`·숫자·퍼센트에만.** 낱말은 밖으로 뺀다.

ds.css 부품 중 `.note .d`와 `.chat-day span`이 통째로 mono라, 그 안에는 날짜만 넣는다.
(숫자가 섞인 「7월 26일 (토)」는 이미 학생 상세 연대기에서 쓰고 있어 그대로 둔다 — 그건 읽힌다.)

#### 그 밖에
- **출석률 정의를 그대로 가져왔다** — 「퇴원」을 뺀 기록 중 「결석」이 아닌 비율.
  명단(`rosterStats`)이 쓰는 규칙과 같은 것이다. `rosterStats`는 `state.allRecords`(교사만 싣는다)를
  보므로 함수는 못 쓰고 **규칙만** 같이 쓴다. 두 화면이 다른 숫자를 말하면 둘 다 못 믿게 된다
- **진도 → 과제 → 단원 순.** 처음엔 시안대로 단원 체크리스트를 진도 바로 아래 뒀더니
  열 줄에 480px을 써서 **손이 닿아야 할 과제가 화면 밖으로 밀렸다.** 읽는 것은 아래로 내린다
- **클리닉은 빈 자리를 먼저 보여주고 고르게 한다.** 시간대를 누르면 신청 칸이 열린다.
  드롭다운(`#clinic-slot`)이 사라졌으므로 `submitClinicRequest()`는 고른 값을 `state`에서 읽는다
- 달 넘김 같은 «작은 화살표»도 44px이다 (`.sap-navbtn`). ds.css의 `.btn.icon`(32px)은 마우스 치수다
- 「결석 0」을 빨갛게 칠하지 않는다 — 없는 일이 있는 것처럼 읽힌다
- 채팅은 열릴 때 **맨 아래로** 간다. 전체 재렌더링이라 매번 위로 되돌아가고 있었다

#### 채팅 시각이 UTC로 찍히고 있었다
`sendChatMessage()`가 `new Date().toISOString()`을 썼다 — KST에서 보낸 말이 **9시간 전으로** 남는다.
`todayStr()`을 로컬로 고칠 때(9절 6번) 놓친 곳이고, 학생 화면이 시각을 말풍선 옆에 띄우면서 드러났다.
`nowStamp()`를 만들어 로컬 `'YYYY-MM-DD HH:MM'`로 바꿨다.
**이미 저장된 메시지의 시각은 그대로 둔다** — 어느 기준으로 찍힌 것인지 알 수 없다.

### 랜딩 합류 — 완료 (2026-08-07) · **화면 이식 끝**
`homeHTML()` 전면 재작성. `landing-v2/index.html`의 다섯 장면을 그대로 옮기고 `.lp` 셸을 씌웠다.
검증 — 390·560·760·900·1180·1440 **여섯 폭 전부** 텍스트 잘림 0 · 부모 침범 0 · 문서 가로 넘침 0 ·
콘솔 오류 0 · `check-css-collisions` 새는 속성 0. 상담 폼은 실제로 넣어 보고 관리자 화면에 뜨는 것까지 확인한 뒤 지웠다.

**시안은 개념 페이지라, 실서비스 홈이 싣고 있던 것 중 네 덩이가 갈 곳이 없었다.**
| | 어디로 |
|---|---|
| 경력 · 성과 | **기록** 섹션 — 시안이 `CONTENT.credits`로 «자리»만 잡아 둔 칸이 바로 여기다 |
| 수강생 후기 9개 | **들은 말** 섹션 — 마퀴를 버리고 메모 종이로 세웠다 |
| 공지사항 | **공지** 섹션 — 실데이터고 헤더 메뉴(`sec-notice`)가 걸려 있다 |
| 커리큘럼 카드 3개 | **뺐다** — 고등수학 어디나 같은 내용이라 브랜드가 없다. 랜딩 5안을 기각한 이유(「아무 수학 강사나 쓸 수 있는 소재」)에 그대로 걸린다. 필요하면 상담에서 말한다 |

최종 순서 — 도착 → 하루 → 전환 → **사람 · 들은 말** → **기록** → **공지** → 상담.

- **척추가 페이지를 반으로 가른다.** 도착·하루·전환·기록·공지에는 있고 사람·들은 말·상담에는 없다.
  앞은 «기록»이고 뒤는 «사람»이다. 원장의 메모와 학생의 후기가 같은 메모 종이색인 것도 같은 이유다
- **후기는 넷만 세우고 나머지는 눌러서 편다.** 여는 것은 `render()` 없이 클래스 하나로 —
  전체 재렌더링이 일어나면 등장 애니메이션이 통째로 다시 돈다
- 구버전 마퀴는 흐르는 글이라 읽으려면 기다려야 했고 멈출 수도 없었다 (9절 7번의 `prefers-reduced-motion` 미대응이 이것이다)
- **전화번호를 지어내지 않았다.** 아직 없어서 인스타그램·운영 시간 두 줄만 뒀다. 생기면 `l-way` 한 줄을 더한다

#### 로고를 data URI 상수로 뽑았다 (`LOGO_H1`)
시안은 `../로고/가로형(1).png`를 부르는데, **경로에 한글이 섞여 있으면 배포본에서 조용히 404가 난다**
(서버·CDN마다 인코딩이 다르다). 구버전 헤더가 이미 같은 파일(28KB · 966×223)을 data URI로 심고 있어서
그걸 상수로 뽑아 **헤더·랜딩 상단바·히어로·푸터가 하나를 같이 쓴다.** 바이트도 요청도 늘지 않는다.

인물 사진은 시안 그대로 `webp`(99KB) + `png`(2.2MB) 폴백이다.
**`profile.jpg`(84KB)는 폴백으로 못 쓴다** — 컷아웃이라 투명 배경이 필요한데 JPEG에는 알파가 없다.

#### ⚠ 시안에 있던 버그 둘을 찾아 양쪽 다 고쳤다
> **1. 히어로의 척추가 아래 본문과 어긋나 있었다.**
> `.l-hero`가 세로 플렉스인데 `.l-wrap`에 `margin:0 auto`가 있다. 세로 플렉스의 **가로축**에서
> `margin:auto`는 stretch를 끄기 때문에, wrap이 1180이 아니라 «내용 크기»(612px)로 줄었다.
> 그 결과 척추가 히어로에서만 x=779, 그 아래는 x=495 — **페이지 전체가 한 줄기 선 위에 있다는
> 이 랜딩의 뼈대가 첫 화면에서 끊겨 있었다.** 로고도 의도한 392px이 아니라 260px로 나왔다.
> → `.l-hero .l-wrap{width:100%}` 한 줄.
> «잘림·넘침» 검사는 이걸 못 잡는다. 어긋난 것이지 넘친 것이 아니라서 — **정렬은 따로 재야 한다.**

> **2. 「금요일」이 「금 요 일」로 벌어졌다.** `.l-date`가 mono인데 JetBrains Mono에 한글이 없다.
> 학생 마이페이지에서 세운 규칙(8절)과 같은 것이다 — 요일만 mono 밖으로 뺐다.
> 「수업」·「들은 말」처럼 **letter-spacing을 일부러 준 라벨은 그대로 뒀다.** 그건 의도한 자간이다.

#### 등장·상단바는 `mountLanding()`이 맡는다
`render()`가 문자열을 `innerHTML`로 넣기 때문에 **시안의 `<script>`는 실행되지 않는다.**
IntersectionObserver 둘(등장 · 상단바)을 렌더마다 새로 만들고, **이전 것을 반드시 끊는다.**

- **한 번 드러난 뒤에는 다시 흐려지지 않는다** (`state.lpRevealed`). 전체 재렌더링 구조라
  토스트 하나에도 페이지가 통째로 다시 페이드인하면 어지럽다
- **IntersectionObserver가 없으면 «전부 보이게» 떨어진다.** `.l-rise`는 `opacity:0`이라
  관찰자가 안 붙으면 공개 페이지가 통째로 백지가 된다. 감출 바에 안 움직이는 편이 낫다
- ⚠ 자동화로 볼 때 IO 콜백은 **렌더링 프레임에 묶여** 있어 백그라운드 탭에서는 오지 않는다.
  `setTimeout`이 아니라 `requestAnimationFrame`을 기다려야 실제 동작이 보인다

#### 강사가 들어오는 문은 푸터 하나다
상단바는 방문자의 것이라 「학생 로그인」·「상담 신청」만 둔다. 「강사 로그인」은 푸터에 작게 뒀다.
로그인한 채로 오면 상단바가 「마이페이지 / 관리자 페이지」 + 「로그아웃」으로 바뀐다.

> **로그인 화면(`login`)은 아직 구버전이다.** 거기 붙은 구버전 헤더의 「강사소개·공지사항·후기」가
> `scrollToId('sec-about' 등)`으로 홈의 앵커를 부른다. **네 앵커를 새 홈에도 그대로 남겨 뒀다** —
> `sec-about`(사람) · `sec-review`(들은 말) · `sec-notice`(공지) · `sec-contact`(상담).
> 같은 페이지 안의 이동은 `lpScroll()`이 맡는다 — `scrollToId()`는 `render()`까지 부르는데
> 이미 홈이면 그건 페이지를 통째로 다시 그리는 일이다.

### 로그인 · 조교 이식 — 완료 (2026-08-08) · **구버전 화면 0**
`loginHTML()`·`assistantHTML()` 전면 재작성. `MIGRATED_VIEWS`에 `login`·`assistant`가 들어가면서
**`body.legacy`가 붙는 화면이 하나도 없어졌다.**
검증 — 로그인 4상태 + 조교 2화면 텍스트 잘림 0 · 부모 침범 0 · 문서 가로 넘침 0 · 콘솔 오류 0 ·
`check-css-collisions` 새는 속성 0. 전 화면 33개(관리자 12 + 학생 11 + 조교 2 + 로그인 3 + 공개 홈 + 상세 4) 재확인.

#### 로그인 — 문 셋을 동등하게 두지 않았다
구버전은 학생·조교·강사가 같은 크기의 탭 셋이었다. 오는 사람은 거의 전부 학생이고
조교는 계정이 아직 없고 강사는 한 명이다. → **학생 폼이 곧 화면**, 나머지는 아래 작은 글.
공개 홈도 이미 같은 비중이다 (상단바에 학생, 푸터에 강사).

- ⚠ **`<form>`으로 감쌌다.** 구버전은 버튼 onclick뿐이라 **입력칸에서 Enter를 쳐도 아무 일이 없었다.**
  로그인 화면에서 Enter가 안 먹는 것은 그 자체로 결함이다
- **오류를 칸 위로 올렸다.** 버튼 아래에 있으면 고치려고 위로 올라간 뒤 화면 밖으로 나간다
- **「한 번 더」를 넣었다** (최초 설정 한정). 여기서 정한 값은 되찾을 수 없는데 오타 하나로 계정이 잠긴다
- **문을 옮길 때 오류를 지운다** (`goLoginMode`). 안 그러면 조교 화면에 학생 오류가 남는다
- 상단 헤더가 사라졌으므로 **로고가 홈으로 가는 문**이다. 그 하나뿐이다

#### 조교 — 화면을 두 벌로 두지 않는다
> **구버전이 남아 있던 이유는 «DS 학생 상세가 삭제·기본정보까지 열어 두기 때문»이었는데,
> 그건 화면의 문제가 아니라 권한의 문제였다.** `sdScreenHTML(s, rec, isAssistant)` —
> 인자 하나로 좁힌다. **`teacherStudentDetailLegacyHTML`(구버전 탭 7개, 137줄)을 통째로 지웠다.**
> 두 벌로 두면 앞으로 모든 변경을 두 번 해야 하고, 반드시 한쪽만 고쳐진다.

조교에게서 빠지는 것 — 우측 칸 통째로(연락처·기본정보·삭제·채팅·클리닉·수업 화면) ·
신호 「7일 해제」 · 「질문·클리닉」 갈래. 그래서 3열이 아니라 2열이다.

- **상단바에 메뉴가 없다.** 갈 곳이 하나뿐인데 메뉴를 만들면 그게 거짓말이다.
  대신 「조교」 뱃지 + 담당 반 — 자기가 누구로 있는지가 늘 보여야 한다
- **권한을 화면이 먼저 말한다.** 눌러 보고 나서야 «안 되는구나»를 알게 하지 않는다
- 명단은 01-roster에서 **조교가 손댈 수 없는 열**을 뺐다 (반·반 진도·신호 등급·시청률).
  남은 열은 전부 조교가 «넣는» 것이다. 일괄 작업도 없다
- **담당 반 밖 학생은 못 연다** — 열려고 하면 명단으로 떨어진다 (구버전에도 있던 방어를 그대로 옮겼다)

> **⚠ 갈래를 뺐으면 연대기에서도 빼야 한다.** 처음엔 「질문·클리닉」 갈래만 없애고 사건은 남겼더니
> **「전체 27」인데 갈래 합이 24**가 됐다. 숫자가 안 맞는 화면은 그 순간부터 아무도 안 믿는다.

#### 조교가 여러 반을 맡는다 (2026-08-11)
예전에는 `assistant.classId` **하나**였고, 게다가 **바꿀 문이 아예 없었다** —
반을 옮기려면 계정을 지우고 새로 만들어야 했다.

**읽는 곳을 `assistantClassIds(a)` 하나로 모았다.** 다섯 곳이 각자 `a.classId`를 보고 있었다
(로그인 · 세션 복원 · 조교 명단 · 학생 상세 권한 · 설정 목록). 두 벌로 두면 한쪽만 고쳐지고,
그러면 **«명단엔 보이는데 상세는 막히는» 학생**이 생긴다.

- **데이터를 한꺼번에 옮기지 않는다.** 옛 계정은 `classId`만 갖고 있으므로 «읽을 때» 맞춰 준다.
  「담당 반 변경」을 저장하는 순간 `classIds`로 바뀌고 **그때 옛 필드를 지운다** —
  둘이 같이 남으면 어느 쪽이 진짜인지 알 수 없게 된다
- **담당 반은 세션에 담지 않는다.** 세션 복원이 «지금의 계정»에서 다시 읽는다.
  안 그러면 강사가 반을 바꿔도 옛 세션이 옛 반을 들고 다닌다
- **고른 반이 «지금도 내 반인지»를 매번 본다** — 강사가 반을 뺏은 뒤에도 `state.asClassId`가
  남아 있으면 그 반 학생이 계속 보인다. 어긋나면 「전체」로 되돌린다

**화면에서 달라진 것**
| | |
|---|---|
| 「반」 열 | 예전엔 «반이 하나다»라며 뺐다. **담당 반이 둘 이상일 때만** 세운다 — 없으면 어느 반 학생인지 알 수 없다. 반을 하나 골라 본 상태에서는 다시 뺀다 (한 열에 같은 값만 스무 줄 서는 것은 자리 낭비다) |
| 반 고르개 | 명단(01)의 «뷰 축»과 같은 부품(`.rst-views`). **둘 이상일 때만** 나온다 — 하나뿐인데 「전체 / 그 반」을 그리면 고를 것이 없는 줄이 된다 |
| 상단바 | 담당 반 이름을 다 적는다. 숫자로 뭉뚱그리지 않는다 — 자기가 누구로 있는지가 늘 보여야 한다 |
| 설정 > 조교 | 담당 반 고르개가 `select` → **체크상자**. select로는 하나밖에 못 고른다. 줄마다 **「담당 반 변경」**이 생겼고, 바꾸면 **변경 이력에 «전 → 후»가 남는다** |

- 체크상자를 읽는 코드는 `readCheckedClassIds(prefix)` 하나다 — 추가 폼과 줄마다의 고르개가
  같은 일을 한다. 접두사만 다르다 (`addHomework(prefix)`와 같은 판단)
- **반을 지울 때 «이 반만 맡은 조교»를 미리 말해 준다.** 안 그러면 그 조교 화면이 통째로 비는데
  강사는 그 이유를 알 길이 없다 (시험지 삭제가 딸린 것을 미리 세어 주는 것과 같다)
- 맡은 반이 0개면 명단이 비는 대신 **「강사에게 배정을 요청하라」**고 적는다

검증 — 임시 계정을 만들어 실계정을 건드리지 않고 돌렸다. 옛 `classId` 계정이 그대로 읽힘 ·
두 반 학생이 한 명단에(반 열 생김) · 반을 고르면 열이 빠지고 걸러짐 · 다른 담당 반 학생 상세 열림 ·
**담당 아닌 반 학생은 막혀 명단으로 떨어짐** · 변경 이력에 «전 → 후» 기록 ·
뺏긴 반을 골라 둔 채 들어오면 「전체」로 되돌아감 · 0개/1개일 때 · 가로 넘침 0.

### legacy 폼 정리 — 완료 (2026-08-08) · **이식 끝**
감싸 둔 폼 다섯(시험지 등록 · 창고 추가 · 설정 6갈래 · 수업 기록 상세 · 질의응답 사진칸)을 옮기고
**구버전 `<style>` 블록 476줄을 통째로 지웠다.** `index.html`에 「legacy」라는 낱말이 하나도 없다.
검증 — 화면 40여 곳(관리자 26 + 학생 12 + 조교 2 + 로그인 3 + 공개 홈) 잘림 0 · 침범 0 ·
문서 가로 넘침 0 · 콘솔 오류 0.

**화면마다 고치지 않고 «부품»을 바꿨다.** 살아 있던 legacy 클래스는 30개였는데
대부분이 여러 화면이 함께 쓰는 폼 원시부품(`lg-field` 51곳 · `lg-btn` 42곳 · `lg-panel` 19곳…)이었다.
→ `.fm-*` 한 벌을 만들고 **class 속성 154개를 토큰 단위로 치환**했다. 화면별로 손댔으면 다섯 배는 걸렸다.

| 구버전 | 지금 |
|---|---|
| `lg-field` `inline-form` `empty` `lg-hint` `lg-panel` | `.fm-field` `.fm-row` `.fm-empty` `.fm-hint` `.fm-panel` |
| `lg-btn btn-primary/red/small` | ds.css의 `.btn` `.btn.p` `.btn.no` `.btn.sm` |
| `lg-badge badge-ok/no/…` · `lg-mono` | ds.css의 `.badge.ok/.no/…` · `.mono` |
| `ledger` 표 | `.fm-panel` 안의 `table` (ds.css `table.grid`와 같은 리듬) |
| `chat-window` `chat-bubble` | ds.css의 `.bub-row`/`.bub` — **학생 채팅과 같은 부품** |
| `att-status-btn` | ds.css의 `.seg` — **수업 화면과 같은 부품** |
| `progress-outer/inner` | ds.css의 `.bar` |

#### ⚠ 스타일 블록을 지우면 CSS 변수도 같이 죽는다
구버전 `:root`가 `.legacy{}` 안에 있었다 (`--ink-soft` `--card-border` `--red-pen` …).
블록을 지우자 **인라인 `style`에 남아 있던 `var(--card-border)` 13곳이 조용히 무효**가 됐다.
색이 «사라지는» 것이라 화면이 깨지지 않고 그냥 흐려진다 — 눈으로는 잘 안 잡힌다.
→ `--ink-soft→--dim` · `--card-border→--line` · `--red-pen→--no`로 옮겼다.
**남의 스타일 블록을 지울 때는 그 안의 커스텀 속성부터 grep할 것.**

#### 지운 것 — 그리고 거기서 잃을 뻔한 것
`headerHTML()`·`footerHTML()`·`notificationBellHTML()`·`goStudentPage()`.
모든 화면이 자기 셸을 갖게 되면서 공용 헤더가 덧씌워질 자리가 없어졌다.

> **⚠ 그 헤더에 알림 벨이 붙어 있었다.** 그냥 지웠으면 **강사가 새 상담·질문·클리닉 알림을
> 받을 곳이 없어진다.** 학생 화면의 `stuBellHTML`을 `bellHTML`로 올려 관리자 상단바에 붙였다 —
> items만 다르고(`teacherNotifItems` / `studentNotifItems`) 껍데기는 하나다.

`goStudentPage()`는 공개 헤더의 「학생 ▾」 드롭다운이 유일한 호출처였고 그 헤더는
랜딩 합류 때 이미 사라졌다. `state.pendingStudentTab`도 같이 지웠다.

### 구버전 «구조» 잔재 정리 — 완료 (2026-08-08)
> **앞선 「legacy 폼 정리」는 클래스만 바꾼 것이었다.** 카드·표·상시 폼이라는 **구버전의 짜임**이
> 그대로 남아 있었고, 사용자가 그걸 짚었다. 이번엔 구조를 바꿨다.

**시안(13-settings)이 정한 언어는 «구역 + 줄»이다.** 흰 상자를 종이 위에 띄우는 것은
구버전(`.lg-panel`)의 어법이었다 — 한 화면에 상자가 서넛 뜨면 «무엇이 무엇 안에 있는지»가 흐려진다.
→ `.st-sec`(구역) · `.st-h`(구역 머리) · `.st-row`(한 줄) · `.st-log`(로그 한 줄) · `.st-form`(접히는 입력).
**`.fm-panel`·`.fm-card`를 쓰는 곳이 0이 됐다.**

| 화면 | 구버전 잔재 | 바꾼 방식 |
|---|---|---|
| 설정 6갈래 전부 | 갈래마다 흰 카드 2~3개 + 상시 펼친 폼 + `<table>` | 카드 제거 → 구역+줄. **만드는 폼은 「추가」를 눌러야 열린다** |
| 설정 > 변경 이력 | `<table>` (열 머리 5개) | **시안 그대로 «줄»** — 시각·뱃지·내용·작성자. 표는 «비교»고 로그는 «훑는» 것이라 열 머리가 필요 없다 |
| 소통 > 클리닉 | 「시간대 추가」·「직접 배정」 카드 둘이 주간 판을 아래로 밀어냄 | 머리말 버튼 둘 → 접히는 폼 |
| 소통 > 공지 | 「새 공지」 폼이 상시 펼쳐져 목록을 밀어냄 | 「새 공지」 버튼 → 접히는 폼 |
| 반 > 영상 | 「영상 배포」 폼이 상시 펼쳐져 «이탈 지점»이 한 화면 아래로 | 「영상 등록」 버튼 → 접히는 폼 |
| 반 > 수업 기록 > 과제 | 카드 나열 + **과제 등록 폼이 두 벌** | 줄로 바꾸고 폼을 하나로 합침 |
| 반 > 수업 기록 > 시험 | 카드 나열 | 줄 + 「문항 보기」 |
| 설정 > 학생 · 오답 > 창고 | 업로드 미리보기 카드 | 구역 + `table.grid`(명단과 같은 표) |
| 오답 > 시험지 등록 · 창고 문항 추가 | 폼을 감싼 카드 | 구역 — 화면 자체가 이미 상자다 |

- **「추가」를 눌러야 열리는 폼**은 이 제품에 이미 있던 장치다 (시험지 「새 시험지」·창고 「문항 추가」·
  학생앱 「틀린 문제 등록」). 새 규칙을 만든 것이 아니라 **일관되게 넓힌 것이다.**
  상시 펼쳐 둔 폼은 화면에서 제일 큰 자리를 차지하면서 제일 가끔 쓰인다
- 출석 상태 버튼은 ds.css의 `.seg`(수업 화면과 같은 부품), 채팅은 `.bub-row`(학생 화면과 같은 부품)

#### 같이 잡은 것
- **⚠ 알림 벨의 «바깥 클릭 닫기»가 죽어 있었다.** 클래스를 `.notif-bell`→`.ds-bell`로 바꾸면서
  `document.addEventListener('click', … closest('.notif-bell'))`를 안 고쳤다. 드롭다운이 안 닫혔다
- **⚠ 과제 등록 폼이 한 화면에 두 벌** — `addHomework`(대상 반까지 고르는 폼)와
  `addDailyHomework`(그날 폼)가 나란히 그려지고 있었다. 그날 화면이라 후자로 합쳤다
- `.st-row .t span`을 **후손 전체**에 걸었더니 줄 안의 `<span class="mono">`까지 블록이 되어
  「test11」과 「· 테스트반」이 두 줄로 갈라졌다 → **직계 자식(`> span`)만** 걸어야 한다
- 변경 이력의 시각이 `toLocaleString('ko-KR')`이라 「26. 08. 04. 오전 12:16」로 두 줄이 됐다
  → 시안이 쓴 24시간 형식으로. 전부 숫자라 mono에서도 읽힌다
- 영상 화면의 `.mono`에 한글 낱말이 또 들어가 있었다 (「공 통 수 학 2」·「완 주」)

### 채팅 알림 · 공지 2단 · 공지 서식 (2026-08-08)

#### ⚠ 학생이 말을 걸어도 강사가 알 방법이 없었다
채팅은 학생마다 문서가 따로다(`kv/chat:{studentId}`) — 그리고 **누른 학생 것만 불러왔다.**
그래서 벨에도, 채팅 탭 목록에도 «새 메시지»가 뜰 수가 없었다. 구조적으로 알 수 없었던 것이다.

- `loadAllChatsIfNeeded()` — 관리자 셸에서 한 번. 기록(`loadAllRecordsIfNeeded`)과 같은 방식이다
- **안 읽음은 학생별로 센다** (`unseenCount('chat:'+sid, 학생이 보낸 수)`).
  **내가 보낸 말은 세지 않는다** — 안 그러면 답장할 때마다 «안 읽음»이 늘어난다
- 채팅 목록을 **이름순에서 «말이 온 순서»로** 바꿨다. 안 읽은 학생이 맨 위, 그다음 최근순.
  마지막 말이 미리보기로 붙는다 — 이름순은 «누가 기다리고 있나»를 말해 주지 않는다
- 벨의 「새 채팅」은 **눌러도 읽음으로 찍지 않는다** (`keepUnseen`). 그 학생 대화를 실제로 열 때 찍힌다
- 반대 방향도 같다 — 학생 벨에 「선생님 답장」이 뜬다
- ⚠ **실시간은 아니다.** Firestore를 REST로 쓰고 리스너가 없어서, 열려 있는 화면에는
  새 메시지가 저절로 오지 않는다. 화면을 다시 그려야 보인다 — 별건이다

#### 공지 — 시안대로 «목록 + 본문» 두 단
카드를 세로로 나열하면 긴 공지 하나가 나머지를 화면 밖으로 밀어낸다.
공지는 훑는 것이 아니라 «하나를 읽는» 것이다.

> **⚠ 시안(10-notice)의 세 번째 칸 「읽음 현황」은 만들 수 없다.**
> 공지 레코드는 `{id, title, content, date}`뿐이고, 학생이 «읽었다»는 사실은
> **그 학생 브라우저의 localStorage**에만 남는다(`markSeen`). 서버에 없는 것을 그리면 그 줄만 거짓이 된다.
> 넣으려면 읽음 기록을 먼저 만들어야 한다 — 시안이 3열인데 실서비스가 2열인 이유가 이것이다.

#### 공지 본문에 서식을 넣었다
`# 큰 줄` · `**굵게**` · `- 목록` · 빈 줄로 문단. **넷뿐이다** — 더 늘리면 «쓰는 법»을 배워야 하는 도구가 된다.
편집기에 버튼 셋과 **실시간 미리보기**를 뒀다.

> **저장되는 것은 언제나 평문이다.** 표시할 때만 태그로 바꾼다 (`noticeBodyHTML`).
> HTML을 그대로 담아 두면 언젠가 남의 글이 섞였을 때 그게 곧 실행된다.
> `escHtml`을 **먼저** 걸고 그 뒤에 표시를 찾으므로, 사용자가 `<b>`를 적어도 글자로만 남는다.
> 관리자·학생앱·공개 홈 **세 곳이 같은 렌더러**를 쓴다 (`.ntx`).

### ⚠ 한글이 「ㅇㅏㄴㄴㅕㅇ」으로 입력되던 버그 (2026-08-08)
공지 본문에 「안녕하세요」를 치면 **「ㅇㅏㄴㄴㅕㅇㅎㅏㅅㅔㅇㅛ」**가 됐다.

**한글은 «조합»으로 들어온다.** ㅇ+ㅏ+ㄴ이 모여 「안」이 되기까지 브라우저는 그 글자를
«조합 중»으로 들고 있는데, `oninput`이 매 글자 `render()`를 부르면 **입력칸이 통째로 새로
만들어지면서 조합이 깨진다.** 자모가 각각 확정돼 버리는 것이다.
(사용자가 «버튼이 한 자 칠 때마다 반짝인다»고 한 것이 바로 그 재렌더링이다.)

→ **조합이 끝난 뒤에만 다시 그린다** — `liveEdit(event, apply)` 하나로 모았다.
`event.isComposing`이 참이면 state만 갱신하고 화면은 건드리지 않는다.

- **같은 패턴이 네 곳에 있었다** — 공지 본문 · **관리자 명단 검색** · 조교 명단 검색 ·
  변경 이력 검색. 즉 **한글 이름으로 학생을 검색하는 것이 계속 깨져 있었다.**
  공지 편집기를 만들면서 드러났을 뿐, 명단 검색은 그 전부터 그랬다
- **커서를 «끝»이 아니라 «있던 자리»로** 되돌린다. 끝으로 보내면 문장 가운데를 고칠 때
  글자 하나마다 커서가 맨 뒤로 튄다 (예전 코드가 그랬다)
- `oninput`과 `oncompositionend` **둘 다** 걸어야 한다 — 조합이 끝나는 순간
  어느 이벤트가 먼저 오는지가 브라우저마다 다르다
- 검증 — 조합 이벤트를 흉내 내어 **조합 중 render 호출 0회**, 확정 후 1회를 확인했다

> **입력칸에서 `render()`를 부를 때는 항상 이걸 생각해야 한다.** 전체 재렌더링 구조의
> 대가다 (9절 4번). state만 갱신하고 화면을 안 건드리는 칸(출결 메모·수업 메모)은
> 원래 안전하다 — 위험한 것은 «치면서 화면이 따라오는» 칸이다.

### 배포본 확인 결과 (2026-08-08 · `https://khhyun827-lang.github.io/hahyunmath/`)

마지막 세 커밋을 **실제 사이트에서** 확인했다. 세 건 다 정상이다.

| 확인한 것 | 결과 |
|---|---|
| **공지 서식 · 세 곳** | 관리자 상세 · 학생앱 소통>공지 · 공개 홈이 **같은 HTML 문자열**을 낸다 (`.ntx` innerHTML 대조). `# 큰 줄`→`h4` · `**굵게**`→`b` · `- 목록`→`ul/li` · 빈 줄→`p`. 사용자가 적은 `<b>`는 **글자로** 남았다(이스케이프 확인) |
| **채팅 알림** | 안 읽은 2건 → 벨 뱃지 `2` · 드롭다운 「새 채팅 2」 · 채팅 탭 「학생 3 · 안 읽음 2」. 벨에서 눌러 들어가도 **2가 그대로**(`keepUnseen`), 테스트학생 대화를 여니 **그 하나만** 꺼지고 편규원 1은 남았다 |
| **한글 입력 — 네 자리 전부** | 관리자 명단(`rst-search`) · 조교 명단(`roster-search`) · 변경 이력(`auditlog-search`) · 공지 본문(`notice-content`). 조합 중 `render()` **0회**, 확정 후 1회, 값은 「테스트」·「안녕하세요」 그대로, **커서가 있던 자리**에 남고 포커스도 유지된다 |
| 명단 검색이 실제로 좁히는지 | 「편」 → 3명 중 편규원 1명. 「테스트」는 **반 이름**까지 봐서 2명 (의도대로) |

> **확인 방법 — 데이터를 건드리지 않았다.** 공지는 `DATA.notices`에 임시 항목을 꽂아
> 세 화면을 그린 뒤 되돌렸고(Firestore 쓰기 없음), 채팅 알림은 `localStorage`의
> `khm-chat:*-seen` 값만 잠시 낮춰 «안 읽음»을 만든 뒤 원래 값으로 되돌렸다.
> 한글은 `compositionstart → input{isComposing:true} → compositionend`를 흘려 넣고
> 조합 중 `render()` 호출을 셌다 (8절 「검증할 때」의 방법 그대로).

~~**아직 안 본 것 — AI 생성 · 이미지 업로드.**~~
**이미지 업로드는 2026-08-09에 확인했다** — 그때 토큰 만료로 죽어 있던 것을 찾아 고쳤고
(9절 2-B), 문서·이미지 두 경로 모두 배포본에서 정상을 확인했다.
**AI 생성만 아직 안 눌러 봤다** — Gemini 호출이라 사용자 확인 없이 태우지 않는다.

### 신호 임계값을 설정으로 열었다 (2026-08-08)

`signalOf()` 안의 상수 일곱 개가 **설정 > 신호 규칙**으로 나왔다. 지금까지는 값을 고치려면
코드를 건드려야 했는데, 그 일곱 숫자가 **명단 · 신호 · 학생 상세 세 화면의 등급**을 한꺼번에 정한다.

| 신호 | 임계값 | 무게 |
|---|---|---|
| 연속 결석 | 최근 8회 중 `absent`회 이상 (기본 2) | 무거움 |
| 출석률 저조 | `attPct`% 미만 (기본 80) | 무거움 |
| 성적 급락 | `drop`점 이상 하락 (기본 8 · 100점 환산) | 무거움 |
| 오답 미제출 | `wrong`건 이상 (기본 3) · 1건부터 경미 | 무거움 / 경미 |
| 시청률 저조 | `vidHeavy`(50) / `vidWarn`(70) / `vidLight`(80) | 무거움 / 등급 승급 / 경미 |
| 과제 누락 | 미완 1건부터 — **임계값이 없다** | 경미 |

- **등급을 «어떻게» 매기는지는 열지 않았다.** 「무거운 신호 2개 = 위험」은 규칙의 뼈대다.
  그것까지 열면 화면마다 «위험»의 뜻이 달라진다. 여는 것은 각 신호가 «언제» 켜지는가뿐이다
- **저장은 신호 해제와 같은 방식** — 컬렉션을 늘리지 않고 `kv/signal-rules` 문서 하나다.
  읽을 때는 반드시 `signalRules()`를 거친다. 저장된 값이 비었거나 깨져 있어도
  기본값에 덮어 씌우고 범위·순서를 바로잡아 돌려준다 (`state.signalRules`를 직접 보지 않는다)
- **불러오기는 `loadAllRecordsIfNeeded()`에 얹었다.** `signalOf()`를 쓰는 세 화면이 전부 이 함수를
  거치므로 기록이 도착한 시점에는 규칙도 와 있다. 따로 부르면 «기본값으로 한 번 그리고 →
  규칙이 와서 등급이 바뀌는» 깜빡임이 생긴다
- **입력을 state에 담지 않는다.** 여기서 `render()`가 돌면 쓰던 값이 날아간다 —
  조교·반 추가 폼과 같이 «저장할 때 DOM에서 읽는» 방식이라 조합 중 커서 문제(9절 4번)가 아예 없다.
  그래서 「기본값 채우기」는 입력칸만 채우고 `showToast()`를 부르지 않는다 (부르면 render가 돌아 되돌아간다)
- **뒤집힌 시청률은 조용히 고치지 않고 되돌려 보낸다.** 말없이 바꾸면 «내가 넣은 값»과
  «화면이 쓰는 값»이 달라지고, 그때부터 이 화면을 못 믿게 된다
- 저장하면 **바뀐 값만** 변경 이력에 남고(`출석률 80→75`), 토스트가 **새 인원 분포**를 말해 준다.
  구역 머리에도 «지금 이 값으로 위험 n · 주의 n · 관찰 n»이 늘 떠 있다 (해제한 학생은 뺀다)
- 신호 화면 머리말에 **「규칙 고치기」** 버튼을 뒀다 — 「이 줄은 왜 위험이지」의 답이 임계값에 있다

> **숫자 칸의 빨강도 같은 값을 본다.** 명단의 출석률·시청률 칸과 학생 상세 KPI가 `<80`·`<50`을
> 하드코딩하고 있었다. 임계값을 75로 낮췄는데 79%가 여전히 빨갛게 보이면 둘 중 하나는 거짓말이 된다.
> **노랑(cau)은 그대로 뒀다** — 그건 신호가 아니라 «가까워지고 있다»는 넓은 띠다.
> 같은 이유로 명단 필터 「출결 주의」(90%)와 조교 명단의 85%도 건드리지 않았다.

**검증** — 잘림·부모 침범·문서 넘침 전부 0 (1425px · 1180px 두 폭). 임계값을 흔들어
등급이 실제로 따라오는 것을 3명 실데이터로 확인했다 (느슨하게 → 셋 다 관찰 / 결석 기준 8 → 「연속 결석」만 꺼짐).
빈 값·숫자 아님·뒤집힌 시청률은 **저장하지 않고** 되돌려 보내는 것까지 확인했다.
실제 Firestore에 저장 → 새로고침 → 값이 살아 있는 것도 확인한 뒤 **기본값으로 되돌려 놨다**
(변경 이력에 그 두 줄이 남아 있다 — 「출석률 80→75」와 되돌린 줄).

### 화면의 «칸»에 배경을 되돌렸다 (2026-08-08) — 아홉 화면

사용자가 «시안에서는 좌우가 하얀색이었는데 왜 바뀌었나»를 물었다. **바꾼 것이 아니라 빠뜨린 것이었다.**
시안은 거의 모든 화면에서 목록·문맥 칸을 `background:var(--card)`로 그렸는데, 이식할 때
«배치»만 옮기고 그 한 줄을 안 옮긴 것이 첫 화면에서 굳어 아홉 화면까지 갔다.
결정으로 남은 기록이 어디에도 없다 — 그래서 되돌렸다.

> **「카드를 쓰지 않는다」와 부딪히지 않는다.** 그 규칙은 본문 한가운데에 흰 상자를 띄우던
> 구버전 `.lg-panel` 얘기다. 화면 가장자리의 «면»은 시안이 처음부터 흰색으로 그렸다.

| 흰색으로 되돌린 칸 (9) | 종이색 그대로 (시안이 그렇다) |
|---|---|
| `.sd-list` · `.sd-ctx` · `.rv-list` · `.chub-side` · `.ex-list`(시험지·창고·설정) · `.co-side` · `.nt-list` · `.ch-list` · `.vd-list` | `.cls-side`(수업 시간표 · 02) · `.rv-queues`(오답 큐 · 04) |

**시안은 1440 캔버스에 `position:absolute`라 칸이 화면 가장자리에 닿았다.** 여기는 스크롤 문서라
본문 패딩(`.app .body{padding:20px}`)을 **음수 마진으로 되먹여** 같은 자리를 만든다 —
칸이 상단바(또는 서브탭) 밑에서 창 아래까지, 왼쪽·오른쪽 끝까지 이어진다.

- **`.rv-list`는 가로로 안 늘인다** — 큐 다음의 «가운데» 칸이라 가장자리에 닿지 않는다
- **`.vd-list`만 예외다** — 반 관리 본문 «안»의 칸이고 위에 「영상 등록」 줄이 하나 더 있다.
  `min-height:100%`를 줘 봤더니 그 줄 높이(38px)만큼 본문에 **빈 스크롤**이 생겨 되돌렸다.
  흰 면은 두 열 중 긴 쪽까지만 간다

> ### ⚠ 배경을 깔자 높이 버그 둘이 드러났다
> 빈 종이 위에서는 «남는 자리»와 «넘친 자리»가 똑같이 안 보인다.
> - **공지가 42px 길었다** — `.nt-screen`이 `calc(100vh - 92px)`인데 공지는 「소통」 **서브탭 아래**라
>   134여야 한다. 문서가 42px 세로로 스크롤되고 있었다
> - **설정이 42px 짧았다** — `.ex-screen`(134)을 시험지·창고와 같이 쓰는데 **설정에는 서브탭이 없다**.
>   `.ex-nosub`로 92를 돌려줬다. 왼쪽 갈래 목록이 서브탭 노릇을 하는 화면이라 그렇다
>
> **높이를 `calc(100vh - N)`으로 잡는 화면은 서브탭 유무를 같이 적어 둘 것** (92 / 134).

**검증** — 14개 화면에서 잘림·문서 넘침·의도치 않은 내부 스크롤 전부 0.
1280px·1024px에서도 가로 넘침 0 (좁아지면 `.sd-ctx`·`.co-side`가 사라지는 기존 규칙 그대로).

### 시안과 어긋난 곳 점검 (2026-08-08) — 세 곳 + 기계 점검 둘

사용자가 짚은 세 곳에서 시작해, **같은 유형의 사고를 파일 전체에서 기계적으로 훑었다.**

| 짚은 곳 | 원인 | 고친 방향 |
|---|---|---|
| 오답 검토의 선과 배경이 어긋남 | 큐는 위아래 패딩만큼 짧아 선이 끊기고, 흰 목록은 `margin-left:16px`으로 떠 있었다 | 큐에도 여백을 되먹이고, 목록의 바깥 여백을 안쪽 여백으로 (시안 `.list{left:216px}`처럼 선에 붙인다) |
| 질의응답이 시안과 너무 다름 | 시안 11은 «좌 목록 + 우 하나»인데 카드 세로 스택으로 만들어 뒀다 | **두 단으로 다시 짰다** → 아래 |
| 설정 좌측 글씨가 다름 | 시안 13의 갈래는 «알약»(`.sn`)인데 **시험지 목록 부품**(`.ex-item`)을 그대로 썼다 | `.ex-nav` 변형을 만들어 설정·창고에 입혔다. 시험지 목록도 시안 07대로 밑줄을 걷어냈다 |

#### 질의응답을 두 단으로 다시 짰다
«형태를 통일하지 않는다»는 판단은 그대로다 — 다만 **시안이 이 화면에 준 형태가 두 단이었다.**
수학 질문은 길고 사진이 붙어서, 카드로 쌓으면 하나를 읽는 동안 다음 질문이 화면 밖으로 나간다.
**답하는 화면은 «다음 것으로 넘어가는» 화면이라 목록이 늘 옆에 있어야 한다** (오답 검토와 같다).

- 좌: 대기 / 완료 / 전체 갈래 + 질문 목록. 우: 머리말 + 스레드 + 답변 작성칸
- **답하면 그 질문이 갈래에서 빠지므로 다음 질문이 저절로 올라온다** — 손이 멈추지 않는다
- 목록 항목은 ds.css의 «처리 큐» 부품(`.qitem`)을 그대로 쓴다. 새로 그리면 검토 큐와 미세하게 어긋난다
- ⚠ **시안의 「19시간째 미답변」은 만들 수 없다** — 질문에 날짜만 있고 시각이 없다.
  날짜로 셀 수 있는 만큼만 말한다 (「2일째 기다리는 중」)

> ### ⚠ 일곱 번째 이름 사고 — 이번엔 **ds.css 안에서** 부딪히고 있었다
> 처리 큐 부품 이름이 `.q`라 **조용한 버튼 `.btn.q`가 `border-left:3px`를 물려받고** 있었다
> (글자가 2px 밀린다). 시안·실서비스 양쪽에 걸린 문제라 **ds.css에서 `.qitem`으로 바꾸고
> 시안 04·11도 같이 고쳤다.** 두 글자 이름은 반드시 겹친다.

#### 기계로 훑은 것 둘
1. **ds.css 부품을 로컬에서 다시 정의한 곳** — `.app` 바로 아래에 ds.css 최상위 부품 이름이
   다시 정의된 것은 넷뿐이고(`topbar`·`subnav`·`body`·`actbar`) **전부 의도된 셸 교체**다
   (절대배치를 스크롤 문서로 바꾸는 것). 다만 `.app .body{padding:20px}`가 연대기 사고의 원인이었다
2. **mono 안의 한글** — 아홉 곳을 찾아 고쳤다. 리터럴 여섯(「정답: 」·「김하현T」·「단원」·「제출」·
   「정답 n/n」·「생성 중…」)과 데이터 셋(시험지 문항의 단원명 · 「다음 단원」의 단원명 · 「가장 높은 문항 n%」).
   **남긴 것 하나** — 학생이 적은 답 자체(`0≤a<1 또는 3<a≤5`)다. 답은 수식이라 mono가 맞다

> **화면 구성(칸의 개수와 배치)은 열여섯 개 전부 대조했다.** 시안과 다른 것은
> 전부 이미 기록된 결정이었다 — 시험지의 「제출 현황」 흡수 · 공지의 「읽음 현황」 불가 ·
> 창고의 단원 축 신설 · 반 관리 셸(반 목록 상시) · 데일리퀴즈의 정답률 불가.
> **남은 점검은 «화면 안쪽 부품»**이다 — 설정에서 나온 것과 같은 유형이라 눈으로 대조해야 한다.

### 시안 16개 전수 대조 — 그리고 채운 셋 (2026-08-08)

시안과 실제 화면을 한 쌍씩 놓고 열여섯 개를 전부 봤다. **시안과 다른 것은 대부분
이미 기록된 도메인 한계**였고(명단의 열 선택·반 진도의 회차·퀴즈의 정답률·공지의 읽음 현황 등),
**그냥 빠져 있던 것 셋**을 채웠다.

| 채운 것 | 시안 | 왜 빠졌었나 |
|---|---|---|
| **수업 기록의 출결 도트 + 월 구분 머리** | 14-log | 부품(`.dots`)도 데이터도 있었는데 글자 요약만 있었다 |
| **시험지 KPI 줄** | 07-exams | 문항별 오답률만 있고 «이 시험지가 어떤 상태인가»가 없었다 |
| **클리닉 날짜 열의 「+ 시간대」** | 09-clinic | 상단 폼 하나뿐이라 날짜를 다시 골라야 했다 |

- **출결 도트**는 명단·학생 상세와 **같은 `.dots` 부품**을 쓴다. tooltip에 이름과 상태가 다 들어간다
- **월 구분 머리**는 순수 표현이라 도메인과 무관하다 — 스무 줄 넘게 훑는 화면에서
  «언제 이야기인지»가 스크롤 중에도 남아야 한다
- **시험지 KPI는 넷이다** (오답 올린 학생 · 평균 오답률 · 쌍둥이 생성 · 검토 대기).
  **시안의 「반 평균」은 만들 수 없다** — 성적(`scores`)과 시험지(`exams`)를 잇는 것이
  도메인에 없다. 이름으로 짐작해 이으면 그 줄만 거짓이 된다
- **「+ 시간대」는 칸이 아니라 열 아래에 둔다** — 세로축이 시각이 아니라 «그날의 슬롯 더미»라
  시안처럼 «빈 칸»이 성립하지 않는다. 날짜를 미리 채우고 시간 칸에 커서를 놓아 준다.
  추가한 뒤에도 그 날짜는 남는다 (같은 날 둘셋을 여는 일이 흔하다)

> **그대로 둔 사소한 차이** — 수업 화면의 「오늘 남은 일」이 시안은 2×2 타일, 실서비스는 세로 4줄.
> 날짜 네비 위치도 다르다 (시안은 상단바가 비어 있고, 실서비스 상단바는 메뉴 7개가 찬다).

### 상담 일지 · 강의자료 (2026-08-09) — 사용자 요청

**둘 다 컬렉션을 늘리지 않았다.** 학생 메모·수업 메모가 낸 길을 그대로 따라간다.

#### 상담 일지 — 학생 상세 연대기의 「상담」 갈래
시안 03의 연대기에 「상담」이 있었지만 도메인에 상담 «기록»이 없어 비어 있었다
(`DATA.consults`는 홈페이지로 들어온 «신청»이지 나눈 이야기가 아니다).
`kv/consultlog:{학생id}`에 `{date, kind, text, at}` 배열 하나로 만들었다.

- **고정 메모와 다른 것은 «날짜가 붙는다»는 점이다.** 상담은 사건이라 연대기에 쌓이고,
  메모는 «늘 참인 한 가지»라 옆에 붙어 있다. 둘은 서로를 대신하지 못한다
- 방식은 셋(전화 · 대면 · 문자). 갈래를 고르면 그 자리에 폼이 붙는 기존 장치를 그대로 쓴다
- **연대기 점은 `k-consult`(강조색)** — 시안이 상담에 물린 색과 같다
- **조교에게는 갈래가 없다** — 학부모와 이야기하는 것은 강사의 일이다
- **변경 이력에 내용은 남기지 않는다** — 「상담 기록 · 2026-08-09 전화」까지만 (학생 메모와 같은 판단)

#### 강의자료 — 반 관리 > 자료 · 학생앱 학습 > 자료
**워커는 손대지 않았다.** `/upload`는 원래 파일 종류를 가리지 않고 드라이브에 올리고
공개 권한까지 붙인다 — 이미지만 그 길을 쓰고 있었을 뿐이다.

| | |
|---|---|
| 저장 | `kv/material:{반id}`에 `{title, name, size, mime, url, fileId, at}` 배열. 파일 자체는 드라이브에 있다 |
| 배포 단위 | **반**이다. 과제·영상과 같다 — 「누구에게」를 따로 묻지 않는다 |
| 상한 | **15MB**. 워커가 본문을 메모리에 쌓아 올리므로 무제한으로 두지 않는다 |

- ⚠ **`uploadImageToDrive`를 쓰지 않는다.** 그건 JPEG로 압축한다 — PDF·한글 파일을 압축할 수는 없다.
  `uploadFileToDrive`를 따로 뒀다
- ⚠ **워커가 돌려주는 `url`은 썸네일이라 이미지에만 맞다.** 문서는
  `drive.google.com/file/d/{fileId}/view`로 열어야 한다 → `materialLink()`가 종류를 보고 고른다
- 학생 화면은 **앱 안에서 파일을 열지 않는다.** 새 창으로 넘긴다 — PDF·한글을 폰에서 여는 것은
  기기가 훨씬 잘한다
- 지우면 **드라이브에서도 지운다**(`deleteFromDrive`). kv에서만 지우면 파일이 남아 쌓인다

> ⚠ **업로드는 로컬에서 확인할 수 없다** — `ALLOW_ORIGIN` 때문에 127.0.0.1에서는 워커를 못 부른다
> (9절 1번). 화면·목록·링크 조립까지는 로컬에서 확인했다.
>
> ### 배포본 확인 완료 (2026-08-09)
> 처음 올려 봤을 때는 **502**였는데 원인이 이 기능 밖에 있었다 — 워커의 드라이브 리프레시
> 토큰 만료(9절 2-B). 사용자가 동의 화면을 프로덕션으로 게시하고 토큰을 재발급한 뒤
> **끝까지 확인했다**: 자료 올리기 → 관리자 목록 → **학생 앱 카드** → 삭제(드라이브·kv 둘 다).
> 링크는 문서라 `file/d/{id}/view` 형태로 나갔다.

### ▶ 다음 차례

> ### 2026-08-19 이후에 여기서 시작한다
> **08-19에 H-1·H-2를 고쳐 배포까지 끝냈다**(맨 위 📌). **H 계열에 남은 것은 H-3 하나뿐이고
> 그건 사용자에게 물어야 한다.** 그래서 **다음 첫 일은 아래 표의 C 또는 0-B다** —
> 둘 다 «코드가 아니라 배포본에서 눌러 보는» 일이다.
>
> 아래 08-14/08-15 상자는 그대로 둔다 — 표의 나머지 줄(C · 0-B · 0-D · L · 2번)은 유효하다.
>
> ---
> **2026-08-14에 열 커밋. 전부 push 완료. 워커는 2026-08-12판 그대로다.**
> AI 한도는 **3/20**만 썼다 (리셋은 한국시간 오전 9시 — 워커가 날짜를 UTC로 본다).
>
> **✅ 브라우저 확장이 붙었다.** 어제까지 「못 봤다」로 남던 것들이 전부 풀렸다.
> **배포본을 눈으로 보는 일은 이제 막히지 않는다.**
>
> 그날은 **셋으로 갈린다** —
> ① **밀린 확인**을 끝냈다: 0-A(조교) · 0-C(표) · 0-E(화면). 그 김에 버그 셋을 고쳤다
>    (좌석 배치도가 «보기 표»로 오판 · 일괄 생성에 멈춤 버튼 없음 · 고아 오답숙제)
> ② **과제를 «내고 · 확인받는» 흐름으로** 새로 만들었다 (사진 제출 → 수락/반려/현장 확인)
> ③ **클리닉을 네 번 고쳤다** (범위로 열기 · 여러 시간 신청 · 주 단위 정리 · 자리 매칭)
>
> 끝난 것 — 0 · **0-A** · **0-C** · 0-E · 0-F · 0-G · 1번 · **L의 교재**.
> `test22`(광남고 20문항)가 올라와 있고, 08-16에 클리닉 여덟 자리가 서 있다.
>
> **⚠ 그날 만든 것은 대부분 «로컬 검증»까지다.** 새 기능 둘(과제 제출·클리닉)은
> 아래 **H**·**C**로 남겨 뒀다 — **배포본에서 눌러 보는 것이 내일의 첫 일이다.**
>
> | 다음에 할 만한 것 | 왜 |
> |---|---|
> | ~~**H** 과제 제출을 배포본에서 한 바퀴~~ | **08-16에 끝났고 거기서 나온 H-1·H-2도 08-19에 고쳤다** |
> | **C** 클리닉 — 범위 제안 승인 하나 | **이제 이게 첫 일이다.** 나머지는 사용자가 이미 확인했다(자리마다 정확히 반영). **승인이 열린 자리에 앉는지**만 남았다 |
> | **0-B** 데일리퀴즈 «범위 안»에서 세 문제 | **재료가 갖춰졌다** — `test22` 20문항이 올라와 있고 단원도 붙었다. 시험 범위만 맞추면 된다 |
> | **0-D** 단원 AI 판정 | **근거가 생겼다 — 낱말 대조가 20문항 중 15개만 맞혔다**(4·5·7·12·20번 미지정). 워커에 엔드포인트 하나, `guessChapter` 하나만 갈아끼우면 된다 |
> | **L** 랜딩 마무리 | **재료를 기다리는 중.** ⚠ **원칙 셋의 «딸린 설명»이 아직 확인 안 됐다** — 지금 공개돼 있다 |
> | **2번** 비밀번호 평문 저장 | **지금 남은 것 중 제일 큰 구멍.** 손대기 전에 설계부터 이야기해야 한다 |
>
> **⚠ 사용자가 아직 안 해 본 것 — 시험 일정에 실제 값이 하나도 없다.**
> 확인은 전부 «메모리에만 꽂아» 했고 **실 DB에는 한 줄도 안 썼다.** 그래서 지금 열면
> 모든 줄이 「날짜 없음」이고 시즌 고르개에는 「지금」 하나뿐이다(보관본이 아직 없다).
> **첫 시즌 종료를 누르는 순간 처음으로 보관본이 생긴다.**
>
> ⚠ **손으로 확인할 것 셋** (제가 못 하는 것) —
> ① **태블릿 실물**로 학생 화면 한 번 (620/680에서 손가락 조작이 어떤지)
> ② **랜딩 원칙 셋의 설명 줄** — 큰 문장은 사용자의 말이지만 **아래 세 줄은 내가 이었다** (9절 8번)
> ③ **폰으로 과제 사진 올리기** — 한 장은 됐다. **여러 장 한꺼번에 고르기·촬영**은 아직이다
>
> ⚠ **네이티브 `confirm()`을 쓰는 버튼은 확장이 못 누른다** (삭제류 · 일괄 생성 확인).
> 화면을 멈춰 버리므로 그런 것은 사용자가 직접 눌러야 한다.
>
> ⚠ **지금 배포본의 데이터 상태**
> · **시험지** — `test22` 하나(20문항, 1번만 쌍둥이가 있고 「1차 검토 대기」).
>   9번 좌석 배치도는 **저장된 내용이 아직 깨진 채다** (고친 파서는 다음에 올리는 것부터 든다)
> · **과제** — 두 건. 「ㅁㄴㅇ」에 테스트학생이 **사진을 올려 제출해 둔 것**이 있다
>   (그래서 벨에 「확인 대기」가 떠 있다). 수락·반려를 눌러 보는 데 쓰면 된다
> · **클리닉** — 08-16에 오후 2시~9시 여덟 자리(정원 20). 두 학생이 잡아 2·3·4시가 2/20
> · **오답숙제** — 테스트학생의 **고아 15건은 그대로 둔다.** 코드가 걸러내므로 화면에는 안 보인다

H. ~~**과제 제출·승인 — 배포본에서 한 바퀴**~~ — **2026-08-16에 끝냈다.** → 위 ✅ 상자
   수락 · 반려(사유 필수 포함) · 현장 확인 · 완료 취소 · 반려 취소 · 벨 착지 ·
   조교 자리 · 옛 기록 흡수 · 사진 확대까지 전부 배포본에서 눈으로 봤다. **흠 둘이 남았다.**
   - ⚠ **사진이 쌓이는 속도를 지켜볼 것.** 한 과제 × 재원생 × 6장이다.
     과제를 지우면 같이 지워지지만, 안 지우면 드라이브에 계속 남는다
H-1. ~~**사진 확대에 닫는 문이 없다**~~ — **2026-08-19에 고쳤다.** × · ESC · 겹침 셋 다.
   닫는 일은 `closeHwPhoto()` 하나로 모았다 → 맨 위 📌 ①. 배포본 확인까지 끝났다
H-2. ~~**강사 연대기의 「(삭제된 시험지) n번」**~~ — **2026-08-19에 고쳤다.**
   `studentTimeline`도 `liveWrongHomework(rec)`를 쓴다 → 맨 위 📌 ②.
   **데이터 15건은 그대로 둔다** (08-14 결정). 배포본에서 0건 확인
H-3. **연대기의 「퀴즈」 줄에 같은 구멍이 남아 있다** — `dailyQuiz.log`(9248행 언저리)도
   문항이 없어지면 「(삭제된 시험지) ?번」으로 선다. **08-19에 일부러 안 고쳤다.**
   오답숙제는 «해야 할 일»이라 없어지면 그만인데, 퀴즈 로그는 «학생이 실제로 푼 기록»이다.
   막으면 학습 이력이 화면에서 사라진다. **어느 쪽인지 사용자에게 물어야 한다.**
   ⚠ 지금은 테스트학생의 퀴즈 로그가 0건이라 **화면에 안 보인다** — 급하지 않다
C. **클리닉 — 배포본에서 남은 것 하나** (2026-08-14에 네 번 고쳤다) → 위 📌 ④⑤⑥
   - ~~시간대를 범위로 열기~~ · ~~학생이 여러 시간대를 골라 신청~~ —
     **사용자가 배포본에서 이미 해 봤고 데이터로 확인했다.**
     08-16에 2시~9시 여덟 자리가 서 있고, 두 학생이 «2시~4시 3개»·«2시~9시 8개»로 잡아
     겹치는 2·3·4시가 2/20, 나머지가 1/20으로 **자리마다 정확히 반영**돼 있다
   - **남은 것은 「희망 시간 범위 제안」을 승인해 보는 것뿐이다.**
     그때 **이미 열린 자리에 앉는지**(새 자리가 안 생기는지)를 본다 — 로컬에서는 확인했다
   - ⚠ 「마지막」 시각에도 자리가 하나 선다 — 뜻이 갈리는 자리다.
     쓰다가 어색하면 «끝나는 시각»으로 바꿀지 정하면 된다
   - ~~옛 표기(「저녁 7시」)~~ — **정리했다.** 실 DB에 자유 문장 시간은 없다
L. **랜딩 마무리 — 골격은 섰고 재료를 기다린다** (2026-08-13 재구성) → 6절 「랜딩을 다시 세웠다」
   - ~~교재 사진~~ — **2026-08-13에 사용자가 준 목업 PNG 세 권으로 넣었다.**
     ⚠ 개념서는 «스프링 제본으로 뽑으면 좋겠다»고 했다가 **그냥 가기로 정했다** — 다시 꺼내지 말 것.
     남은 것은 **펼친 내지** 사진뿐이다 — 있으면 「직접 선별한 문항」을 눈으로 보여줄 수 있다
   - **원칙 문장 셋을 사용자가 읽고 확정** — 큰 문장은 말씀을 한 겹 다듬은 것이고
     딸린 설명 세 줄은 내가 이은 것이다. **틀린 뉘앙스가 있으면 그 줄부터 고친다** (9절 8번)
   - **「생각의 흐름」에 부를 이름** — 메가스터디의 「3단계」·「3 in 1」에 해당하는 자리다.
     수업에서 이미 쓰시는 말이 있으면 그게 제일 좋다
   - **클리닉** — 「관리도 열심히, 클리닉도 많이」가 원칙 03의 절반인데 지금 페이지에 한 줄뿐이다.
     주 몇 회·누가 오는지·무엇을 하는지를 들으면 한 칸을 세울 수 있다
   - **장치 섹션의 화면 캡처 3장** — 지금은 글만 있다. 강사 검토 · 학생 오답숙제 · 데일리퀴즈를
     배포본에서 찍어 넣으면 「말이 아니라 실물」이 된다
   - **대상·지역** — 고1~고3 · 광진 일대로 보이는데 명시 범위를 정해야 한다. 학부모의 첫 필터다
0. ~~화면에서 (B) 한 바퀴~~ — **2026-08-11 밤에 끝냈다.** ①②③④ 전부 의도대로 돌았다.
   자세한 것은 맨 위 ✅ 상자. 아래 셋은 다음에도 그대로 쓸 수 있는 요령이라 남긴다.
   - **`figureSpec`이 시원찮으면** 검토 화면에 편집칸이 이미 있으니 강사가 직접 쓴다.
     프롬프트의 [figureSpec 작성 형식]만 손보면 된다 — 거기 한 곳이다
   - ⚠ **«세는 문제»의 정답은 검산해야 한다** — 지도 색칠이 세 번 다 다른 답을 냈다 (위 경고).
     그래프·도형처럼 **식으로 푸는 것**은 이번에도 두 번 다 맞았다
   - **워커를 화면 없이 눌러 보는 길이 있다** — `node tools/probe-worker.mjs cases.json`.
     익명 로그인으로 토큰을 받아 Node에서 부른다 (CORS는 «브라우저» 제약이라 안 걸린다).
     **`/quota`만 볼 때도 이 길이 제일 빠르다** — 한도를 안 쓰고 남은 건수를 알 수 있다.
     cases.json 만드는 법은 그 파일 머리말에 있다 (`.gitignore` — 시험지 본문이라 안 올린다)
0-E. ~~「AI로 다시 생성」 뒤에 빈 큐를 마주한다~~ — **2026-08-13에 고쳤다.**
   그림 업로드 쪽에 두 벌로 있던 같은 처리까지 **`followBankToQueue()` 하나로 모았다**
   → 위 「큐를 문항 따라 옮긴다」. 소스에서 떼어 여섯 경우로 확인했다.
   **화면 확인만 남았다** (위 시작 상자 ③).
0-F. ~~그림 초안을 기계가 낸다~~ — **2026-08-12에 1·2·3단계 다 끝났다. 배포·배포본 확인까지 완료.**
   `figure.js` · `tools/figure-preview.mjs` · `tools/probe-figure.mjs` · 워커 `/figure` ·
   검토 화면 「초안 그려 보기」 → 6절 「그림을 코드로 그린다」. **남은 것은 아래 둘이다.**
   - ⚠ **(B)가 싸지면 프롬프트의 억제력이 사라진다.** 지금 「(C)→(A)→(B) 순으로 따져라」가
     먹히는 이유의 절반은 «(B)를 고르면 사람이 그려야 한다»는 대가다. **갈래 분포를 다시 재 볼 것**
   - 다음에 넓힐 곳 — 스키마 v1은 `kind:'graph'` 하나다. **v2 후보는 «좌표평면 위의 도형»**
     (선분·원·각 표시). 지도·입체는 계속 사람 몫이다
0-G. ~~`dbGet`이 실패를 «비어 있음»과 구분하지 않는다~~ — **2026-08-12에 고쳤고 배포본에서 확인했다**
   → 6절 「기록을 지우는 두 가지 길을 막았다」. 학생으로 제출까지 해 봤고 차단 경고는 안 떴다
   (= 새 문이 정상 경로를 막지 않는다).
0-A. ~~조교 여러 반 — 배포본에서 눈으로~~ — **2026-08-14에 끝났다.**
   옛 형식(`classId`)은 **감사 로그가 증명한다** — 2026-08-11 「김하현 · **테스트반** →
   테스트반2 · 고20.5B」. 단일 `classId` 상태에서 읽혀 변경까지 됐다.
   조교 화면에 「담당 반 3개」·**「반」 열**·**칩 고르개**(전체 4 · 테스트반2 0 · 고20.5B 1 ·
   테스트반 3)가 서고 숫자도 실제 배정과 맞는다. 교사쪽 설정 > 조교도 정상.
0-B. **데일리퀴즈 전체 흐름 — 배포본에서 한 바퀴.** 로컬에서는 저장 경로를 막은 채 확인했다.
   순서 — **설정 > 시험 범위**에서 시즌 맞추기 → **반 관리 > 진도**에서 반 과목 정하기 →
   시험지 등록(**문항마다 단원**) → 학생이 틀린 번호 등록 → 쌍둥이 생성·승인 →
   **오답숙제 제출** → 그때 퀴즈에 뜬다.
   - **뒤쪽 절반은 2026-08-11 밤에 봤다** — 오답 등록 → 오답숙제 카드 → 제출 → **자동 채점**까지
     배포본 실데이터로 확인. 그 뒤 데일리퀴즈가 **「범위 밖으로 접어 둔 문제 2개」**로 접는 것도 봤다
   - **아직 못 본 것은 «범위 안»으로 들어왔을 때 실제로 세 문제가 뽑히는가**다.
     시즌을 그 단원이 든 것으로 맞추거나 **설정 > 시험 범위**에서 범위를 넓혀 놓고 다시 본다
   - **날짜가 바뀔 때 새로 뽑히는지**는 하루 지나야 안다 (로컬에서는 `todayStr()`을 갈아끼워 확인했다)
0-C. ~~표 모양 재확인~~ — **2026-08-14에 끝냈다.** 그림 쪽은 2026-08-11 밤에 봤고,
   표는 `test22`를 다시 올려 훑었다. **조건 상자 다섯(8·9·16·17·18)은 «상자»로 정상**,
   그런데 **9번 좌석 배치도가 무너져 있었다** → 파서를 고쳤다
   (6절 「자리표는 선지가 아니다」). ⚠ 배포본에 저장된 9번은 **아직 깨진 채다** —
   내용은 파싱 «시점»에 평문으로 박히므로 다시 올려야 고쳐진다
0-D. **단원 AI 판정** — 지금은 낱말 대조(`guessChapter`)다. 워커에 엔드포인트를 하나 더하고
   재배포하면 품질이 오른다. **그 함수 하나만 갈아끼우면 된다.** 로컬 검증이 안 되므로
   배포본에서 바로 확인해야 한다 (9절 1번)
1. ~~학생 화면 태블릿 대응~~ — **2026-08-12에 끝냈다** → 아래 「학생 화면을 태블릿 폭으로 푼다」.
   남은 것은 **실제 기기로 한 번 보는 것**뿐이다 (측정은 진짜 뷰포트로 했지만 손가락은 못 재 봤다).
2. **비밀번호 평문 저장** (9절 3번) — **지금 남은 것 중 제일 큰 구멍이다.**
   학생 `pw` · 조교 `pw` · `teacher-pw`가 전부 Firestore에 그대로 있다.
   강사는 아이디도 없이 비밀번호 하나뿐이라 «누가 시도했는지»도 안 남는다(4-D).
   워커 인증 때 미룬 「강사만 접근」(커스텀 클레임) 문제와 맞닿아 있어서
   **손대기 전에 설계부터 이야기해야 한다** — 화면 여러 개가 같이 움직인다
3. ~~워커 재배포~~ · ~~AI 생성 배포본 확인~~ — **2026-08-10에 둘 다 끝났다.**
   워커는 그날 두 번 배포했고(그림 프롬프트 · `/quota`), AI 경로는 실제로 불러
   **429까지 받아 봤다** (그래서 한도 어긋남을 찾았다). 남은 것은 «생성 품질»뿐 → 0번
4. 9절의 나머지 — `href` 없는 `<a onclick>`의 키보드 접근 · state만 갱신하는 입력칸의 커서 튐
5. 태블릿 다음에 볼 만한 것 — 학생앱 홈·성적을 **태블릿에서만 2열**로 (1번을 실제 기기로 본 뒤 판단).
   같이 볼 것: **폰에서 그림 눈금이 9.5px**다. 캔버스를 480으로 좁히고 `width:100%`로 올려 쓰면
   11px가 되는데, **이름표 자리 규칙을 다시 재야 한다** (`figure-preview.mjs`로 배포 없이 볼 수 있다)
6. **옛 시험지는 표가 안 살아난다** — 파서를 고치기 «전»에 올린 것은 본문이 이미 뭉개져
   저장돼 있고 원본 hwpx를 보관하지 않는다. **지우고 다시 올리는 것이 유일한 방법이다**

> **시안에 있으나 못 만드는 것** (물어보면 이렇게 답한다)
> - 공지 「읽음 현황」 — 읽음이 학생 브라우저 localStorage에만 남는다
> - 조교 권한 토글 — assistant 레코드에 권한 필드가 없다
> - AI 「유사도 하한」 — 유사도 점수라는 것이 아예 없다 (시안에서 지어낸 수치)
> - 학생 상세의 「과제 추가」·「보충영상 배정」 — 과제는 반 단위로만 배포되고,
>   개인 영상은 결석을 찍을 때 함께 등록된다 (경로를 두 벌로 만들지 않는다)
>
> ~~학생 「고정 메모」~~ — **2026-08-08에 만들었다.** 도메인에 없으면 못 만드는 것이 아니라,
> «컬렉션을 늘리지 않고 kv 문서 하나로 되는가»를 먼저 물으면 되는 것이 있다 (수업 메모가 그 선례였다).
>
> ~~데일리퀴즈 정답률~~ — **2026-08-09에 만들었다.** 퀴즈에 정답이 없던 것이 문제였는데,
> **문제를 내는 곳을 바꾸니**(교사 작문 → 학생 오답) 정답이 딸려 왔다.
> 못 만드는 것처럼 보이는 지표가 «데이터가 없어서»가 아니라 «엉뚱한 데서 뽑고 있어서»일 수 있다.

**리디자인은 여기서 끝났다.** 이 다음부터는 «디자인 이식»이 아니라 제품 개선이다.

> **수업 화면과 반 관리 > 수업 기록의 출결 중복은 그대로 둔다.** 겹쳐 보이지만 역할이 다르다 —
> 수업은 «지금 하는 수업을 4버튼으로 빨리 찍는 곳», 수업 기록은 «지난 날짜를 5상태로 고치고
> 성적·시험까지 넣는 곳». 시안도 둘을 따로 그렸다.

화면 하나를 옮기는 절차는 명단·신호·수업에서 확정됐고, 끝까지 그대로 반복했다.
이제 «옮길 화면»은 없지만 **새 화면을 만들 때도 같은 순서**다.
1. `teacherXxxHTML()`를 새로 만들고 `teacherTabBodyHTML()`의 해당 분기를 갈아끼운다
2. 화면별 스타일은 `.app` 블록에 **접두사를 붙여** 둔다 (명단은 `rst-`). ds.css 클래스 이름을 다시 정의하지 않는다
3. 브라우저에서 **행 높이 · 잘림 · 넘침**을 DOM 기하로 잰다. 스크린샷만 보면 놓친다
4. 같은 일을 하던 옛 진입점을 이쪽으로 모은다

학생 화면도 같은 절차를 따랐다. 다른 것은 두 가지뿐이다 —
`STUDENT_TAB_SECTION`이 «화면 → 탭»을 되찾아 주고(그래서 옛 탭 id를 안 바꿔도 된다),
스타일 접두사가 `sap-`이며 셸은 `.app.sap`이다.

### 시안대로 신설할 화면은 남지 않았다
「신호」와 「수업」이 마지막이었다. 상단바 7메뉴가 전부 제 화면을 갖고 있고,
학생 하단 탭 4개도 마찬가지다.

`ds.js`는 **실서비스에 로드하지 않는다.** `fitCanvas`와 «가짜 학생 22명»이 들어 있어 그대로 쓰면 예시 데이터가 제품에 섞인다. 메뉴 정의는 `index.html`의 `TEACHER_NAV`/`TEACHER_SUBNAV`/`STUDENT_NAV`/`STUDENT_SUBNAV`에 따로 있다. 아이콘도 `ICON_PATHS`에 따로 두고, ds.js에서 쓰는 것이 생기면 그때 옮겨 적는다.

---

## 1. 결정 기록 — 막힌 결정은 없다

**2026-08-06에 막혀 있던 결정 3건이 전부 해소됐다.**

| | 결정 | 결과 |
|---|---|---|
| ① | 랜딩 「전환」 문구 | **「짐작으로 가르치지 않습니다」** — 반영 완료 |
| ② | 로고 차콜 지정값 | **`#3F3537`** — 매뉴얼북에서 직접 확인, 반영 완료 |
| ③ | 다크 테마 | **라이트 유지** — 5절 권고대로 확정 |
| 2-C | 구현 방식 | **기존 SPA에 이식** — 아래 참고 |

### 이식과 별개로 남아 있는 것
구현 순서는 **0-B**가 갖고 있다. 여기 있는 것은 그 바깥의 일이다.

1. 2-C에 남은 두 가지 — 렌더링 구조(전체 재렌더링 유지 여부) · 관리자 모바일 대응 수준.
   **이식을 끝까지 «전체 재렌더링 유지»로 했고 막히지 않았다.** 다만 그 구조 때문에 화면마다
   대비를 해 둬야 했다 — 채팅은 열릴 때 맨 아래로 보내고(9절 아님, 0-B), 랜딩은 한 번 드러난 뒤
   다시 페이드인하지 않게 막고, 검색창은 `focus()`로 손보고 있다(9절 4번).
   관리자 모바일은 여전히 미정이다
2. 랜딩 5안 기각 사유 청취 → 필요하면 랜딩 재검토.
   **「하루」로 합류를 끝냈으므로 사실상 닫혔다** — 다시 꺼낸다면 그때 듣는다
3. ~~`AI_SITE_TOKEN` 회전~~ → **3단계만 남았다.** 0-B의 «다음 차례» 1번

> **①·② 반영 결과는 확인했다** — 랜딩 `h2`가 데스크톱 62px / 모바일 29px에서 «짐작으로 / 가르치지 않습니다» 두 줄로 정확히 접힌다. 잘림·부모 침범·가로 스크롤 0건.

---

## 2. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 저장소 | `github.com/khhyun827-lang/hahyunmath` (main) |
| 로컬 | `C:\Users\hahyun\Desktop\claude work` |
| 추적 파일 | 시안·로고·도구까지 전부 추적 중 (2026-08-06 커밋). 예전엔 `index.html` 등 3개뿐이었다 |
| index.html | 4,875줄 / 322KB. CSS·JS·HTML·데이터가 전부 한 파일 |
| 빌드 도구 | 없음 (package.json / 번들러 / 프레임워크 전무) |

### 2-A. 기술 스택
- **렌더링**: 프레임워크 없는 순수 JS SPA. 템플릿 리터럴 → `app.innerHTML = html` 로 **전체 화면 통째로 재렌더링** (`render()`, index.html:2925)
- **인증**: Firebase Auth (compat SDK v10.12.2)
- **DB**: Firestore를 SDK가 아닌 **REST + fetch**로 직접 호출 (index.html:497-501). WebChannel 차단 이슈 회피 목적이라 주석에 명시됨
- **수식**: KaTeX 0.16.11 (`$...$` 델리미터)
- **파일**: SheetJS(xlsx), JSZip 지연 로딩 + HWPX 파서 직접 구현 (index.html:2424-2721)
- **AI**: Cloudflare Worker(Gemini) 프록시로 유사문항 자동 생성, 일 20건 제한
- **이미지**: Google Drive 업로드 프록시 + 클라이언트 압축
- **라우팅**: 해시 기반 (`#teacher/home`, `#student/quiz`, `#assistant/students`)

### 2-B. 데이터 구조 (리디자인해도 반드시 보존)
Firestore `kv/` 하위 **문서-당-레코드 컬렉션**. 구버전 blob은 `migrateBlobToCollection()`이 자동 이관.
```
students / classes / notices / consults / quizzes / clinics / clinicslots
qnas / videos / assistants / exams / problembank / assignments / auditlog
kv/teacher-pw          단일 blob
kv/signal-mutes        {학생id: 'YYYY-MM-DD까지'} — 신호 화면의 «해제»
kv/classnote:{반id}    {날짜: 글} — 수업 화면의 «메모»
kv/record:{studentId}  scores, attendance, assignments, progress,
                       quizAnswers, wrongHomework, videoProgress, assignmentsDone
kv/chat:{studentId}    메시지 배열
```
- `student`: studentId, name, pw, grade(고1/고2/고3), school, parentPhone, phone, classId
- `class`: id, name, schedule(`"월,수,금 7시~9시"`), period, status
- 출석 상태 5종: **출석 / 지각 / 조퇴 / 결석 / 퇴원** (퇴원 시 로그인 차단)
- 오답 검토 3단계: **pending → review1 → approved**
- 클리닉 상태: 대기 / 승인 / 완료 / 취소
- 상수: `UNITS`(7과목), `UNIT_CHAPTERS`(과목별 단원 체크리스트) — index.html:849-861

**유지해야 할 핵심 기능** — 오답 파이프라인(시험지 등록 → 학생 오답 체크 → AI 쌍둥이 문항 생성 → 3단계 검토 → 오답숙제 배포) · 영상 시청률 실시간 트래킹(YouTube IFrame API) · 반별 진도 체크리스트 · 출석 5상태 · 감사 로그 · HWPX/엑셀 일괄 등록 · 조교 권한 분리

### 2-C. 구현 전에 정해야 할 것
- [x] 리디자인을 **기존 SPA 안에 이식**할지, 별도 파일로 새로 만들지
      → **기존 `index.html`에 이식 (2026-08-06 확정).** Firestore REST · HWPX 파서 ·
      오답 파이프라인 · 영상 트래킹이 전부 이 파일에 있고, 그게 이 제품에서 제일 비싼 부분이다.
      화면을 하나씩 갈아끼우면 기능 유실 위험이 가장 낮다
- [ ] 전체 재렌더링(`innerHTML`) 구조를 유지할지, 부분 렌더링으로 바꿀지
- [ ] 관리자 화면의 모바일 대응 수준 (태블릿·폰에서 실제로 쓸 것인지)

---

## 3. 디자인 시스템 — 확정

**기준: 관리자 시안 A3 「학생 도시에」.** 색은 **자체 로고**에서 나온다.
상세 문서는 `design-v1/00-system.html`. 아래는 요약이다.

### 3-A. 색 — 새 색은 만들지 않는다
```
--paper   #FAF9F6   앱 배경 (순백 안 씀)
--card    #FFFFFF   패널·카드
--ink     #22221E   본문
--sub     #5C5C54   보조
--dim     #96968C   흐림
--line    #E7E5DE   패널 경계
--line2   #F1EFE9   내부 구분

--accent      #3F3537   주조 = 로고 차콜. 선택·주요 액션·버튼 (매뉴얼북 지정값)
--accent-d    #2A2325
--accent-bg   #ECE9E6
--accent-line #D5D0CA

--brand       #BA5054   브랜드 = 로고 와인. UI 강조에 쓰지 않는다
--brand-d     #9E4246
--brand-gray  #A8A4A4   로고 워드마크 KIM HA HYUN MATH

상태색 (의미 있을 때만)
--ok    #3F7A4D 출석   --late  #9A7000 지각   --leave #6B46A8 조퇴
--no    #B03A2E 결석   --gone  #96968C 퇴원   --blue  #39607F 성적

--note #FDFAEF / --note-line #EDE3C6   «사람이 적은 것»에만
```

> **와인을 UI 강조색으로 쓰지 않는 이유** — 위험 `#B03A2E`와 색상이 가까워
> 고밀도 표에서 «선택된 행»과 «위험한 학생»이 구분되지 않는다. 취향이 아니라 사용성 문제다.

### 3-B. 서체
- **Pretendard** 본문 (관리자 13px / 학생앱·랜딩은 더 큼)
- **Noto Serif KR** — «그 화면이 다루는 대상의 이름»에만. 그 외 어디에도 쓰지 않는다
- **JetBrains Mono** — 숫자·시각·ID·전화 전부. `tabular-nums`

### 3-C. 반경 · 모션 · 인터랙션
```
--r-xs 5 / --r-sm 7 / --r-md 9 / --r-lg 12 / --r-xl 14 / --r-pill 20
--t 140ms cubic-bezier(.4,0,.2,1)
```
- 선택 = **accent 배경 + 좌측 3px accent 바**. 리스트·표·큐 어디서나 같은 모양
- hover = 웜 그레이 틴트 한 단계. 그림자·확대 없음
- 포커스 = accent 링 3px @ 12%. 절대 지우지 않는다

### 3-D. 로고
`로고/` 폴더. 2024-01 PAPERFIT 제작, 매뉴얼북 있음.
- 가로형/세로형 × 3종 — (1) 컬러 (2) **흰색 녹아웃** (3) 단색
- 심볼은 **곡선 + 붉은 점** (삼차함수 그래프처럼 보인다). 랜딩 척추의 점이 이 점의 반복이다
- **브랜드 이름은 로고로만 말한다.** 같은 이름을 서체로 또 쓰지 않는다

---

## 4. 만들어진 화면

### 4-A. 관리자 16개 (`design-v1/`, 1440×900)
메뉴 구조 — 상단바·서브탭은 `ds.js`의 `NAV`/`SUBNAV` 한 곳에서 생성된다.

| 메뉴 | 화면 |
|---|---|
| **명단** | `01-roster` → `03-student`(학생 상세) |
| **신호** | `05-signals` |
| **반** | 학생 명단(→01) · `14-log`(수업 기록) · `06-classes`(진도) · `15-quiz`(데일리퀴즈) · `16-videos`(영상) |
| **수업** | `02-class` — 출결 → 진도 → 과제 → 메모 |
| **오답** | `04-review`(검토) · `07-exams`(시험지) · `08-bank`(창고) |
| **소통** | `09-clinic` · `10-notice` · `11-qna` · `12-chat` |
| **설정** | `13-settings` |

각 화면의 «역할»과 «무엇을 흡수/버렸는가»는 `preview.html`과 `00-system.html`에 적혀 있다.

### 4-B. 학생 마이페이지 11개 (`20-student-app.html`, 모바일 390×812)
**기존 11개 탭을 4개로 묶었다. 기능은 하나도 빼지 않았다.**
```
홈    오늘 할 일 · 다음 수업 · 이번 주 요약
학습  데일리퀴즈 · 오답숙제 · 진도·과제 · 강의시청
기록  성적 · 출석
소통  공지사항 · 질의응답 · 클리닉 신청 · 채팅
```
하단 탭바에 들어가는 것은 4~5개가 한계다. 11개를 가로로 밀게 하면 뒤쪽은 아무도 안 누른다.

**실서비스 이식 완료 (2026-08-07).** 시안과 다르게 간 곳은 0-B의 「학생 마이페이지 이식 결과」에 있다.

### 4-C. 랜딩 (`landing-v2/index.html`) — 컨셉 「하루」
**이 학원이 일하는 하루를 방문자가 목격한다.**
기존 5안이 전부 «수학»을 소재로 썼다면, 이번은 «서비스»를 소재로 쓴다.

```
도착   로고 + 오늘 날짜(자동) + 첫 로그가 접힌 채로 보임
하루   16:00 수업 / 19:00 수업 / 21:10 수업 / 21:14 검토 / 23:41 질문
       ──── 다음 날 ────
       09:12 답변          ← 날짜가 넘어가는 이 한 칸이 핵심
전환   차콜 풀블리드 + 흰 세리프 한 문장   ← 유일하게 큰 소리 「짐작으로 가르치지 않습니다」
사람   메모 종이 + 인물 컷아웃
행동   상담 신청
```
- 시각·행위 종류만 쓰고 **수치를 넣지 않았다** — 공개 페이지의 숫자는 방문자가 사실로 읽는다
- 16시·19시·21시10분은 **실제 반 시간표**다
- 콘텐츠는 `CONTENT` 객체 한 곳에 모여 있다. 문자열만 갈면 그대로 돌아간다
- **경력·성과 자리는 주석으로 미리 잡아 뒀다** (`CONTENT.credits`). 나중에 끼워 넣으면 레이아웃이 깨진다

**실서비스 합류 완료 (2026-08-07).** 다섯 장면에 **들은 말 · 기록 · 공지**를 더하고 커리큘럼은 뺐다.
합류하면서 시안에서 찾은 버그 둘(히어로 척추 어긋남 · mono 한글 벌어짐)은 **시안에도 같이 고쳤다** —
여기가 「따라 그릴 기준」이라 시안이 틀린 채로 남으면 다음에 그걸 또 베낀다. 상세는 0-B.

### 4-D. 로그인 (`17-login.html`) — 상태 5개 · **2026-08-08 신설**
**역할: 누구로 들어갈지 고르고 들어간다. 그 이상은 하지 않는다.**
1440 캔버스로 그리지 않았다 — 폰과 데스크톱에서 «같은 한 칸»이라 캔버스를 씌우면 거짓이 된다.

- **문 셋을 동등하게 두지 않았다.** 지금은 학생/조교/강사가 같은 크기의 탭 셋이다.
  그런데 오는 사람은 거의 전부 학생이고, 조교는 계정이 아직 없고, 강사는 한 명이다.
  → **학생 폼이 곧 화면**, 조교·강사는 아래 작은 글. 공개 홈이 이미 같은 비중이다
- **오류는 칸 «위»에 온다.** 지금은 버튼 아래라, 다시 입력하려고 올라가면 안 보인다
- **「최초 비밀번호 설정」을 제 상태로 그렸다.** 지금은 힌트 한 줄로 지나가는데,
  여기서 정한 값은 되찾을 수 없다. 「한 번 더」 확인 칸을 **새로 넣었다** — 순수 클라이언트 검증이라
  도메인에 없는 기능을 만드는 것이 아니다
- **강사 로그인에는 아이디가 없다.** 비밀번호 하나뿐이고, 그게 지금 제품의 사실이라 그대로 그렸다.
  다만 약점이다 — 누가 시도했는지가 남지 않는다. 9절 3번과 함께 다룰 일이다
- ⚠ **지금 로그인 폼은 Enter가 안 먹는다** (`<form>`도 keydown도 없다). 옮길 때 같이 고친다

### 4-E. 조교 (`18-assistant.html`) — 화면 2개 · **2026-08-08 신설**
**역할: 담당 반 하나의 성적·출결·과제·오답을 넣는다.**

> **핵심 판단 — 조교 화면은 «따로 만드는 화면»이 아니라 «권한으로 좁힌 관리자 화면»이다.**
> 지금 구버전이 남아 있는 이유가 «DS 학생 상세는 기본정보·삭제까지 열어 두기 때문»인데,
> 그건 화면의 문제가 아니라 **권한의 문제**다. 화면을 두 벌로 두면 앞으로 모든 변경을
> 두 번 해야 하고, 반드시 한쪽만 고쳐진다 (신호 규칙을 `signalOf()` 하나로 합친 것과 같은 이유).

- **상단바에 메뉴가 없다.** 갈 곳이 하나뿐인데 메뉴를 만들면 그게 거짓말이다.
  대신 「조교」 뱃지와 담당 반 이름을 둔다 — 자기가 누구로 있는지가 늘 보여야 한다
- **권한을 화면이 먼저 말한다.** 「성적·출결·과제·오답을 넣을 수 있습니다. 학생 등록·삭제, 반 이동,
  개인정보 수정은 강사만 할 수 있습니다.」 눌러 보고 나서야 «안 되는구나»를 알게 하지 않는다
- **명단** — 01-roster에서 조교가 손댈 수 없는 열을 뺐다.
  반·반 진도(반이 하나다) · 신호 등급(05가 전담) · 시청률(영상은 강사가 올린다).
  남은 열은 전부 조교가 «넣는» 것이다. 일괄 작업도 없다
- **학생 상세** — 03-student의 연대기를 그대로 쓴다. 갈래는 일곱이 아니라 넷(전체·성적·출결·과제·오답).
  우측 «빠른 작업» 칸은 통째로 없다 (연락처·기본정보·삭제·채팅이 전부 강사의 것이라 빈 칸이 된다) → 3열이 아니라 2열
- **신호는 보이되 «해제»가 없다.** 상태를 아는 것은 조교에게도 필요하지만,
  규칙을 다루는 것은 강사의 판단이다

---

## 5. 다크 vs 라이트 — **라이트 유지로 확정 (2026-08-06)**

**결정: 라이트 유지. 다크는 «화면별»이 아니라 «제품 전체 테마»로만 — 지금은 하지 않는다.**
아래는 그 근거이며, 나중에 다크를 다시 꺼낼 때 읽으면 된다.

1. **오답 검토는 흰 배경 콘텐츠를 다루는 화면이다.** 시험지 스캔·HWPX·수식·학생 답안이 전부 흰 바탕이다. 어두운 UI에 흰 패널이 박히면 대비가 극단으로 벌어져 오히려 눈이 더 피로하다. 다크가 편한 것은 «화면 전체가 어두울 때»뿐이다.
2. **하루에 명단 → 수업 → 검토를 계속 오간다.** 한 화면만 다크면 이동할 때마다 명순응·암순응이 일어난다.
3. 사용자가 정한 최우선 원칙이 «하나의 제품처럼 보이는 것»이다.

전체 테마로 간다면 `:root` 색 값만 갈아끼우면 되어 구조적으로 싸다. 다만 **상태색 5종과 메모 종이색의 다크 대응은 새로 설계**해야 한다. **우선순위는 낮게.**

---

## 6. 결정 기록

### 6-A. 확정
- ~~**`index.html` 수정 금지**~~ — 설계 기간의 규칙이었다. 2026-08-06 이식 착수와 함께 해제됐다 (0-B)
- 기존 디자인을 개선하지 않는다. 완전히 새 방향
- 브랜드 성격: 학원이 아니라 **개인 브랜드**. Premium / Modern / Minimal / Trustworthy / Sophisticated
- 피할 것: 흔한 학원 홈페이지, 과도한 카드 나열, AI가 만든 것 같은 랜딩, 복잡하고 촌스러운 디자인
- **관리자 시안 A3를 디자인 시스템의 기준으로 확정** — 버튼·카드·색·폰트·간격·아이콘·인터랙션 전부
- 다른 시안은 화면을 합치지 않고, **화면별 역할을 먼저 정의한 뒤 그 역할에 맞는 UX만** A3 언어로 번역해 흡수
  - 전체 학생 관리 → **A1**(고밀도 표·저장된 뷰·일괄 작업) / 수업 진행 → **A2**(단계형 워크플로·큰 터치)
  - 학생 상세 → **A3** 그대로 / 오답 검토 → **A4**(큐·2열 비교·키보드) / 신호 → **A5**(근거 문장)
- **버린 것**: A4의 다크 테마, A5의 3열 칸반 보드, A1의 파랑 선택색·IBM Plex, 이모지 아이콘
- **주조색을 로고 차콜로 교체** (원래 올리브 `#4E5A42`였다 — 로고를 못 보고 정한 내 실수)
- **로고 지정색 2개는 매뉴얼북 PDF에서 직접 확인함 (2026-08-06)** — 차콜 `#3F3537` · 와인 `#BA5054`
  - 매뉴얼북은 **텍스트가 전부 아웃라인 처리**돼 있어 문자열 추출이 안 되고, Chrome PDF 뷰어는 이 파일에서 렌더러가 멈춘다(두 번 겪었다). 뷰어로 읽으려 하지 말 것
  - 대신 **PDF 콘텐츠 스트림을 zlib로 풀어 «칠해진 사각형»의 색과 면적을 쟀다.** 스크립트는 스크래치패드의 `pdfswatch.js` / `pdfrects.js`
  - p6이 컬러 지정 페이지 — 같은 높이(219.5)의 색면 두 개가 나란히 있다. 이게 지정값이다
  - p7의 `#9E9E9E`는 **배경 사용 규정 페이지의 타일**이지 워드마크 회색이 아니다. `--brand-gray #A8A4A4`는 그대로 둔다
  - 검증: 이 방법으로 뽑은 와인이 사용자가 정정해 준 `#BA5054`와 정확히 일치했다
  - `--accent-d`는 기존 비율(×0.667)을 그대로 적용해 `#2A2325`로 재계산. `--accent-bg`/`--accent-line`은 accent의 틴트가 아니라 **paper 계열 웜 뉴트럴**이라 손대지 않았다
- **랜딩 「전환」 문구 = 「짐작으로 가르치지 않습니다」 (2026-08-06 확정)**
  - 위의 로그가 «증거», 이 문장이 «주장», 아래 메모가 «이유» — 셋이 서로 다른 일을 한다
  - 기각: 「무엇을 틀렸는지 기억합니다」·「가르친 것은 전부 남습니다」는 아래 메모의 «전부 적습니다»와 겹친다.
    「한 명도 같은 방식으로…」는 62px에서 두 줄로 접히며 앞머리가 약해진다.
    「틀린 문제는 두 번 만납니다」는 차점이었으나 오답 시스템 하나만 말한다
  - 후보 5개와 각각의 찬반은 `landing-v2/copy.html`에 그대로 남겨 뒀다
- **AI 유사문항의 정답 형태를 못박았다 (2026-08-07)** — «999 이하 자연수» 아니면 «객관식» 둘 중 하나
  - 학생 답안칸이 받는 것은 숫자 입력과 ①~⑤ 보기 버튼 둘뿐이다. 분수·무리수·음수·문자식이
    정답이면 학생은 풀고도 제출할 방법이 없다. 예전 프롬프트는 «깔끔한 정수 **또는 간단한 기약분수**»를
    허용했는데 기약분수가 바로 그 사례였다
  - 워커 프롬프트(`answerFormRule`) + 받는 쪽 안전망 셋. 상세는 `worker/README.md`
  - **기존 데이터에 이미 어긋난 것이 있었다** — 생성된 7개 중 5개, 그중 2개는 공개 상태였다.
    제일 나쁜 것은 «객관식인데 정답이 숫자 4» — 학생이 ④를 누르면 «④»가 저장되어
    문자열 비교에서 오답 처리된다. **맞혀도 틀린 것으로 채점된다**
- 조교 계정은 **없음**. 필요하면 설정에서 새로 만들어야 함
- 강사 비밀번호는 전달받았으나 **이 파일에 기록하지 않음** (공개 저장소)

### 6-B. 랜딩 5안 — 전부 기각 (보관: `mockups/`)
| | 컨셉 | 히어로 |
|---|---|---|
| 01 | 정리와 증명 | 「정리 1」 한 문장, 이미지·버튼 없음 |
| 02 | 좌표계 | 화면 전체가 좌표평면, 성적 곡선 실시간 드로잉 |
| 03 | 한 사람 | 인물 컷아웃 + 극단적 여백 |
| 04 | 관측소 | 카피 대신 실제 기록 대시보드 |
| 05 | 한 문제 | 실제 문제 1개, 선택지를 누르면 풀이 시작 |

**기각 사유는 아직 못 들었다.** 사용자 말로는 "예쁘지 않아서가 아니라 서비스의 첫인상과 브랜드 경험이 부족해서".
내 진단 — 1·2·5는 «수학»을 소재로 썼고 3은 인물 포트폴리오였다. 아무 수학 강사나 쓸 수 있는 소재라 브랜드가 없었다.
04 관측소는 「하루」와 인접하지만 **대시보드는 성과 자랑, 로그는 태도**라는 점이 다르다.

### 6-C. 현행 index.html 디자인 (참고 — 계승하지 않음)
컨셉 "채점된 답안지". 종이 질감 + 접힌 모서리 + 빨간 첨삭펜.
```
--paper #F5F1EB  --ink #3D3335  --blue-deep #AE4B53(실제는 와인)  --red-pen #6E2A2F
```
> `--blue-deep`이 와인색이었던 것 = **토큰 이름에 색 이름을 넣으면 안 되는** 실제 사례.
> 그리고 이 값이 로고 와인과 같은 계열이다 — 원래 사이트는 로고에서 색을 가져왔던 것이다.

---

## 7. 파일 위치

```
claude work/
├─ index.html                     ← 실제 서비스. 화면 이식 완료 (0-B)
├─ profile.png / profile.webp     ← 인물 컷아웃. 공개 홈이 webp 99KB를 쓰고 png 2.2MB가 폴백이다
├─ profile.jpg                    ← 84KB. **컷아웃이 아니라 폴백으로 못 쓴다** (JPEG에 알파가 없다). 지금은 쓰는 곳이 없다
├─ CLAUDE.md                      ← 이 파일
│
├─ ds.css                         ★ 유일한 디자인 시스템. 토큰은 :root. 실서비스·시안이 같이 쓴다
├─ ds.js                          ★ 아이콘 34종 · NAV/SUBNAV · 피팅 · 예시 데이터 — 시안 전용, 실서비스는 로드하지 않는다
│
├─ design-v1/                     ★ 관리자 16 + 학생앱 11 + 로그인 + 조교 (시안)
│   ├─ 00-system.html             ← 디자인 시스템 문서
│   ├─ 01-roster / 02-class / 03-student / 04-review / 05-signals
│   ├─ 06-classes / 07-exams / 08-bank / 09-clinic / 10-notice
│   ├─ 11-qna / 12-chat / 13-settings
│   ├─ 14-log / 15-quiz / 16-videos          ← 반 관리 서브탭
│   ├─ 17-login / 18-assistant               ← 상단바 메뉴 «밖»의 화면 둘 (4-D · 4-E)
│   ├─ 20-student-app.html                   ← 학생 마이페이지 11개 (모바일)
│   └─ preview.html                          ← 통합 프리뷰. 카드 목록은 관리자 16개,
│                                              나머지는 위쪽 링크 넷(시스템 문서·학생앱·로그인·조교)
│
├─ landing-v2/                    ★ 랜딩페이지 (시안 — 실서비스는 homeHTML로 합류했다)
│   ├─ index.html                 ← 「하루」. 실서비스와 같이 고친다 (여기가 따라 그릴 기준이다)
│   ├─ preview.html               ← 데스크톱 1440 + 모바일 390 동시
│   ├─ copy.html                  ← 전환 문구 후보 5개 (실제 크기)
│   └─ concept.html               ← 컨셉 보드 (로고 반영 전, 보관)
│
├─ 로고/                          ★ 자체 로고 (2024-01 PAPERFIT)
│   ├─ 가로형(1~3).png            ← (1) 컬러 (2) 흰색 녹아웃 (3) 단색
│   └─ 세로형(1~3).png
│   ※ 매뉴얼북 원본: Documents/카카오톡 받은 파일/[김하현T] 로고 매뉴얼북.zip
│
├─ mockups/                       ← 랜딩 5안 (기각, 보관)
├─ mockups-admin/                 ← 관리자 5안 (선정 완료, 보관)
├─ worker/                        ★ Cloudflare 워커 — 코드 완성, 배포 대기 (9절 1번)
│   ├─ gemini-proxy.js            ← 배포본 + Firebase ID 토큰 검증 · 서버 쪽 할당량
│   ├─ verify-test.mjs            ← 토큰 검증 테스트. node worker/verify-test.mjs
│   └─ README.md                  ← 왜 회전이 아니라 교체인지 · 3단계 배포 순서
│   ※ 손댄 곳은 CORS · 인증 게이트 · 할당량 셋뿐. 드라이브·Gemini 로직은 배포본 그대로다
└─ tools/                         ★ 개발 도구 (배포와 무관)
    ├─ serve.js                   ← 로컬 정적 서버. node tools/serve.js
```

관리자 목업은 **1440×900 캔버스를 화면 크기에 맞춰 축소**하는 구조(`fitCanvas()`).
표시된 학생 이름·수치는 전부 **구조 확인용 예시**이며 실제 데이터가 아니다.

**검증 상태** — 관리자 16 + 학생앱 11 + 로그인 5상태 + 조교 2화면 + 랜딩(6폭) 전부 **0건**
(캔버스 이탈 / 텍스트 잘림 / 부모 침범 3종. 스크롤 컨테이너 안쪽과 KaTeX 내부는 제외)
`mockups/`(랜딩 5안)에는 `word-break:keep-all`이 안 들어갔다. 보관용이라 두었다.

---

## 8. 작업 규칙

### 만들 때
- **시안을 만들 때**는 별도 폴더에. **실서비스에 이식할 때**는 `index.html`을 직접 고친다 (0-B)
- **이식하다 시안의 버그를 찾으면 시안도 같이 고친다.** 시안이 「따라 그릴 기준」이라
  틀린 채로 두면 다음에 그걸 또 베낀다 (랜딩의 히어로 척추가 그 사례다)
- **`.mono`에 한글 «낱말»을 넣지 않는다.** JetBrains Mono에 한글이 없어 고정폭 대체 글꼴로
  떨어지면서 「오늘」이 「오 늘」처럼 벌어진다. mono는 `YYYY-MM-DD`·숫자·퍼센트에만
- **새 화면은 루트 `ds.css`를 링크해서 만든다.** 화면별 `<style>`에는 «배치»만 두고 색·서체·반경·간격을 새로 정하지 않는다
- **«칸»에는 배경(`--card`)을 준다.** 목록·문맥 칸은 시안에서 흰 면이고, 경계선 하나만으로는
  «칸»이 아니라 «여백을 띄운 목록»으로 읽힌다. 화면 가장자리에 닿는 칸은 본문 패딩(20px)을
  음수 마진으로 되먹인다. **단, 시안이 종이색으로 둔 칸은 그대로 둔다** (수업 시간표 · 오답 큐)
- **카드를 쓰지 않는다.** 흰 상자를 종이 위에 띄우는 것은 구버전(`.lg-panel`)의 어법이다.
  구역은 `.st-sec`+`.st-h`(제목 아래 실선), 그 안은 `.st-row`(얇은 선으로 나눈 줄)로 짓는다
- **만드는 폼은 접어 둔다.** 「추가」·「새 ○○」 버튼을 눌러야 열린다 (`.st-form`).
  상시 펼쳐 둔 폼은 화면에서 제일 큰 자리를 차지하면서 제일 가끔 쓰인다
- **입력칸은 `.fm-field` / `.cls-form`** 을 쓴다. 화면마다 다시 그리지 않는다 —
  그렇게 51곳으로 늘어난 것이 구버전 `lg-field`였다
- **후손 선택자로 `display:block`을 걸지 않는다.** `.t span{display:block}`은 줄 안에 넣은
  `<span class="mono">`까지 블록으로 만들어 한 줄을 두 줄로 갈라 놓는다. 직계 자식(`> span`)만 건다
- **상단바·서브탭을 손으로 적지 않는다.**
  - 시안: `<nav id="nav">` / `<div class="subnav" id="subnav">`만 두고 `renderNav()` / `renderSubnav()`를 부른다. 메뉴가 바뀌면 `ds.js`의 `NAV`/`SUBNAV`만 고친다
  - 실서비스: `index.html`의 `TEACHER_NAV` / `TEACHER_SUBNAV` 한 곳만 고친다. **둘은 따로다** — 시안은 href로, 실서비스는 탭 id로 움직이기 때문이다. 메뉴를 바꾸면 양쪽을 같이 고쳐야 한다
- **두 화면에서 쓰이는 컴포넌트는 ds.css로 올린다.** 로컬에 두면 한쪽만 고쳐진다
- 새 HTML의 리셋에 **`word-break:keep-all; overflow-wrap:break-word;`**. 없으면 한글이 음절 단위로 갈라진다("목표"→"목/표")
- CSS 커스텀 속성은 **`:root`에.** 특정 클래스 안에 두면 그 클래스를 안 쓰는 문서 페이지에서 색이 조용히 사라진다

### ⚠ 클래스 이름 — 같은 사고를 여섯 번 냈다
**화면별 `<style>`에서 ds.css의 클래스 이름을 다시 쓰지 않는다.**
**그리고 `index.html` 자신의 `.app` 블록에 이미 있는 이름도 쓰지 않는다** (여섯 번째가 그것이다).
`.stage` · `.ev` · `.tag` · `.row` · `.body` · `.ph` 를 로컬에서 재정의했다가 매번 조용히 망가졌다.
(`.stage`는 목업 뷰어의 검은 배경, `.body`는 앱 셸의 `absolute; top:52px` — 장면이 통째로 헤더 위로 올라갔다)
→ **랜딩처럼 접두사를 붙이는 쪽이 안전하다** (`l-` 등).

> **다섯 번째는 `.ph`였다 (2026-08-08 · 조교·로그인 시안).** 「placeholder」의 줄임으로 썼는데
> ds.css의 `.ph`는 **학생앱 폰 액자(`width:390px; height:812px`)**다.
> 입력칸 하나가 390px로 부풀어 캔버스를 111px 넘어갔다.
> **두 글자짜리 약어가 제일 위험하다** — 겹칠 확률이 높은데 눈에는 안 띈다.
>
> **여섯 번째는 `.body`였다 (2026-08-08 · 학생 상세 연대기).** 이번엔 ds.css가 아니라
> **`index.html` 자신의 앱 셸**과 부딪혔다 — `.app .body{padding:20px}`가 연대기 한 줄의
> 본문에 그대로 걸려 행 높이가 1.6배가 돼 있었다. ds.css만 봐서는 안 잡힌다.
>
> **새 클래스 이름은 ds.css «와 index.html»에서 한 번 찾아보는 것**이 유일한 방어다
> (`grep -n "\.이름" ds.css index.html`).
> 이걸 잡아 주던 `check-css-collisions.js`는 이식이 끝나면서 지웠고, 애초에 `index.html`만
> 보던 도구라 `design-v1/`·`landing-v2/`의 시안은 검사 대상도 아니었다.

### 색을 정할 때
- **브랜드 자산(로고·매뉴얼)이 있는지 먼저 묻는다.** 로고를 못 보고 주조색을 정했다가 24개 화면을 만든 뒤 갈아엎었다
- **토큰 이름에 색 이름을 넣지 않는다.** `--olive`가 차콜을 담는 순간 거짓말이 된다
- **브랜드색을 UI 강조색으로 바로 쓰지 않는다.** 상태색과 겹치는지 먼저 본다

### 판단할 때
- **통일해야 하는 것은 색·서체·간격·버튼이지 레이아웃이 아니다.** 질의응답은 세로 스택, 채팅은 말풍선 — 글의 성격이 다르면 형태는 달라야 한다. 형태를 억지로 맞추는 것은 일관성이 아니라 게으름이다
- **같은 곳으로 가는 문은 하나만 만든다.** (명단의 「위험 신호」 뷰 제거, 반의 「학생 명단」 → 01로)
- **학생 화면은 모바일 기준으로 그린다.** 누르는 것은 최소 46px
- 실제 도메인 값을 쓴다: 7과목(공통수학1·2, 대수, 미적분Ⅰ, 확률과통계, 기하, 미적분Ⅱ), 출석 5상태, 오답 3단계, 실제 학교명(건대부고·광남고·대원고·동대부여고·가람고·광양고·대원여고)
- 실존 인물에 대한 사실(경력 연수 등)을 **지어내지 않는다.** 수치는 예시임을 명시한다
- **공개 페이지에는 검증 불가능한 수치를 넣지 않는다.** 방문자는 사실로 읽는다
- 비밀번호·토큰을 문서나 코드에 기록하지 않는다

### 검증할 때
- 목업을 만들면 **반드시 브라우저로 렌더링을 확인한다.** 축소된 스크린샷만 보면 놓친다
- 캔버스를 `scale(1)`로 되돌린 뒤 **DOM 기하를 재는 편이 확실하다** — 1440×900 이탈 / `scrollWidth>clientWidth` 텍스트 잘림 / `overflow:hidden` 부모 침범 3종
- **스크롤 컨테이너 안쪽 요소는 제외**해야 한다 (안 그러면 오탐이 쏟아진다). KaTeX 내부(`.katex`)도 의도적 클리핑이라 제외
- **잘림·넘침 3종으로는 «어긋남»을 못 잡는다.** 랜딩의 히어로 척추가 아래 본문과 256px 어긋나 있었는데
  검사는 전부 0이었다 — 넘친 게 아니라 «틀린 자리에 있던» 것이라서.
  같은 것이 반복되는 요소(척추·열 시작점·라벨 폭)는 **`getBoundingClientRect().left`를 모아 비교**한다
- **반응형은 창 크기로 확인한다.** 창이 안 줄면 같은 문서를 **iframe에 넣어** 실제 뷰포트를 만든다 —
  미디어쿼리는 iframe 폭을 따른다. 규칙만 손으로 흉내 내면 중단점 자체는 확인되지 않는다
  (`let`/`const` 최상위 선언은 `window`에 안 붙으므로 `iframe.contentWindow.eval()`로 들어간다)
- **IntersectionObserver·requestAnimationFrame은 렌더링 프레임에 묶여 있다.** 자동화로 볼 때
  백그라운드 탭에서는 콜백이 아예 안 온다 — `setTimeout`이 아니라 `rAF`를 기다려야 실제 동작이 보인다
- ⚠ **입력칸이 있으면 «한글로» 쳐 본다.** 기하만 재고 넘어가면 못 잡는 것이 있다 —
  한글은 조합으로 들어와서, 매 글자 `render()`가 돌면 「안녕하세요」가 「ㅇㅏㄴㄴㅕㅇ…」이 된다.
  실제로 명단 검색이 이식 때부터 계속 깨져 있었고, 영문 아이디로만 쳐 봐서 못 잡았다.
  자동으로 볼 때는 `compositionstart` → `input{isComposing:true}` → `compositionend` 순으로
  이벤트를 흘려 넣고, **조합 중 `render()` 호출이 0회인지**를 센다

---

## 9. 리디자인과 별개로 처리해야 할 이슈

1. ~~**`AI_SITE_TOKEN`**~~ — **해결됨 (2026-08-06 3단계 완료 · 2026-08-07 확인).**
   유출된 값은 죽었다. 아래는 «왜 회전이 아니라 교체였는가»의 기록이다.
   - ~~토큰 회전 필요~~ — **회전은 효과가 거의 없다.** 정적 클라이언트라 토큰이 모든 방문자
     브라우저로 내려간다. 저장소에서 지워도 배포본에는 남아야 하고, 갈아도 새 값이 똑같이 공개된다.
     **처음부터 비밀이 될 수 없는 값이었다**
   - **진짜 위험은 그 뒤에 방어가 없다는 것** — 일 20건 제한이 클라이언트에만 있다(731·758).
     토큰만 있으면 워커를 루프로 호출해 Gemini 비용을 태울 수 있다
   - 해법: 워커가 **Firebase ID 토큰을 검증**하고 **할당량을 서버에서 uid별로** 강제한다.
     `getAuthToken()`이 이미 있고 Firestore REST가 그걸 쓰고 있다 — 재료는 다 있다
   - ⚠ **「강사만 접근」은 이걸로 안 된다.** 5401이 `signInAnonymously()`라 Firebase 신원이
     «사이트를 연 사람»까지만 말한다. 학생/강사 구분은 Firestore 대조라 토큰에 안 실린다.
     커스텀 클레임을 심으려면 권한 있는 백엔드가 따로 필요하다 — 별건
   - **순서를 지켜야 한다. 클라이언트를 먼저 바꾸면 실서비스가 죽는다** —
     새 `Authorization` 헤더가 프리플라이트를 바꾸는데 워커의 `Access-Control-Allow-Headers`에
     `authorization`이 없으면 브라우저가 요청을 통째로 막는다
   - 상태: **1·2·3단계 전부 완료**
     - 1단계 — 워커 배포 (사용자). 프리플라이트로 확인함
     - 2단계 — `index.html`에서 `AI_SITE_TOKEN` 상수와 `X-Site-Token` 헤더 **4곳**을
       `getAuthToken()`으로 교체. `deleteFromDrive()`는 await가 필요해 async가 됐다
       (부르는 곳 8곳은 전부 결과를 안 기다리는 호출이라 그대로 둬도 된다)
     - **3단계 — Cloudflare에서 `SITE_TOKEN` 비밀 삭제 (2026-08-06, 사용자).**
       이때 유출된 값이 죽었다
   - **2026-08-07 배포본에 직접 물어 확인했다** — 커밋 이력의 옛 토큰으로 부르면 **401**.
     인증 없음·틀린 Bearer도 401. 프리플라이트 200에 `Authorization` 허용, Origin은 배포 도메인 그대로.
     (`/delete`는 할당량을 안 태우는 경로라 이 확인은 비용이 들지 않는다)
   - 소스에서도 옛 경로를 지웠다 (CORS 헤더의 `X-Site-Token` · `authenticate()`의 분기).
     **반영하려면 워커를 재배포해야 하지만 안 해도 동작은 같다** — 비밀이 없어 이미 닫혀 있었다
   - **배포본에서 AI 생성·이미지 업로드 둘 다 정상 동작을 확인했다 (2026-08-07, 사용자).**
     막힌 쪽(옛 토큰 401)과 통하는 쪽(새 경로) 양쪽이 다 확인됐으므로 이 건은 끝났다
   - ⚠ **`ALLOW_ORIGIN`을 걸어서 로컬(`127.0.0.1:8777`)에서는 워커를 못 부른다.**
     AI 생성·이미지 업로드 확인은 **배포된 사이트에서** 해야 한다.
     다만 CORS는 브라우저의 규칙이라 **curl로는 그냥 닿는다** — 인증만 보는 확인은 로컬에서 된다
   - 커밋 이력의 옛 토큰은 회수되지 않는다. 무효화가 유일한 해결이었고, 그게 끝났다
2. ~~**홈 히어로 이미지 깨짐**~~ — **해결됨.** 세 파일 전부 추적 중이고 배포본에서 열린다.
   랜딩 합류 뒤로 **공개 홈은 `profile.webp`(99KB) + `profile.png`(2.2MB) 폴백**을 쓴다.
   `profile.jpg`(84KB)는 **컷아웃이 아니라 폴백으로 못 쓴다** (JPEG에 알파가 없다) — 지금은 쓰는 곳이 없다
2-B. ~~**드라이브 업로드가 죽어 있다**~~ — **해결됨 (2026-08-09, 사용자).**
   OAuth 동의 화면을 **「프로덕션」으로 게시**하고 **클라이언트 보안 비밀 + 리프레시 토큰을
   새로 발급**해 워커 비밀(`DRIVE_OAUTH_CLIENT_SECRET` · `DRIVE_REFRESH_TOKEN`)을 갈아 끼웠다.
   배포본에서 **문서·이미지 두 경로 모두 정상**을 확인했다 (강의자료 업로드 → 학생 화면 →
   삭제까지, 그리고 `uploadImageToDrive`로 질의응답·시험지가 쓰는 경로까지).
   - **다시 이 증상이 나오면 제일 먼저 게시 상태를 보라.** 「테스트」면 리프레시 토큰이
     **7일**마다 죽는다. 증상은 `/upload` 502 + `invalid_grant`이고, 코드는 멀쩡하다
   - **저장용량은 Google One 그대로 쓴다** — API로 올린 파일도 토큰을 내준 계정이 소유하고
     그 계정 용량에서 차감된다. «API 전용 저장공간»은 없고 드라이브 API 호출 자체도 무료다.
     ⚠ **서비스 계정으로 바꾸면 안 된다** — 자체 용량이 0이라 결제한 용량을 못 쓰고 업로드가 막힌다
   - 클라이언트 보안 비밀을 새로 만들 때 **클라이언트 ID는 그대로 둔다.** 새 클라이언트로 갈면
     `drive.file` 범위에서 **예전 클라이언트가 올린 파일에 접근하지 못해** 삭제가 조용히 깨진다
   - 아래는 당시 진단 기록이다.

   배포본에서 `/upload`가 **502**를 돌려줬다. 워커가 남긴 이유는 분명했다:
   ```
   auth failed — token refresh failed: 400
   invalid_grant · "Token has been expired or revoked."
   ```
   - **내 코드나 새 기능의 문제가 아니다.** 기존 «이미지» 경로(질의응답 사진 · 시험지 이미지 ·
     창고 이미지)도 **똑같이 502**다. 1×1 PNG로 확인했다
   - 즉 **지금 이 순간 사진 첨부와 이미지 업로드가 전부 실패한다.** 2026-08-07에는 됐다
   - **가장 그럴듯한 원인 — OAuth 동의 화면이 「테스트」 상태다.** 그 상태의 리프레시 토큰은
     **7일 뒤 만료**된다. 8/7에 되고 8/9에 죽은 것이 정확히 그 주기다
   - 고치는 길 둘 (둘 다 구글 계정 소유자만 할 수 있다)
     1. **동의 화면을 「프로덕션」으로 게시한다** → 7일 만료가 사라진다. 근본 해결
     2. 리프레시 토큰을 새로 발급받아 워커의 `DRIVE_REFRESH_TOKEN` 비밀을 갈아 끼운다
        → 지금은 살지만 테스트 상태로 두면 7일 뒤 또 죽는다
   - **AI 생성은 별개다** — 그쪽은 `GEMINI_API_KEY`를 쓰고 드라이브 토큰과 무관하다
3. **비밀번호 평문 저장** — 학생 `pw`, 조교 `pw`, `teacher-pw` 전부
4. **전체 재렌더링으로 인한 포커스 유실** — 「치면서 화면이 따라오는」 입력칸은
   전부 `liveEdit()`을 쓴다. 조합 중에는 안 그리고, 커서는 있던 자리로 되돌린다 (0-B).
   - ~~한글이 「ㅇㅏㄴㄴㅕㅇ」으로 입력되던 것~~ — **고쳤다 (2026-08-08).** 네 곳 전부
   - **남은 것** — state만 갱신하는 칸(출결 메모·수업 메모·일별 성적명)은 다른 이유로
     `render()`가 돌면 커서가 끝으로 튄다. 자주 겪는 길은 아니라 그대로 뒀다
6. ~~**`todayStr()`이 UTC 기준이다**~~ — **고쳤다 (2026-08-07).** `ymdLocal()`을 만들어
   「오늘」을 로컬 날짜로 통일했다. `new Date().toISOString().slice(0,10)`을 쓰던 12곳이
   전부 `todayStr()`을 부른다. 이미 저장된 기록의 날짜는 그대로 뒀다 — 고쳐 봐야 어느 쪽이 맞는지 모른다
   - 미룰 수 없었던 이유: 반 관리 > 수업 기록의 날짜 목록이 **요일은 로컬로 판정하고
     날짜는 UTC로 기록**해서 통째로 하루씩 밀려 있었다. 월·수·금 반이 **일·화·목**으로 보였고,
     그 상태로 출결을 넣으면 하루 전 날짜에 저장된다
   - 수업 화면도 같은 이유로 그 시간대에 「이 날짜에 수업이 없습니다」를 띄우고 있었다
   - `shiftYmd()`는 문자열 산술이라 이 변경과 무관하게 그대로 맞다
   - **한 곳을 놓쳤었다 — 채팅 시각.** `sendChatMessage()`가 `toISOString()`으로 «분»까지 찍어
     KST에서 보낸 말이 9시간 전으로 남고 있었다. 학생 채팅 화면이 시각을 띄우면서 드러났다.
     `nowStamp()`(로컬 `'YYYY-MM-DD HH:MM'`)로 고쳤다 (2026-08-07)
7. **SEO/접근성 공백** — **공개 홈은 해결됐다 (2026-08-07).** 랜딩 합류와 함께
   meta description·OG 태그가 `<head>`에 들어갔고, skip link·focus-visible·reduced-motion도 같이 왔다.
   자동으로 흐르던 후기 마퀴(`prefers-reduced-motion` 미대응)는 통째로 사라졌다
   - **로그인도 해결됐다 (2026-08-08)** — `<form>` + `<button type="submit">`이라 Enter와 탭 이동이 먹는다.
     구버전은 버튼 onclick뿐이라 **Enter를 쳐도 아무 일이 없었다**
   - **남은 것** — 화면 곳곳의 `href` 없는 `<a onclick>`(`.pill`·서브내비·문 링크 등)은
     여전히 키보드로 못 간다. DS 화면은 대부분 `<button>`을 쓰지만 전부 검증하진 않았다.
     한 번에 훑어야 할 일이지 화면마다 고칠 일이 아니다
8. ⚠ **«있을 법한 말»을 지어내서 공개 페이지에 실었다** (2026-08-13에 드러남 · 고침)
   랜딩 시안 작업 중에 쓴 두 문장이 실서비스에 실렸고, **사용자가 한 말이 아니었다.**
   더 나쁜 것은 그 뒤에 내가 그것을 «사용자의 교육철학»으로 읽고 다음 판을 설계했다는 것이다.
   사진 `alt`의 「원장」도 사실이 아니었다(**개인 강사**다).
   - **«수치를 지어내지 않는다»는 지켜 왔는데 «태도를 나타내는 문장»에는 같은 규칙을 안 걸고 있었다.**
     공개 페이지에서 방문자는 둘 다 사실로 읽는다
   - **규칙** — 실존 인물의 생각·태도·이력을 나타내는 문장은 **사용자의 말에서 나온 것만** 쓴다.
     재료가 없으면 그 자리를 비워 두고 «필요하다»고 말한다. 자리를 채우려고 짓지 않는다
   - 지금 랜딩의 원칙 셋과 시연 흐름은 전부 사용자가 직접 말한 것에서 나왔다 → 6절
