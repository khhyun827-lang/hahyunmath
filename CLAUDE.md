# 김하현수학연구소 — 작업 노트

마지막 갱신: **2026-08-07**

---

## 0. 지금 상태 — 여기부터 읽으면 된다

**설계는 끝났고, 2026-08-06부터 실제 이식이 시작됐다.**
이제 `index.html`(실서비스)을 **직접 고친다.** 화면을 하나씩 새 디자인으로 갈아끼우는 중이며,
진행 상황과 «다음 차례»는 **0-B**에 있다. `design-v1/`·`landing-v2/`는 이제 «따라 그릴 기준»이다.

| | 상태 |
|---|---|
| 디자인 시스템 | **확정** — 루트의 `ds.css` + `ds.js`. 로고 기준 색까지 정리 완료 |
| 관리자 화면 | **16개 완성** — 렌더링 검증 전부 0건. 실서비스 이식 **14/16** (남은 것: 시험지·창고·설정) |
| 학생 마이페이지 | **11개 완성** — 모바일 390×812 |
| 랜딩페이지 | **1개 완성** — `landing-v2/index.html`, 컨셉 「하루」 |
| 실제 구현 | **착수함** — 0-B 참고 |

### 이어서 시작하는 절차
```bash
cd "C:\Users\hahyun\Desktop\claude work"
git log --oneline -4              # 어디까지 왔는지
node tools/serve.js               # 127.0.0.1:8777
node tools/check-css-collisions.js  # 새는 속성 0이어야 정상
```
`file://`로 열면 iframe 썸네일이 막힌다. Windows Store 파이썬 스텁은 동작하지 않으므로 **node**를 쓴다.

그 다음 **0-B의 «다음 차례»**를 읽고 이어가면 된다.

### 바로 열어볼 곳
```
http://127.0.0.1:8777/index.html                  실서비스 (이식 중)
http://127.0.0.1:8777/design-v1/preview.html      관리자 16개 한눈에
http://127.0.0.1:8777/design-v1/00-system.html    디자인 시스템 문서
http://127.0.0.1:8777/design-v1/20-student-app.html  학생앱 11개
http://127.0.0.1:8777/landing-v2/preview.html     랜딩 (데스크톱+모바일 동시)
http://127.0.0.1:8777/landing-v2/copy.html        랜딩 전환 문구 후보 5개
```
관리자 화면은 로그인이 필요하다. 브라우저 콘솔에서 아래로 들어갈 수 있다.
```js
state.currentUser = {type:'teacher'}; state.teacherTab = 'classhub'; goto('teacher');
```

---

## 0-B. 구현 진행 상황 (2026-08-06 착수)

**`index.html` 이식이 시작됐다.** 아래 «이행 발판»을 먼저 읽어야 이 파일의 나머지가 이해된다.

### 이행 발판 — 두 CSS의 공존 장치
새 디자인은 `ds.css`, 구버전은 `index.html`의 `<style>`이다. 화면을 하나씩 옮기려면 둘이 한동안 공존해야 하는데, **두 쪽이 클래스 이름을 10개 공유하고 있었다.**

> 이름이 같으면 캐스케이드는 «합집합»을 취한다. 구버전 규칙이 선언하지 않은 속성은 ds.css 것이 그대로 얹힌다.
> `.field`가 특히 심했다 — ds.css의 `display:flex`·`height:32px`·`border`가 구버전 폼 래퍼 102곳에 들어와 레이아웃이 무너진다.
> **스코프를 아무리 좁혀도 이건 못 막는다. 이름을 갈라야 한다.**

그래서 **버려질 쪽인 구버전** 클래스에 `lg-` 접두사를 붙였다 (`btn field badge hint mono who panel tag`).

| 장치 | 하는 일 |
|---|---|
| `body.legacy` | 구버전 `<style>` 전체가 이 클래스 안에 갇혀 있다. `:root`도 `.legacy`로 내려 `--ink`/`--paper` 충돌을 막았다 |
| `MIGRATED_VIEWS` | 여기 든 뷰는 `body`에서 `legacy`를 떼고, 공개 헤더·푸터도 붙이지 않는다 (자기 셸을 갖기 때문) |
| `TEACHER_TABS_DS` | 관리자 탭 중 새 디자인으로 옮긴 것. 없는 탭은 `<div class="legacy">`로 감싸 구버전 스타일로 그린다 |
| `.app` (index.html `<style>`) | 실서비스 셸. 목업의 절대배치(1440×900 캔버스)를 스크롤 문서에 맞게 **position만** 바꾼다 |

**이식이 끝나면** `<style>` 블록 · `body.legacy` · 두 집합 · `lg-` 클래스가 통째로 사라진다.

### 지금까지 옮긴 것
- [x] `ds.css`/`ds.js` 루트 승격 (`design-v1/`은 `../ds.css`를 본다)
- [x] 이행 발판 설치 — 공개 홈 문서높이 4151px·버튼 122px로 이전과 동일함을 확인
- [x] **관리자 셸** — 좌측 사이드바 5그룹 → 상단바 7메뉴 + 서브탭. 13개 탭 전부 동작 확인
- [x] **명단** (`01-roster`) — 첫 DS 화면. 진입점 통합까지 완료 → 아래
- [x] **신호** (`05-signals`) — 홈 대시보드를 대체했다 → 아래
- [x] **수업** (`02-class`) — 출결→진도→과제→메모 단계형 → 아래
- [ ] 나머지 관리자 화면 · 학생 마이페이지 · 랜딩

### 명단 이식 결과 (2026-08-06)
`teacherRosterHTML()` 신설, `TEACHER_TABS_DS = {'roster'}`. 검증 — 행 높이 전부 40px 정확,
머리말·헤더 잘림 0건, 문서 가로/세로 넘침 0건, `check-css-collisions` 새는 속성 0.

**실데이터로 계산한 열** — 신호 / 반 진도 / 성적 추이 / 출결 최근 8회 / 출석률 / 과제 / 오답 / 시청률.
전부 `state.allRecords`와 `getClassProgress()`에서 나온다. `rosterStats()` 한 곳에 모여 있다.

- **출석률 = 「퇴원」을 뺀 기록 중 「결석」이 아닌 비율.** 지각·조퇴는 «온 것»으로 센다.
  제품에 출석률 정의가 없어서 여기서 처음 정했다. 다른 화면에서도 이 정의를 쓴다
- **뷰(누구를) / 필터(어떤 상태를) 두 축으로 갈랐다.** 목업은 반 하나를 고른 상태만 그렸지만
  실서비스는 반이 여러 개고 미배정·퇴원이 있다. 반을 고르면 「반」·「반 진도」 열이 사라진다
- **퇴원 학생은 「퇴원」 뷰에서만 보인다.** 재원 명단에 섞이면 모든 숫자가 흐려진다
- 신호 임계값은 잠정이다. 규칙을 **편집**하는 것은 `05-signals`가 맡는다

**넣지 않은 것** — 열 선택(15/22) · 사용자 저장 뷰 · 과제 일괄 배포 · 학부모 문자 · 클리닉 배정.
전부 도메인 레이어에 없는 신규 기능이라 «눌러도 아무 일 없는 버튼»이 된다.
(과제는 **반 단위로만** 배포되게 돼 있어 «선택한 학생에게»가 데이터 구조상 성립하지 않는다.)
일괄 작업으로 실제로 넣은 것은 **반 이동**(감사 로그 남김)과 **선택 학생 엑셀 내보내기** 둘이다.

#### 진입점을 하나로 모았다
| 예전 | 지금 |
|---|---|
| 설정 > 학생 «전체 계정 목록» 표 | 지웠다. 설정은 «계정을 만드는 곳»만 남았다 (추가 · 엑셀 일괄 등록) |
| 반 > 학생 명단 (`classRosterHTML`) | 함수째 지웠다. 반 관리 머리말의 「학생 명단 →」이 명단의 반 뷰로 보낸다 |
| 반 관리의 성적·출결 내보내기 | 반 관리 머리말로 옮겼다 (`exportClassExcel`은 그대로) |

`state.classHubTab` 기본값이 `'students'` → `'sessions'`로 바뀌었다.

#### 이 화면에서 배운 것 — sticky 헤더와 가로 스크롤은 같이 못 쓴다
고밀도 표는 1440을 전제로 그려서 좁은 화면에서 가로 스크롤이 필요하다.
그런데 **`overflow-x:auto`를 주는 순간 그 상자가 새 스크롤포트가 되어**
`thead th{position:sticky;top:52px}`의 기준이 뷰포트에서 그 상자로 바뀐다.
헤더가 52px 내려앉아 **첫 행을 덮는다.** (`overflow-y:visible`로는 못 막는다 — 브라우저가 auto로 바꾼다.)

→ 목업과 같은 구조로 갔다. `.rst-screen`을 `height:calc(100vh - 92px)` 플렉스 칼럼으로 잡고
머리말은 고정, 표만 자체 스크롤(`flex:1;min-height:0;overflow:auto`), `th`는 `top:0`.
**92 = 상단바 52 + `.body` 상하 패딩 40.** 서브탭 있는 화면으로 이 구조를 옮기면 +42다.

### 신호 이식 결과 (2026-08-06)
`teacherSignalsHTML()` 신설, `TEACHER_TABS_DS = {'roster','signals','home'}`.
검증 — 텍스트 잘림 0 · 부모 침범 0 · 문서 가로/세로 넘침 0 · 콘솔 오류 0 · `check-css-collisions` 새는 속성 0.
40행을 넣어 스크롤시켜도 sticky 등급 머리가 첫 행을 덮지 않는다(명단에서 겪은 사고의 재발 확인).

**규칙을 `signalOf()` 하나로 합쳤다.** 명단(01)의 신호 열도 이제 이 함수만 본다.
규칙이 두 벌이면 화면끼리 숫자가 어긋나고, 그러면 두 화면 다 못 믿게 된다.
실데이터 3명으로 «명단 등급 == 신호 등급»을 대조해 확인했다.

- **등급은 셋.** 위험=무거운 신호 2개 이상 / 주의=무거운 1개 또는 시청률<70 / 관찰=경미한 것만
- 시안은 위험을 «출석률<80 **그리고** 시청률<50»으로 못박았는데, 그러면 결석이 잦아도
  영상만 잘 본 학생이 어디에도 안 걸린다. «무거운 신호 2개 이상»으로 바꿨다
- **점수(0~99)는 등급을 정하지 않는다.** 등급 «안에서 줄 세우는» 값이다.
  점수로 등급까지 정하면 임계값이 두 벌이 되어 어느 쪽이 진짜인지 알 수 없게 된다
- **해제**를 넣었다. 지울 수 없는 트리아지 목록은 곧 아무도 안 보게 된다.
  새 컬렉션 대신 `kv/signal-mutes` 문서 하나에 `{학생id: 'YYYY-MM-DD까지'}`. 기간이 지나면 저절로 풀린다

**넣지 않은 것** — 신호 규칙 «편집» UI · 클리닉 배정 · 메모.
규칙을 고치는 것은 아직 `signalOf()`를 고치는 일이다. 클리닉은 «학생이 신청하고 강사가 승인»하는
구조라 강사가 배정하는 문이 없고, 메모는 도메인에 아예 없다.

#### 홈 대시보드를 지웠다
상단바에 「홈」이 없다. 그런데 로그인 직후 기본값이 `home`이라 **메뉴는 「신호」가 켜진 채
반 목록이 그려지고 있었다.** `TEACHER_TAB_SECTION`도 home을 「신호」로 잡고 있었으니 원래 의도가 그거였다.
→ `home`·`signals` 둘 다 신호로 보낸다. `teacherHomeHTML()`은 함수째 지웠고, 담고 있던 것은 제 자리로 갔다.

| 홈에 있던 것 | 간 곳 |
|---|---|
| 과제 빠른 등록 | 반 관리 > 수업 기록 > 과제 (보는 곳과 만드는 곳을 붙였다. 반·마감일이 그 화면 값으로 미리 채워진다) |
| 시험지 빠른 등록 | 오답 > 시험지 |
| 강의 업로드 | 반 관리 > 영상 |
| 새 상담 알림 | 알림 벨 · 설정 > 상담 |
| 반별 진도 현황 | 반 관리 > 진도 |

> **남겨 뒀으면 `hw-title`·`video-url` 같은 입력 id가 두 화면에 동시에 살아 있게 된다.**
> 둘이 같이 그려지는 날 `getElementById`가 엉뚱한 칸을 읽는다. 지운 뒤 각 id가 하나씩만 남은 것을 확인했다.

### 수업 이식 결과 (2026-08-07)
`teacherSessionHTML()` 신설, `TEACHER_TABS_DS`에 `session` 추가.
검증 — 5단계 전부 텍스트 잘림 0 · 부모 침범 0 · 문서 넘침 0 · 콘솔 오류 0 · 새는 속성 0.

**1차 축이 반이 아니라 날짜와 시간이다.** 명단·신호가 «훑는» 화면이라면 이건 «하는» 화면이라,
왼쪽에 그날의 시간표를 세우고 거기서 지금 하는 수업을 고른다. 오른쪽에서 단계를 순서대로 끝낸다.

- **출결 저장을 새로 쓰지 않았다.** 기존 `attDraft`/`ensureAttDraft`/`setAttStatus`/
  `saveClassAttendance`를 그대로 쓴다 — 결석 보충 영상 자동 배정까지 붙어 있는 로직이고,
  저장 경로가 두 벌이 되면 한쪽만 고쳐진다
- **출결 버튼은 4개다.** 「퇴원」은 수업 중에 누를 일이 없고 잘못 누르면 학생 로그인이 막힌다.
  5상태 전부와 일괄 변경은 **반 관리 > 수업 기록 > 출석**에 그대로 남아 있다 — 기능 손실이 아니다
- **0단계 「지난 수업 확인」을 실제로 만들었다.** 시안은 `fin` 표시만 있었는데,
  지난 수업의 출결 집계·그날 낸 과제·그날 메모를 보여 준다. 「저번에 어디까지 했더라」가 이 화면의 첫 물음이다
- 데일리퀴즈는 이 흐름에 없다 (학생이 제출하고 자동 채점된다) — 시안의 판단을 그대로 따랐다

#### 메모는 새로 만들었다
도메인에 수업 메모가 없었다. 신호 「해제」와 같은 방식으로 **`kv/classnote:{반id}` 문서 하나에
`{날짜: 글}`**로 둔다. 컬렉션을 늘리면 loadAll·마이그레이션까지 건드려야 하는데 이 정도 데이터엔 과하다.
저장 → 다시 읽기 → 다음 수업의 「지난 수업 확인」에 뜨는 것까지 실제 Firestore로 확인했다.

> 글자마다 `render()`를 부르면 전체 재렌더링이라 커서가 튄다. 초안(`sessionNoteDraft`)만 담아 두고
> 저장할 때 읽는다. 그래서 단계를 옮겼다 와도 쓰던 글이 남아 있다.

#### ⚠ 날짜 산술 — `toISOString()`으로 하면 안 된다
`'YYYY-MM-DD'`는 **UTC 자정**으로, `'YYYY-MM-DDT00:00:00'`은 **로컬 자정**으로 읽힌다.
후자를 만든 뒤 `toISOString()`으로 되돌리면 KST(+9)에서 하루가 깎인다.
실제로 **▶ 버튼이 제자리걸음**을 했고 「지난 수업」이 하루 더 옛날(08-05 대신 08-04)을 가리켰다.
→ `shiftYmd()`를 만들어 **양쪽 다 UTC로 맞춰** 문자열 산술만 한다.

> **`todayStr()`은 UTC 기준이다** (`new Date().toISOString()`). KST 00:00~09:00 사이에는
> «오늘»이 하루 전을 가리킨다. 이건 이 화면만의 문제가 아니라 출결 날짜·과제 마감 전부에 걸린
> **기존 문제**라 여기서 고치지 않았다 — 한쪽만 로컬로 바꾸면 수업 화면이 저장한 날짜와
> 반 관리가 보여 주는 날짜가 어긋난다. 고친다면 앱 전체를 한 번에 고쳐야 한다. (9절 6번)

#### 진입점 정리
| | |
|---|---|
| `teacherAttendanceHTML`의 반·날짜 선택기 | 지웠다. 이걸 쓰던 유일한 화면이 수업 탭이었고, 이제 왼쪽 시간표와 날짜 네비가 그 일을 한다. 함수는 `(classId, date)`를 반드시 받는다 |
| `state.attendanceClassId` · `attendanceDate` | 위와 함께 지웠다 |
| `addHomework()` | `addHomework(prefix)`로 일반화했다. 과제 폼이 두 화면(반 관리 > 과제 · 수업 > 과제)에 있는데, **폼을 복제하는 대신 id 접두사만 받는다** — 등록 로직이 두 벌이 되면 한쪽만 고쳐진다. 접두사가 다르니 id도 안 부딪친다 |

> **반 관리 > 수업 기록 > 출석과 겹친다.** 지금은 둘 다 남겨 뒀다 — 저쪽은 5상태·일괄 변경·
> 성적·시험까지 있고 아직 DS로 옮기지 않았다. `14-log`(수업 기록)를 옮길 때 정리한다.

### 반 관리 이식 — 완료 (2026-08-07)
서브탭이 넷이라 **하나씩** 옮긴다. `CLASSHUB_TABS_DS`에 든 것만 DS로 그리고
나머지는 `.chub-body` 안에서 `<div class="legacy">`로 감싼다 — 탭 단위 `TEACHER_TABS_DS`와 같은 장치다.

- [x] **셸 + 반 목록** — 좌측 상시 목록. 예전엔 «목록 화면»과 «상세 화면»이 따로여서
      반을 바꿀 때마다 「← 반 목록으로」로 되돌아가야 했다
- [x] **진도** (`06-classes`) — KPI 6개 + 단원 체크리스트
- [x] **수업 기록** (`14-log`) — 되짚는 목록 + 그날 상세 2단
- [x] **데일리퀴즈** (`15-quiz`) — 회차 스트립 + 제출 답안
- [x] **영상** (`16-videos`) — 이탈 지점 + 학생별 진행

**화면 안의 서브탭 막대를 지웠다.** 상단 서브내비가 이미 같은 넷을 그리고 있어 두 줄로 겹쳐 있었다.

**시안과 다른 점** — 시안은 단원을 「미분/적분」 대단원으로 묶고 각 단원에 진행 날짜·회차를 달았다.
도메인에 그 둘이 다 없다 (`UNIT_CHAPTERS`는 과목별 평평한 목록이고 회차 개념이 없다).
없는 것을 지어내지 않고 **«다음에 나갈 단원»만** 표시한다 — 체크 안 된 첫 단원이라 실제로 도출되는 값이다.

지운 것 — `teacherClassesHTML`(반 목록 표) · `teacherClassProgressHTML`(구버전 진도) ·
`teacherClassSessionsHTML`(날짜 버튼 줄 + 서브탭 막대).
반 목록 표에 있던 「종료/재개」·「삭제」는 반 관리 머리말로 옮겼다.

#### 수업 기록은 «읽는 화면»이 아니라 두 단이다
시안(`14-log`)의 역할은 «되짚는다»라 읽기 전용으로 그려져 있다. 그런데 **여기서만 되는 입력이 셋**
있다 — 퇴원 지정 · 일별 성적 · 시험 등록. 시안대로 만들면 그게 통째로 사라진다.

| 단 | 무엇 | 상태 |
|---|---|---|
| 목록 | 한 행 = 한 번의 수업. 출결 요약 · 메모 · 그날 낸 과제 · 성적 입력 인원 | DS (새로 만듦) |
| 상세 | 행을 누르면 그날의 출석 · 성적 · 시험 · 과제 입력 | 구버전 그대로 (`.legacy`) |

- 필터 4개(전체 · 메모 있음 · 결석 발생 · 과제 배포)는 전부 실데이터에서 센다
- **메모는 수업 화면과 같은 `kv/classnote:{반id}`를 본다.** 수업에서 적은 것이 여기 목록에 그대로 뜬다
- **시안의 「회차」와 「단원」은 넣지 않았다.** 도메인에 둘 다 없다 — 기존 코드도
  «차시 번호 없이 날짜만 다룬다»고 못박아 두었고, 진도는 반 단위지 날짜 단위가 아니다
- 옛 화면의 **임의 날짜 입력칸**은 필터 줄에 남겼다. 목록은 반 일정에서 만든 날짜만 담으므로
  보충 수업처럼 일정 밖의 날짜로 들어갈 문이 필요하다

#### 데일리퀴즈 — 시안의 「정답률」은 만들 수 없다
시안(`15-quiz`)은 회차별 **정답률** 막대와 **문항별 오답률**을 그렸는데, 둘 다 도메인에 근거가 없다.

- 퀴즈에 **정답 필드가 없다** (`{id, classId, title, content, date}`). 학생 기록에도
  제출 답안만 남는다 (`quizAnswers[qid] = {answer, submittedAt}`). **채점이라는 개념 자체가 없다**
- 한 퀴즈가 **한 문항**이라 「문항별」이 성립하지 않는다

→ 같은 자리에 **제출률**을 뒀다. 축의 의미(회차별로 잘 돌고 있나)는 그대로다. 문항별 오답률은 뺐다.

> **없던 것을 하나 채웠다 — 제출 답안 보기.**
> 학생은 답을 내는데 **교사가 그걸 볼 화면이 이제껏 없었다.** 옛 화면은 퀴즈 목록과 삭제 버튼뿐이었다.
> 정답이 없어 자동 채점은 못 하지만 답안을 나란히 놓으면 교사가 눈으로 본다.
> 자동 채점이 안 된다는 사실은 화면에 적어 뒀다 — 감추면 «채점된 줄» 알게 된다.

지운 것 — `teacherQuizHTML`(등록 폼 + 퀴즈 카드 나열). 카드 나열은 회차 스트립이 흡수했다.
입력 id가 `quiz-*` → `qz-*`로 바뀌었다 (`addQuiz`도 같이).

> **CSS 순서가 곧 우선순위다.** `.qz-b.low`를 `.on` 뒤에 뒀더니 «지금 보고 있는 회차»가
> 노란색(낮음)에 덮여 어느 회차인지 알 수 없었다. 선택이 상태보다 위여야 한다.

#### 영상 — 「시청률」이 아니라 「이탈 지점」
시안이 못박은 역할을 그대로 따랐다. 몇 %가 봤나가 아니라 **어디서 멈췄나**가 다음 수업을 바꾼다.

**이 히스토그램은 지어낸 것이 아니다.** `videoProgress[영상id] = {completed, percent}`가
학생마다 «어디까지 보고 멈췄는지»를 이미 담고 있다. 그걸 10구간 + 「완주」 칸으로 묶은 것이 이탈 분포다.
(시청 «시점»의 시계열은 없다. 있는 것은 최종 도달 지점 하나라 그 분포까지가 한계다.)

- 동점이면 **가장 이른 구간 하나만** 빨갛게 칠한다 — 넷이 동시에 빨개지면 신호가 아니라 소음이다.
  수업이 학생을 놓치기 시작한 지점이 제일 먼저 손볼 곳이기도 하다. 나머지 인원은 tooltip에 있다
- 「완주」 칸은 이탈이 아니므로 최고점 계산에서 뺐다. 색도 초록이다
- **영상 재생(iframe)은 뺐다.** 강사가 자기 영상을 이 화면에서 볼 이유가 없고,
  목록 수만큼 iframe을 띄우면 그만큼 유튜브를 부른다. 학생 화면(`videoEmbedHTML`)은 그대로다

지운 것 — `videoByClassHTML` · `state.videoByClassId` · `state.videoStudentDetailId`.
시청 현황 표는 이탈 히스토그램과 학생별 진행 목록이 흡수했다.

> 시안은 값 라벨을 `left/right:-14px`로 막대보다 넓게 잡았는데, 실서비스에서는 그만큼
> `.vd-bars`의 `scrollWidth`가 커져 **가로 넘침으로 잡힌다.** 막대 폭이 충분하므로 제 박스 안에 뒀다.

> ⚠ **PowerShell로 index.html을 고치지 말 것.** `Get-Content`가 기본 인코딩으로 읽어
> 한글이 통째로 깨진다 (2026-08-07에 한 번 겪었다 — `git checkout`으로 되돌리고 다시 했다).
> 파일 편집은 Read/Edit/Write 도구로만 한다.

### 오답 검토 이식 (2026-08-07)
`teacherReviewHTML()` 전면 재작성. A4에서 흡수한 것 셋 — **큐 · 2열 비교 · 키보드**. 다크 테마는 버렸다.

예전 화면은 카드를 세로로 나열했다. 한 건을 판단하려면 원본과 쌍둥이를 나란히 봐야 하는데
카드가 길어 스크롤하는 동안 원본이 화면 밖으로 나갔다. 2열로 고정하면 그 왕복이 사라진다.

**큐는 시안과 다르다.** 시안은 「재생성 요청 · 난도 확인 필요 · 보류함」을 그렸는데 도메인에 그런 상태가 없다.
실제 상태는 `pending → review1 → approved` 셋뿐이다. 실제로 갈라지는 축으로 다시 짰다.

| 큐 | 뜻 |
|---|---|
| 내용 없음 | `variantContent`/`variantAnswer`가 비어 있다 — AI로 채우는 것이 첫 일 |
| 1차 검토 대기 | 내용은 있고 아직 `pending` |
| 2차 검토 대기 | `review1` |
| **답 형태 문제** | `answerFormProblem()`이 걸린 것 — 학생이 제출할 수 없다 |
| 공개됨 | `approved` |

- 「보류」는 넣지 않았다. 상태를 만들려면 필드를 더해야 하는데, `pending`에 머무는 것이
  이미 «아직 안 봤다»와 같은 뜻이라 층을 하나 더 둘 이유가 없다
- 키보드 — `J`/`K` 이동, `E` 다음 단계, `X` 되돌리기, `R` 재생성.
  **입력칸에 커서가 있으면 아무 것도 하지 않는다** (문항을 고치다 `J`를 치면 다음 문항으로 튄다)
- 승인·반려 뒤에는 **다음 것으로 넘어간다.** 큐를 비우는 화면이라 손이 멈추면 안 된다.
  지금 항목이 큐에서 빠지므로 빠지기 «전»에 다음 대상을 정해 둔다
- 「학생이 답을 제출할 수 없는 형태입니다」 경고가 `.legacy`에서 `.app .rv-warn`으로 올라왔다.
  2차 승인 버튼도 그 조건이면 비활성이다

지운 것 — `bankCardHTML` · `.legacy .bank-warn` · `state.reviewShowAll`/`reviewFocusMode`/`reviewFocusIndex`.

> **⚠ 플렉스 항목에 `min-height:0`을 빠뜨리면 안에 든 KaTeX가 상자를 밀어낸다.**
> `min-height`의 기본값이 `auto`(=내용 크기)라, 화면 높이는 고정인데 문서가 50px 세로로 스크롤됐다.
> 수식을 감추면 넘침이 0이 되는 것으로 원인을 잡았다. `.rv-work`·`.rv-list`에 `min-height:0`을 넣어 해결.
> **세로로 쌓는 플렉스 칸을 만들 때마다 확인할 것.**

### 학생 상세 이식 (2026-08-07)
**디자인 시스템의 기준 화면.** 이 시스템 전체가 그 사상 위에 있다 —
**탭을 없애고 하나의 연대기로 합친다.** 출결·성적·과제·오답·퀴즈·질문·클리닉이
종류를 가리지 않고 날짜 순으로 한 줄기에 쌓인다.
「이 학생에게 무슨 일이 있었나」는 원래 시간 순 질문이지, 탭을 일곱 번 눌러 맞춰 볼 질문이 아니다.

3열 — 좌: 같은 반 학생 목록(명단에서 들어와도 맥락을 잃지 않는다) /
중: 머리말 + KPI 5 + 신호 + 연대기 / 우: 연락처 · 반·진도 · 빠른 작업 · 출결 8회.

#### 탭을 없애면서 잃을 뻔한 것 — 입력
예전 7개 탭에는 **성적 추가/삭제 · 출결 추가/삭제 · 과제 완료 토글 · 기본정보 수정 ·
학생 삭제 · 개인 영상 삭제 · 채팅**이 들어 있었다. 전부 살렸다.

| | 어디로 |
|---|---|
| 성적·출결 추가 | 연대기 갈래를 고르면 그 갈래의 **입력 폼이 연대기 위에 붙는다** |
| 성적·출결 삭제 | 연대기 항목마다 삭제 버튼. **배열 인덱스를 사건에 실어 보낸다** — 연대기는 날짜순으로 다시 세우므로 |
| 과제 완료 토글 | 「과제·오답」 갈래의 폼 |
| 기본정보 · 학생 삭제 · 개인 영상 | 우측 「빠른 작업」의 **기본정보 모드** |
| 채팅 | 우측 「빠른 작업」의 **채팅 모드** (말풍선이라 연대기와 성격이 다르다) |

- 신호는 별도 보드를 만들지 않고 **근거 문장으로 화면 안에 앉혔다** (A5에서 흡수).
  「7일 해제」도 여기서 된다 — 신호 화면과 같은 `signalMutes`를 본다
- `rosterOpen(sid, tab)`이 쓰던 옛 탭 이름은 `ROSTER_OPEN_MAP`으로 **«어느 갈래로 열 것인가»**가 됐다.
  명단·신호·수업에서 「출결」·「성적」·「오답」·「연락」을 누르면 그 갈래가 열린 채로 들어온다
- **조교는 구버전 탭 화면을 그대로 쓴다** (`teacherStudentDetailLegacyHTML`).
  권한이 달라 DS 화면은 기본정보·삭제까지 열어 두기 때문이다. 그 함수가 스스로 `.legacy`를 두른다

> **`teacherHTML()`의 `onDs` 예외를 지웠다.** 「`studentDetailId`가 있으면 DS 취급을 푼다」는
> 학생 상세가 구버전이던 동안의 임시 조건이었다.

### 소통 4종 이식 (2026-08-07)
**네 화면의 형태를 통일하지 않았다.** 통일해야 하는 것은 색·서체·간격·버튼이지 레이아웃이 아니다 —
글의 성격이 다르면 형태는 달라야 하고, 억지로 맞추는 것은 일관성이 아니라 게으름이다.

| | 형태 | 왜 |
|---|---|---|
| 클리닉 (`09`) | 주간 판 + 승인 대기 패널 | 시간이 자원이라 «어디가 비었나»가 보여야 한다 |
| 공지 (`10`) | 폼 + 목록 | 가장 단순하다. 쓰는 곳이 위, 읽는 곳이 아래 |
| 질의응답 (`11`) | 세로 스택 | 질문 하나가 한 덩어리이고 답이 그 아래 «붙는 글»이다 |
| 채팅 (`12`) | 좌 학생 목록 + 말풍선 | 이건 «주고받는 말»이다 |

- **클리닉 — 시안의 시간축 격자는 만들 수 없다.** 슬롯의 `time`이 「저녁 7시」 같은 자유 텍스트라
  시각으로 정렬·배치할 수 없다. 세로축(시각) 대신 **날짜별 열 안에 그날 슬롯을 쌓았다.**
  「어디가 비었나」는 그대로 읽힌다. 승인 대기는 **신호 있는 학생을 위로** 올린다
- **질의응답 — 미답변이 기본 갈래다.** 이 화면에 오는 이유가 그것이다
- **채팅 — 학생 드롭다운을 목록으로 바꿨다.** 누구와 이야기 중인지, 다음에 누구에게 가야 하는지가
  화면에 남는다. `studentSelectorHTML`은 그래서 지웠다

지운 것 — 구버전 클리닉 화면(시간대 표 + 신청 목록) · `studentSelectorHTML`.

#### 클리닉 신청 경로를 셋으로 늘렸다 (2026-08-07)
원래 있던 것은 **①교사가 시간대를 열고 학생이 고른다** 하나뿐이었다 (정원 마감·중복 방지 포함).
여기에 둘을 더했다. **컬렉션은 늘리지 않았다** — `clinic` 레코드에 `day`·`time`이 이미 있고,
`slotId`를 비우면 «슬롯 밖»이 된다.

| | 누가 | `slotId` | 시작 상태 |
|---|---|---|---|
| ① 열린 시간대에서 고르기 | 학생 | 있음 (정원에 반영) | 대기 |
| ② **희망 시간 직접 제안** | 학생 | 없음 | 대기 — 교사 승인이 있어야 확정 |
| ③ **교사 직접 배정** | 교사 | 고르면 있음 / 아니면 없음 | **승인** (교사가 넣은 것이므로) |

- 열린 시간대가 하나도 없으면 학생 화면은 **②로 시작한다** — 빈손으로 돌려보내지 않는다
- ②는 지난 날짜·같은 날짜+시간 중복을 막는다
- **슬롯 없이 잡힌 것도 주간 판에 세운다** (점선 · 「시간대 밖」).
  판에 안 보이면 교사가 이미 잡아 둔 시간을 모르고 이중으로 앉힌다
- 승인 대기 패널에서 ②는 「직접 제안」 뱃지로 구분된다

### ▶ 다음 차례
시험지(`07`)·창고(`08`)·설정(`13`), 그리고 학생 마이페이지 11개와 랜딩이 남는다.

> **수업 화면과 반 관리 > 수업 기록의 출결 중복은 그대로 둔다.** 겹쳐 보이지만 역할이 다르다 —
> 수업은 «지금 하는 수업을 4버튼으로 빨리 찍는 곳», 수업 기록은 «지난 날짜를 5상태로 고치고
> 성적·시험까지 넣는 곳». 시안도 둘을 따로 그렸다.

화면 하나를 옮기는 절차는 명단·신호·수업에서 확정됐다. 그대로 반복하면 된다.
1. `teacherXxxHTML()`를 새로 만들고 `teacherTabBodyHTML()`의 해당 분기를 갈아끼운다
2. `TEACHER_TABS_DS`에 탭 id를 더한다 → `.legacy` 래퍼가 벗겨지고 ds.css가 걸린다
3. 화면별 스타일은 `.app` 블록에 **접두사를 붙여** 둔다 (명단은 `rst-`). ds.css 클래스 이름을 다시 정의하지 않는다
4. `node tools/check-css-collisions.js`로 새는 속성 0 확인
5. 브라우저에서 **행 높이 · 잘림 · 넘침**을 DOM 기하로 잰다. 스크린샷만 보면 놓친다
6. 같은 일을 하던 옛 진입점을 이쪽으로 모은다

> **학생 상세(`03-student`)를 아직 안 옮겼다.** 그래서 `teacherHTML()`의 `onDs`는
> `state.studentDetailId`가 있으면 DS 취급을 푼다. 03을 옮길 때 이 조건도 같이 지워야 한다.

### 시안대로 신설할 화면은 남지 않았다
「신호」와 「수업」이 마지막이었다. 상단바 7메뉴가 전부 제 화면을 갖고 있다.

`ds.js`는 **실서비스에 로드하지 않는다.** `fitCanvas`와 «가짜 학생 22명»이 들어 있어 그대로 쓰면 예시 데이터가 제품에 섞인다. 상단바 정의는 `index.html`의 `TEACHER_NAV`/`TEACHER_SUBNAV`에 따로 있다.

---

## 1. 결정 기록 — 막힌 결정은 없다

**2026-08-06에 막혀 있던 결정 3건이 전부 해소됐다.**

| | 결정 | 결과 |
|---|---|---|
| ① | 랜딩 「전환」 문구 | **「짐작으로 가르치지 않습니다」** — 반영 완료 |
| ② | 로고 차콜 지정값 | **`#3F3537`** — 매뉴얼북에서 직접 확인, 반영 완료 |
| ③ | 다크 테마 | **라이트 유지** — 5절 권고대로 확정 |
| 2-C | 구현 방식 | **기존 SPA에 이식** — 아래 참고 |

### 이식과 별개로 남아 있는 것
구현 순서는 **0-B**가 갖고 있다. 여기 있는 것은 그 바깥의 일이다.

1. 2-C에 남은 두 가지 — 렌더링 구조(전체 재렌더링 유지 여부) · 관리자 모바일 대응 수준.
   지금은 «전체 재렌더링 유지»로 진행 중이며 문제 없다. 막히면 그때 정한다
2. 랜딩 5안 기각 사유 청취 → 필요하면 랜딩 재검토 (지금 「하루」로 충분하면 생략)
3. 9절 이슈 중 **`AI_SITE_TOKEN` 회전**은 리디자인과 무관하게 우선순위가 높다.
   **2026-08-06 push로 origin/main에 확실히 올라갔다** — 더 미룰 이유가 없다
4. ~~아직 push하지 않았다~~ — **2026-08-06 `585a77a`까지 push 완료.** main = origin/main

> **①·② 반영 결과는 확인했다** — 랜딩 `h2`가 데스크톱 62px / 모바일 29px에서 «짐작으로 / 가르치지 않습니다» 두 줄로 정확히 접힌다. 잘림·부모 침범·가로 스크롤 0건.

---

## 2. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 저장소 | `github.com/khhyun827-lang/hahyunmath` (main) |
| 로컬 | `C:\Users\hahyun\Desktop\claude work` |
| 추적 파일 | 시안·로고·도구까지 전부 추적 중 (2026-08-06 커밋). 예전엔 `index.html` 등 3개뿐이었다 |
| index.html | 4,875줄 / 322KB. CSS·JS·HTML·데이터가 전부 한 파일 |
| 빌드 도구 | 없음 (package.json / 번들러 / 프레임워크 전무) |

### 2-A. 기술 스택
- **렌더링**: 프레임워크 없는 순수 JS SPA. 템플릿 리터럴 → `app.innerHTML = html` 로 **전체 화면 통째로 재렌더링** (`render()`, index.html:2925)
- **인증**: Firebase Auth (compat SDK v10.12.2)
- **DB**: Firestore를 SDK가 아닌 **REST + fetch**로 직접 호출 (index.html:497-501). WebChannel 차단 이슈 회피 목적이라 주석에 명시됨
- **수식**: KaTeX 0.16.11 (`$...$` 델리미터)
- **파일**: SheetJS(xlsx), JSZip 지연 로딩 + HWPX 파서 직접 구현 (index.html:2424-2721)
- **AI**: Cloudflare Worker(Gemini) 프록시로 유사문항 자동 생성, 일 20건 제한
- **이미지**: Google Drive 업로드 프록시 + 클라이언트 압축
- **라우팅**: 해시 기반 (`#teacher/home`, `#student/quiz`, `#assistant/students`)

### 2-B. 데이터 구조 (리디자인해도 반드시 보존)
Firestore `kv/` 하위 **문서-당-레코드 컬렉션**. 구버전 blob은 `migrateBlobToCollection()`이 자동 이관.
```
students / classes / notices / consults / quizzes / clinics / clinicslots
qnas / videos / assistants / exams / problembank / assignments / auditlog
kv/teacher-pw          단일 blob
kv/signal-mutes        {학생id: 'YYYY-MM-DD까지'} — 신호 화면의 «해제»
kv/classnote:{반id}    {날짜: 글} — 수업 화면의 «메모»
kv/record:{studentId}  scores, attendance, assignments, progress,
                       quizAnswers, wrongHomework, videoProgress, assignmentsDone
kv/chat:{studentId}    메시지 배열
```
- `student`: studentId, name, pw, grade(고1/고2/고3), school, parentPhone, phone, classId
- `class`: id, name, schedule(`"월,수,금 7시~9시"`), period, status
- 출석 상태 5종: **출석 / 지각 / 조퇴 / 결석 / 퇴원** (퇴원 시 로그인 차단)
- 오답 검토 3단계: **pending → review1 → approved**
- 클리닉 상태: 대기 / 승인 / 완료 / 취소
- 상수: `UNITS`(7과목), `UNIT_CHAPTERS`(과목별 단원 체크리스트) — index.html:849-861

**유지해야 할 핵심 기능** — 오답 파이프라인(시험지 등록 → 학생 오답 체크 → AI 쌍둥이 문항 생성 → 3단계 검토 → 오답숙제 배포) · 영상 시청률 실시간 트래킹(YouTube IFrame API) · 반별 진도 체크리스트 · 출석 5상태 · 감사 로그 · HWPX/엑셀 일괄 등록 · 조교 권한 분리

### 2-C. 구현 전에 정해야 할 것
- [x] 리디자인을 **기존 SPA 안에 이식**할지, 별도 파일로 새로 만들지
      → **기존 `index.html`에 이식 (2026-08-06 확정).** Firestore REST · HWPX 파서 ·
      오답 파이프라인 · 영상 트래킹이 전부 이 파일에 있고, 그게 이 제품에서 제일 비싼 부분이다.
      화면을 하나씩 갈아끼우면 기능 유실 위험이 가장 낮다
- [ ] 전체 재렌더링(`innerHTML`) 구조를 유지할지, 부분 렌더링으로 바꿀지
- [ ] 관리자 화면의 모바일 대응 수준 (태블릿·폰에서 실제로 쓸 것인지)

---

## 3. 디자인 시스템 — 확정

**기준: 관리자 시안 A3 「학생 도시에」.** 색은 **자체 로고**에서 나온다.
상세 문서는 `design-v1/00-system.html`. 아래는 요약이다.

### 3-A. 색 — 새 색은 만들지 않는다
```
--paper   #FAF9F6   앱 배경 (순백 안 씀)
--card    #FFFFFF   패널·카드
--ink     #22221E   본문
--sub     #5C5C54   보조
--dim     #96968C   흐림
--line    #E7E5DE   패널 경계
--line2   #F1EFE9   내부 구분

--accent      #3F3537   주조 = 로고 차콜. 선택·주요 액션·버튼 (매뉴얼북 지정값)
--accent-d    #2A2325
--accent-bg   #ECE9E6
--accent-line #D5D0CA

--brand       #BA5054   브랜드 = 로고 와인. UI 강조에 쓰지 않는다
--brand-d     #9E4246
--brand-gray  #A8A4A4   로고 워드마크 KIM HA HYUN MATH

상태색 (의미 있을 때만)
--ok    #3F7A4D 출석   --late  #9A7000 지각   --leave #6B46A8 조퇴
--no    #B03A2E 결석   --gone  #96968C 퇴원   --blue  #39607F 성적

--note #FDFAEF / --note-line #EDE3C6   «사람이 적은 것»에만
```

> **와인을 UI 강조색으로 쓰지 않는 이유** — 위험 `#B03A2E`와 색상이 가까워
> 고밀도 표에서 «선택된 행»과 «위험한 학생»이 구분되지 않는다. 취향이 아니라 사용성 문제다.

### 3-B. 서체
- **Pretendard** 본문 (관리자 13px / 학생앱·랜딩은 더 큼)
- **Noto Serif KR** — «그 화면이 다루는 대상의 이름»에만. 그 외 어디에도 쓰지 않는다
- **JetBrains Mono** — 숫자·시각·ID·전화 전부. `tabular-nums`

### 3-C. 반경 · 모션 · 인터랙션
```
--r-xs 5 / --r-sm 7 / --r-md 9 / --r-lg 12 / --r-xl 14 / --r-pill 20
--t 140ms cubic-bezier(.4,0,.2,1)
```
- 선택 = **accent 배경 + 좌측 3px accent 바**. 리스트·표·큐 어디서나 같은 모양
- hover = 웜 그레이 틴트 한 단계. 그림자·확대 없음
- 포커스 = accent 링 3px @ 12%. 절대 지우지 않는다

### 3-D. 로고
`로고/` 폴더. 2024-01 PAPERFIT 제작, 매뉴얼북 있음.
- 가로형/세로형 × 3종 — (1) 컬러 (2) **흰색 녹아웃** (3) 단색
- 심볼은 **곡선 + 붉은 점** (삼차함수 그래프처럼 보인다). 랜딩 척추의 점이 이 점의 반복이다
- **브랜드 이름은 로고로만 말한다.** 같은 이름을 서체로 또 쓰지 않는다

---

## 4. 만들어진 화면

### 4-A. 관리자 16개 (`design-v1/`, 1440×900)
메뉴 구조 — 상단바·서브탭은 `ds.js`의 `NAV`/`SUBNAV` 한 곳에서 생성된다.

| 메뉴 | 화면 |
|---|---|
| **명단** | `01-roster` → `03-student`(학생 상세) |
| **신호** | `05-signals` |
| **반** | 학생 명단(→01) · `14-log`(수업 기록) · `06-classes`(진도) · `15-quiz`(데일리퀴즈) · `16-videos`(영상) |
| **수업** | `02-class` — 출결 → 진도 → 과제 → 메모 |
| **오답** | `04-review`(검토) · `07-exams`(시험지) · `08-bank`(창고) |
| **소통** | `09-clinic` · `10-notice` · `11-qna` · `12-chat` |
| **설정** | `13-settings` |

각 화면의 «역할»과 «무엇을 흡수/버렸는가»는 `preview.html`과 `00-system.html`에 적혀 있다.

### 4-B. 학생 마이페이지 11개 (`20-student-app.html`, 모바일 390×812)
**기존 11개 탭을 4개로 묶었다. 기능은 하나도 빼지 않았다.**
```
홈    오늘 할 일 · 다음 수업 · 이번 주 요약
학습  데일리퀴즈 · 오답숙제 · 진도·과제 · 강의시청
기록  성적 · 출석
소통  공지사항 · 질의응답 · 클리닉 신청 · 채팅
```
하단 탭바에 들어가는 것은 4~5개가 한계다. 11개를 가로로 밀게 하면 뒤쪽은 아무도 안 누른다.

### 4-C. 랜딩 (`landing-v2/index.html`) — 컨셉 「하루」
**이 학원이 일하는 하루를 방문자가 목격한다.**
기존 5안이 전부 «수학»을 소재로 썼다면, 이번은 «서비스»를 소재로 쓴다.

```
도착   로고 + 오늘 날짜(자동) + 첫 로그가 접힌 채로 보임
하루   16:00 수업 / 19:00 수업 / 21:10 수업 / 21:14 검토 / 23:41 질문
       ──── 다음 날 ────
       09:12 답변          ← 날짜가 넘어가는 이 한 칸이 핵심
전환   차콜 풀블리드 + 흰 세리프 한 문장   ← 유일하게 큰 소리 (문구 미정)
사람   메모 종이 + 인물 컷아웃
행동   상담 신청
```
- 시각·행위 종류만 쓰고 **수치를 넣지 않았다** — 공개 페이지의 숫자는 방문자가 사실로 읽는다
- 16시·19시·21시10분은 **실제 반 시간표**다
- 콘텐츠는 `CONTENT` 객체 한 곳에 모여 있다. 문자열만 갈면 그대로 돌아간다
- **경력·성과 자리는 주석으로 미리 잡아 뒀다** (`CONTENT.credits`). 나중에 끼워 넣으면 레이아웃이 깨진다

---

## 5. 다크 vs 라이트 — **라이트 유지로 확정 (2026-08-06)**

**결정: 라이트 유지. 다크는 «화면별»이 아니라 «제품 전체 테마»로만 — 지금은 하지 않는다.**
아래는 그 근거이며, 나중에 다크를 다시 꺼낼 때 읽으면 된다.

1. **오답 검토는 흰 배경 콘텐츠를 다루는 화면이다.** 시험지 스캔·HWPX·수식·학생 답안이 전부 흰 바탕이다. 어두운 UI에 흰 패널이 박히면 대비가 극단으로 벌어져 오히려 눈이 더 피로하다. 다크가 편한 것은 «화면 전체가 어두울 때»뿐이다.
2. **하루에 명단 → 수업 → 검토를 계속 오간다.** 한 화면만 다크면 이동할 때마다 명순응·암순응이 일어난다.
3. 사용자가 정한 최우선 원칙이 «하나의 제품처럼 보이는 것»이다.

전체 테마로 간다면 `:root` 색 값만 갈아끼우면 되어 구조적으로 싸다. 다만 **상태색 5종과 메모 종이색의 다크 대응은 새로 설계**해야 한다. **우선순위는 낮게.**

---

## 6. 결정 기록

### 6-A. 확정
- ~~**`index.html` 수정 금지**~~ — 설계 기간의 규칙이었다. 2026-08-06 이식 착수와 함께 해제됐다 (0-B)
- 기존 디자인을 개선하지 않는다. 완전히 새 방향
- 브랜드 성격: 학원이 아니라 **개인 브랜드**. Premium / Modern / Minimal / Trustworthy / Sophisticated
- 피할 것: 흔한 학원 홈페이지, 과도한 카드 나열, AI가 만든 것 같은 랜딩, 복잡하고 촌스러운 디자인
- **관리자 시안 A3를 디자인 시스템의 기준으로 확정** — 버튼·카드·색·폰트·간격·아이콘·인터랙션 전부
- 다른 시안은 화면을 합치지 않고, **화면별 역할을 먼저 정의한 뒤 그 역할에 맞는 UX만** A3 언어로 번역해 흡수
  - 전체 학생 관리 → **A1**(고밀도 표·저장된 뷰·일괄 작업) / 수업 진행 → **A2**(단계형 워크플로·큰 터치)
  - 학생 상세 → **A3** 그대로 / 오답 검토 → **A4**(큐·2열 비교·키보드) / 신호 → **A5**(근거 문장)
- **버린 것**: A4의 다크 테마, A5의 3열 칸반 보드, A1의 파랑 선택색·IBM Plex, 이모지 아이콘
- **주조색을 로고 차콜로 교체** (원래 올리브 `#4E5A42`였다 — 로고를 못 보고 정한 내 실수)
- **로고 지정색 2개는 매뉴얼북 PDF에서 직접 확인함 (2026-08-06)** — 차콜 `#3F3537` · 와인 `#BA5054`
  - 매뉴얼북은 **텍스트가 전부 아웃라인 처리**돼 있어 문자열 추출이 안 되고, Chrome PDF 뷰어는 이 파일에서 렌더러가 멈춘다(두 번 겪었다). 뷰어로 읽으려 하지 말 것
  - 대신 **PDF 콘텐츠 스트림을 zlib로 풀어 «칠해진 사각형»의 색과 면적을 쟀다.** 스크립트는 스크래치패드의 `pdfswatch.js` / `pdfrects.js`
  - p6이 컬러 지정 페이지 — 같은 높이(219.5)의 색면 두 개가 나란히 있다. 이게 지정값이다
  - p7의 `#9E9E9E`는 **배경 사용 규정 페이지의 타일**이지 워드마크 회색이 아니다. `--brand-gray #A8A4A4`는 그대로 둔다
  - 검증: 이 방법으로 뽑은 와인이 사용자가 정정해 준 `#BA5054`와 정확히 일치했다
  - `--accent-d`는 기존 비율(×0.667)을 그대로 적용해 `#2A2325`로 재계산. `--accent-bg`/`--accent-line`은 accent의 틴트가 아니라 **paper 계열 웜 뉴트럴**이라 손대지 않았다
- **랜딩 「전환」 문구 = 「짐작으로 가르치지 않습니다」 (2026-08-06 확정)**
  - 위의 로그가 «증거», 이 문장이 «주장», 아래 메모가 «이유» — 셋이 서로 다른 일을 한다
  - 기각: 「무엇을 틀렸는지 기억합니다」·「가르친 것은 전부 남습니다」는 아래 메모의 «전부 적습니다»와 겹친다.
    「한 명도 같은 방식으로…」는 62px에서 두 줄로 접히며 앞머리가 약해진다.
    「틀린 문제는 두 번 만납니다」는 차점이었으나 오답 시스템 하나만 말한다
  - 후보 5개와 각각의 찬반은 `landing-v2/copy.html`에 그대로 남겨 뒀다
- **AI 유사문항의 정답 형태를 못박았다 (2026-08-07)** — «999 이하 자연수» 아니면 «객관식» 둘 중 하나
  - 학생 답안칸이 받는 것은 숫자 입력과 ①~⑤ 보기 버튼 둘뿐이다. 분수·무리수·음수·문자식이
    정답이면 학생은 풀고도 제출할 방법이 없다. 예전 프롬프트는 «깔끔한 정수 **또는 간단한 기약분수**»를
    허용했는데 기약분수가 바로 그 사례였다
  - 워커 프롬프트(`answerFormRule`) + 받는 쪽 안전망 셋. 상세는 `worker/README.md`
  - **기존 데이터에 이미 어긋난 것이 있었다** — 생성된 7개 중 5개, 그중 2개는 공개 상태였다.
    제일 나쁜 것은 «객관식인데 정답이 숫자 4» — 학생이 ④를 누르면 «④»가 저장되어
    문자열 비교에서 오답 처리된다. **맞혀도 틀린 것으로 채점된다**
- 조교 계정은 **없음**. 필요하면 설정에서 새로 만들어야 함
- 강사 비밀번호는 전달받았으나 **이 파일에 기록하지 않음** (공개 저장소)

### 6-B. 랜딩 5안 — 전부 기각 (보관: `mockups/`)
| | 컨셉 | 히어로 |
|---|---|---|
| 01 | 정리와 증명 | 「정리 1」 한 문장, 이미지·버튼 없음 |
| 02 | 좌표계 | 화면 전체가 좌표평면, 성적 곡선 실시간 드로잉 |
| 03 | 한 사람 | 인물 컷아웃 + 극단적 여백 |
| 04 | 관측소 | 카피 대신 실제 기록 대시보드 |
| 05 | 한 문제 | 실제 문제 1개, 선택지를 누르면 풀이 시작 |

**기각 사유는 아직 못 들었다.** 사용자 말로는 "예쁘지 않아서가 아니라 서비스의 첫인상과 브랜드 경험이 부족해서".
내 진단 — 1·2·5는 «수학»을 소재로 썼고 3은 인물 포트폴리오였다. 아무 수학 강사나 쓸 수 있는 소재라 브랜드가 없었다.
04 관측소는 「하루」와 인접하지만 **대시보드는 성과 자랑, 로그는 태도**라는 점이 다르다.

### 6-C. 현행 index.html 디자인 (참고 — 계승하지 않음)
컨셉 "채점된 답안지". 종이 질감 + 접힌 모서리 + 빨간 첨삭펜.
```
--paper #F5F1EB  --ink #3D3335  --blue-deep #AE4B53(실제는 와인)  --red-pen #6E2A2F
```
> `--blue-deep`이 와인색이었던 것 = **토큰 이름에 색 이름을 넣으면 안 되는** 실제 사례.
> 그리고 이 값이 로고 와인과 같은 계열이다 — 원래 사이트는 로고에서 색을 가져왔던 것이다.

---

## 7. 파일 위치

```
claude work/
├─ index.html                     ← 실제 서비스. 이식 중 (0-B)
├─ profile.png / profile.webp     ← 인물 컷아웃. webp 100KB(랜딩이 씀) / png 2.2MB(폴백)
├─ CLAUDE.md                      ← 이 파일
│
├─ ds.css                         ★ 유일한 디자인 시스템. 토큰은 :root. 실서비스·시안이 같이 쓴다
├─ ds.js                          ★ 아이콘 34종 · NAV/SUBNAV · 피팅 · 예시 데이터 — 시안 전용, 실서비스는 로드하지 않는다
│
├─ design-v1/                     ★ 관리자 16 + 학생앱 11 (시안)
│   ├─ 00-system.html             ← 디자인 시스템 문서
│   ├─ 01-roster / 02-class / 03-student / 04-review / 05-signals
│   ├─ 06-classes / 07-exams / 08-bank / 09-clinic / 10-notice
│   ├─ 11-qna / 12-chat / 13-settings
│   ├─ 14-log / 15-quiz / 16-videos          ← 반 관리 서브탭
│   ├─ 20-student-app.html                   ← 학생 마이페이지 11개 (모바일)
│   └─ preview.html                          ← 관리자 16개 통합 프리뷰
│
├─ landing-v2/                    ★ 랜딩페이지
│   ├─ index.html                 ← 실제 페이지 「하루」
│   ├─ preview.html               ← 데스크톱 1440 + 모바일 390 동시
│   ├─ copy.html                  ← 전환 문구 후보 5개 (실제 크기)
│   └─ concept.html               ← 컨셉 보드 (로고 반영 전, 보관)
│
├─ 로고/                          ★ 자체 로고 (2024-01 PAPERFIT)
│   ├─ 가로형(1~3).png            ← (1) 컬러 (2) 흰색 녹아웃 (3) 단색
│   └─ 세로형(1~3).png
│   ※ 매뉴얼북 원본: Documents/카카오톡 받은 파일/[김하현T] 로고 매뉴얼북.zip
│
├─ mockups/                       ← 랜딩 5안 (기각, 보관)
├─ mockups-admin/                 ← 관리자 5안 (선정 완료, 보관)
├─ worker/                        ★ Cloudflare 워커 — 코드 완성, 배포 대기 (9절 1번)
│   ├─ gemini-proxy.js            ← 배포본 + Firebase ID 토큰 검증 · 서버 쪽 할당량
│   ├─ verify-test.mjs            ← 토큰 검증 테스트. node worker/verify-test.mjs
│   └─ README.md                  ← 왜 회전이 아니라 교체인지 · 3단계 배포 순서
│   ※ 손댄 곳은 CORS · 인증 게이트 · 할당량 셋뿐. 드라이브·Gemini 로직은 배포본 그대로다
└─ tools/                         ★ 개발 도구 (배포와 무관)
    ├─ serve.js                   ← 로컬 정적 서버. node tools/serve.js
    └─ check-css-collisions.js    ← ds.css ↔ 구버전 스타일 누수 검사. 화면 옮길 때마다 돌린다
```

관리자 목업은 **1440×900 캔버스를 화면 크기에 맞춰 축소**하는 구조(`fitCanvas()`).
표시된 학생 이름·수치는 전부 **구조 확인용 예시**이며 실제 데이터가 아니다.

**검증 상태** — 관리자 16 + 학생앱 11 + 랜딩(데스크톱/모바일) 전부 **0건**
(캔버스 이탈 / 텍스트 잘림 / 부모 침범 3종. 스크롤 컨테이너 안쪽과 KaTeX `.katex-mathml`은 제외)
`mockups/`(랜딩 5안)에는 `word-break:keep-all`이 안 들어갔다. 보관용이라 두었다.

---

## 8. 작업 규칙

### 만들 때
- **시안을 만들 때**는 별도 폴더에. **실서비스에 이식할 때**는 `index.html`을 직접 고친다 (0-B)
- **새 화면은 루트 `ds.css`를 링크해서 만든다.** 화면별 `<style>`에는 «배치»만 두고 색·서체·반경·간격을 새로 정하지 않는다
- **상단바·서브탭을 손으로 적지 않는다.**
  - 시안: `<nav id="nav">` / `<div class="subnav" id="subnav">`만 두고 `renderNav()` / `renderSubnav()`를 부른다. 메뉴가 바뀌면 `ds.js`의 `NAV`/`SUBNAV`만 고친다
  - 실서비스: `index.html`의 `TEACHER_NAV` / `TEACHER_SUBNAV` 한 곳만 고친다. **둘은 따로다** — 시안은 href로, 실서비스는 탭 id로 움직이기 때문이다. 메뉴를 바꾸면 양쪽을 같이 고쳐야 한다
- **두 화면에서 쓰이는 컴포넌트는 ds.css로 올린다.** 로컬에 두면 한쪽만 고쳐진다
- 새 HTML의 리셋에 **`word-break:keep-all; overflow-wrap:break-word;`**. 없으면 한글이 음절 단위로 갈라진다("목표"→"목/표")
- CSS 커스텀 속성은 **`:root`에.** 특정 클래스 안에 두면 그 클래스를 안 쓰는 문서 페이지에서 색이 조용히 사라진다

### ⚠ 클래스 이름 — 같은 사고를 네 번 냈다
**화면별 `<style>`에서 ds.css의 클래스 이름을 다시 쓰지 않는다.**
`.stage` · `.ev` · `.tag` · `.row` · `.body` 를 로컬에서 재정의했다가 매번 조용히 망가졌다.
(`.stage`는 목업 뷰어의 검은 배경, `.body`는 앱 셸의 `absolute; top:52px` — 장면이 통째로 헤더 위로 올라갔다)
→ **랜딩처럼 접두사를 붙이는 쪽이 안전하다** (`l-` 등).

### 색을 정할 때
- **브랜드 자산(로고·매뉴얼)이 있는지 먼저 묻는다.** 로고를 못 보고 주조색을 정했다가 24개 화면을 만든 뒤 갈아엎었다
- **토큰 이름에 색 이름을 넣지 않는다.** `--olive`가 차콜을 담는 순간 거짓말이 된다
- **브랜드색을 UI 강조색으로 바로 쓰지 않는다.** 상태색과 겹치는지 먼저 본다

### 판단할 때
- **통일해야 하는 것은 색·서체·간격·버튼이지 레이아웃이 아니다.** 질의응답은 세로 스택, 채팅은 말풍선 — 글의 성격이 다르면 형태는 달라야 한다. 형태를 억지로 맞추는 것은 일관성이 아니라 게으름이다
- **같은 곳으로 가는 문은 하나만 만든다.** (명단의 「위험 신호」 뷰 제거, 반의 「학생 명단」 → 01로)
- **학생 화면은 모바일 기준으로 그린다.** 누르는 것은 최소 46px
- 실제 도메인 값을 쓴다: 7과목(공통수학1·2, 대수, 미적분Ⅰ, 확률과통계, 기하, 미적분Ⅱ), 출석 5상태, 오답 3단계, 실제 학교명(건대부고·광남고·대원고·동대부여고·가람고·광양고·대원여고)
- 실존 인물에 대한 사실(경력 연수 등)을 **지어내지 않는다.** 수치는 예시임을 명시한다
- **공개 페이지에는 검증 불가능한 수치를 넣지 않는다.** 방문자는 사실로 읽는다
- 비밀번호·토큰을 문서나 코드에 기록하지 않는다

### 검증할 때
- 목업을 만들면 **반드시 브라우저로 렌더링을 확인한다.** 축소된 스크린샷만 보면 놓친다
- 캔버스를 `scale(1)`로 되돌린 뒤 **DOM 기하를 재는 편이 확실하다** — 1440×900 이탈 / `scrollWidth>clientWidth` 텍스트 잘림 / `overflow:hidden` 부모 침범 3종
- **스크롤 컨테이너 안쪽 요소는 제외**해야 한다 (안 그러면 오탐이 쏟아진다). KaTeX `.katex-mathml`도 의도적 클리핑이라 제외

---

## 9. 리디자인과 별개로 처리해야 할 이슈

1. **`AI_SITE_TOKEN`** (index.html:594) — **「회전」이 아니라 「교체」다. 계획은 `worker/README.md`에 있다**
   - ~~토큰 회전 필요~~ — **회전은 효과가 거의 없다.** 정적 클라이언트라 토큰이 모든 방문자
     브라우저로 내려간다. 저장소에서 지워도 배포본에는 남아야 하고, 갈아도 새 값이 똑같이 공개된다.
     **처음부터 비밀이 될 수 없는 값이었다**
   - **진짜 위험은 그 뒤에 방어가 없다는 것** — 일 20건 제한이 클라이언트에만 있다(731·758).
     토큰만 있으면 워커를 루프로 호출해 Gemini 비용을 태울 수 있다
   - 해법: 워커가 **Firebase ID 토큰을 검증**하고 **할당량을 서버에서 uid별로** 강제한다.
     `getAuthToken()`이 이미 있고 Firestore REST가 그걸 쓰고 있다 — 재료는 다 있다
   - ⚠ **「강사만 접근」은 이걸로 안 된다.** 5401이 `signInAnonymously()`라 Firebase 신원이
     «사이트를 연 사람»까지만 말한다. 학생/강사 구분은 Firestore 대조라 토큰에 안 실린다.
     커스텀 클레임을 심으려면 권한 있는 백엔드가 따로 필요하다 — 별건
   - **순서를 지켜야 한다. 클라이언트를 먼저 바꾸면 실서비스가 죽는다** —
     새 `Authorization` 헤더가 프리플라이트를 바꾸는데 워커의 `Access-Control-Allow-Headers`에
     `authorization`이 없으면 브라우저가 요청을 통째로 막는다
   - 상태 (2026-08-07): **1단계·2단계 완료. 3단계만 남았다**
     - 1단계 — 사용자가 워커 배포 완료. 프리플라이트로 확인함:
       `Access-Control-Allow-Headers`에 `Authorization` 있음,
       `Access-Control-Allow-Origin: https://khhyun827-lang.github.io`,
       잘못된 Bearer 토큰 → 401
     - 2단계 — `index.html`에서 `AI_SITE_TOKEN` 상수와 `X-Site-Token` 헤더 **4곳**을
       `getAuthToken()`으로 교체 완료. `deleteFromDrive()`는 await가 필요해 async가 됐다
       (부르는 곳 8곳은 전부 결과를 안 기다리는 호출이라 그대로 둬도 된다)
     - **3단계가 남았다 — Cloudflare에서 `SITE_TOKEN` 비밀 삭제.**
       이때 비로소 유출된 값이 죽는다. 그 전까지는 살아 있다
   - ⚠ **`ALLOW_ORIGIN`을 걸어서 로컬(`127.0.0.1:8777`)에서는 워커를 못 부른다.**
     AI 생성·이미지 업로드 확인은 **배포된 사이트에서** 해야 한다
   - 커밋 이력의 옛 토큰은 회수되지 않는다. 3단계에서 무효화하는 것이 유일한 해결이다
2. ~~**홈 히어로 이미지 깨짐**~~ — **해결됨 (2026-08-07 확인).** `profile.jpg`(82KB)가 추적되고
   배포본에서 200으로 열린다. `profile.png`(2.2MB)·`profile.webp`(97KB)도 함께 추적 중이다.
   `homeHTML()`은 `index.html:3327, 3334`에서 `profile.jpg`를 쓴다
3. **비밀번호 평문 저장** — 학생 `pw`, 조교 `pw`, `teacher-pw` 전부
4. **전체 재렌더링으로 인한 포커스 유실** — 검색창마다 `render(); getElementById(...).focus()`로 수동 보정 중(3443)
6. ~~**`todayStr()`이 UTC 기준이다**~~ — **고쳤다 (2026-08-07).** `ymdLocal()`을 만들어
   「오늘」을 로컬 날짜로 통일했다. `new Date().toISOString().slice(0,10)`을 쓰던 12곳이
   전부 `todayStr()`을 부른다. 이미 저장된 기록의 날짜는 그대로 뒀다 — 고쳐 봐야 어느 쪽이 맞는지 모른다
   - 미룰 수 없었던 이유: 반 관리 > 수업 기록의 날짜 목록이 **요일은 로컬로 판정하고
     날짜는 UTC로 기록**해서 통째로 하루씩 밀려 있었다. 월·수·금 반이 **일·화·목**으로 보였고,
     그 상태로 출결을 넣으면 하루 전 날짜에 저장된다
   - 수업 화면도 같은 이유로 그 시간대에 「이 날짜에 수업이 없습니다」를 띄우고 있었다
   - `shiftYmd()`는 문자열 산술이라 이 변경과 무관하게 그대로 맞다
7. **SEO/접근성 공백** — meta description·OG 태그 없음, `href` 없는 `<a onclick>`(키보드 접근 불가), 후기 마퀴에 `prefers-reduced-motion` 미대응
   → 랜딩(`landing-v2/index.html`)에는 이미 meta·OG·skip link·focus-visible·reduced-motion을 넣어 뒀다
